[build]
  command = "rm -rf node_modules package-lock.json && cd frontend && npm install && VITE_API_URL=https://pi-dev.tailecb90f.ts.net VITE_VAULT_API_KEY=nick-agent-vault-2026 npm run build"
  publish = "frontend/dist"
