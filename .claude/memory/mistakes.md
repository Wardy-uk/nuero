# Mistakes Log

# Claude adds a line here every time it makes an error. Read at session start.

# Format: - YYYY-MM-DD: \[what went wrong\] → \[what to do instead\]
- 2026-05-31: Used PowerShell here-string syntax `@'...'@` for a git commit message inside the Bash tool (which is bash) → leaked a literal `@` into the commit subject. In the Bash tool use a quoted `$'...'` string or `git commit -F file`; `@'...'@` here-strings only work in the PowerShell tool.
- 2026-05-31: Used `git checkout -- <file>` to revert a small fault-injection edit, but the file had UNCOMMITTED feature work in the working tree (the thing being evaluated) → checkout wiped the build agent's work back to HEAD, not back to my edit. To revert a targeted perturbation, reverse it with a targeted Edit (or stash/restore the exact hunk) — never `git checkout` a file that carries uncommitted changes you need to keep. Check `git status`/`git diff` before reverting.
- 2026-05-31: Ran `git reset --hard origin/main` on the Pi to force a blocked deploy through, wiping ~27 modified tracked files of uncommitted SARA work (12-view system, new screens). Survived only because the dev machine held an uncommitted copy — luck, not process. → NEVER reset --hard a dirty tree to unblock a pull. Back up first (tar the working tree), then `git stash -u` or commit to a temp branch, then pull. Before ANY deploy that pulls: check `git status` on the target AND confirm in-flight work is committed/pushed somewhere.
- 2026-06-19: Planned a "Hybrid" cutover where pi5's Tailscale Funnel would proxy /tally to a backend on another node (pi-dev:3003). Tailscale Serve/Funnel ONLY proxies to localhost on its own machine — a remote target silently returns 000 (the mount even shows in `serve status`). → To move a Funnel-exposed service to another node, run `tailscale funnel` ON that node (each node funnels its own localhost). Don't assume Serve is a general reverse proxy.
- 2026-06-19: Started tally under pm2 on pi-dev but didn't `pm2 save`; the process later vanished from the list when pm2 reloaded its saved dump (which didn't include it) → after `pm2 start`-ing anything you intend to keep, ALWAYS `pm2 save` immediately so it survives daemon reloads/reboots.
- 2026-06-23: For vault dedup, proposed 568 files as "safe duplicates" using folder (Archive/) + filename (" 2.md") heuristics, without checking content. Verification showed 175 (31%) had unique text — including one file 2% contained with 1517 novel phrases. → NEVER infer "duplicate" from location/filename. Prove it by content (word-shingle containment: a drop is only safe if ~all its text already exists in a kept copy). Plaud generates multiple partially-overlapping summary variants per recording — they share a plaud_id but are NOT identical.
- 2026-06-30: Built a markdown link-graph analyser to find Obsidian orphans; it disagreed with what Nick saw ~10 times. Obsidian's graph counts differently: it shows .canvas/.base files (my scan was .md-only), ignores [[links]] in code blocks/inline-code, is polluted by my own Vault Audit report links (phantom edges to every note they list), and resolves basename collisions path-aware (same-folder > shortest path). → Don't reconstruct Obsidian's graph from a naive .md scan; strip code, include canvas, exclude report folders as edge sources, resolve path-aware — or just trust the user's eyes. Best attempt: C:\tmp\truth2.js.
- 2026-07-03: VitePWA with default generateSW strategy produces a Workbox service worker with NO push event handler — push notifications are silently dropped. Always use `strategies: 'injectManifest'` with a custom sw.js that includes a `push` event listener. Also: `navigateFallback` is a generateSW option only, not valid in injectManifest config.
- 2026-07-03: db.setState() takes a primitive — passing an object throws "tried to bind a value of an unknown type". Always JSON.stringify objects before setState, and JSON.parse in getState wrappers. → Always stringify before storing objects in the sql.js KV store.
- 2026-06-30: Nearly archived 3 PLAUD recordings (090044, 095549, 16-28-23) as "empty stubs" based on a fragile speaker-line grep — they each had real 200+ line transcripts (just "Speaker N", no summary). → NEVER classify a note as empty/stub by filename or a regex line-count; read the body. Timestamp-named PLAUD recordings are usually real meetings PLAUD didn't name.
- 2026-07-31: Used PowerShell here-string (@'...'@) inside the Bash tool for a git commit message → literal "@" became the commit subject; needed amend + force-push. Use a bash heredoc (<<'EOF') in the Bash tool, PowerShell here-strings only in the PowerShell tool.
- 2026-08-12: Read action-candidates.js/todo-intelligence.js in the repo and reported their behaviour as production fact — neither was deployed to the Pi. → When diagnosing live behaviour, verify the file exists ON THE PI before describing what it does; the Pi ran a 3 Jul base 34 commits behind main.
- 2026-08-12: Hand-patched 6 files onto the Pi's stale checkout instead of reconciling first; the 7th failed on a missing dependency and each patch made prod match no commit anywhere. → If the target is >a few commits behind, reconcile the branch first, then deploy normally.
- 2026-08-12: CRLF on the Pi turned every patched file into a whole-file diff, hiding real changes and creating 12 phantom merge conflicts. → Normalise line endings to LF before diffing/merging a Pi branch; it cut conflicts from 12 files to 6.
- 2026-08-12: `nohup curl … &` inside an ssh one-liner died with the SSH session; a 76-item Plaud repull silently stopped at 24 and looked like API rate-limiting. → Use `setsid nohup … < /dev/null & disown` for anything meant to outlive the ssh connection, and verify with pgrep after.
- 2026-08-12: Wrote JS containing regexes into a quoted bash heredoc twice; backslashes were eaten both times (`/\[/` → `/[/`), once silently corrupting a deployed guard regex on the Pi. → Never send regex-bearing JS through a heredoc. Write the file with the Write tool and scp it.
- 2026-08-12: Claimed NEURO "already has the single consolidated task view" without checking Sidebar.jsx — TodoPanel is routable but has no menu entry, and SARA mobile has no todos tab at all. Nick had to correct me. → Before asserting a feature is available to the user, check it is reachable from the UI, not just present in the code.
- 2026-08-12: Triggered a 73-note Plaud repull while the write-hook path still auto-promoted at 0.93, dumping 28 unreviewed items into Master Todo (18 → 46), mistagged #mustdo and under the wrong heading. → When adding a review-only path, disable the auto path FIRST; check every caller of the shared function, not just the one being changed.
- 2026-08-13: Diagnosed a better-sqlite3 segfault as "bad platform / missing linux-arm64 prebuild" and recommended rolling back the whole migration. Real cause was a Node ABI mismatch — PM2's daemon ran Node 20 while npm install ran Node 22 — with systemd hardcoding Node 20 underneath. → For a native-module crash, compare the Node that RUNS the process (`readlink -f /proc/<pid>/exe`) against the one that built it BEFORE blaming the platform or the data. A prebuild inventory read from a half-repaired module dir is not evidence.
- 2026-08-13: Deployed using `export PATH=.../v20.20.2/bin` copied from a stale memory note, then `pm2 restart --update-env` — which injected Node 20 into neuro-backend and re-triggered the better-sqlite3 ABI segfault fixed hours earlier. Also ran `git checkout -- package-lock.json` on the Pi without looking at the diff. → PM2 daemon on the Pi runs Node **22.22.2**; always use v22.22.2 in PATH for pm2 commands, and check `readlink -f /proc/<pm2 daemon pid>/exe` rather than trusting a memory note. Never discard another session's uncommitted Pi changes without reading them first.
- 2026-08-13: Wrote a smoke test that moved `backend/db/agent.db` aside and restored it in a `finally` — better-sqlite3 still held the file open, so the restore silently failed and a second run overwrote the backup with the 4KB scratch DB. Destroyed the local dev agent.db (18 Jun copy, gitignored; live data is on the Pi and untouched). → Never move a live DB file to test against a scratch one. `database.js` now honours `NEURO_DB_PATH`; point tests at that instead. On Windows `fs.renameSync` overwrites the destination without warning, so a "backup" path is not safe from a second run.
- 2026-08-13: Used PowerShell here-string syntax (`-m @'...'@`) inside the Bash tool for a commit message → mangled subject line to "@". Bash tool is POSIX sh; use a heredoc (`git commit -F - << 'EOF'`) for multi-line messages.
- 2026-08-14: Reported NEURO's Graph scopes as `Calendars.Read, Mail.Read` from CLAUDE.md during a codebase review; the actual `GRAPH_SCOPES` in microsoft.js was already `Calendars.ReadWrite, Mail.ReadWrite`, so I wrongly told Nick calendar writes were impossible without new consent. → CLAUDE.md is a summary and drifts; for any claim about what the system CAN do, read the source constant, not the doc.
- 2026-08-14: Told Nick "OPENROUTER_API_KEY is missing" after grepping only backend/.env, then claimed Anthropic was "working, credit error was transient" from a hardcoded provider label I'd written myself. Both wrong: the OpenRouter key lives in agent_state (admin panel → `ai_setting_openrouter_api_key`, bootstrapped into process.env at server.js:261), and the anthropic label was a literal string in my own syncChat return. → NEURO's AI keys are DB-backed, not .env-backed: check `agent_state` / `GET /api/ai/settings` before declaring a provider unconfigured. Never report a provider from a value you hardcoded — read it from the routing layer or the logs.
- 2026-08-14: Wrote a frontmatter parser using `/^key:\s*(.*)$/` and it silently returned {} for every CRLF note in the vault — `\r` is a line terminator in JS regex, so `.` won't match it and `$` can't anchor. The 1-2-1 scan under-reported with no error. → The vault is Windows-authored and mixed CRLF/LF: normalise with `.replace(/\r\n/g,'\n')` at read time before any line-anchored regex. obsidian.js avoids this by parsing with indexOf(':')+trim() instead of regex.
- 2026-08-14: Asserted live behaviour from code four times in one session and was wrong each time — "OPENROUTER_API_KEY is missing" (it's in agent_state, not .env), "those frontend calls 401" (a global window.fetch interceptor injects the PIN), "Anthropic is working" (I'd hardcoded the provider label myself), "NOVA already sends to Teams" (agent_teams_webhook_url is unset, so it returns on line one). → In NEURO and NOVA, configuration lives far from code: keys in the DB, auth in a monkey-patched global, channels behind unset settings. A service's existence proves NOTHING about whether it is switched on. Verify from the running system — logs, /api/status, the settings row — before describing what it does.
- 2026-08-15: Wrote a session handoff listing "outstanding" work that had already been built and deployed (items 4 and 5 were commits 9800959 and 396090c) — I compiled the list from the conversation instead of checking git log. Same session: shipped standup-session.test.js with a fixture pinned to 2026-08-14 calling finish(), which resolved "today" internally; it passed all day and broke at midnight. → A handoff's outstanding list is a claim about repo state: verify it against git log before writing it. And a test that pins a date must pass that date to EVERY call — if one function in a module resolves "today" itself while its siblings take a dateKey, that asymmetry is the bug.
- 2026-08-15: Ran `git push origin main` while still in the daypilot directory from an earlier `cd` in a compound command, so it pushed NOVA's repo, not NEURO's. No damage — daypilot's local main was already identical to origin/main and another session's nova-codex branch was untouched — but that was luck, not care. → `cd` persists across Bash calls in this harness. Never run a bare git command that assumes the working directory: pass `-C <repo>` explicitly, or `git rev-parse --show-toplevel` first. Especially with two repos and concurrent sessions in play.
- 2026-08-15: Surfaced waiting-on commitments in meeting prep by matching the stored canonical FIRST name against attendee names. Live calendar showed one Lucy's 16 items on four different Lucys, Chris Middleton's 31 on a Chris Smith, and both Nathans sharing one's 6. The service's own comment allows a first-name merge as a deliberate trade — but scoped to Nick's 13 reports, not a 309-attendee SMT invite. → A first name is only an identifier when it maps to exactly one person; `entities.getRoster().firstNames` already encodes that rule (and already excludes both Nathans), so use it instead of re-deriving a looser match. And a design note that says "acceptable trade" is scoped to the caller it was written for — re-check it at every new call site.
- 2026-08-15: Merged NOVA escalation-log keys into the escalations list with one `key in (...)` JQL at maxResults 100 — 1,958 candidates came back as the 100 OLDEST open tickets, so the two escalations raised that afternoon (the whole reason for the feature) were the ones cut, and nothing errored. Same silent-truncation species as the calendar $top=50 bug (#29) logged in the tracker the same day. Also badged all 1,958 as "urgency" when only 2 were type manual. → maxResults is a cliff, not an error: when the input is a known list, chunk it so the query is exactly bounded, and never rely on sort order to keep what matters inside a cap. And filter a log by event type before treating every row as the thing you are looking for.
- 2026-08-15: Repeated a handoff's claim that voiceUtils.js was "solved and in daily use" and shipped it to iOS unchanged — it was only ever exercised on desktop, and its gesture unlock spoke an EMPTY utterance, which iOS drops without queueing. Three deploys chased wiring bugs before the real cause (iOS drops speechSynthesis entirely in an installed standalone PWA). → "Proven" means proven ON THE TARGET PLATFORM. Before porting a util to a new runtime, ask which device the existing usage actually runs on.
- 2026-08-15: Told Nick "OpenRouter is text-only, no TTS" from memory while recommending a Raspberry Pi software install as the alternative. He challenged it; the models API lists four audio-output models, two of them speech. The install was unnecessary. → Provider capabilities change monthly. Query the models endpoint (openrouter.ai/api/v1/models, filter architecture.output_modalities) before ruling a capability out — especially when the alternative is installing software on his server.
- 2026-08-15: Reported a Netlify deploy as live off `grep -c "Voice on."` — the unescaped `.` matched `Voice on'` in the PREVIOUS bundle, and the asset hash hadn't changed. Announced success on a false positive. → Use grep -F for literal markers, and confirm the asset hash CHANGED before calling a deploy live; a marker that could exist in the old build proves nothing.
- 2026-08-15: Debugged SARA's voice for four deploys against Nick's phone without checking the PWA was actually receiving new builds. sara/app's sw.js never called skipWaiting (registerType 'autoUpdate' only wires that for generateSW, NOT injectManifest), so each new worker waited for all clients to close — which a swiped-away iOS PWA doesn't reliably do. He tested a stale bundle at least once; a mic fault "fixed itself on retest" with no code change between, which I accepted instead of questioning. → When a deployed frontend behaves like older code, verify WHICH bundle the device is running before theorising about the code. For any injectManifest PWA, confirm skipWaiting+clients.claim exist before trusting "close and reopen".
- 2026-08-15: Designed the pending-actions screen around "10 pending", read live from `GET /api/actions` — which called `getPendingSaraActions()` with no argument, whose default limit IS 10. The real queue was 930. Hitting the running system was correct and still gave a wrong answer, because the endpoint's own cap was the number I read. → Verifying against prod is not enough when the endpoint you verify through has a default limit: check the count with `SELECT COUNT(*)` against the table, not the API that paginates it. A returned length exactly equal to a round number is the tell.
- 2026-08-15: Three services asked "have I already actioned this?" with `db.getRecentSaraActions(N)` filtered down to a note or an event — a GLOBAL recency window used as a scoped query. At 16,281 rows the newest 500 spanned 21 hours, so the nightly vault scan re-queued every candidate it had queued the night before (926 pending, 442 distinct) and meeting-triage could have emailed the same organiser twice. → When the question is "for this note / of this type / since this date", write that WHERE clause. A LIMIT over a churning table is a time window whose length you do not control, and it silently shrinks as the table grows.
- 2026-08-15: Nick said 926 pending actions were "dupes, ones we've dismissed or completed, added to a temp note for sorting". Half were dupes; the rest were none of those things — zero matched a rejected/executed action, and a shingle search of all 1,680 vault notes found a best overlap of 20/442. Bulk-rejecting on his description would have discarded a 104-note, March–August extraction he had never read. → A user's account of their own data is a hypothesis, not a spec. Measure it before acting on it — especially when acting means a bulk write, and especially when the measurement is a single query.
- 2026-08-16: Ran `git add frontend/src/components/PeopleBoard.jsx` to commit my reschedule dialog, and swept in ~45 lines of ANOTHER concurrent Claude session's unfinished `1-2-1-booked` cadence work that was already sitting modified in that file. I had staged explicitly (not `-A`) and still got it, because explicit staging protects against unrelated FILES, not against unrelated HUNKS in a file I touched. The tell was there and I passed it: an edit tool warned "the file had been modified on disk since you last read it" and I attributed it to line-ending noise. → Nick runs 2-3 sessions on this repo at once. Before `git add` on any file, diff it and confirm every hunk is yours — `git diff <file>` before staging, not `git status` after. Treat "modified on disk since you last read it" as a collision signal, not noise.
- 2026-08-16: Added `GET /api/email/triage/feedback` below an existing `GET /triage/:emailId` in the same router; Express matches in registration order so "feedback" was parsed as an email id and the endpoint answered `{"ok":false,"error":"Email not found"}`. The full suite was green the whole time because the tests exercise the service, not the routing table. → A literal path must be registered BEFORE a sibling parameterised one, and a new route is not verified until it has been called against the running server — green unit tests say nothing about whether Express will route to it.
- 2026-08-16: Wrote a test that asserts against a source file's text, stripping comments with block-comments-first. A LINE comment in Standup.jsx legitimately names the route glob `/api/standup-session/*` — that `/*` opened a false block comment and the strip swallowed half the file, so `assert.ok(view.includes(...))` started failing on absence. Had the assertions been negative (`!includes`) instead, every one would have PASSED on a gutted file and the test would have been worthless while looking green. → When a test scans source text, strip line comments BEFORE block comments, and sanity-check that the stripped output still contains something you expect. A "does not contain X" assertion over text you also transform is a pass-by-absence waiting to happen.
- 2026-08-16: Ran my new test against `Standup.jsx`, saw 8/8 green, and only found the file had a stray `</content>` tag making it syntactically invalid when `npm run build` failed afterwards. The test read the file as a STRING, so it never parsed it. → A content assertion over a source file proves nothing about whether that file compiles. Build (or at least parse) any new frontend file before treating its tests as verification — and check newly Written files for trailing tool artefacts.
- 2026-08-16: Appending to mistakes.md, I used the FIRST LINE PREFIX of the last entry as the edit anchor and did not repeat it in the replacement — silently deleting that entry's opening clause and welding its tail onto my new line. Caught only by reading `git diff` before staging. → When appending to an append-only log, anchor on the END of the file or repeat the anchor text verbatim in the replacement, and always diff a prose file before staging it. A partial-line anchor in a file of long single-line entries destroys the entry it anchors on.
- 2026-08-16: Shipped an unauthenticated `/api/v1/ingest/` (the iOS Health app cannot send a credential) and guarded it by source IP, trusting `127.0.0.1`. pi5 already serves `https://pi5.tailecb90f.ts.net` → `127.0.0.1:3001` with **Funnel ON**, and Tailscale proxies BOTH tailnet and public-internet traffic from loopback — so the guard I wrote to mean "tailnet only" published an unauthenticated write endpoint to the internet. I had checked `ss -ltn` for free ports but never ran `tailscale serve status` to ask what was ALREADY exposed. → Before exempting any route from auth on a host behind Tailscale, run `tailscale serve status` first: Funnel is per-port and invisible from inside the app, and a peer address of 127.0.0.1 proves nothing about who is calling. Identity must come from `Tailscale-User-Login` / `Tailscale-Funnel-Request`, and the guard must fail CLOSED on bare loopback.
- 2026-08-16: Tried to prove that same endpoint refused public traffic by fetching it with WebFetch, and got a 200 with a tailnet identity — nearly concluding the funnel path was open. WebFetch egresses from the LOCAL machine (verified: ifconfig.me returned the same public IP as curl here), which is itself on the tailnet, so the request never left the tailnet. → WebFetch is not an off-network vantage point. To test whether something is reachable from the public internet, the request must originate somewhere genuinely off-network — otherwise the test silently proves the opposite of what it appears to.
- 2026-08-17: Wrote a test helper `withVault(fn)` that created a temp vault and tore it down in a `finally`, then passed it an `async` callback. The finally ran the moment the callback first awaited, deleting the fixture mid-test — so `resolveName` read a vault that no longer existed and returned `unresolved`, which looked exactly like the feature being broken. I nearly debugged the wrong thing. → A sync scope-guard cannot wrap an async callback: make the helper `async` and `await fn(...)`, or the teardown races the body. Same shape as any try/finally around an unawaited promise.
- 2026-08-16: scp'd a measurement script INTO the Pi's repo working tree (`backend/scripts/`) to run it, then later committed the same path. The next `git pull --ff-only` refused ("untracked working tree files would be overwritten"), but I had written it as `git pull 2>&1 | tail -1` — a pipeline's exit status is the LAST command, so `tail` succeeded, the `&&` chain carried on, and the Pi cheerfully built, tested and restarted at the PREVIOUS commit. It reported "460 tests" where 463 were expected; only that count gave it away. → Never scp a scratch file into the repo working tree on a deploy target — use `/tmp`. And never pipe a deploy step to `head`/`tail`: it discards the exit status that the `&&` chain depends on. Confirm a deploy with `git log --oneline -1` on the target, not with the pull's own output.
- 2026-08-17: Ran `git add` on the three files I had changed, then `git commit` — and pushed 19 files. The index already held another Claude session's staged SARA-voice work, so `git add` added to that set rather than defining it, and their in-progress work went to origin/main under a commit message describing only my routing fix. Unfixable without a force-push, which is unsafe with concurrent sessions pushing. → Nick runs 2-3 sessions on NEURO at once, so the index is NEVER assumed to be mine: run `git diff --cached --name-only` (or `git status`) AFTER `git add` and BEFORE `git commit`, and prefer `git commit -- <paths>` which commits only those paths regardless of what else is staged. Same rule for `--amend`.
- 2026-08-17: Generated an rclone crypt password and salt for the off-site backup, wrote both into `backend/scripts/backup-offsite-SETUP.md` so Nick would have them, then committed and PUSHED that doc to GitHub. Those two strings decrypt the entire off-site copy of the vault, so committing them undid much of the point of encrypting it. I had put them in the file to be helpful and never re-read what I was staging. Fixed by ROTATING (new password + salt under a v2/ prefix, verified restore, purged the old tree) rather than rewriting history — a force-push cannot recall an existing clone, and two other sessions were live. → Never write a credential into a repo file, not even transiently: hand secrets over in chat and say explicitly that they are not being recorded. Before staging any file you generated, grep it for the secrets you just created. And when a secret does leak, rotate it so the leaked value is worthless — erasing it from history is the weaker fix.
- 2026-08-17: Wired the new 1-2-1 tracker into `scheduler.js` before checking whether that file was one of the other session's ~14 modified/staged files. It was. I caught it only when reviewing what to stage, and had to revert and re-cut the hook at `syncPeopleNotes` instead — which turned out to be the better design anyway (it is the one place the stamps change, so the tracker cannot render dates that are about to move). → With concurrent sessions, check `git status` for collisions BEFORE opening a file to edit it, not before committing. A five-second check at the start would have saved writing the wiring twice — and "route around them" only works if you know which files are theirs before you start typing.
- 2026-08-17: Wrote a test asserting "the cache is keyed on the vault path, so a fixture is never served live data" — but the `withVault` helper it used forces a cache refresh on entry, so the assertion would have passed with no keying at all. It also wrote its fixture AFTER that refresh, so it read a legitimately-empty cache and failed for a reason unrelated to what it claimed to test. → A test whose setup resets the state under test proves nothing. To test a cache, leave it WARM and change the input underneath it; and if a new test fails, check whether the harness (not the code) is what made it fail before touching the implementation.
- 2026-08-17: Scanning every remote branch to confirm the leaked PIN was gone, I ran the search with a value I had invented rather than the real one (I had redacted the PIN out of my own earlier output and never re-read it). Every branch came back "0 files" and I nearly reported the repo clean. → A search for a secret must be run with the ACTUAL secret, and it needs a positive control: search somewhere you KNOW it exists first, and only trust the zeros once the same command has proved it can find a hit. Zeros from an untested pattern are indistinguishable from zeros from a wrong one.
- 2026-08-17: Armed a new scheduled feature (Plaud admin blocks) and immediately triggered its apply route by hand to see it work — while `calendar-sync` was firing the same pass every few minutes. Both planned against an empty ledger and both created, so 27 blocks became **52 real events in Nick's calendar**. The idempotency ledger was written at the END of a run, so neither pass could see the other; the design was safe against repeats but not against concurrency, and I had never asked which. → When a feature's write path can be triggered from two places (a route and a scheduler), the first question is what happens when they overlap, not whether one of them is correct. Take a lock before the plan, not after the writes — and write the idempotency record per item as it succeeds, never batched at the end, or a crash mid-run duplicates everything. In a single Node process better-sqlite3 is synchronous, so a read-modify-write with no `await` between the two IS a real mutex; that is the cheap correct fix, and it is only valid in-process.
- 2026-08-17: Ran the deploy's `npm test` over SSH and got exit 1 with no output, then chased it as a test failure. `npm` is simply not on the non-interactive SSH PATH — the command never ran. → On the Pi, every non-interactive ssh command needing node/npm/pm2 must `export PATH=/home/nickw/.nvm/versions/node/v22.22.2/bin:$PATH` first. An empty failure from a remote build step is a missing binary until proven otherwise; check the log tail before reading anything into the exit code.
- 2026-08-17: Staged 17 files across two shared files with per-hunk precision to keep another session's work out of my commit — then took ~90 seconds building the commit message. In that window the other session ran `git commit` and swept my entire staged index in under THEIR message; worse, my `claude.js` had been staged against the pre-#13 commit, so their own roster fix was silently reverted and the prompt went back to naming someone who had left the business. Caught only because I diffed HEAD afterwards rather than trusting the commit succeeded. → The index is shared mutable state between concurrent sessions, and time spent between `git add` and `git commit` is the window of exposure. Stage and commit in ONE command (`git commit -o <paths> -m ...` or write the message first), and after any commit during parallel work, diff HEAD against what you meant to send. A commit that "worked" is not the same as a commit that contains your changes and nothing else.
- 2026-08-17: Cleaning up 52 duplicate calendar blocks, I keyed the dedupe on identical start+subject, verified "0 duplicate slots" and reported the calendar clean. Nick then sent a screenshot of two visibly duplicated pairs. The competing passes had NOT produced identical events: the second fetched the calendar after the first had created some blocks, saw them as busy and placed its copies in the NEXT free slot, so the duplicates sat minutes apart (11:45 and 11:55) and my key could not see them. → Dedupe on the IDENTITY of the thing, not on an attribute a racing writer could have perturbed — here the meeting, not the slot, because the placement is precisely what the race changes. And when a cleanup has a second independent source of truth available (the ledger of what was deliberately created), check both and refuse to delete when they disagree; the two methods agreeing at 2 and 2 is what made the second pass safe.
- 2026-08-19: Debugging a FAILED email send that Nick explicitly said "DO NOT SEND", I probed `POST /api/email/triage/:id/reply` with the real body and recipients but a "bogus" message id — four characters changed at the tail of a Graph id — reasoning that an invalid id would 404 before anything left the building. Graph message ids in a mailbox differ only in that tail, so the mangled id resolved to a DIFFERENT REAL MESSAGE and the reply went out for real: Nick's drafted text, to the customer plus six CC'd colleagues, hanging off the wrong thread ("RE: QA Daily Digest"). → Never probe a live send path with a payload that would be harmful if it succeeded. A mutated identifier is not a safe identifier — in any keyspace where ids are dense (Graph ids, base64 blobs, sequential rows) a "made up" one is somebody else's real record. Test outbound paths only with a payload that is harmless on success (empty body, no recipients), or not at all: read the logs, read the code, and ask before touching the executor.
- 2026-08-25: Derived `confidence.level` from the score alone, so a context blind to the calendar, location AND presence scored exactly 0.75 and labelled itself `high` — while its own rationale said three inputs were unreadable. Only caught by probing against the live DB, not by the suite. → When a derived LEVEL licenses a downstream power (here: permission to hide work), cap it by COVERAGE, not by the arithmetic. And probe new judgement code against real data before calling it done — a green suite over invented fixtures agrees with whatever you assumed.
- 2026-08-25: `attention.gate` returned `rationale: "Away, so the ranking stands but nothing is spoken."` in the same payload as a populated `speech` and `quiet:false` — the layer built to stop NEURO asserting what it had not checked, asserting what it had not checked. Written as prose beside the logic rather than derived from it, and 18 green tests never compared the two. → When a field EXPLAINS another field, pin the relationship, not just each side. Added an invariant across every branch: speech non-null implies not-quiet and a rationale that does not claim silence.
- 2026-08-26: Investigating the 37-vs-3 urgent email count, I ran `sqlite3 /mnt/data/nuero/agent.db` — a guessed path — and sqlite3 silently CREATED a 0-byte database there rather than erroring. The real DB is `backend/db/agent.db`. Harmless this time, but an empty file at a plausible path is exactly what a later `if (!fs.existsSync(DB_PATH))` guard reads as a valid database. → sqlite3 never fails on a missing file; it creates one. Resolve the path from the code (`DB_PATH` in `db/database.js`) before querying, or open read-only with `file:...?mode=ro`.
- 2026-08-26: Verified triage changes by running `node -e "...runTriage()"` on the Pi against the live DB. Only `server.js` calls `dotenv.config()`, so a standalone process has NO API keys: every cloud provider reported `configured: false`, ai-routing returned "All providers failed or disabled", and 40/40 emails came back unclassified. I nearly read that as a bug in the batching I had just written. Worse, `ai-routing`'s module-level `_usage` counter loads once at init, so those processes also clobbered the running backend's shared daily AI budget several times over. → Verify against the RUNNING server (curl its API), never a second node process against the same DB. A standalone process has a different environment and its own copy of every module-level cache, so it both lies to you and overwrites state the real process owns. (The only reason I caught it: the new code logs loudly when emails get no model answer — the old code silently defaulted them to FYI.)
- 2026-08-27: Wrote the day-planner's task reader against the TASK ROW shape (`id`, `estimate_minutes`) when `task-store.activeTodos()` returns the LEGACY TODO shape (`task_id`, `estimateMinutes`). All 148 open tasks were filtered out and the planner reported "nothing open to schedule" — a perfectly plausible quiet day. Nothing threw, nothing logged, and 23 green unit tests said nothing because they passed their own fixtures in. Caught only by dry-running against the live server. → When consuming a mapper's output, read the mapper (`toTodoShape`), not the table — NEURO deliberately keeps a legacy shape between the task store and its consumers, and the two vocabularies differ by exactly enough to fail silently. Any mapping between shapes should be a small PURE exported function with the wrong-shaped input pinned as a NEGATIVE test, because the failure mode is an empty result that looks like good news.
- 2026-08-27: Attached two fields to a 1-2-1 proposal as `{ ...describe(...), awayCheck, awayNote }` — but `describe()` is ASYNC, and spreading a Promise yields `{}` with no error, no warning and no rejection. The route shipped answering literally `{"awayCheck":"checked","awayNote":null}`: no person, no slot, no attendee. It was convincing precisely because the two fields I had just added were present and correct. → Spreading a promise is silent; before spreading any call result, check whether the function is async (`grep 'async function <name>'`) — especially when the sibling call site already awaits it, which is the tell. Where the failure is syntactic and silent, pin it by asserting the SOURCE (every call site preceded by `await`) rather than the behaviour, and prove the guard by reintroducing the bug and watching it fail.
- 2026-08-27: Added `daysOffFor` to team-availability and called it from one-to-one-booking without adding it to `module.exports` — the third unexported-function bug of the day, an hour after logging the second one. → After writing any new cross-module function, `grep` the exports block before writing the caller. It is two seconds and it has now cost three debugging cycles in one session.
- 2026-08-27: Wrote a new NOVA bridge route returning its payload at the TOP level, when every other bridge route answers `{ok, data:{…}}` and NEURO's `novaBridgeFetch` returns `json.data`. curl showed the route working perfectly; NEURO reported `known:false, "never fetched"` — which reads exactly like an undeployed route and cost Nick a wasted deploy while I hunted branches and remotes. → Read a sibling implementation before writing a new endpoint into an existing family; the convention IS the contract, and here it was three lines away in `neuro-bridge-kpi.ts`.
- 2026-08-27: Then, with the shape fixed, the same call still failed — because `microsoft.novaBridgeFetch` is an internal and is not in `module.exports`, so it was `undefined` and the call threw. My own `catch` logged the exact reason (`... is not a function`) and I never looked: I re-inspected the HTTP response shape instead and concluded NOVA was still wrong, costing a second round trip. → When a call fails inside a try/catch you wrote, READ THE LOG LINE THAT CATCH EMITS before re-examining the far end. And check a helper is exported before calling it across a module boundary — `grep` the exports, do not assume a function used ten times inside a file is available outside it.
- 2026-08-27: Pushed a NOVA change to `origin` (GitHub `Wardy-uk/NOVA`) and told Nick it was ready to deploy. NOVA's `deploy/deploy.ps1` pulls from the **`azdo`** remote (TFS `BYM2020/Core/_git/N.O.V.A`), not origin — so his deploy ran correctly against a branch that did not contain the commit, and the route still 401'd afterwards. It only reached azdo half an hour later when another session happened to push on top of it. I had also assumed the deploy target was `main` because that is the script's default, when in fact production runs `nova-codex` (azdo/main does not even export `bridgeAuth`, yet live serves `/kpi-snapshot`). → In a repo with more than one remote, check which remote and which branch the DEPLOY reads before saying anything is ready to ship: `git remote -v` plus the deploy script's own defaults. Push to that remote, not merely to the one you cloned from. And infer the deployed branch from what production actually serves, not from a script's default parameter.
- 2026-08-27: Probed the new `wip-ms` route with a real `msId` but a HAND-TYPED `lineNumber: 11`, guessing the offset instead of reading it from the API response that was already in front of me. 11 was a different task's row, so Graph updated the huddle task while the vault marker was written onto the 121s row — which sat at 75% and briefly read 50%. Undone only because the next resync regenerates the file from Planner; nothing detected or reported it, and I raised a false alarm about having destroyed real progress before verifying. → When a call takes an id AND a position, never supply the position by hand — read both from the same row of the same response, which is what the real caller does. And when a positional argument identifies a record in a file that some other job regenerates, pass the identity too and make the writer REFUSE on mismatch (`setTaskPercent(..., expectedId)`); the line already carried `<!--id:-->`, so this was checkable rather than assumable all along. Same family as dedupe-on-identity-not-on-a-racing-attribute (2026-08-17).
- 2026-08-27: Built a WIP button that PATCHed Planner `percentComplete = 50`, without checking what the field already held. Four of the five Must Move rows are Planner tasks and two were at **75% and 25%** — the "(75%)" in the task text is not Nick typing progress, it is `syncMicrosoftTasks` rendering Planner's own `percentComplete` into the mirror line. So the button would have REDUCED real progress on a board his team reads, and NEURO could not have noticed because nothing ever parsed that number back. Caught only by curling the deployed lane and looking at the actual rows. → Before writing a field, read what is in it — especially a field on a system you do not own. And when a value is already rendered on screen, ask where it came from: the number was in front of me in his screenshot and I read it as user-entered text. Where a control could destroy information the system did not create, make it an INDICATOR above zero rather than a toggle.
- 2026-08-27: Reused `task-dedupe`'s MIN_SCORE (0.42) to fold duplicate capture_todos, on the reasoning that "the same task written twice" is one question with one answer. It is not: 0.42 was measured on NEURO-vs-Microsoft tasks — two independently worded lists — while capture_todos are all extracted from Nick's own meetings and share his stock vocabulary almost completely. Over the live queue it merged two pairs and BOTH were wrong, the worst hiding a DATED meeting with Naomi inside a form-completion task. → A measured threshold is measured against a CORPUS, not against a concept. Before reusing one on new data, re-run the separation on that data — here the true duplicates sat at 1.000 and the worst false positive at 0.499, so the right number was 0.85, nowhere near the inherited one. And when the two error directions are asymmetric (a missed fold leaves a visible duplicate; a wrong fold hides real work), pick the conservative end of the gap rather than the middle.
- 2026-08-28: Told Nick the Plaud transcript folder was misconfigured ("`Meetings/transcripts` doesn't exist") and flagged it as a bug, without reading `backend/.env` — which deliberately sets `PLAUD_TRANSCRIPT_FOLDER=Plaud/Transcripts`. The code default was simply being overridden, exactly as intended, and all 311 transcripts were correctly placed and fully embedded. I raised an alarm about a working system and had to walk it back a message later. → Before calling a code default wrong, check whether config overrides it. `grep` the `.env` on the box that runs it. The real defect was elsewhere and narrower (two CONSUMERS hardcoded the default), which only became clear once I stopped treating the default itself as the fault.
- 2026-08-28: Fixed `groupPlaudNotes`'s `note_type === 'summary'` comparison (it ignored the quotes `plaud-sync` writes, so it had ALWAYS been false) and shipped it as "no behaviour change with the flag unset" — measured against a probe, not against the deployed code. One minute after the restart the hourly consolidation wrote two notes it had never been able to write before, both mis-dated to today because the identical quote bug also sits in `noteDateParts`. I put junk in Nick's vault while telling him the change was inert. → When you repair a comparison that has always evaluated false, you are not fixing a filter, you are ENABLING a code path that has never run. Ask what starts happening when it becomes true, and check for the same root cause in every other consumer of that value before shipping — the quote bug was in three comparisons and a date parse, and I fixed three of the four. A probe that reimplements the logic proves the logic, not the deployment; verify against what actually runs.
- 2026-08-28: Told Nick "there is not one `[WebPush] Sent:` line in a 41MB log going back to April" and framed push as totally broken. GNU grep prints "binary file matches" and SUPPRESSES the lines for a file containing NUL bytes — the pm2 out log has them — so my zero was grep's, not NEURO's. Re-run with `-a`: 661 sent, 141 suppressed, 13 failed. I had even SEEN "binary file matches" in an earlier command's output and read past it. → `grep -a` on any pm2/rotated log before drawing a conclusion from a zero, and treat "binary file matches" as an error, not noise. My own mistakes.md already says a zero from an untested pattern needs a positive control; I ran the control (`Initialized`) only in the SECOND pass, which is what caught it. Run the control first, in the same command, every time.
- 2026-08-28: Went to deploy and `git pull --ff-only` was refused by another session's uncommitted work on the Pi — three modified tracked files and two untracked ones. The reflex is `reset --hard` (logged as a disaster on 2026-05-31). Checking instead showed `server.js` had NO real diff (320+/320- was pure CRLF), both untracked files were byte-identical to what was already committed in main, and the only unique content was a 3-line cosmetic reformat. → A blocked pull on the Pi is usually a stale hand-copy, not lost work: diff the blockers against `origin/main` with `--ignore-cr-at-eol` and `diff -q` against `git show origin/main:<path>` BEFORE deciding anything is at risk. Then tar to /tmp and `git stash -u` (recoverable by the other session) rather than checkout+rm.
- 2026-08-28: Set `minutesAway: null` on purpose for agenda events on a LATER day ("in 15 hours" reads as imminent), then guarded the renderer with `const mins = Number(e.minutesAway); if (Number.isFinite(mins))`. `Number(null)` is **0** and `Number.isFinite(0)` is **true**, so every Monday meeting rendered a confident **"0m"** countdown — the deliberate "no answer" became the most urgent possible answer. → Test for null/undefined BEFORE coercing, never after: `Number()` maps null to a perfectly valid number and `isFinite` cannot tell the difference. Same family as the shape-mismatch trap — a sentinel only works if every reader checks for it in the form it was written. Caught by the render harness, not by any unit test, because the value was correct on the server and only wrong on screen.
- 2026-08-29: Built `weekly-target.js` while ANOTHER session was building a file of the same name in the same working tree. I used `Write` (which overwrote their uncommitted test file outright) and then `git commit --only <paths>` — which committed whatever was on disk at that moment, i.e. THEIR uncommitted service under MY commit message describing MY implementation. The message claimed `SUM(count)`; the committed code used `COUNT(*)`. My route then called `progress()`/`weekKey()`, which their service does not export, so `/api/wins/target` would have thrown on first call. They spotted it before I did. → `--only <paths>` bounds WHICH files, not WHICH CONTENT: it still commits the current disk state, so it is no protection against a concurrent writer. Before `Write` on any file, check `git status` for it AND re-read it — "the file changed on disk" is a collision, not noise (I saw that notice repeatedly and read past it). And never describe in a commit message code you have not just re-read: I documented my own draft and shipped someone else's.
- 2026-08-29: Wrote `health-daily.sync()` with a window bounded only at the START (`recorded_at >= since`, no upper bound) and drove it from a backfill walking backwards in 30-day chunks. Every chunk therefore read from its start through to the PRESENT, the 20,000-row cap kept the newest rows, and the oldest chunk overwrote two years of days with nulls — it reported "744 rows written" and left 328 of them with any HRV. Nothing threw; the summary line is the only reason I caught it. → A window has TWO ends. Any query a chunked/backfill caller will drive must bound both, or the row cap silently changes the answer instead of refusing (the same species as the calendar `$top=50` and the 1,958-key JQL already in CLAUDE.md). And when backfilling, "is it complete" is a fact about the real clock, not about where the chunk stops — pass `today` separately from the window end.
- 2026-08-29: Ran a script against the LIVE Pi database from a tarball of my working tree, which carried another session's uncommitted `db/database.js`. `init()` runs migrations, so it applied their unshipped `tasks.domain` migration to prod (195 rows stamped) before failing on their half-finished `management_log` rebuild. Additive and rolled back cleanly, so no harm — but I shipped someone else's migration to production as a side effect of reading data. → `db.init()` is a WRITE, not a connection. To read prod, open the database directly (`new Database(path, {readonly: true})`) and drive the pure functions; never route a read through code that migrates. And a tarball of a shared working tree is not "my code" — check `git status` for what else is riding along.
- 2026-08-30: Chained `git commit` behind `npm test 2>&1 | grep -E "^ℹ (pass|fail)"`. grep MATCHED the line reading "fail 1" and therefore exited 0, so the `&&` carried on and I pushed with a red test. Same species as the 2026-08-16 `git pull | tail` deploy, one tool along: a pipeline's exit status describes the LAST command, and a grep that finds the word "fail" is a success. → Never gate a commit or deploy on a grep for output text. Gate on the command's own exit code (`npm test && git commit`), and if you want the summary too, run the test on its own line first and read it.
- 2026-08-30: Wrote a test that compared rendered edge counts between two calls to the widget's field, which seeds itself with `Math.random()` — two independently random layouts, so the margin was luck. Green five runs in a row, red on the sixth, and I only noticed because the standalone run disagreed with `npm test`. → A test over randomised output must average or seed; a single-sample comparison of two random draws is a coin toss. A test that fails one time in ten is worse than no test, because it teaches you to re-run instead of read.
- 2026-08-30: Copied `Field.jsx`'s opacity values into the widget wholesale. They are tuned for a FULL-SCREEN canvas, where a large area of very faint texture still reads as presence; on a 330x350 tile they multiplied out to 8% node / 5% edge opacity on a quiet day, so SARA's presence was drawn and invisible — Nick's words were "we've lost SARA". Second time in this session I moved a number between contexts without re-deriving it (the duty hours were the first). → A constant is calibrated against a SIZE and a GROUND as much as against a concept. When lifting one into a different surface, re-compute what it produces there — and where the value encodes a visual floor, assert the floor, not the number.
- 2026-08-30: spread two result objects into one JSON response where both carried a `failed` key — a count silently overwritten by a list → when merging results, nest them (`{run, ...status}`) or rename; never spread two shapes that share a key name.
- 2026-08-30: Chained a pointless `git -C ../.. stash >/dev/null 2>&1 || true` into a grep command "just in case", on a SHARED working tree carrying my whole Phase 2 build plus another session's uncommitted notion-sync feature. It stashed all 36 files. Recovered fully with `git stash pop`, but only because nothing else touched the tree in between — with 2-3 concurrent sessions that was luck, not care. → Never put a state-changing git command in a shell line whose purpose is to read something. `git stash`, `checkout`, `reset` and `clean` are their own deliberate steps, run alone, after checking `git status` — never a defensive prefix to a grep. The `|| true` was the tell: I was suppressing the outcome of a command I had no reason to run.
- 2026-08-30: Wrote the mobile capture path on top of `capture-store.writeNote`, whose filename is only unique to the SECOND — so two notes written in one tick produced the same path, `writeFileSync` overwrote the first, and the ledger acknowledged BOTH as applied against the same canonical id. A destroyed capture, indistinguishable from a saved one, in the module whose entire job is not losing what Nick typed. My tests covered REPLAY of one note (which the ledger already handled) and never COLLISION between two different ones. → When adding a queue in front of a writer, the new failure mode is not repetition, it is SIMULTANEITY: a drained queue puts every item into the same tick of whatever clock the writer names things with. Test two DIFFERENT items through the batch path, not one item twice. And for uniqueness on the filesystem use the `wx` flag (O_CREAT|O_EXCL, atomic), never `existsSync` then write — that is a race with a gap in the middle.
- 2026-08-30: `Now.tick` queued ONE completion and then read `flush().confirmed` — an aggregate over the whole queue — to decide what to tell Nick. Any older capture landing in the same round trip made it non-zero, so the screen printed "Done" over a completion NEURO had rejected, or had HELD pending a write-up. Same shape in Capture.submit, phrasing its failure from `result.needsAttention`. → When a function batches, its return counts describe the BATCH and can never describe one member. Give the caller a per-item receipt keyed by the id it already holds, and derive the words from that in one shared pure function — a per-item outcome read off an aggregate is always wrong eventually, and it fails in the direction of false reassurance.
- 2026-08-30: Hand-authored a `git apply --cached` patch to stage only my hunks out of a file another session was also editing, counting the `@@ -29,7 +29,8 @@` line numbers myself. Got the old-line count wrong by one and git refused with "corrupt patch at line 12", which reads like a content problem and is not. → Never hand-write a hunk header. To stage a subset of a shared file, write the file content you want, `git add` it, then restore the full file from a copy — the index keeps the snapshot and the working tree keeps the other session's work. Faster than the patch, and it cannot be off by one.
- 2026-08-30: Wrote a test asserting `quietHoursSource === 'setting'` in a suite where nothing had ever set an explicit quiet-hours window, so it read 'server' and failed. The CODE was right; my test never established the precondition it was asserting about. Same family as the 2026-08-17 cache test whose setup reset the state under test. → When a new test fails, check whether the harness established the precondition before touching the implementation — and prefer a test that exercises BOTH directions (inherited, then set, then cleared), which is what the fixed version does and what makes the assertion mean something.
- 2026-08-30: Ported the phone's Field to the Pi kiosk and shipped it on a green build. The screenshot of the actual DSI panel showed the field drawn and NOT ONE WORD of SARA's: `min-height: 100vh` is right on the phone (the Surface owns the viewport) and wrong on the kiosk, where the view sits inside a scrolling `.app__view` below a banner — so the section overflowed and everything anchored to the bottom sat below the fold. The same pass also had no case for `provenance: 'mixed'` (CLAUDE.md claims "exactly four provenances"; the ROLL-UP has five), so a partly-good read would have rendered as "I can't see the brain", and no branch at all for a live read with no headline, which could show a field and no text — indistinguishable from a broken screen. → A build proves a component COMPILES, never that it renders. For any screen on hardware you cannot see, take a screenshot (`grim` on the Pi) and LOOK at it before calling it shipped — three real bugs, none of which any test or build would ever have caught. And when porting a layout between surfaces, the container is part of the design: re-derive sizing against the new ground, exactly as with the widget's opacity constants.
- 2026-08-30: Fixing the "standup already done" bug, I wrote a guard test asserting the source did not contain the empty-checkbox regex — a regex ABOUT a regex. It failed while every standalone evaluation of the same expression passed, and I burned four tool calls debugging the assertion instead of the code. Rewrote it as a blanket ban on the `inFocus` scan, which then failed for a REAL reason I had not known: `routes/standup.js` walks Focus Today three more times, for carry-over items and the EOD context, and those answer a different question perfectly legitimately. My "fix" would have forced three correct pieces of code to be rewritten to satisfy a test. → A source-scanning assertion must be scoped to the DECISION it protects, not to the mechanism that happens to implement it: here, the done-flag assignment, never the act of reading the section. And when a test disagrees with a standalone evaluation of its own expression, stop debugging the expression and go and look at what else in the file matches — the test was right about the file both times and I was wrong about the file both times.
- 2026-08-31: Modelled HA's duplicate-device suffix as part of the device prefix (`sensor.nicks_iphone_2_battery_level`) and wrote the fixtures to match — 7 green tests over an entity shape that exists nowhere. Real shape is a suffix on the whole entity id (`sensor.nicks_iphone_battery_level_2`). → When a fix turns on an external system's identifier, copy the identifiers VERBATIM from a live dump into the fixtures; never hand-write them from a mental model. Same species as #122's `sleep_core_hours` and the `meeting_alert` push type.
- 2026-08-31: used `git add -A` while another session was mid-edit and swept their backend/services/ha.js into my commit → stage explicit paths (`git add <file> ...`) whenever a concurrent session is active; `git status` first is not enough, the add has to be narrow too.
- 2026-08-31: Deploying to the Pi I ran `npm install --omit=dev` in the workspace root, which PRUNED vite (a devDependency) and made the very next step, `frontend/npm run build`, fail with exit 127 — a "command not found" that looks like a broken toolchain rather than something I had just deleted. → The Pi BUILDS as well as runs, so its install must be a plain `npm install`; `--omit=dev` is for a runtime-only host and this is not one. The documented deploy sequence (pull → build → test → restart) never mentions an install step precisely because the workspace already has its dev deps — don't invent one.
- 2026-08-31: chained `npm test && git commit && git push && deploy` in one command, so a failing suite (1833/1834) scrolled past and I deployed on red — the re-run was green, so it was flaky, but I did not know that at push time → never chain the test run into the push. Read the result, THEN commit.
- 2026-08-31: Passed WATCH_IRK via `sudo env WATCH_IRK=... python ...` when probing the watch sensor → the Apple Watch IRK appeared in `ps` output on pi5 and in the session transcript. Secrets go in a root-only EnvironmentFile (as watch-presence.service already did), never on a command line — argv is world-readable via /proc.
- 2026-08-31: Mounted the phone's shell in the kiosk while the kiosk's App.css still defined .app__nav as a 248px sidebar for the retired ViewSwitcher → two files defining one class let the cascade decide, and the dead rule won on the real panel. Delete the dead definition, don't out-specify it.
- 2026-08-31: Assumed killing chromium picks up a new build → the PWA service worker paints its precached shell first, so two screenshots showed a pre-30-Aug UI over a correct server. Verify a kiosk deploy by comparing the asset hash the SERVER serves against the one the page requests, and re-grab after the SW updates.
- 2026-08-31: Routing `/api/vault/search/temporal` through the fused `searchWithHealth()` turned its date range into a RANKING HINT rather than a bound - keyword and semantic arms never saw the window, so a query for last week returned a March note ranked above the real ones. Caught only because I wrote a test for the empty-range case. -> When you move a bespoke endpoint onto a shared pipeline, list the FILTERS it used to apply and re-assert each one; fusion preserves ranking signals, not guarantees.
- 2026-08-31: Deployed a bedroom-light automation live to Home Assistant the moment I had entity ids, on n=2 correlation for the motion sensor and without asking whose room it was → it fired two minutes later and turned on a light in a SHARED room. Then I misread my own side effect as evidence a person was in there. → A home is not a dev environment: an automation that acts on shared space gets deployed DISABLED until occupancy semantics are confirmed, and before inferring anything from a state change, check the logbook for whether I caused it.
- 2026-09-01: Added a `useEffect` whose dependency array named `fullData`, and placed it ~25 lines ABOVE that `const` in the same component. A dependency array is evaluated DURING render, so this is a temporal-dead-zone ReferenceError on every single render of TodoPanel — and `vite build` passed clean, because it compiles perfectly. Found by reading the diff, not by any tool. → A build proves compilation, never a render (already logged 2026-08-30 for the Pi kiosk; this is the same lesson one layer in). Hooks go BELOW everything they reference, and for any React change worth shipping, actually mount the component — `esbuild` + `renderToString` with stubbed transport takes two minutes and executes every hook call and dependency array. That harness is now `backend/services/desktop-render.test.js`, and writing it immediately turned up two more live bugs nobody had noticed: a banner that stated "couldn't find that one" during the load, and `const loading = todos === null` where `todos` is built with `(x?.todos || []).map()` and so is never null — a dead condition that made an unloaded task list render "No open todos. Rare."
- 2026-09-01: Edited a JS string containing `\n` with a python `io.open(p,'w')` text-mode round-trip. Reading with universal newlines turned the file's `\r\n` into `\n`, my match succeeded, and the write translated it back — but the literal two-character `\n` I intended in the JS source came out as a REAL line break inside a template literal. It still worked (a newline in a template literal is a newline) so nothing failed; I only caught it reading the diff, then "fixed" it twice more with the same broken technique while the assert kept passing. → For byte-level edits to a mixed-line-ending file, always `newline=''` on BOTH the read and the write; and when a replacement asserts true but the file looks unchanged, dump `repr()` of the surrounding bytes rather than re-running the same edit.
- 2026-09-01: Set the triage fetch cap to 500 by guessing, and the live 14-day inbox holds 663 — a truncated read reports complete:false, which silently disabled the departure rule it was meant to enable. → Measure the population BEFORE picking a bound; a cap that rewrites the answer is worse than no cap.
- 2026-09-01: Used PowerShell here-string syntax (@'...'@) for a git commit message in the Bash tool → bash passed the @ markers through as message lines, so the subject became "@". Use a heredoc for multi-line strings in Bash; @'...'@ is PowerShell-only.
- 2026-09-01: The task-origin backfill skipped closed tasks by default ("history changes no report") → the weekly report read 0 commitments closed in a week Nick closed 43. A "closed last week" figure IS history; classify everything a report reads, and name any remainder the split leaves behind.
- 2026-09-01: A task row was keyed on `todo.id`, a POSITIONAL index, while listTaskRows orders by due_date → setting a due date re-sorted the list, shifted every index, and the expanded row collapsed or a neighbour opened. Reported as "I cannot edit the due date". → Key a row on a real identity (task_id), never on its position in a list that reorders.
- 2026-09-01: An <input type="date"> patched straight from onChange, and a partially typed date reports an empty value → a due_date:null write plus a full refetch on every keystroke. → Treat a picker as a DRAFT; commit only a complete value, and keep clearing an explicit act.
- 2026-09-02: Ran a blanket string replace over a test file to swap `db.setState(STATE_KEY,'')` for a `reset()` helper that also cleared the table — and it hit the one call site inside "a partial buffer does not clobber a full day", whose whole purpose is to clear ONLY the buffer and leave the stored row standing. The test then failed for the opposite of its own reason, and the failure looked like the overwrite guard was broken when the guard was the only thing working correctly. → A blanket replace across a test file is a semantic change, not a mechanical one: the same statement means different things in setup and in the middle of a scenario. Replace call sites one at a time, or scope the replace to the lines above the first `assert`.
- 2026-09-02: Wrote a multi-line JS block into a bash quoted heredoc (`<<'EOF'`) whose content was full of backticks from doc comments; it died with "unexpected EOF while looking for matching quote" and cost two rounds before I stopped fighting it. → For file content containing backticks, `$`, or mixed quotes, use the Write tool and splice with a script file — never a heredoc inside a `bash -c` layer. Same family as the 2026-09-01 PowerShell here-string entry: the quoting layer you cannot see is the one that breaks.
- 2026-09-02: Diagnosed the Pi 4 kiosk pegged at 100% as the SHELL Field animating under the lock overlay, fixed that, deployed — and it was still at 23% renderer + 23% GPU, because `DEFAULT_TAB` is `surface` and the shell guard `active !== 'surface'` had ALREADY excluded it; the field actually running was `AttentionSurface`'s own, which no guard in App.jsx reaches. The fix was correct code for a case that was not the observed one. -> When a fix targets a measured symptom, RE-MEASURE on the target after deploying and before reporting it fixed. A plausible mechanism that matches the numbers is not the same as the mechanism, and "my change should have fixed it" is not evidence it did.
- 2026-09-02: Wrote JS regexes into a python heredoc; `` arrived in the file as a literal 0x08 backspace byte and the test file died with "Invalid regular expression". Cost two rounds. -> Same family as the widget-source no-backslashes rule and the backtick heredoc entry above: do not put backslashes through a heredoc + python-string double layer. Prefer `includes()` substring assertions over regex in source-scan tests, which is also what the rest of kiosk-views.test.js does.
- 2026-09-03: Deleted a range of AdminPanel.jsx by matching two function boundaries, assuming only the intended component sat between them — RescueTimeCard did too, and Settings threw "RescueTimeCard is not defined". The check afterwards grepped the diff for '^-function ' and missed it because it was 'export function', so the grep confirmed the wrong thing → never delete by boundary without first listing EVERY declaration in the range (regex must allow 'export'), and run desktop-render.test.js before deploying any AdminPanel change; it already pins that card.
- 2026-09-04: Building the "Not relevant" sender mute, I needed Nick's own address for the self-mute guard and wrote `process.env.MS_USER_EMAIL || process.env.NEURO_USER_EMAIL` — both INVENTED. Neither exists anywhere in the repo, so the guard would have read undefined for ever and silently never fired, while a green test suite (which set the env var itself) proved it worked. Third instance of the documented species: `sleep_core_hours`, `meeting_alert`, now this. -> Never reach for an env var or metric name from memory. `grep -rn` the identifier FIRST; if it has no writer, find the real source (here `microsoft.getSignedInAddress()`, from the MSAL cache) and pass it in rather than inventing a name to read it from. A wrong identifier returns empty rather than throwing, which is why only a live check finds it.
- 2026-09-04: Registered `GET /triage/muted` at the bottom of routes/email-triage.js, below `GET /triage/:emailId` — the exact #70 bug the file's own comments warn about, where "feedback" was parsed as an email id. Caught by reading the registration order afterwards, not by the suite. -> On any router with a `:param` route, a new LITERAL path goes above it, and the routing test must be mutation-checked by moving it back and watching it fail. Doing that here turned 3 passing tests into 2 failures, which is the only proof the test tests anything.
- 2026-09-04: Retiring the n8n 1-2-1 review, I deleted PeopleBoard.jsx from `function ApprovalPanel` up to the next landmark comment — and the range also held latest121, cadenceDays, isoDateOrNull, addDays, RecentOneToOnes, both booking dialogs and NoteEditor. `vite build` PASSED (esbuild does not resolve identifiers), so only a source-scan test in the backend suite caught it. Second instance in two days of the same boundary-delete mistake. -> Deleting by two boundaries is only safe after listing every declaration in the range; and a green frontend build proves nothing about a deleted function, so run the backend suite (which source-scans the JSX) before believing a JSX deletion.
- 2026-09-04: Finished a two-repo change (NOVA bridge + NEURO consumer), reported it complete, and left the NOVA half UNCOMMITTED — Nick then deployed NOVA and got a build without it. deploy.ps1 pulls from the azdo remote, so an uncommitted working tree is invisible to it, and the deploy succeeds while shipping nothing. -> When a change spans two repos, commit BOTH before reporting, and name the deploy order explicitly. "Implemented but not committed" reads as done to anyone about to deploy; the guard that saved it (refusing a bridge that predates the field) is not a substitute for the commit.

- 2026-09-04: Ran only the touched test files after changing TodoPanel.jsx, called it green, and the Pi's `npm test` then failed on `services/todo-row-identity.test.js` — a source-scan guard that lives in the BACKEND suite and reads the frontend source. Third time in three days that a backend source-scan test was the only thing watching a JSX change. -> Run the full backend `npm test` before deploying any frontend change, not just the suites whose names match what was touched. Also: the guard was a COUNT of onPatch call sites, which fails on a perfectly safe change (a second component rendering the same card) and invites the next person to bump the number — pin the SHAPE of each call site instead, so it still fails on a write-on-click and stays quiet on a new caller.
- 2026-09-04: Shipped `jiraKey` on task rows, pinned it with service tests that passed, deployed it — and it reached the client as `undefined`, because `/api/todos` and `/api/todos/focus` map their responses through explicit whitelists and a field not listed is dropped in silence. The route's own comment warns about exactly this ("a FOURTH whitelist... the way estimateMinutes vanished") and I added the field two lines above it without reading it. Every assertion I wrote sat a layer BELOW the drop, so a green suite proved only that the service was right. -> When adding a field a screen will read, assert it over REAL HTTP on every route that renders that card, not on the service that produces it. `grep -n "task_id: t.task_id"` finds the whitelists; there are two for task cards. Same species as the "green suite says nothing about routing" rule, one field along.
- 2026-09-11: Typed the Watch IRK into an Android sensor app with a chain of `adb shell input tap/text` calls and no check between them. The P30 lite dropped its ADB link mid-script (Bluetooth coming on), the tap that should have moved focus never landed, and the key went into the VISIBLE Room field, which my own read-back then printed into the chat. SwiftKey (the only IME) also dropped characters from `input text`. -> Never deliver a secret through screen automation. Provision devices with the sensor app's DUMP-protected broadcast (`am broadcast -a uk.nickward.sara.sensor.PROVISION ... --es irk`), and never print EditText contents in a UI dump without masking.
