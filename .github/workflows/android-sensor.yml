name: android-sensor

# Builds the SAiM room sensor APK for the study tablet (saim/android-sensor).
# Built here rather than locally: the Windows box is ARM64, where the Android build
# tools are awkward, and nothing else in the estate needs an Android SDK installed.
#
# Every green build on main is published to ONE rolling pre-release,
# `android-sensor-latest`, so the tablet can download it straight from its browser:
#   https://github.com/Wardy-uk/nuero/releases/download/android-sensor-latest/saim-sensor.apk
# The APK carries no secrets — the Watch IRK is typed into the tablet, never built in.
on:
  push:
    branches: [main]
    paths:
      - 'saim/android-sensor/**'
      - '.github/workflows/android-sensor.yml'
  workflow_dispatch:

permissions:
  contents: write   # to update the rolling release

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Decode the signing key
        env:
          KEYSTORE_B64: ${{ secrets.SAIM_SENSOR_KEYSTORE_B64 }}
        run: |
          if [ -n "$KEYSTORE_B64" ]; then
            echo "$KEYSTORE_B64" | base64 -d > "$RUNNER_TEMP/saim-sensor.p12"
            echo "SAIM_SENSOR_KEYSTORE=$RUNNER_TEMP/saim-sensor.p12" >> "$GITHUB_ENV"
          else
            echo "::warning::SAIM_SENSOR_KEYSTORE_B64 is not set - signing with a throwaway debug key. It will install, but every update needs an uninstall first (and loses the settings)."
          fi

      - name: Test and build
        working-directory: saim/android-sensor
        env:
          SAIM_SENSOR_KEYSTORE_PASSWORD: ${{ secrets.SAIM_SENSOR_KEYSTORE_PASSWORD }}
        run: gradle --no-daemon :app:testReleaseUnitTest :app:assembleRelease

      - name: Name the APK
        run: cp saim/android-sensor/app/build/outputs/apk/release/app-release.apk saim-sensor.apk

      - uses: actions/upload-artifact@v4
        with:
          name: saim-sensor-apk
          path: saim-sensor.apk

      - name: Publish to the rolling release
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view android-sensor-latest >/dev/null 2>&1 || \
            gh release create android-sensor-latest --prerelease \
              --title "SAiM room sensor (latest)" \
              --notes "Rolling build of saim/android-sensor. Install on the study tablet; see saim/android-sensor/README.md."
          gh release upload android-sensor-latest saim-sensor.apk --clobber
