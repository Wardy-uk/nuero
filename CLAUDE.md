# NEURO — Nick's Executive Utility & Reasoning Orchestrator

Personal AI-powered productivity system. Aggregates Jira queue, Obsidian vault, Microsoft 365 (calendar/tasks/email), Strava, location tracking, and AI chat into a single PWA. Runs on Raspberry Pi 5 (16GB) via Tailscale, installed as PWA on iPhone.

## NEURO vs SARA — the division of labour (Nick, 15 Aug 2026)
**NEURO is the brain**, and the NEURO app is Nick's *direct* access to it when he goes looking. **SARA is the J.A.R.V.I.S. layer**: she uses NEURO's brain and **comes to him** — prompts, pushes, helps — and is the visual/audio route for interacting with the brain conversationally rather than by navigating a UI. Neither replaces the other, and **nothing in `sara/` is scaffolding to retire.** Practical consequences: `sara/app` (phone PWA) already talks DIRECTLY to the NEURO backend and never touches `sara/backend` — it is a NEURO client, so there is nothing to "fold in". `sara/backend` (:3005) exists to serve the **Pi 4 desk kiosk** (`sara/frontend`), which is the *ambient* half of SARA. Judge any SARA change by whether it helps her come to Nick; judge any NEURO change by whether it makes the brain better or the direct access clearer.

## ⏳ PARKED DIRECTION — one interface, and it is the chat (Nick, 27 Aug 2026)
Nick's stated longer-term intent: **"maybe the longer term plan should be that there is only one interface — the chat — and we do everything there."** Explicitly parked ("let's pick that up later"), so **do not start migrating screens.** It is recorded because it is the tie-breaker for design decisions taken in the meantime: where two options are otherwise close, prefer the one that makes a surface feel like *talking to SARA* over the one that adds another panel or another tab. It is the same instinct as the Surface refocus below, one level up — and it is corroborated by the measurements taken on 27 Aug (26 flat tabs, `pi-health` the second most-opened screen, `tab_open` falling 266 → 89 → 17 across three weeks). Adding SARA's identity to the standup/EOD conversation was the first step *towards* it and cost nothing if it never happens; retiring a working screen is not, and needs his say-so.

## SARA has no menus (25 Aug 2026) — the refocus
Nick's restatement of the ecosystem: **NEURO collates and directs; the NEURO app is his direct, day-to-day interface to it; SARA is the J.A.R.V.I.S. layer — voice, ears and eyes — and should NOT be a menu.** She surfaces what he needs based on context, rather than waiting to be navigated. Both SARA surfaces are therefore becoming renderers of ONE feed. Phase 1 is built (see the attention pattern below); Phase 2 (phone) and Phase 3 (kiosk) are not.

⚠ **`sara/backend/src/state/inference.js` is the thing to retire.** It had grown an activity enum, a confidence model and a recommended-view map — a second brain — assembled from **seeded** inputs (`state/seed.js`), so it was a second opinion about data that did not exist. Its rules and its honesty are ported into `backend/services/context-state.js` against real inputs; its four-domain contract is NOT ported, because that shape was invented for the seed. `sara/backend` keeps its TRANSPORT half (HA telemetry bus, presence lock, proxy) and loses its decision half.

## SARA stores nothing, and never invents Nick's day (30 Aug 2026)
Three things, all in `sara/backend`, all fixed together because they were one failure wearing three hats: **SARA looked like it was working when it was not.**

⚠ **The kiosk's capture had never worked.** `sara/frontend` has always POSTed to `/api/capture/note` and `/api/capture/todo`, and `sara/backend/server.js` **never mounted a capture route** — every capture made at the desk kiosk 404'd. `src/integrations/neuroCapture.js` + `src/routes/capture.js` are the bridge, and the shape is the point: it **forwards to NEURO's canonical capture routes and stores nothing** (no SARA table, no queue, no local copy — a capture is a NEURO capture or it is not a capture), and **`saved` is true ONLY when NEURO acknowledged it**. Acknowledgement is `success: true`, **not** `res.ok` — a 200 carrying anything else (a proxy error page, a login redirect, a half-written handler) is not an acknowledgement, and treating it as one is precisely how a lost capture gets reported as a saved one. Six named failure reasons (`not-configured` 503 / `unreachable` 504 / `timeout` 504 / `unauthorized` 502 / `rejected` upstream-4xx / `upstream-error` 502), each `saved:false`; an unconfigured bridge **refuses before the network**, because "we were never told where NEURO is" needs a different fix from "NEURO is down". The Capture screen prints `NOT saved — <reason>` and **leaves the words in the box** — clearing it on a failure destroys the one remaining copy of the thought, which is what capture exists to prevent. **Deliberately no offline write queue** (Phase 1 non-goal): a queue that silently holds writes is another way for SARA to become a store. The bridge is a **named door, not an open proxy** — a kind not in the table is refused locally rather than passed through. Nothing logs the captured text or a credential.

⚠ **The State Engine fell back to `seed.js` on every outage.** `stateEngine.js` returned hardcoded content whenever NEURO was unreachable — two invented breaching tickets, an invented probation review, *"Nathan has gone quiet"* against a real colleague, and a specific claim about where Nick physically was ("Office — Little Eaton since 08:40"). A dead feed rendered as a plausible, populated, entirely fictional day, indistinguishable from a working one. `frontend/src/state/presentation.js` held a second copy of the same fiction for when `/api/state` itself failed. Now `src/state/provenance.js` defines four PER-DOMAIN provenances — `neuro` / `neuro-stale` / `unavailable` / `demo` — carried per domain AND rolled up into `model.provenance` — ⚠ **the ROLL-UP has a FIFTH value, `mixed`** ("partly live"), which is what the live kiosk is usually in; a consumer switching on provenance with only four cases falls through and renders a partly-good read as total blindness (caught on the Pi panel, 30 Aug 2026), with `frontend/src/components/ConnectionStatus.jsx` as the banner (**silent when live**, or a permanent green badge is one nobody reads by week two). Two rules do the work: an unread domain is **structurally empty and null, never 0** (`open: null`, so no screen can read a fact out of it), and the briefing **never says "Queue is calm" over a queue it could not see** — it says it cannot see it and that this is not an all-clear. `seed.js` survives for **demo only**: reachable solely under `SARA_DEMO_MODE=true`, stamped `demo` end to end, and **refused outright under `NODE_ENV=production`**. Pinned by a negative test asserting no seeded name can appear in production state. Also: a failed poll now **degrades to `stale` keeping the last good payload** (with `ageMs`) rather than blanking, for up to `MAX_STALE_MS` (15 min) — but **losing the configuration is not an outage** and drops straight to `not-configured`, because data whose origin is no longer configured has no provenance left. A **stale connection outranks the domain roll-up** for `provenance.state`, or "NEURO stopped answering four minutes ago" hides behind "partly live".

⚠ **The NEURO dependency was implicit.** `neuroSnapshot.js` and `neuroChat.js` each carried `https://nuero.nickward.co.uk` as a **default base URL**, so an unconfigured SARA silently reached a public host over the open internet *and reported itself available*. There is **no default any more**: `src/integrations/neuroConfig.js` is the one place that knows where NEURO is and how it authenticates (`NEURO_API_TOKEN` preferred over `NEURO_PIN` — SARA is a machine client), it is **validated at startup** and exposed non-sensitively on `GET /api/health` under `neuro` (`configured` / `ready` / `baseUrl` / `credentialConfigured` / `credentialKind` / `problems`), and it reports **whether** a credential is set, never what it is. It **never refuses to boot** on a bad config — the kiosk is also where a PIN gets entered, so a SARA that will not start cannot be repaired from the device in front of you. ⚠ Two consequences worth knowing: **a deployed `.env` without `NEURO_BASE_URL` now reports not-configured instead of silently working**, and NEURO's **`/api/queue/summary` no longer exists** (the Jira queue was deleted in July 2026), so it is marked `optional` in the snapshot — a permanently-404ing endpoint counted against reach would pin the snapshot at "partial" for ever and make the word meaningless. Pinned by `capture.test.js` (17, incl. real HTTP wiring — the original bug WAS a routing bug), `neuroSnapshot.test.js` (7) and the rewritten `stateEngine.test.js`.

## NEURO vs VANTAGE — the division of labour (3 Sep 2026)
**NEURO is INBOUND and PERSONAL. VANTAGE is DEPARTMENTAL and DIRECTIVE.** NEURO collects what arrives at Nick — meetings, email, tickets, the vault, his body, his diary — and answers *what do I owe, and what should I do next*. VANTAGE looks at the SERVICE DESK and answers *what is going wrong, and what should we do about it*; it is also his coaching layer. Both deal in tasks, which is exactly why the boundary needs writing down: it is a **more** confusable boundary than NEURO-vs-SARA, and the two repos are `Wardy-uk/nuero` and `Wardy-uk/vantage` (working directory `Service Desk Continual Improvement` — three names for one thing).

⚠ **The department services in this repo are HOSTED here, not OWNED here.** `weekly-risk`, `management-log`, `overtime`, `team-health`, `development-plan`, `evidence-register`, `knowledge-gaps` and `nova-121-*` are all departmental by subject; they live in NEURO because NEURO existed first. That is a fact about history, not a decision about ownership, and it is the thing most likely to be mistaken for one. **A new departmental feature goes to VANTAGE unless there is a reason written down.**

**How they talk.** VANTAGE → NEURO over HTTP, and it is a CLOSED SET of **five writes** (`neuro.js`'s header is the register of them, and its count is load-bearing): create a task, queue a suggestion, score a dedupe match, link a task to Microsoft, put a line on the weekly report. Everything else it does is a GET. NEURO → VANTAGE is one direct SQLite read (below). Nothing else connects them.

**The criticality handoff (3 Sep 2026).** `vantage/backend/services/criticality.js` is the single place that decides whether a VANTAGE finding becomes a NEURO **task** (`direct`) or a **pending action** in the approval queue (`pending`). It errs towards `pending`: a wrong `direct` puts unasked-for work in the list Nick uses to decide what to do next, which is the failure that got auto-promote removed. NEURO's half is `POST /api/actions` (intake), `criticality` on `POST /api/tasks` (**provenance only — NEURO stores the claim and never re-derives it**), the `vantage_suggestion` presenter case and its executor. `VANTAGE_AUTO_PUSH_ENABLED` gates VANTAGE's own timer, which writes unattended and only ever on the `direct` route.

⚠ **`services/estate-cost.js` reads VANTAGE's SQLite file DIRECTLY**, on the Pi, outside the HTTP contract above — the only place either system reaches into the other's storage. It depends on VANTAGE's `docs` table (`docs.collection`, `docs.json`), which is a JSON-document store rather than a modelled schema, and it reads the `llm_calls` collection for the AI spend panel. That shape is VANTAGE's to change, so **changing it is a decision that breaks NEURO's estate panel silently** — the read degrades to "no data", which looks exactly like a quiet week. The live database is **`/mnt/data/vantage-data/vantage.db`** — which is `estate-cost.js`'s compiled-in default and needs no `VANTAGE_DB_PATH` on the Pi. ⚠ There is a **decoy**: `/mnt/data/vantage/backend/data/vantage.db` is a stale 16KB file created by VANTAGE's own `db.js` default (`__dirname/data`), and querying it returns an empty `docs` table — which reads as "VANTAGE has no findings" rather than as "wrong file". Checked 3 Sep 2026, after being caught by exactly that.

**Vocabulary, because four things are called some variant of "action".** VANTAGE has **findings** (something spotted, with a severity and a tense) and **plan actions** (items from the Support Review improvement plan). NEURO has **tasks** (the `tasks` table, the source of truth), **action items** (`action-items.js` — checkboxes in vault notes), **action candidates** (`action-candidates.js` — commitments extracted from meeting notes, pending review as `capture_todo`) and **actions** (`sara_actions`, the approval queue). A finding is not a task until something routes it into one.

## Tech Stack
- **Backend:** Express 4 + Node.js (CommonJS, `require()` — NOT ESM)
- **Frontend:** React 19 + CSS modules (per-component `.css` files) + Vite
- **Database:** better-sqlite3 (native SQLite, WAL). Writes commit immediately — no flush step, and external scripts can read while the server runs.
- **Auth:** PIN-based (`X-NEURO-PIN` header) + API token for machine clients (n8n). ⚠ **The PIN was ROTATED on 17 Aug 2026 (#123)** — the old value had been committed in plaintext to `docs/sara-watch-siri-mvp.md` since 15 July, and the repo is PUBLIC. Never write the PIN into a tracked file, a doc, a test fixture or a commit message; read it from `backend/.env`. Rotating it breaks every client that sends it (Watch/Siri shortcut, both PWAs, the MCP server's `NEURO_PIN`, any n8n flow not using the API token), so a rotation is not done until those are updated.
- **AI:** Anthropic SDK (Claude) + Ollama (local models on Pi) with AI routing layer
- **External:** Jira REST, Microsoft Graph (MSAL device code), Strava OAuth, OwnTracks, Obsidian vault filesystem, n8n webhooks, web-push notifications
- **Monorepo:** npm workspaces — `backend/`, `frontend/`, `worker/`
- **Worker:** RETIRED 2026-08-15 (`PI4_WORKER_ENABLED=false`). The Pi 4 managed 12 tokens/sec prompt processing on the SMALLEST model available, so a ~3,700-token triage payload needed five minutes just to read the prompt. Not a model-sizing problem — a hardware ceiling. Code and config are left in place so re-enabling is one env var. Background tasks now route: heavy prose work (email_triage, transcript_processing) to cloud, light work (import_classification, journal_prompts) to Pi 5 Ollama.

## Project Commands
```bash
npm run dev              # API (3001) + Vite concurrently
npm run dev:backend      # nodemon server.js
npm run dev:frontend     # vite
```

## Architecture

### Backend (`backend/`)
- `server.js` — Express bootstrap, PIN auth middleware, route wiring, SPA fallback (262 lines)
- `db/database.js` — sql.js init, state KV store, query helpers
- `db/schema.sql` — Full schema (conversations, jira cache, todos, calendar, inbox, vault embeddings, entities, do-next, location visits, task MoSCoW, nudges, SARA actions, daily summary)
- `routes/*.js` — ~40 Express route modules (CommonJS `module.exports`), incl. `vault-hygiene` (lint / contextual-link / alias-suggest)
- `services/*.js` — ~45 business logic modules including AI pipelines, vault access, Jira sync, Microsoft Graph, scheduling, and `vault-hygiene` (deterministic vault-graph maintenance)

### Frontend (`frontend/src/`)
- `App.jsx` — Main SPA shell, tab navigation
- `components/*.jsx` — ~35 view components, each with matching `.css` file
- `api.js` — API client with PIN header injection
- `cacheStore.js` + `useCachedFetch.js` — IndexedDB offline caching (idb library)
- `usePushNotifications.js` — Web push subscription management
- `voiceUtils.js` — Voice input utilities

### SARA mobile (`sara/app/`)
- The PWA on Nick's phone. **The root view is `views/Surface.jsx` (25 Aug 2026) and the tab strip is no longer navigation** — it renders `GET /api/attention`: one thing, in the context the brain decided it in. The other nine views are still all here, but as places the brain routes TO. ⚠ **The strip is HIDDEN, not deleted** — `navOpen` reveals it via "Show me everything", and it stays revealed while Nick is off the Surface, because the one screen with no menu must not also be the only way back. Nick's failure mode is avoidance: a thing he cannot find is worse than a menu he does not need, and an ambient surface that is sometimes wrong must always have a way round it. `.app__nav[hidden]{display:none}` is load-bearing — the rule above it is `display:flex`, which beats the bare `hidden` attribute, so without it the escape hatch renders permanently open. Three renderings the Surface must keep distinct, because conflating them is how a broken feed comes to look like a good day: **pool unavailable** ("I can't see your work — this isn't an all-clear"), **quiet** (in a meeting / day off), and **genuinely nothing pending**. It renders `context.unknowns`, `gaps` and `dropped` rather than swallowing them — held is not lost, and "couldn't look" is not "nothing there". Speech uses the brain's pre-composed `speech` line and its `quiet` flag; the phrasing is NOT rebuilt client-side, since three surfaces saying the same thing differently is how they drift. Card→tab routing reuses `resolveSaraLiteTab`, so a card and the notification for the same thing cannot land on different tabs.
- Seven tabs became nine (#26), then ten with the Surface.
- `views/Tasks.jsx` is the browsable task list — before it, capture was one-way on mobile and the only way to tick something off was to arrive via a notification. Reads `/api/todos/focus` so its ranking matches Focus and the rest of NEURO.
- `completeTask.js` is the ONE place that knows how to close a task, shared with `NotificationActionCard`. Owner order is `task_id` → `ms_id` → `filePath`+`lineNumber`; getting it wrong is silent (a null `filePath` just 400s and the task stays open).
- Tab ids must also exist in `SARA_LITE_TABS` in `shared/action-surfaces.cjs`, or notification routing falls back to Focus.
- **Voice loop (15 Aug 2026, #90)** — `sara/app/src/voiceUtils.js` started as a verbatim copy of `frontend/src/voiceUtils.js` and has since **diverged, deliberately**: the shared version was only ever proven on desktop, and three things it does are wrong on iOS. (1) The gesture unlock spoke an EMPTY utterance — iOS drops a zero-length one without queueing it, so the unlock silently no-ops and every later `speak()` is ignored; it now speaks a single space at volume 0, and `unlockAudio` is exported so the 🔊 tap can unlock explicitly rather than relying on whichever touch wins the `{once:true}` race. (2) `cancel()` ran unconditionally before every `speak()`, a known way to wedge iOS; it is now guarded on `speaking||pending` and followed by `resume()`. (3) `speakSara` returns the utterance so callers can watch `onstart`/`onerror`. **Port these back to `frontend/` before touching either copy** — they are harmless on desktop and the two files are meant to track each other. `pickVoice()` falls through an en-GB ladder, logging the full voice list when nothing matches. `views/Chat.jsx` owns both halves: a persisted 🔊/🔇 toggle (`sara_voice_out`, OFF by default — a PWA that talks unprompted on a train gets disabled forever) and a 🎤 composer button. **Speech-out is gated on `busy`, never on `messages`** — keying that effect on `messages` speaks every streamed token. Dictation is `continuous`, so the stop tap is what sends (also the iOS audio-unlock gesture); `onend` fires from a stale closure, hence `dictatedRef`/`busyRef` rather than state. Note the `voice` tab mounts **Capture** with `autoRecord`, not Chat — the talk→hear loop lives on the Chat tab. Chat only: briefings and nudges (NEURO's `speakIfEnabled`) are not spoken on the phone yet, and there is no wake word (#11 parked).

### Worker (`worker/`)
- Separate Express app on port 3002 for Pi 4
- Ollama-powered AI tasks only — email triage, import classification, journal prompts, transcript processing
- Auth via `WORKER_SECRET` header
- Stateless: processes tasks, returns results, owns no data

### MCP Server (`mcp-server/`)
- Standalone MCP server exposing NEURO APIs as tools for external Claude Code sessions
- Vault tools are **NEURO-first with local-vault fallback**: they call `/api/vault/*` (writes re-index embeddings/entities via vault-hooks); on transport failure (Pi down) they fall back to direct filesystem access via `OBSIDIAN_VAULT_PATH`, served by the `localVault` helper + `vaultDispatch` circuit breaker. NEURO reconciles offline writes on next startup (hash-based `rebuildEmbeddings`). `delete_note` is NEURO-only (needs index cleanup); `vault_backlinks`/`related_notes` degrade gracefully offline.
- Env: `NEURO_URL`, `NEURO_PIN`, `NEURO_VAULT_KEY` (→ `X-Api-Key` for `/api/vault`), `OBSIDIAN_VAULT_PATH` (local Syncthing copy for fallback)

### VESTA (`vesta/`)
- The shared home surface on **vesta.nickward.co.uk** (Netlify, base dir `vesta`, push to main), talking to NEURO's one public mount `/api/v`. React + Vite, port 5176 in dev. SARA is Nick's; VESTA is the household's.
- ⚠ **`VITE_API_URL` is the Pi's public Tailscale FUNNEL address, NOT the `.ts.net` tailnet one `sara/app` uses.** Her phone is outside the house and has no Tailscale, which is the entire reason `/api/v` is exempt from the PIN.
- ⚠ **The client enforces NOTHING and must never start.** Redaction is `services/vesta.js` (a work event's subject and location are ABSENT from the response object, not hidden by CSS); scopes are re-checked on every route; a private catalogue answers the same 404 a missing one does. `scopes` from `login` decides what to **render** — a convenience, never the boundary. Filtering a title in a component means something upstream has broken.
- **Three renderings that must stay distinct**, because conflating them is how the surface starts lying: **gap** (the server could not read it — amber, names why, *"don't treat it as nothing"*), **empty** (read fine, nothing there), and **absent** (no permission — the section is not mounted at all). *"I couldn't read the kitchen"* and *"the fridge is empty"* send her to different shops. Same rule on `meals.known === false`, which means *nothing is recorded*, not *there is nothing to eat*. `Section.jsx` is the one place that knows the difference.
- ⚠ **Calendar times are SLICED OUT OF THE STRING, never parsed into a `Date`.** The backend already asked Graph for Europe/London wall-clock times; re-parsing re-applies an offset and shows every BST event an hour out — the bug NEURO's calendar had once already.
- The kitchen renders off **`kitchenSections`** (display names) and keys into `items` by the lowercased name. Rendering off `items` alone loses his capitalisation and silently drops a section he has created but not yet filled — an empty Freezer must still show, or she cannot add to it.
- Until Nick creates her account (with scopes) AND a `kitchen` catalogue marked `shared: true`, the app is correct and empty — it **says which is missing** rather than rendering a blank panel that looks broken. Full detail in `vesta/README.md`; the build brief is `docs/handoff-vesta.md`.
- ⚠ **The tasks path was DEAD on arrival and nothing noticed (31 Aug 2026).** `routes/vesta.js` passed `req.account.username` to `capture.submissions()` and `capture.submit()`, which take the **account OBJECT** — so `sourceFor()` keyed on `capture:undefined`, every read returned `[]` and every write 429'd *"no such account"*. **VESTA's first feature had never worked**, behind a green suite, because the only assertion was `assert.ok(Array.isArray(json.tasks))` — true of `[]`, which is precisely what the broken path returns. Identical species to the `upsertCalendarEvent` camelCase trap the same day (guessing snake_case from the schema writes a row of nulls, and *every* "the subject didn't leak" assertion passes on it, because an empty calendar contains no customer names either). **The rule both cost: PAIR EVERY NEGATIVE ASSERTION WITH A POSITIVE ONE.** Pinned now by a round trip (add a task → it comes back on her home screen, with its four-key shape) plus a negative that one account never sees another's.

### VESTA accounts, in Settings (`components/VestaAccounts.jsx`)
- The admin half of VESTA, in the NEURO sidebar under **Settings**, against `/api/capture-links` behind the PIN. Create an account, change what it sees, reset a PIN (which also clears a lockout), revoke. Before it, adding someone was a curl command against the Pi.
- ⚠ **`setScopes` had shipped with NO ROUTE** — reachable from the test suite and nowhere else — so the only way to widen an account was to DELETE it and issue a new PIN, over a change that is none of that person's business. `POST /api/capture-links/:username/scopes` closes it. **Omitting the `scopes` field is a 400, and is deliberately NOT the same as sending `[]`** (which legitimately narrows back to tasks-only): treating a missing field as the defaults would silently change an account nobody asked to change.
- The checkbox vocabulary comes from **`GET /api/capture-links/scopes`**, never a copy in the component — a second list is how a screen offers a permission that does not exist. ⚠ That literal path sits on a router that also has `/:username/pin` and `/:username/scopes`; Express matches in registration order and this codebase has shipped a literal path swallowed as a parameter before, so it is **registered first** and pinned by a test asserting it returns the vocabulary rather than an account named "scopes".
- `tasks` is not togglable — an account without it is a login that can do nothing, which is a confusing way to spell "disabled". Enforced by `normaliseScopes`, with the checkbox merely disabled so it does not look like a choice.
- Verified live end to end: a scope change takes effect on the **EXISTING token** with no re-login (`requireAccount` re-reads the account every request, which is what makes revocation immediate), and revoking kills a live session on its next call. Pinned by `capture-links-scopes.test.js` (7, real HTTP).

### VESTA photo → items (`services/vesta-vision.js`)
- ⚠ **Switched from NEURO → Settings → Switches** (`feature-flags` key `vesta_photo`), not an SSH session and a pm2 restart. `isEnabled()` asks the registry rather than reading `process.env` directly, so `VESTA_PHOTO_ENABLED` still WINS when it is set — the panel then disables the control and says which variable owns it, instead of offering a toggle that silently does nothing. Unset (as on the Pi) the stored value decides. **Defaults FALSE**, and that IS Nick's gate from the handoff — *"do this last, after the typed path is proven; if the list is wrong, a photo just makes it wrong faster."* Same idiom as `DAY_PLANNER_ENABLED` / `NOTION_SYNC_ENABLED`. Lifting it is his call.
- **It PROPOSES and never writes.** `POST /api/v/catalogue/:slug/scan` returns a list she ticks; each kept item then goes through the SAME `/add` route a typed one does. Pinned by a test asserting the **vault file** is byte-identical after a scan — a route that answered politely while writing would pass any check made on its own output.
- ⚠ **The photo is never persisted, logged, echoed back or stored.** It lives in memory for one request. A photo of a kitchen is a photo of a home — the post on the worktop, a prescription, a laptop screen, whoever is standing in it. Stricter than the rest of VESTA needs, because this is the only route that receives what a camera saw.
- ⚠ **It is the only route on the PUBLIC mount that spends money**, so it is gated three deep: the `kitchen` scope, the flag, and a **per-account rolling-24h cap** (`withinCap`, pure — a window, never a total that resets on restart, which is the bug `ai-routing`'s counter had). The slot is claimed **before** the call, so an expensive failure still costs one; the read-modify-write is **synchronous with no `await` between** (`plaud-admin-blocks.acquireLock`'s rule — a real mutex in one process, not across them).
- ⚠ **`/api/v` gets its own 6mb body parser REGISTERED BEFORE the global 50mb one** in `server.js`. body-parser skips a request whose body is already parsed, so **the order is the whole mechanism** — swapping those two lines silently republishes the 50mb ceiling to the internet. Verified live: an 8MB post answers 413.
- **An unreadable answer REFUSES; it never returns an empty list** — "I couldn't read the photo" and "there's no food in the photo" are different facts, and an empty array would render as a fridge she has just been told is bare. An empty array from the model IS a real answer (an empty worktop exists) and is told apart by `ok`. A truncated array (the too-small-`max_tokens` shape that cost email triage whole runs) refuses rather than keeping the part that parsed.
- **An invented section becomes `null`, never itself** — `catalogue.addItem` refuses a section it does not know, so passing "Larder" through would fail at the moment she taps Confirm, with the error attached to the item rather than the guess. Null renders as a real choice she makes, and is deliberately NOT defaulted to the first shelf.
- ⚠ **A refusal is HTTP 200 with `stop_reason: "refusal"`, not a thrown error** — reading `.content` without checking turns a decline into an empty answer. Handled in `anthropic-provider.vision()`, which is where every Anthropic SDK call lives. The API error message is **never** passed through to her (it can name a model, a key state or an account, and this mount is public) — logged for Nick, generalised for her.
- Model is `claude-opus-5` (`VESTA_VISION_MODEL` overrides) at **effort `low`** — reading labels off a shelf is extraction, not reasoning. Priced in `ai-cost.PRICES_PER_MTOK` ($5/$25); NOVA holds no twin of that row, so it is outside the parity check. Usage is reported explicitly via `aiRouting.recordUsage({provider,model,taskType:'vesta_photo'})` because this bypasses the routing tiers — `ai-routing.isCloudAllowed` is now **exported** for the budget question rather than re-implemented, since a second opinion about whether cloud spend is allowed today is how `AI_MODE` comes to mean two things.
- Client: `vesta/src/photo.js` downscales to **1600px / JPEG 0.8 before upload** — not an optimisation, the difference between working and not, since a modern phone photo is 3–5MB against a 4MB server cap. It strips the `data:` prefix (sending it is the classic unhelpful-400). `PhotoProposal.jsx` is the confirm step; the camera button renders only when `/home` says `photo: true`, because a button that answers 503 when tapped is worse than no button. Pinned by `vesta-vision.test.js` (18) + 4 routing tests.

## Key Feature Areas
| Area | Frontend | Backend |
|------|----------|---------|
| Chat | ChatPanel | chat-context-v2, claude service, AI routing |
| Queue | QueueTable | jira service, jira cache, SLA tracking |
| Vault | VaultBrowser | obsidian service, vault-cache, vault-hooks, vault-logger, embeddings |
| Standups | StandupsPanel, StandupEditor | standup routes, transcript-processor |
| Focus | FocusPanel, DoNextPanel | focus routes, do-next, next-action-engine, task-scoring |
| Briefings | sara/app Focus view (sara.briefing) | briefing.js (build+deliver), email-sender.js (Graph Mail), teams.js (Graph Chat), routes/briefing.js |
| People | PeopleBoard, PersonDetail | person-profile, person-timeline, one-to-one-detect, one-to-one-booking, meeting-prep, development-plan, people-gap |
| Waiting on | WaitingOn (in PeopleBoard + PersonDetail), sara/app MeetingPrep | waiting-on (service + routes), routes/meeting-prep-view |
| Calendar | CalendarView, EventComposer | microsoft service (Graph API), routes/calendar, event-parser, contact-directory |
| Plaud admin blocks | — (calendar itself) | plaud-admin-blocks, calendar-sync hook, routes/plaud |
| Inbox | InboxPanel | email-triage (two-way: summary/full/reply) — the ONLY inbox scan |
| Journal | JournalPanel | journal routes |
| Tasks | TodoPanel | tasks routes, task-store (SOURCE OF TRUTH), task-import, task-export, task-capture-drain, todos routes, Microsoft Tasks sync |
| Task de-dupe (NEURO↔MS) | TaskDedupe (in TodoPanel) | task-dedupe (service + routes) |
| Task → calendar block | TaskBlocks + BlockTimeControl + BatchComposer (in TodoPanel) | task-blocks (service + routes), `task_blocks` + `task_block_items` |
| Insights | InsightsPanel, MetricsRow | activity service, health service, stress-score |
| Health (Apple) | HealthCard | apple-health (ingest + sleep rollup), health-daily (one row per day + readiness), health-signals (trends), routes/health, stress-score |
| Health findings ack | HealthPanel "What's changed" | health-signals `partitionAcked`/`acknowledge` (`/api/health/signals/:id/ack`) |
| Watchdog | — | watchdog (backups, task export, scheduled jobs, AI, host) |
| Time | TimeFitCard (in TodoPanel) | time-fit, routes/time |
| Focus sessions | AdhdPanel (session + recovery cards) | focus-session, routes/focus-session (`/api/session`) |
| Initiation (starts/shrinks/triage) | AdhdPanel Momentum `InitiationRow` + close-out card | initiation-signals (pure), `GET /api/session/signals`, `task_triaged` from task-store |
| Wins | AdhdPanel Momentum + "Done today" (Today tab) | wins (service + routes, `/api/wins`) |
| Pi Health | PiHealthPanel | pi-health (routes/pi-health) |
| State of Play | StateOfPlay (sidebar top, above Briefing) | state-of-play (service + routes) |
| Attention (ambient SARA feed) | sara/shared-ui AttentionSurface + Dashboard (phone Surface, kiosk, desktop Electron) + sara/widget | context-state (pure), attention, **sara-surface** (pure — surface/dashboard/utterances), attention-lifecycle, attention-settings (routes/attention) |
| Attention controls | sara/app Controls | attention-settings, `/api/attention/{records,history,settings,records/:id/act}` |
| Neuro Mobile contract | sara/app Now + Review + Capture, `src/mobile/*` | mobile-snapshot, mobile-sync, capture-store (routes/mobile) |
| Weekly risk (PIP) | WeeklyRiskPanel | weekly-risk, management-log (routes/weekly-risk); NOVA: neuro-bridge-kpi |
| PIP deliverables | WeeklyRiskPanel `DeliverableTracker` | pip-deliverables (pure), `GET /api/weekly-risk/deliverables` |
| Actions | ActionsPanel | routes/actions, action-presenter, suggestion-engine.executeAction |
| QA | QATab | qa routes |
| Training | — | training-sync |
| Strava | StravaPanel | strava service (OAuth) |
| Location | — | location service, OwnTracks integration |
| Nudges | NudgeBanner | nudge routes, scheduler |
| Capture | CapturePanel, sara/app Capture | capture routes, feature-tracker |
| VESTA (shared home) | `vesta/` (its own app) | vesta (redaction + meals), catalogue, capture-links scopes (routes/vesta, PUBLIC `/api/v`) |
| Imports | ImportsPanel | imports, import classification |
| Knowledge | — | kb-article, knowledge-gaps, embeddings, retrieval |
| Evidence | — | evidence-register |
| Checkpoint | NinetyDayPlan | checkpoint-progress |
| Catalogues | CataloguesPanel | catalogue (routes/catalogue) |
| VESTA (shared home surface) | `vesta/` (not built) | vesta (routes/vesta, public `/api/v`), capture-links |
| Vault Hygiene | — | vault-hygiene (routes/vault-hygiene); MCP: vault_lint, vault_contextual_link, vault_alias_suggest, vault_fix, vault_connect_orphans, vault_graph_config, vault_plaud_reconcile, vault_plaud_repull |

## Key Patterns
- **CommonJS throughout backend** — `require()` / `module.exports`. NOT ESM. No `import` statements.
- **PIN auth middleware** — all `/api/*` routes require `X-NEURO-PIN` header or `?pin=` query param. Machine clients use `X-NEURO-API-TOKEN` instead.
- **State KV store** — `db.getState(key)` / `db.setState(key, value)` for runtime state (sync status, last errors, etc.)
- **AI routing (policy rewritten 14 Aug 2026)** — `ai-routing.js` routes by **latency need, not by vendor ladder**. `LATENCY_SENSITIVE_TASKS` (chat, standup/EOD interactive + questions, email draft/summary, briefing synthesis) → **OpenRouter first**, because the Pi's local models are too slow to hold a conversation with. Everything else — light, scheduled, background — → **Ollama first**, cloud only if local fails. `_providerOrder(taskType)` is the single source of that decision and both `runTask` and `runStreamingChat` iterate it. **Anthropic and OpenAI are backstops, never the default**: they were tiers 1 and 2 until the Anthropic key ran out of credit and, being first in line, took chat and the standup down with it. Pinned by `ai-routing.test.js` — if you change the order, those tests should fail. Tool calling: `getToolProvider(taskType)` picks the provider for a tools turn (OpenRouter preferred, Anthropic fallback, null if neither) — both providers expose an identical `chatWithTools(systemPrompt, messages, tools, runTool, options)` returning `{text, usage, toolCalls}`, so callers stay provider-agnostic. Ollama has no function-calling API, so no cloud provider = no tools, and chat/standup degrade to plain conversation rather than pretending.
- **One vault exclude list (15 Aug 2026, #85/#34/#86)** — `services/vault-exclusions.js` is the single source of what is not content. It separates **generated** (`Vault Audit`, `SARA Import Reports`, and by FILENAME the task export / `Master Todo` / MoSCoW worksheets / `.backup-` / `.sync-conflict-`) from **retired** (`Archive`, `_toDelete`), because a linter resolving link *targets* still needs to see the retired half — that distinction is the fix for #81, not an oversight. `Daily/` is excluded from embeddings and **kept** for entity extraction: "who did I mention on Tuesday" is exactly what a daily note answers, and exactly what a semantic index of hundreds of scratchpads ruins. Used by `embeddings.js` and `entities.js`; the other copies (`obsidian.js`, `people-gap.js`, `plaud-sync.js`, `one-to-one-detect.js`) are deliberately untouched for now — each is scoped to its own job. **Skipping on the walk only stops NEW rows**, so both consumers prune: `rebuildEmbeddings` drops rows no longer indexable, `entities.pruneExcludedEntities()` runs at the head of the nightly sweep.
- **Embeddings are multi-chunk (15 Aug 2026, #79)** — `prepareFile` returns EVERY chunk; it used to compute them all and return `chunks[0]`, so the DB held 3,216 rows all at `chunk_index 0` and everything decided in the back half of a meeting was unreachable by search, chat RAG and the MCP tools. Invisible, because search always returns something. Bounded at `MAX_CHUNKS_PER_FILE = 60`, logged when hit — **sized against the live vault, not guessed**: 1,091 indexable notes, median 4 chunks, p90 20, p99 60, max 151. The first cut used 20 and would have truncated 107 files, i.e. the top 10% and precisely the long transcripts this fix exists for. The bound is for cost on outliers only; result dominance is handled by the best-chunk-per-file fold, not by this number. A full re-index is **~8,400 chunks** against 3,215 before. Three invariants: the unchanged-check compares content hash **AND** `getEmbeddingChunkCount` (or files indexed under the old code look done forever); rows are **deleted before re-save** (a shortened note has fewer chunks and `INSERT OR REPLACE` alone orphans the tail); and `semanticSearch` folds to the **best chunk per file** before slicing, or `maxResults` counts chunks and one transcript fills the results with itself. `isConfigured()` reads `VOYAGE_API_KEY` — it read `ANTHROPIC_API_KEY`, a different vendor, and only worked because both are set on the Pi. Pinned by `embeddings.test.js`. **The index does not improve until a rebuild runs.**
- **Graph collections are paged (15 Aug 2026, #29)** — `graphFetchAll()` follows `@odata.nextLink`; `$top` is a page size, never the answer. Used by `calendarView`, To Do tasks and Planner. Two traps: `graphFetch` returns `null` on 401 and callers read that as "use the NOVA bridge", so the helper returns `null` rather than an empty collection — an empty one reads as *"you have no meetings"* and the fallback never fires. And a page cap or a mid-collection failure sets `truncated: true` **and logs**, because a silent cap is the bug (third instance of that species in two days: calendar `$top=50`, the 1,958-key JQL, Planner `$top=200`).
- **Action candidates gate on content, not mtime (15 Aug 2026, #78)** — `syncNoteActionCandidates` hashes the body **after stripping frontmatter** and short-circuits when it is unchanged, so a bulk `people:` restamp is invisible by construction. That flood was real: 229 notes restamped on 14 Aug moved every mtime into `scanRecentNotes`'s 7-day window and 911 candidates landed in one night — one automation's bulk rewrite triggering another's. `scanRecentNotes` also takes `maxCreate` (default 60) and **logs loudly** when it caps: a run proposing hundreds has failed regardless of whether each item is defensible. Pinned by `action-candidates-hash.test.js`.
- **The AI budget survives a restart (15 Aug 2026, #55)** — `_usage` in `ai-routing.js` persists to `agent_state.ai_routing_usage` on every recorded call. A module-level counter made the daily cap really mean *per uptime*, and the backend restarts several times a day. Yesterday's stored blob is discarded, not carried.
- **A suggested task says where it came from, in words (4 Sep 2026)** — `services/candidate-provenance.js` (pure `describeCandidateSource`) + `provenance` on `/api/todos`'s `suggested` rows + the source line in TodoPanel's **Spotted, waiting on you** queue. A `capture_todo` carries a `sourcePath` that means two entirely different things depending on who produced it — a vault path from `action-candidates`, `email:<Graph message id>` from `email-actions` — and the review card rendered that field RAW. So an email-sourced suggestion showed **150 characters of base64** under the words "waiting on you": an id that identifies the email to Microsoft and to nobody else. Nick cannot find the email, so he cannot check the claim, so the only safe thing left to do with the card is dismiss it — a review queue whose provenance is unreadable is one that stops being read. ⚠ **Nothing was missing from the data**: `payload.email` has carried the sender, subject and received date since the extractor shipped, and the screen was simply rendering the wrong field. ⚠ **An opaque id is NEVER a label** — where the sender was not recorded it says *"An email — sender not recorded"*, which is a fact Nick can act on, rather than falling back to the blob; the raw value still travels as `ref` (title attribute) for matching a row back to its source. ⚠ **ONE describer, two surfaces** — `action-presenter`'s `capture_todo` case had already fixed this INLINE for the Actions approval card, and a second copy is how the two screens come to name different senders for one suggestion; the presenter now calls the shared function and its raw-path fallback is gone with it. Pure (no DB, no clock), so it pins without a database and cannot be slow on a path that renders a list. Pinned by `candidate-provenance.test.js` (6, incl. negatives asserting no `AAMk` reaches a label on EITHER surface).
- **A task card says its id (4 Sep 2026)** — `taskIdBadge()` in TodoPanel, rendered on both the Must Move lane row and the list row below it, first on the meta line. Nick's ask, and the reason is that a task you can NAME is one you can quote to SARA, put in a commit or point at in conversation — without it, referring to a task means retyping its wording and hoping it matches. ⚠ **Only NEURO's own `task_id` is shown.** A Microsoft mirror has no NEURO id and its Graph id is another 150-character blob — putting one on a card would be the bug above MOVED rather than fixed; those rows keep the source badge, which already says whose board the work is on, and get **no chip rather than a fake one**. `null` means "NEURO has no id for this", never "no id was recorded".
- **Vault sync** — Syncthing over Tailscale (replaced Git-based sync). Obsidian vault at `C:\Users\NickW\Documents\Nicks knowledge base`
- **Plaud intake** — `plaud-sync.js` writes transcript + preferred Plaud summary/Obsidian note, then `imports.js` routes the note of record into canonical vault paths: meetings in `Meetings/YYYY/MM/`, while enriching people links and keeping backlinks aligned. ⚠ **Transcripts land in `PLAUD_TRANSCRIPT_FOLDER`, which is `Plaud/Transcripts` on the Pi — NOT the `Meetings/transcripts/YYYY/MM/` this line used to claim (27 Aug 2026).** That folder holds **zero** files while `Plaud/Transcripts` holds **311**, and they are the single largest source in the embeddings index (308 files / 4,378 chunks), so search, chat RAG and the MCP tools reach them fine. Two places had been written against the empty one, and **scanning an empty folder finds nothing and raises nothing**, so neither surfaced: `knowledge-memory.RAW_FOLDERS` (the raw-intake pass had therefore never read a transcript, and `scorePromotionCandidate`'s `+3` arm was dead code — both now name **both** locations, because the old one is still where a rescued stray goes), and `imports.canonicalizePlaudTranscript`, which would have relocated all 311 — and since `routePlaudSummary` runs BEFORE the move with the pre-move path, left 300+ summaries' `transcript_path:` pointing at files that had gone. It now **refuses to move a transcript already inside the configured folder** while still rescuing a genuine stray. Measured before changing anything: adding the folder to `RAW_FOLDERS` moves consolidation items **2 → 2**, so it reads more and writes nothing new. Pinned by `transcript-location.test.js`. ⚠ **Still outstanding and NOT fixed:** `groupPlaudNotes` filters `path.startsWith('Plaud/')`, which was true when both halves lived under `Plaud/` — but summaries are now routed to `Meetings/YYYY/MM/`, where **222 notes carry a `plaud_id`**, so the Plaud consolidation pipeline sees **2 recordings out of 222**. Keying the group on `plaud_id` wherever the note lives would switch a dormant pipeline back on and write ~222 consolidated notes (30/run, hourly) — a decision, not a bug fix.
- **Plaud admin blocks (17 Aug 2026)** — `services/plaud-admin-blocks.js` puts a 5-minute block after every meeting Nick CREATED or ACCEPTED, carrying *"process and update Plaud meeting for [title]"*. Back-to-back meetings push it past the whole run to the first free slot; a day solid to 18:00 spills to the next **working** day (reuses `working-days`, so never a weekend or bank holiday). **The load-bearing filter is Nick's own: a meeting has OTHER PEOPLE in it** — half his diary is time blocked out for specific work and a focus block has no recording to process. `attendeesOther()` measures that against the **signed-in address, never a count**: Graph lists the organiser among the attendees on some events and not others, so `attendees.length >= 2` is true for a solo block roughly at random. **Everything unknowable fails CLOSED** — "created or accepted" is `isOrganizer`/`responseStatus`, which `fetchCalendarEvents` was not selecting (added, along with `type`/`seriesMasterId`) and which are **both null on the NOVA bridge path**, so an unknown response is not a yes; likewise no signed-in address means nothing qualifies. Measured live on 96 events: 25 blocks, 23 skipped as solo work blocks, 16 not accepted, 27 marked free, 2 cancelled. **Idempotency is a ledger in `agent_state.plaud_admin_blocks`, deliberately NOT a calendar scan** — the scan is the obvious implementation and is wrong in the one direction that matters: deleting a block is a DECISION, and a scan recreates it forever with no way to refuse. `POST /api/plaud/admin-blocks/forget` is the way back. The ledger is keyed per **occurrence**, so a recurring meeting gets a block each time (Nick's call, 17 Aug — a daily standup earns nine a fortnight); `PLAUD_ADMIN_SKIP_SUBJECTS` is the escape hatch and is empty by default. ⚠ **The run lock is not optional.** The first live pass created **52 blocks where 27 were wanted**: the `calendar-sync` hook overlapped a manual apply, both planned against an empty ledger, and the ledger was only written at the END of a run. A pass makes ~25 sequential Graph creates and calendar-sync fires every few minutes, so overlap is the NORMAL case. `acquireLock` is deliberately **synchronous with no `await` between reading the key and writing it** — both contenders are in one Node process and better-sqlite3 is synchronous, so that read-modify-write genuinely cannot interleave; it is a real mutex here and would **not** be safe across processes. Stale after 10 min so a restart cannot wedge it, and the ledger is now written **per successful create**. A dry run takes no lock — looking must stay possible mid-pass. Hooked onto `calendar-sync` (the one place a fresh calendar exists), **above the cold-start return and NOT gated on `newEventIds`**: accepting an invite that arrived yesterday is the central case and that event is not new. Gated on `PLAUD_ADMIN_BLOCKS_ENABLED`, `apply()` dry-runs by default, capped at 25 a run and loud when it caps. Routes live in `routes/plaud.js` (`/admin-blocks`, `/plan`, `/apply`, `/forget`) rather than a router of their own, because `server.js` was held by a concurrent session. `microsoft.deleteCalendarEvent` was added for the one-off `scripts/dedupe-plaud-admin-blocks.js`. Pinned by `plaud-admin-blocks.test.js` (30).
- **Plaud cleanup** — `/api/plaud/cleanup` and `imports.backfillPlaudNotes()` backfill historical Plaud notes, re-route legacy meeting-note copies into correct folders, archive duplicate variants, and write a cleanup report into `Documents/System/SARA Import Reports/`
- **Nine days of meetings PLAUD had and NEURO could not see (27 Aug 2026)** — three bugs in `plaud-sync.js`, all silent, found because Nick processed a 25 Aug 1-2-1 and it never appeared. Measured: **22 of the 27 recordings between 19 and 27 Aug were not in the ledger**, and the other 5 were in the vault under names he could not find. (1) **`get_file` now returns valid JSON followed by a human-readable hint paragraph in the SAME text block** — `JSON.parse` rejects that as "Extra data" and `callTool`'s fallback returned the raw **STRING**, so `details.id`/`.name`/`.start_at` were all `undefined`. **A string has properties; they are just undefined, so nothing threw** — every note carried `plaud_id: "undefined"`, the generic "Summary" title and the **sync** date rather than the meeting date, and the 01:30 dedupe pass then *correctly* archived ~86 of them as duplicates. `parseLeadingJson` parses the leading value and discards trailing prose; it lives in the shared `callTool` because only `get_file` is affected **today** (list_files/get_note/get_transcript parse clean — verified, not assumed). `assertUsableDetails` then **refuses** unusable metadata per recording: a note titled "Summary" and dated today is a perfectly valid file, which is exactly why this was invisible for nine days — refusing costs one recording and a log line, writing costs a note nobody can find plus a ledger entry saying the work is done. (2) ⚠ **The incremental window started AT `lastSuccessfulSyncAt` instead of lagging it.** A recording is only ledgered once PLAUD has produced a summary and until then is correctly skipped as "not ready — retry next cycle" — but the sync runs **every 30 minutes and succeeds**, so that stamp is always today and the effective window was **"today only"**. Anything still processing at midnight fell out and was **never asked for again**. Now a LOOKBACK (`incrementalDateFrom`, pure; `PLAUD_SYNC_LOOKBACK_DAYS`, default 14) — the ledger already prevents rework, so the cost is one larger `list_files` per sync. This is the general trap in any "sync since last success" loop whose readiness is decided by a *third party*: the window must lag by longer than the thing you are waiting for. (3) **`isRetryableError` matched `timeout`, but the MCP SDK says `Request timed out`** (-32001), so all 4 recordings in the failed ledger died on their first attempt with 3 retries unused. Pinned by 7 tests in `plaud-sync.test.js` including the real trailing-hint payload and a **negative** test for the filename the string path produced. ⚠ Recovery is `reconcilePlaudRecordings`/`repullPlaudRecordings` (they bypass the incremental skip and never restore from Archive); cleanup of the mis-named survivors is a **move to `Archive/`, never a delete**, and must be done on the **Windows** vault, which is canonical.
- **Vault hygiene** — `services/vault-hygiene.js` is the deterministic graph-maintenance engine (pure CommonJS, takes vault root so it runs in-process or standalone via `node`). Capabilities: `lint`, `contextualLinkPlan`/`Apply`, `aliasSuggest`, `fixPlan`/`fixApply` (tiered: skip|conservative|moderate|aggressive — links default conservative=exact-only; fuzzy mislinks here so it's review-only), `connectOrphans` (fallback), `graphConfig`. Read-only by default; mutating ops are append-only/surgical, back up every touched file to `Scripts/.lint-backups/<ts>/`, write a changelog, and are idempotent (markers `<!-- ctx-links -->`, `<!-- hub-link -->`, `<!-- daily-nav -->`). Precision rules: FULL-NAME matching only, strip wikilinks/code/paths before matching, resolve path-form links when deduping, skip names mapping to >1 person. Reports → `Documents/System/Vault Audit/`. Exclude set must include generated output (`Vault Audit`, `.lint-backups`, `Archive`…) or scans self-pollute. Wired into the scheduler as a READ-ONLY Friday 4:35pm pass (refreshes lint + link cards, never applies). **A link into Archive is not a broken link (16 Aug 2026, #81)** — `lint()` returns `archivedTargets` as its own class beside `broken`, because archiving is correct behaviour and reporting all 114 inbound links as defects buried the 58 real ones. Measured on the live vault: **177 reported → 58 real + 114 archived + 5 `_about`**, 67% noise. Three parts, and the middle one is the load-bearing one: (1) `[[_about]]` can never resolve because `walk()` skips `_about.md` by design, so it is excluded outright — broken by construction, not by content. (2) **Archive discovery is now at ANY depth** (`collectArchiveDirs`), not just `<root>/Archive` — the notes behind 54 of the false positives live in `Projects/Archive/90 Day Plan (retired 2026-08-12)/`, so a top-level-only lookup classified every one as missing, and `buildFixModel` had the same blind spot. `EXCLUDE_DIRS` still applies to everything else, which is what keeps `.stversions` and `Scripts/.lint-backups` — both stuffed with Archive-shaped copies — from resolving dead links against backups. (3) `buildArchiveIndex(root, normalise)` **takes the normaliser** so each caller matches archived targets exactly as it matches active ones (`lint` case-only, `fixPlan` the aggressive `norm`); mixing them would let a punctuation variant resolve to Archive while an active note existed. An active note always beats an archived one of the same name. The count is logged but deliberately **kept out of the push notification** — informational, and a second number on the banner is nudge noise (#17). Pinned by `vault-hygiene.test.js`. **Note the file contains a literal NUL byte** as a dedup-key separator, so `file` calls it binary and exact-match string editing fails — patch it with a script that writes `\u0000`. Ports of the throwaway `Scripts/_*.js` prototypes; full spec in `Projects/NEURO/Vault Hygiene Engine — Build Handoff` (vault).
- **A transcript is pulled once, so it is pulled when the speakers have names (1 Sep 2026)** — the
speaker-naming gate in `plaud-sync.js` (`assessSpeakerNaming` + `decideSpeakerHold`, both PURE) plus
`pendingSpeakers` in the sync ledger, and the Plaud cron moved **30 → 15 minutes**. PLAUD assigns real
names some minutes AFTER the transcript itself lands, and nothing re-pulls a recording once it is
ledgered — so pulling the moment a transcript exists reliably captured it at its least useful:
*"Speaker 2"* throughout, permanently. **The signal was already in data the sync had already paid for**
— every segment carries BOTH `speaker` and `original_speaker`, so an unnamed voice is one whose
`speaker` still equals its raw label. Verified live: a named 1-2-1 reads `"Nick Ward" / "Speaker 1"`,
an unnamed one `"Speaker 2" / "Speaker 2"`. **Deterministic — no model call, no cost**, and the check
itself costs no API calls because [the transcript is fetched before the readiness decision anyway]
(`fetchTranscriptSegments`, split out of `fetchTranscriptBody` for exactly this).

  ⚠ **THE DEADLINE IS WHAT MAKES HOLDING SAFE, so every path that cannot measure it PULLS** — an
unreadable or missing first-seen stamp, an unreadable `now`, no transcript to judge on. A hold that can
outlive its own clock is a recording that never arrives, and a transcript nobody can find is far worse
than an unattributed one. On expiry it pulls ANYWAY and **stamps the note** (`speakers_named`, plus a
line in the body), because an unattributed transcript that looks identical to an attributed one is how
a "Speaker 2" quote ends up in somebody's mouth. ⚠ **`speakers_named` is THREE-VALUED** — `null` is
"never judged" (no transcript, or a note predating this), which is not the fact "judged, and never
named". ⚠ **The clock is PERSISTED per recording, never an in-memory timer**: the backend restarts
several times a day on deploys, so an in-process clock would reset the hour on each one and the hold
could outlive every deadline. Stamped per recording, never batched to the end of a run.

  ⚠ **A SOLO recording is never held** — one voice has nothing to disambiguate, so PLAUD never names it
and waiting burns the full hour on every memo to pull exactly what was there at the start. ⚠ **A slot
counts as named if ANY of its segments names it**: attribution is per utterance and PLAUD is not
consistent across a long meeting, so requiring every line to agree would hold a fully named recording
on one stray label for ever. ⚠ **It says names were ASSIGNED, never that they are CORRECT** — live data
has a segment labelled "Nick Ward" whose raw label is "Speaker 2", two voices merged; nothing here can
see that and nothing here claims to. Held recordings are **not ledgered**, so `shouldProcessRecording`
re-offers them, and the 14-day lookback keeps them in the list window. Surfaced on `getStatus().speakerWait`
— a recording NEURO has deliberately not written must be visible, or it is silently absent from the vault
while everything reports healthy. The cost is a re-fetch per held recording per cycle (~4 instead of 1
over the hour); a tick with nothing pending costs ONE `list_files`. `PLAUD_SPEAKER_WAIT_ENABLED=false`
switches it off, `PLAUD_SPEAKER_WAIT_MINUTES` moves the deadline. ⚠ **Repull NEVER waits** — it is the
manual recovery for a MISSING recording, and holding the one route back for an hour is the wrong trade;
it pulls and stamps what it found. Pinned by `plaud-speaker-wait.test.js` (18).
- **PLAUD reconcile/repull** — `plaud-sync.js` `reconcilePlaudRecordings()` (read-only) lists every PLAUD recording and finds those with no ACTIVE note (Archive excluded) by plaud_id or date+title-token Jaccard ≥0.5; `repullPlaudRecordings({ids?,limit?})` re-pulls them FRESH (never from Archive) through the existing throttled (750ms gap, concurrency 1), 429-backoff-with-jitter, ledger-persisted-per-recording pipeline — resumable: a crash continues from the ledger. The §9.3 "no throttling" lesson was about the separate Obsidian plaud-mcp-sync plugin, NOT this service.
- **Pi health** — `services/pi-health.js` reads live host state (load, SoC temp, `vcgencmd get_throttled` decoded to named flags, `/proc/meminfo` MemAvailable, `df`, SMART via `sudo -n smartctl -d sat`, `pm2 jlist`, `systemctl is-active`, `ps`). Pure read-only, no DB. Every section degrades to `null` off-Linux or when a tool is missing, so it doesn't break Windows dev. SMART cached 5min (spins the disk), PM2 15s; a 60s in-memory ring buffer (120 samples, `.unref()`) feeds the trend sparklines. `assess()` turns raw numbers into the ranked issue list the panel's focus band renders — that judgement lives in the backend, not the component. ⚠ **zram is not a disk (2 Sep 2026)** — the swap rule fired on `swapPct >= 25` with no idea what backs the swap, so it sat permanently amber saying *"Swapping hurts on an SD card / HDD"* over a Pi 5 whose only swap device is `/dev/zram0`. Measured live: 1.98GB swapped out occupying **16MB of RAM** (~224:1 — the pages are untouched model buffers, faster-whisper alone is 1.0GB against an 11MB RSS), 10GB of 16GB available, 77,859 swap-ins in 51 days. A full zram pool is the mechanism working. `getSwapBacking()` classifies `zram`/`disk`/`mixed`/`none`/`null` off `/proc/swaps` and `readZramCost()` takes the real cost from `mm_stat` (the individual `orig_data_size` files are the older layout and are **absent** on this kernel); on zram the rule judges RAM consumed rather than pool percentage and fires only above 20% of MemTotal, because the pool's cost already sits inside MemAvailable and the two memory rules cover genuine pressure. ⚠ **Not knowing is never the harmless answer** — an unreadable `/proc/swaps` keeps the warning and says it could not tell; mixed backing warns and names the disk device. `assess()` is now exported so the ranking pins without a Pi under it. Pinned by `pi-health-swap.test.js` (7).
- **Tasks: NEURO is the source of truth** (13 Aug 2026) — the `tasks` table owns text, status, MoSCoW, priority 1-3, due date and a provenance backlink. `task_moscow` is folded in and no longer written — **true for the first time as of 16 Aug 2026 (#50)**: `POST /api/todos/moscow` used to fall through to a legacy path-keyed `task_moscow` row when given no `taskId`, so a file-backed mirror (Microsoft, a daily note) could be "rated" and the call returned `{ok:true}` into a place with no readers. It wrote **one** row in the system's life and had no caller at all once `/moscow/review` was scoped to NEURO-owned tasks. It now **requires `taskId` and 400s with the reason**; GET still surfaces the historical row and DELETE still clears it, so nothing recorded is stranded. All four capture routes write to the DB: `/api/capture/todo` (Watch/Siri + Capture tab), chat, MCP `create_task`, and SARA suggestion approval — `obsidian.addTodoToMasterList()` is now a thin wrapper that creates a task rather than appending markdown, which retired both the `📥 Inbox` heading guessing and `#mustdo`. `services/task-store.js` is the only writer (dedupe_key = normalised text, UNIQUE, so a second sighting folds rather than duplicates); `task-export.js` regenerates `Tasks/NEURO Tasks (export).md` (read-only copy + "last exported" stamp = the offline safety net and the rollback path; `verifyExport()` compares it to the DB by task id); `task-import.js` seeded 130 tasks from `Master Todo.md` with MoSCoW/priority from the 12 Aug triage worksheets. `parseVaultTodos()` merges DB rows and dedupes file-backed lines against them, so every consumer sees tasks unchanged. Microsoft Tasks and daily notes stay file-backed — Microsoft and the daily note own that data. `Master Todo.md` was **retired 16 Aug 2026** (#27) — `agent_state.tasks.master_todo_retired` is set, the file is at `Tasks/Archive/Master Todo (retired 2026-08-16).md` and nothing parses it. `POST /api/tasks/retire-master` did it and refuses unless the export verifies; `{retired:false}` flips the flag back if ever needed. The eleven superseded copies went to `Tasks/Archive/` in the same move (#46), leaving `Tasks/` holding only live files: `Capture.md` (drop-box), `Microsoft Tasks.md` (Microsoft owns it), `NEURO Tasks (export).md` (read-only copy), `_about.md`, `Task System Boundary.md`. **Archiving them is what finished #34**, not the filename patterns: `vault-exclusions`'s `GENERATED_FILE_PATTERNS` never matched `MUST - Prioritise 1-3.md`, `All Tasks.md` or `Rescued from Archive - MoSCoW.md`, which were still holding 36 `extracted_entities` rows against real people. Under `Archive/` they are excluded by DIRECTORY instead — the general fix, rather than a regex list that has to anticipate every worksheet name.
- **Feature capture (15 Aug 2026)** — `services/feature-tracker.js` appends to `Projects/NEURO/NEURO Feature Tracker.md` in the vault, the file the whole NEURO/SARA/NOVA backlog lives in. Two front doors: the `capture_feature` chat tool (tier **write** — internal and reversible, so it runs immediately) and `POST /api/capture/feature`, which is what SARA Capture's **Feature** mode and NEURO CapturePanel's `feature:`/`idea:`/`neuro:`/`sara:`/`nova:` prefix both call. **Deliberately not a task** — a feature idea filed as a todo is one nobody reads next to the other 92, and the tracker is where ranking happens. The writer is append-only and surgical like vault-hygiene: one row into a `## Captured — raised in passing` section (created above `## Related` on first use), plus the `updated:` stamp. It **never renumbers or reorders** — the ranked sections are Nick's editorial. Numbering is `max(existing)+1`, **not a count**, because the file already contains a duplicate #103 and a count would silently mint a third. Pinned by `feature-tracker.test.js`. The web panel only routes on an **explicit prefix**: everything else there is guessed from prose, and guessing "that's a feature" would file thinking as backlog. **Reading them back (#114)** is `GET /api/features/captured` in `routes/features.js` — a NEW file rather than an addition to `routes/capture.js`, deliberately, because that file was held uncommitted by a concurrent session. It powers a count and the last three under the CapturePanel composer, shown while a feature is being TYPED as well as after one is filed, and is **scoped to the capture section only** — listing the ranked sections would make it a second, worse backlog view. Handles the file's duplicate numbers (sorts by number, file order as tie-break, dedupes nothing) and separates "none captured" from "couldn't reach the tracker".
- **The same task, written twice (17 Aug 2026)** — `services/task-dedupe.js` + `routes/task-dedupe.js` + `components/TaskDedupe.jsx`, mounted **inside TodoPanel** (the question is only ever asked while looking at the task list; a screen you have to go and find is one that never gets found — the `DecisionsPanel` lesson one step earlier). NEURO owns tasks and Microsoft owns its own, so `parseVaultTodos`'s DB merge only ever suppressed **Master Todo and Daily** lines — Microsoft lines were deliberately excluded, and **nothing had ever compared the two**. `dedupe_key` cannot close it: it is normalised text, UNIQUE, so it matches a re-import of the SAME wording, and the whole complaint is that the wording differs. Live proof: Planner holds *"Succession plan"*, NEURO #58 holds *"Build succession plan — cover for HoTS and emerging team leads…"*. **Matching is deterministic — token overlap weighted by IDF over the two lists**, so it is instant, free, identical every run and works with the Pi offline. IDF is what makes it usable on Nick's corpus specifically: "review", "support" and "customer" are in dozens of his tasks and carry almost no evidence, while "succession" or "sandford" nearly identify a task alone; an unweighted overlap ranks them equally and buries the real pairs. Two measures, higher wins — **containment** (how much of the SHORTER task sits inside the longer, which is the succession case: 0.14 on Jaccard, 1.0 on containment) and **jaccard** (two rewordings of similar length). **`MIN_SCORE` 0.42 is MEASURED, not picked**: over all 2,544 live pairs there is exactly one true duplicate at 1.0 and the highest-scoring non-duplicate is 0.397 (*"Extract Production Ops from Support"* vs *"Provide realistic headcount for production/support per squad"*), and 0.42 sits in that gap — both are pinned, the second as a NEGATIVE. Containment needs **2+ shared content tokens** or a one-word Microsoft task ("FOC report") scores 1.0 against anything mentioning reports. **Dates play no part in scoring** — a pair is judged on wording alone, and every open task is re-scored on every open, so two sightings months apart match exactly as well as two on the same day; due dates appear only as a `notes` line for Nick to read (pinned: same-date and differing-date pairs score identically). **Stemming folds `-ing` and `-ed`, not just plurals, and collapses the doubled consonant** (planning → plann → plan): Nick writes the same job as a noun in one list and an activity in the other, and unfolded, *"Succession plan"* vs *"Succession planning for team leads"* shares ONE token, drops under the two-token rule and is **never offered** — 0.195 before, 1.0 after. ⚠ **What it CANNOT do is semantic**: with no shared vocabulary there is no match, so *"Work out who covers my role if I go under a bus"* scores 0 against *"Succession plan"*. Closing that needs the Voyage embeddings NEURO already has; deliberately not built, because a lexical miss is invisible and cheap while a semantic false positive is a hidden task. **It suggests, never merges**: a wrong auto-merge hides a real task behind an unrelated one, silently, in the one place Nick looks to find what he owes — and the populations are small enough (16 vs 159) that reviewing is cheap. **Confirming is a LINK, NEURO leading** (Nick's call, 17 Aug): the NEURO row takes the `ms_id`, the Microsoft line is skipped in `parseVaultTodos`, and completing the task PATCHes Graph via the existing `completeMicrosoftTask`. **Nothing is deleted in Microsoft.** New `tasks.ms_source` carries 'MS Planner'/'MS ToDo' as a completion hint — unknown stays **null** rather than guessed, because null makes `completeMicrosoftTask` try both and a wrong hint sends it to the wrong API. `tasks.ms_id` is the ONE answer to "is this linked" (the column already existed); only the **rejections** need storage and they go in `agent_state.task_ms_dismissed` — not remembering them is what would make the screen unusable, since 2,544 pairs are re-scored every open. ⚠ **The trap when testing suppression**: once linked the NEURO row carries the SAME `ms_id`, so `some(t => t.ms_id === x)` finds the survivor and passes for free — assert on the **file-backed** row (`/^MS /.test(t.source)`) or the suppression can break unnoticed. The push is **never allowed to fail the tick** (the task is already done; reporting failure would say the tick didn't land) and returns `msPush` so the client can say what Microsoft actually did. `dbTasks:false` skips suppression entirely — the importer reads the vault alone and would otherwise lose a task from its input. **The screen is empty most days and that is the correct answer**, not a broken matcher: the value is catching pairs as meetings keep promoting tasks, not a one-off cleanup. Pinned by `task-dedupe.test.js` (12).
- **NEURO was never compared against ITSELF (31 Aug 2026)** — `task-dedupe.rankInternalCandidates()` (pure) + `mergeInternalPair`/`unmergeInternalPair` + `/api/task-dedupe/{merge,unmerge,internal-dismiss,internal-undismiss}` + a second group in `TaskDedupe`. The 17 Aug feature above compares NEURO against **Microsoft**, and nothing has ever compared NEURO tasks against each other — the only guard on that side is `task-store.dedupeKey`, the first 80 characters of normalised text, UNIQUE, which matches a re-import of the SAME wording and nothing else. **Measured on the live Pi before writing anything: 143 open tasks, 10,153 pairs, SIX genuine duplicates standing — four of them at 1.000**, all one commitment captured out of two wordings of the same meeting (*"Consult Annabelle for insights"* against *"Nick Ward will consult Annabelle, who is further ahead in this process, for insights"*; the Annabelle commitment is in there THREE times). ⚠ **It does NOT inherit 0.42, and that is the finding, for the same reason `action-candidates.FOLD_SCORE` does not**: 0.42 was measured on two INDEPENDENTLY worded lists, and this corpus is Nick's own vocabulary throughout. Measured separation — true duplicates 1.000 × 4, 0.844, 0.706; **worst false positive 0.584** (*"Career progression pathways defined by Day 45"* vs *"Kayleigh Russell — career progression plans for DD team by Day 30"*), then 0.533 (*"Compile list of EXP/LSL dashboard users"* vs *"Provide timescales for LSL and EXP dashboards"*). **`INTERNAL_MIN_SCORE = 0.65`** sits in that gap; both false positives are pinned as NEGATIVE tests. Confirming **MERGES** rather than links: the kept row absorbs what the other knew (blanks only — `createTask`'s "a second sighting fills in, never overwrites" rule) and the other is marked **`dropped`, never deleted**, with the dropped wording and an Undo kept in `agent_state.task_internal_merges` — this is a matcher making a judgement, so disagreeing with it afterwards has to be possible. The **older** task is the one offered to keep (it is the one with history hanging off it — a session, a block, a link). It **REFUSES** to merge away a Microsoft-linked row rather than moving the link, which would make the kept task responsible for pushing completion to a board it was never matched against. Internal rejections share the Microsoft rejection map, keyed `task:<lo>::task:<hi>` — lowest id first, so a pair has ONE key however it is ordered. ⚠ **`db.listTaskRows` IGNORES `includeDone` whenever `status` is `'all'`** — passing `{status:'all', includeDone:false}` alone returns done AND dropped rows, which silently put finished work into the pool and (worse) would feed a just-merged `dropped` row straight back as a fresh suggestion; both call sites filter in JS, as `activeTodos()` already did. That was caught by a test, not by reading. The pool is open **AND in-progress** — `status:'open'` is a literal column match, so a task Nick has actually started was invisible to de-duplication on BOTH halves, which is backwards. Pinned by 14 tests in `task-dedupe.test.js` (31 total).
- **"Do I already have this?" is asked at CAPTURE time (31 Aug 2026)** — `task-store.findSimilar(text, {excludeId, minScore})`, plus `createTask({checkSimilar:true})` returning `similar` on the result. The other half of the same finding: those six duplicate pairs got created because `dedupeKey` is the ONLY thing that had ever answered that question, and it cannot see a reword. **It REPORTS and never folds** — the task is always created, because the asymmetry the rest of this area is built on holds here too: a missed match leaves a visible, cheap duplicate, a wrong one silently loses a commitment in the place Nick goes to find what he owes. **Opt-in, not automatic**: it costs a pass over the open list and the bulk paths (`task-import`, the capture drain) create in loops with nobody to tell, so only the two surfaces where a person is present are wired — the chat `create_task` tool (told to SAY so and offer the merge screen, never to assume) and `POST /api/capture/todo` (which returns `similar` alongside `steps`). `task-dedupe` is required **lazily** because it requires `task-store` back — the cycle is real, and resolving it at call time is how `action-candidates` handles the same pair. A failed check returns null and is logged: not knowing must never cost the capture. Pinned by 5 tests in `task-store.test.js`.
- **A task is either a COMMITMENT or CONTINUAL IMPROVEMENT (1 Sep 2026)** — `shared/task-origin.cjs` (pure) + `tasks.origin` / `tasks.origin_proposed` + the Origin control on the expanded task row + the split in `weekly-risk`. The weekly risk report to Chris counted open and overdue over the whole list, which conflates two populations sharing nothing but a due date: work **somebody else asked for or is waiting on**, and work **Nick set himself** to make the department better. Counting them together makes the improvement backlog PENALISE him — a man who writes down thirty ideas and dates them optimistically reads, in a compliance report, exactly like a man who has broken thirty promises. **Overdue now counts COMMITMENTS ONLY** (Nick's call), and the improvement figures sit beside it worded as target dates, never "overdue". ⚠ **There is deliberately NO DEFAULT, unlike `shared/task-domain.cjs`** — and a test pins the absence of `originOrDefault`, because a future tidy-up "making the two consistent" would decide invisibly whether Nick's stretch goals reach his manager as broken promises. Domain's two mistakes are asymmetric with one of them visible; these two are asymmetric in OPPOSITE directions and both are expensive (guessing commitment manufactures a broken promise, guessing improvement hides one), so **`null` is a first-class value and the report counts it as a NAMED THIRD BUCKET**, with the commitment figure declared a FLOOR until it is zero. ⚠ **Inference reads PROVENANCE, never wording.** Measured on the live store before anything was written: *"Prepare MyAudience vs iMail price comparisons (for Chris → SLT)"* and *"Build escalation accuracy view in NOVA"* are obviously different to a human, arrived by the SAME route, and no keyword rule separates them without mangling the eighty rows between — both are pinned as NEGATIVE tests. Three evidenced rules only: a **management-log mirror** (every row is a recorded conversation with a named person, and only Nick-owned ones mirror), a **`meeting-promotion` task from `Meetings/`** (he said it with people in the room — his own test is *is somebody waiting*, so volunteering still counts), and a **Planner card** — **MS To Do is excluded on purpose**, being his own private list. Everything else is unclassified, which on the live store is 81 of 110 and is the correct answer, not a failure. A guess is stamped `origin_proposed` (the `moscow_proposed` contract: importing a guess as a decision invents calls Nick never made), shown with a trailing `?`, and **the report says how many of its classifications are still guesses**. Setting it by hand clears the flag — confirming a proposal unchanged IS making the call. `null` is accepted to clear it back, because disagreeing with a proposal without yet knowing the answer must be possible. Backfill is `backend/scripts/backfill-task-origin.js` (dry-run default, only touches `origin IS NULL`). ⚠ `WeeklyRiskPanel` was updated in the same change: it is what Nick reads before pressing send, and a single whole-list "Overdue" stat there would contradict the document he is about to sign. Pinned by `task-origin.test.js` (22, real scratch DB) + 10 in `weekly-risk.test.js`.
- **A Microsoft task says which BOARD it is on (27 Aug 2026)** — `microsoft.fetchPlannerPlanNames()` + a `### <plan>` heading in `Tasks/Microsoft Tasks.md` + `task.msPlan` out of `parseVaultTodos` + `shared/ms-task.cjs`. Every Planner card read *"MS Planner"*, which is true of all of them and therefore identifies none — and the board is most of what says whose work a task is. `/me/planner/tasks` returns a bare `planId` and no title, so titles are resolved one plan at a time (Graph has no batch read) and cached in `agent_state.ms_planner_plan_titles` with a **7-day TTL**, because a renamed board would otherwise read wrong for ever; a failed refresh **keeps the stale title** rather than blanking the card. The plan is carried as a **heading, never appended to the task line** — anything on the line lands in the task's own text and then in its dedupe key. **Unknown is silence, never a guess**: an unreadable plan groups under the literal `(plan unknown)` (written and parsed in ONE module so the halves cannot drift — an unrecognised marker would arrive on a card as a board name), and a plan **resets at every `## `** or a Planner board leaks onto the To Do section below it. The default To Do list "Tasks" is now named too; omitting it as noise left exactly the tasks in it with no list at all. `tasks.ms_plan` (display only — the completion push still reads `ms_source`) carries it onto a **LINKED** row, which is the one place it would otherwise vanish: the Microsoft mirror line is suppressed once a pair is linked, so the provenance disappears at the moment Nick confirms it. `msPlanBadge(task, {withSystem})` is pure and shared, because three surfaces render task cards; `withSystem:false` is for the rows whose source badge already reads "MS Planner". ⚠ **Nothing changes until `syncMicrosoftTasks` next rewrites the mirror** — the file on disk has no headings yet. Pinned by `ms-task-plan.test.js` (8).
- **A Microsoft task is EDITABLE from NEURO, and Microsoft still owns it (27 Aug 2026)** — `microsoft.readMicrosoftTask` / `updateMicrosoftTaskFields`, `GET`/`PATCH /api/todos/ms/:msId`, `obsidian.setTaskFields`, `MicrosoftTaskControls` in TodoPanel. The expanded row said *"Mirrored from MS ToDo — edit it there"*, which was a POLICY, not a limit: NEURO already PATCHed Planner for progress and completion. **Nothing is stored locally** — an edit is a PATCH to Graph and the mirror line is repainted afterwards purely so the screen is not stale; a local copy would be the second source of truth `task-dedupe` exists to undo. **The whitelist IS the safety model**: `title`, `dueDate`, `notes` are reachable and assignments / buckets / checklists / categories are not reachable AT ALL rather than guarded — they are board structure other people maintain, and deletion has no undo from here. **Only the fields Nick actually changed are sent**, which is the ONLY defence on To Do: Planner PATCHes carry an `If-Match` etag so a stale write is REFUSED (412 → `conflict`, surfaced in words), while To Do has no etag and a NEURO edit silently wins over anything changed there since the last sync — so a concurrent edit to a *different* field survives, and that is all that can be promised. **The fields are READ live on expand, never taken from the row**: the mirror carries no description, so a notes box filled from it would render empty over a Planner description that has content and the first save would erase it; `notesReadable:false` disables the box and says why. Planner's description is a **separate resource with its own etag**, so a partial save (title lands, notes fail) is a real outcome and is reported per field — "saved" over that is the silent half-failure shape. **GRAPH FIRST, mirror second** — the opposite order to `wip-ms`/`complete-ms`, deliberately: those write a trivially reversible state where instant feedback is the point, whereas a rename is visible to Nick's team and painting it locally before Graph accepts it would show an edit that never landed. A title change on **Planner** is the one thing that stops and asks, because the board is shared. Due dates are written at **MIDDAY**, never midnight — Planner stores an instant rendered in the reader's zone, and midnight UTC lands on the previous day west of here (the calendar's bug, one API over); `dueDate: null` clears it and must stay distinct from `undefined` all the way to Graph. `setTaskFields` re-checks the `<!--id:-->` before touching a line (a rename onto the wrong row reads as Nick's own edit — `setTaskPercent`'s lesson, with a worse blast radius), preserves the `(50%)` marker / `⚡` / id comment or the line stops parsing and completion breaks on it, and **strips markup out of a pasted title** so a title carrying an id comment cannot re-parse as a different task. Pinned by `ms-task-edit.test.js` (16) + `routes/ms-task-edit-routing.test.js` (3 — Express matches in registration order).
- **A block in the diary is a plan, not finished work (18 Aug 2026)** — `services/task-blocks.js` + `routes/task-blocks.js` + the `task_blocks` table + `components/TaskBlocks.jsx`, mounted **inside TodoPanel** beside TaskDedupe, with `BlockTimeControl` on the expanded task row itself (blocking an hour is a thought you have while looking AT the task). Two halves. **Pushing the task out**: `tasks.estimate_minutes` already knew how long a thing takes (#87) and `calendar_cache` already knew where the diary was empty, and nothing joined them — so "block an hour for this" was a manual copy between two systems NEURO owns both ends of. `plan()` proposes and creates nothing, `schedule()` creates, the same two-step as `event-parser` and `one-to-one-booking`. It creates on a plain confirm with **no approval gate, because the event has no attendees** — that is `action-presenter`'s "leaves the building" test applied, not an exception to it; the queued `schedule_focus_block` in chat stays queued because chat is not a confirm. **Deciding when it is done**: yesterday's rule from `meeting-notes-source` — *a meeting in the diary does not mean Nick attended it*, the Plaud note is the evidence. A time block has the identical hole and **no recording to close it, because nobody Plauds a solo work block**, so the evidence is an outcome note Nick writes, and until one lands the task **holds at `awaiting-writeup` rather than going done** (Nick's call, 18 Aug). ⚠ **The load-bearing refusal: an empty stub does not release the task.** NEURO writes the stub, so a detector that only checked the file EXISTS would create the evidence for its own test and mark the work done with nothing in it — the feature failing in the exact silent way it exists to prevent. `isOutcomeWritten()` is **pure**, strips frontmatter, headings, empty bullets and the `<!-- neuro:task-outcome-stub -->` fence it wrote, and needs `MIN_OUTCOME_CHARS` (25) of real prose underneath; a **marker fence rather than "strip blockquotes"**, because Nick quoting an email in his write-up IS a write-up. **The hold lives in `task-store.updateTask`**, the only writer — a check in the route would be one the SARA funnel, the MCP tool and the chat tool all walk past. Held means held at **`in-progress`, not refused**: the tick was a real statement and throwing it away makes Nick tick twice; `held` rides back on the returned row so the tick can SAY what happened (a hold Nick cannot see the cause of is a task that mysteriously refuses to complete). A knock-on worth knowing: `pushCompletionToMicrosoft` now checks `status === 'done'`, or a held tick would close the linked Planner task while the NEURO one stayed open — in Microsoft, where NEURO cannot put it back. **`dropped` is never held** (abandoning is not a claim needing proof), and **an unreadable vault fails OPEN** — the evidence rule is worth enforcing against forgetfulness, not against an unmounted disk, and a Syncthing hiccup refusing every completion in the one screen Nick uses to find what he owes would end the feature. **`release(reason)` is not optional and the reason is required**: plans change, and a hold with no way out wedges a task forever, but a reasonless release quietly becomes a second way of saying done — `released` stays distinct from `complete` on the record. Idempotency is the **UNIQUE (task_id, date_key, start_time) index, checked BEFORE the Graph create** (by the time Graph answers the duplicate exists) and never a calendar rescan — deleting the block in Outlook is a DECISION, `plaud-admin-blocks`'s ledger lesson unchanged. The block row is written **first**, so a Graph failure leaves a recoverable row rather than an orphaned note. Slot search deliberately uses **09:00–17:30, NOT one-to-one-booking's AM/PM windows** — those are a rule about meetings with other people in them and would refuse most of the day; it honours `BUFFER_MINUTES`, bank holidays via `working-days`, and returns a **reason** when there is no room. The sweep runs every 10 min (not a `TRACKED_JOBS` job — idempotent, nothing to replay) and is the MECHANISM, not a backstop: Nick writes the note in Obsidian and nothing in that act touches NEURO, since vault-hooks do not fire for Syncthing-delivered files. It reports **`gaps`, never a quiet zero** — a sweep reading 0 because the vault was unreachable is the bug being fixed. A renamed note is still found by `task_id` in frontmatter, and the record follows it. Fixed alongside: **`POST /api/tasks` silently dropped `estimateMinutes` on create** (the whitelist omitted it while `createTask` had always accepted it), so every freshly created task blocked at the assumed 30. Pinned by `task-blocks.test.js` (25, pure) + `task-blocks-hold.test.js` (12, scratch DB + vault).
- **A block holds MANY tasks, and the window is chosen, not derived (18 Aug 2026)** — Nick's two asks the day it shipped: set the duration AT the moment of blocking, and pack several short jobs into one window ("I might choose a 5 min task and block a 30 min window"). Both change the model. **The block is now the unit and `task_block_items` is the membership** — the first cut keyed a block on one `task_id`, which is wrong for how the work arrives, and the deciding argument is the write-up: a batch must produce ONE note, because four notes for one sitting is friction that stops the write-up happening at all. Migrated by DROP-and-recreate, **guarded on the table being empty and refusing loudly otherwise** — the new shape has no home for rows the old one would hold, so the migration cannot be written honestly and is only safe on the day it shipped with zero rows. ⚠ **The trap the batch introduces**: the checklist of task names is real text in the note, so unfenced it reads as prose and **releases every batch the instant it is created** — the empty-stub bug again wearing a different hat. Hence a SECOND fence (`<!-- neuro:task-outcome-list -->`), separate from the stub's because the two have opposite lifetimes: the stub is scaffolding, the checklist is the record of what the window held and must survive into the finished note. Ticking every box is still not a summary and does not clear the bar. An **unclosed fence fails towards "not written"**, so a truncated note asks again rather than closing work on nothing. **Only the tasks Nick TICKED are completed when the note lands** — `task_block_items.awaiting` is per item, not per block, because a window of four routinely finishes three; completing the fourth because a note exists would put work in the wins ledger nobody did, which is exactly what "a win is detected, not declared" exists to stop. The sweep and `release()` both follow that rule and both report `stillOpenTaskIds` rather than hiding them. **`resolveWindow` precedence is requested → sum of estimates → assumption**, and an explicit request is **never snapped to a bucket**: the block is a real window in a real diary and a 45-minute request that silently became an hour is the feature disagreeing with Nick in his own calendar. The BUCKETS apply only to the estimate written back onto the task — verified live: a 45-minute block stores `minutes: 45` and stamps `estimate_minutes: 60`. A **partly** estimated batch counts as ASSUMED, never a sum: filling the gaps with the assumption and presenting the total as a measurement is the one thing #87 rules out. The write-back is the point of asking here at all — **0 of 154 open tasks carried an estimate** because nothing had ever asked at a moment Nick was already thinking about duration. Only a SINGLE-task block learns from its window; splitting a batch window across its members would invent a number per task. ⚠ **`findSlot` must count NEURO's OWN blocks, not just `calendar_cache`** — the cache only refreshes on a calendar sync, so a block created a minute ago is not in it, and if Graph refused the event it never will be; without this, blocking two tasks in a row proposes the same slot twice and the second is rejected as a duplicate. That is `one-to-one-booking.planAll()`'s lesson word for word. `released` and `dropped` blocks free their slot again. The uniqueness guard moved to **(date_key, start_time)** — a block holds many tasks, so what must be prevented is two events landing on each other. `BatchComposer` lives in the TaskBlocks panel rather than as checkboxes down the task list, which is filtered, grouped and progressively expanded — a selection made there is one Nick loses the moment he changes a filter. Overpacking is **reported, never refused**: a deliberately roomy or tight window is his call. Capped at 12 tasks a block. Pinned by `task-blocks.test.js` (39) + `task-blocks-hold.test.js` (19).
- **Route 3 — the Obsidian capture drop-box** — `Tasks/Capture.md` is a DROP-BOX, NOT A STORE. `task-capture-drain.js` creates the tasks and CLEARS the line (scheduler every 10 min + on startup, audit trail in `Tasks/Archive/Capture drain log.md`). If lines lingered it would silently become a second source of truth. Being one-way is why capture keeps working while the Pi is down. Inline hints: `!must` `p3` `@YYYY-MM-DD`. Deliberately NOT named `New ToDos.md` — an old file by that name is in `Tasks/Archive/`.
- **Two-way Microsoft writes** — Graph delegated scopes are `Calendars.ReadWrite, Mail.ReadWrite, Mail.Send, Tasks.ReadWrite, User.Read, Chat.Read, People.Read` (`GRAPH_SCOPES` in `microsoft.js`) — the three widened/added on 14 Aug 2026 (calendar create, mark-read on dismiss, name→email) are all user-consentable, so the device-code flow still completes without admin approval; MSAL persists to `.ms365-token-cache.json` so restarts keep auth. Adding a scope needs a fresh device-code flow (`POST /api/microsoft/auth`) AND tenant admin approval — the consent screen raises the request. **A device code EXPIRES and that is now tracked (17 Aug 2026, #66)** — `startDeviceCodeFlow` returned the cached `deviceCodeInfo` whenever a flow was pending with nothing recording when the code was issued, so after the admin-approval wait, asking for a fresh code handed back the dead one twice and only a backend restart cleared it. That is the exact moment Nick is locked out, so the one path back in must not be the stuck one. `isDeviceCodeUsable(info, now)` is a **pure function of issue time**, unit-tested as one — exercising a real flow means asking Microsoft for a code nobody will type in. The response's own `expiresIn` **beats** the 15-minute default (a tenant may shorten it), with a 30s margin so a code cannot die mid-typing, and a legacy unstamped code reads as **unusable** rather than trusted so the fix cannot no-op on what is already in memory. `graphFetch` is GET-only; writes go through `graphWrite`. Email replies use `/me/messages/{id}/reply` (threads + quotes) and dismiss from triage on send. Task completion: To Do PATCHes `status`, Planner needs an `If-Match` etag so it reads first — `Tasks.ReadWrite` covers both, `Group.ReadWrite.All` is NOT required. The vault stores only a bare `<!--id:...-->` with no list, so `fetchTodoTasks` caches task→list and a cold start searches the lists. **That cache is persisted (16 Aug 2026, #71)** — in memory it made the cold-start walk the NORMAL path, because the backend restarts several times a day on deploys, so most completions paid for it. Now `agent_state.ms_todo_list_by_task`. Persisting flips the failure mode, since an in-memory cache self-corrected every restart and a stored one does not, so two guards: `fetchTodoTasks` **re-keys the whole list** rather than appending (a completed task falls out on its own, the map stays the size of "tasks currently open", and the re-key returns whether anything changed — that flag gates the DB write, which otherwise fires per list per sync for nothing), and `completeTodoTask` **forgets the entry and re-walks once on a 404**, so a task moved between lists heals on the next completion rather than at the next deploy. Verified live: 9 entries / 2.5KB, reloaded intact across a restart. Pinned by `todo-list-cache.test.js`. `/api/todos/complete-ms` reports whether the push landed and keeps the Power Automate webhook as fallback; the vault line toggles either way.
- **Standup/EOD as a session (14 Aug 2026)** — `services/standup-session.js` replaces the fixed three-question stepper with a tool-using conversation. State machine `active → ready → finished`, one session per day per kind, persisted in `agent_state` as `standup_session_<kind>_<date>` (KV, not a table — a schema migration on the live DB is a bigger risk than the query convenience is worth). **The transcript is saved before AND after every turn**, which is the whole point: the old flow held everything in browser state until one final POST and lost it all when that POST failed. Tools: `resolve_commitment` (a carry of 3+ days must be decided, and `scheduled` only counts if it creates a dated task), `set_focus` / `set_eod_summary` (mark the session ready), `create_task`, `complete_task`. Context is gathered deterministically up front from `standup-accountability` + working memory + task store — a standup that pauses to search the vault is a standup that gets skipped. `finish()` renders the note; it does NOT scrape a `===DAILY_NOTE_START===` marker out of the model output like `/interactive` did. **The `## Focus Today` / `## Carry-Overs` headings are a contract** — `standup-accountability.parseDailyNote()` reads them back tomorrow to work out what was carried, so changing them silently breaks accountability. Routes: `/api/standup-session/:kind/{start,reply,finish,abandon}` + GET to resume; failures return 503 `retryable:true` WITH the saved session so the client can retry without retyping. Falls back to a tool-less conversation when no tool-capable provider is available (`session.degraded`). The old `GuidedStandup`/`GuidedEod` components in `StandupEditor.jsx` are now unreferenced — left in place deliberately while a second session is active in the repo.
- **The standup could not see yesterday (4 Sep 2026)** — SARA chased a task Nick had ticked off the day before and discussed at EOD, and all three ways she could have known were broken. **(1) Nothing read completions at all.** `buildContext` loads `taskStore.activeTodos()`, the OPEN pool, so a task closed yesterday leaves it and leaves **no trace anywhere in the context** — the wins ledger (`task_done`) was never read. `ctx.closed` now names what was FINISHED on the day the accountability scan calls yesterday (so the two cannot disagree about which day that was — on a Monday it is Friday), or today for an EOD. ⚠ **Commits are excluded** (folded one row per repo per day, they would dominate a list whose job is to name closed commitments), and ⚠ **a failed read is a NAMED GAP, never an empty list**: *"nothing was finished"* and *"I could not look"* license opposite things to say. **(2) The EOD conversation was written and read back by nothing.** A session lives at `agent_state.standup_session_eod_<date>` and the only reader is `load()` for the same kind and day; the one durable trace is the `## EOD` section, and `parseDailyNote` matched only `**Didn't go to plan:**` and `continue`d past everything else — so **the `**Done:**` bullets were parsed by nothing**. They are now `eodItems`, with the list ending at the next bold label so *"Tomorrow starts with"* cannot be read back as work finished. ⚠ **A commitment an EOD reported done is STAMPED (`reportedDoneOn`), never folded away** — his own account is evidence, not a tick, so the standup asks him to confirm and close it rather than chasing it a fourth time; closing it on prose alone is the wrong direction. **(3) The one thing that should have worked was a straight bug**: `_renderContext` read `acc.yesterday.focus`, **a key `buildAccountability` has never returned** (it is `{date, committed, done, eodDone, unresolved}`), so every standup opened with *"committed to 0 things, 0 done"* and an empty checkbox list however full the note was — the counts were computed correctly all along and then not used. `yesterday` now carries `items` too. ⚠ Note ticking a task in NEURO does **not** tick a daily-note line, which is why the ledger read is the only one of the three that closes the gap on its own. Pinned by `standup-yesterday.test.js` (9, incl. three negatives).
- **Chat tool use (14 Aug 2026)** — `services/chat-tools.js` gives chat hands; before this it was read-only RAG, so "mark that done" / "book an hour for it" were impossible by construction. Three tiers, and the tier IS the safety model: `read` (get_queue, get_tasks, get_calendar, search_vault, read_note, get_urgent_emails) has no side effects; `write` (create_task, complete_task, append_daily_note) is reversible and internal, so it runs immediately; `queued` (draft_email_reply, schedule_focus_block) NEVER executes in chat — it creates a pending `sara_action` and the send/booking happens only on approval. The agentic loop is `anthropic-provider.chatWithTools()` (max 5 rounds, history normalised first because the tool API rejects the malformed histories the text path tolerates). Only Anthropic supports tools, so other providers fall back to the text-only path rather than pretending. Gated by `ai-routing.isCloudAllowed('chat_sync')` so AI_MODE and the daily budgets apply; the loop reports its own usage via `ai-routing.recordUsage()` or tool turns under-count. Kill switch `CHAT_TOOLS_ENABLED=false`. With tools live the `[ADD TODO:]` marker is suppressed in the prompt — otherwise the task is created twice. `read_note` resolves inside the vault root and refuses anything else: a model-supplied path is untrusted input.
- **SARA actuators (14 Aug 2026)** — `suggestion-engine.executeAction` is async and does real work, not just navigation: `reply_email` (Graph send + triage dismiss), `complete_task` (task store + Graph push + vault mirror), `schedule_focus_block` (Graph calendar create). Outbound email is deliberately two-gated — `draft_reply` writes the words and queues a separate `reply_email` action carrying them, so nothing sends until Nick has read the draft and approved again. Knock-ons: `/api/actions/batch` no longer runs inside `db.batchSaves()` (better-sqlite3 transactions are sync-only, and the flush-batching it existed for died with sql.js), and `action-candidates.js` lost its hard-disabled auto-promote branch because that function is sync and would have marked every promotion failed. `suggestion-engine.queueAction()` is the front door for anything that wants SARA to act without acting itself.
- **Approving an action (15 Aug 2026, #97)** — `components/ActionsPanel.jsx` (sidebar **Actions**, primary group + mobile bottom nav) is the approval surface for the whole queue. Before it, nothing in either frontend read `GET /api/actions`: `TodoPanel` handled only the todo suggestions in its own payload and `WaitingOn` only `chase_commitment`, so the SECOND gate on outbound email was reachable only via a push notification's action card. **Every word on a card comes from `action.presentation`**, built by `services/action-presenter.js` on the server, from the same stored payload `executeAction` will read — reconstructing it client-side gives a screen free to drift from what actually sends. Per type it returns the **full body verbatim** (never a summary, for anything outbound), the real recipient, `warnings`, and `blockers` restating `executeAction`'s own `{ok:false}` guards; a non-empty `blockers` **disables approve and says why**, rather than an approve that quietly fails. Only payload-inspectable guards live there — anything needing I/O stays the executor's to report. Outcomes persist on screen after the card has gone, because a failed action is marked `failed` and drops out of pending, so a vanished card is otherwise indistinguishable from success. `action-presenter.test.js` parses the `case` labels out of `executeAction`, so **adding an action type without a presenter fails a test** rather than showing a bare card months later. **`getPendingSaraActions` defaults to limit 10** — `/api/actions` called it bare and reported 10 pending against a real queue of **930** (926 `capture_todo` promotion candidates). The route now reads all of them, sorts **outbound first** (`capture_todo` last of the writes — bulk-generated nightly, and it already has a home on Tasks), caps the payload at 120 and returns `pendingTotal` / `pendingByType` / `pendingByKind` so no count on screen is the capped one. That ordering is what makes the cap safe: nothing that would leave the building can be what gets dropped. **A navigation action gets its own type per destination, and it expires (17 Aug 2026)** — the meeting-prep nudge reused `open_task` with `navigate:'meeting-prep'` in the payload, and since the presenter keys on TYPE, every one rendered as *"Open tasks — jump to your task list"*: six cards identical above their reason line, each naming a destination it did not go to. `open_meeting_prep` fixes new rows; `open_task` now names a non-`todos` destination so the ones already pending stop lying. It stores the **start**, not `minutesAway` — a stored relative time is wrong a minute after it is written — and that start is what `suggestion-engine.navigationExpiry()` reads. Nothing had ever aged a shortcut out, so a prep card for a 09:45 meeting was still asking to be approved at 11:40, when the only honest thing left to do with it was reject it. Two rules: the moment in the payload has passed, or **it was raised on an earlier day** (the general one — "now" is the whole premise of a shortcut). The predicate is **pure** (action + now, no DB, no clock) so it pins without a database; **what counts as navigation is `action-presenter`'s call**, never a list of type names, the same rule that keeps three places agreeing on "leaves the building" — so a week-old drafted reply or queued chase is never swept. Status is **`expired`**, following `superseded`: Nick decided nothing, and the rejection history is a record of what he turned down. Runs on the Focus path (before the dedupe read, or a spent row blocks today's fresh one) and at the head of `GET /api/actions`. Pinned by `navigation-expiry.test.js`.
- **A suggestion that DID something is not offered again (7 Sep 2026)** — the offered-once rule in `suggestion-engine.generateSuggestions` + the pure `suggestionIdentity` + `db.getDecidedSaraActionsByType`. Nick: approving the *"Draft a reply to Simon Greenhalgh"* card made it vanish, and actioning the next card brought it straight back. **The dedupe had only ever read PENDING actions**, on the stated grounds that *"navigation actions are repeatable — the user should always have a 'Do it' option available"*. ⚠ **That is true of a SHORTCUT and false of an ACTUATOR**: approving `draft_reply` spends a model call, writes the words and queues a `reply_email` for the second gate. So approving it took it out of the pending set, the email was still urgent (it only leaves triage when dismissed or replied), and the very next `/api/focus` — every Focus load, every agent loop, every briefing — regenerated the identical card. **Measured on the live DB: ONE email accounts for 1,168 `superseded`, 6 `executed` and 3 `rejected` `draft_reply` rows since 14 August** — six separate approvals, six paid drafts, and at one point **two `reply_email` actions pending for the same message**. The reappearing card was the visible half of a loop quietly duplicating an OUTBOUND send path. ⚠ **What counts as repeatable is `action-presenter`'s call, never a list of type names here** — the same rule that already keeps `navigationExpiry`, the Actions queue, `bulk-reject` and `state-of-play` agreeing on what "leaves the building"; `NAVIGATE` stays repeatable, everything else is offered once per decision. ⚠ **`focus_item_id` DOES NOT IDENTIFY THE THING**: every urgent email arrives as the literal item `email-urgent`, so deduping against history on that alone would let one drafted reply silently suppress the offer for **every future email** — an offered-once type therefore names the payload field that identifies its subject (`IDENTITY_FIELD`, `draft_reply` → `emailId`). ⚠ **No identifying field means DO NOT SUPPRESS**, and a failed read falls back to pending-only: between a duplicate card and a silently withheld one, the duplicate is the failure that can be SEEN. ⚠ **`superseded`, `expired` and `pending` are not decisions** (Nick chose nothing — `navigationExpiry`'s rule) and **neither is `failed`** (he approved it and it did not happen, so offering it again is correct). ⚠ **The status filter is in SQL, not applied to a page of rows in JS** — 1,168 superseded rows sat in front of the decisions, so any recency window cheap enough to run was small enough to miss every one: that is `getSaraActionsBySource`'s *"a LIMIT is a cliff, not a page"* one type along. Pinned by `suggestion-repeat.test.js` (10, real scratch DB, **mutation-checked** — disabling the branch fails 3, including the thousand-superseded-rows case).
- **Waiting on — commitment chasing (15 Aug 2026)** — `services/waiting-on.js` + the `waiting_on` table record what OTHER people owe Nick (`standup-accountability` covers the other direction). 287 items / 29 people / oldest 107 days, backfilled from 232 meeting notes. UI is `components/WaitingOn.jsx`, mounted twice: on the **People board** above the roster (all 29 people, grouped, worst offender first, top 5 with "show all"), and inside **PersonDetail** filtered to one person via the `person` prop — which is a canonical **first name**, because that is all `waiting_on` stores. Pending chases are approved **in the panel itself** — nothing else in either frontend reads `GET /api/actions`, so before that the Chase button queued into a hole. `queueChase` is async and stores BOTH the built message and the resolved recipient on the payload at queue time, so the approval card shows the exact words and the exact address; the executor prefers `payload.to.email` over re-resolving, or a manual override would be silently discarded. The address is editable via `POST /api/waiting-on/chase/:actionId/recipient` (scoped to `chase_commitment` — `/api/actions/:id/approve` stays a plain approve with no general payload-edit door), because `contact-directory` resolves a canonical FIRST name and "Chris" legitimately comes back `ambiguous`. An override is stamped `source: 'manual'`: a *guess* must clear the `resolved` gate, a *choice* need not. No address = approve disabled, rather than an approve that quietly fails. Four row actions (Q12): `chase` **queues a `chase_commitment` sara_action and never sends** (a chase goes to a direct report; an automated one reads as surveillance), `done`, `drop` (the backfill was automatic, so some rows are misparses), `snooze` (a date, not a boolean — snoozed rows are hidden client-side but stay reachable behind a counter). **PULL-ONLY (Q14): nothing here notifies or pushes.** 287 items would have made a very loud first day, and nudge volume is the one signal allowed to argue against building more. Per-person rows are capped at 5 + "N older" — the worst offender's 13 items measured 1878px on a 390px phone and buried the roster. Also surfaced **read-only in 1-2-1 prep**: `routes/meeting-prep-view._buildPrep()` attaches `att.waitingOn` + up to 3 topic lines, rendered by `sara/app` MeetingPrep. That match uses `entities.getRoster().firstNames` (unambiguous in `People/` AND the full name agrees) — **matching on the bare first name attributed one Lucy's 16 commitments to four Lucys and Chris Middleton's 31 to a Chris Smith.** Enrichment is skipped above 8 attendees: a 309-person SMT update or the 13-person standup is a broadcast, not somewhere you work through a commitment list.
- **Teams (15 Aug 2026)** — both paths built, both dark until an external step Nick owns; **an unset webhook and an unconsented scope are NORMAL states, not bugs** (Q8). **DM (NEURO):** `teams.sendDm()` sends as Nick over `ChatMessage.Send`, which needs tenant admin approval. **NEVER add it to `GRAPH_SCOPES`** — `getAccessToken()` passes that whole list to `acquireTokenSilent`, so one unconsented entry makes the silent call throw and takes Calendar, Mail, Tasks and briefings with it; "add Teams" becomes "Microsoft is down". Optional scopes go through `microsoft.getScopedToken(scopes)`, which acquires them ALONE and touches neither `graphTokenCache` nor `lastTokenError`. Probed live: `ChatMessage.Send` → `consent` (AADSTS65001), `Mail.Send` → GRANTED, main token unaffected — so a capability built on it lights up on approval alone, no code change, no re-auth. `sendDm` mirrors `email-sender.sendMail`'s `{sent, reason}` shape so callers can fall back without special-casing, finds the existing 1:1 chat via `Chat.Read` (already granted) and deliberately does NOT create one (`Chat.ReadWrite` is a second unconsented scope for a case email covers). `GET /api/microsoft/teams-send-status` makes the waiting inspectable. **Channel (NOVA `42b412c`):** `services/teams-webhook.ts` posts an Adaptive Card to a **Power Automate Workflows** webhook, replacing the O365 connector `MessageCard` in `alert-service.ts` — a retired API that had never sent anything anyway, since `agent_teams_webhook_url` was always unset. A `webhook.office.com` URL is detected and refused with the reason rather than posted into a void; 8s abort so a hanging webhook can't stall the alert queue. **Delivery is email-first with Teams layered on (Q9)** — the chase carries `payload.channel`, and a Teams DM that can't be delivered FALLS BACK to email with the reason in the result, because the point is that the person gets asked. The channel picker only renders when the status endpoint says the scope is consented.
- **What the AI stack costs (26 Aug 2026)** — `services/ai-cost.js` (pure) + the `ai_calls` table + `ai-routing.getCostSummary()` + the Spend block in `PiHealthPanel`. `ai_routing_usage` answers *"am I near the cap today"* and nothing else: one number that resets at midnight, so *"what did last week cost"* and *"what is spending it"* were unanswerable. **Deliberately NOT a new pattern — it adopts NOVA's** (`agent_llm_calls` in `daypilot`): one denormalised row per cloud call, cost frozen at write time, a per-million price map, `SUM/GROUP BY` for every rollup. ⚠ **`PRICES_PER_MTOK` is a deliberate COPY of NOVA's `MODEL_PRICING`** — separate repos on separate machines, so it cannot be shared as code; a test pins the overlapping values so a change on either side is a visible decision rather than drift. **Two things had to be fixed before any cost figure could mean anything.** (1) **`streamChat` returned hardcoded zero usage** — chat is the biggest consumer and is OpenRouter-first by policy, so every chat recorded a call costing **nothing**, and it was invisible to the daily token cap too, which had therefore been under-counting the largest single consumer since the cap existed. OpenRouter reports usage in a final stream chunk but **only if asked** (`stream_options.include_usage`); measured live, one trivial chat turn is **5,433 prompt tokens** of RAG context, so the cap was out by roughly that much per turn. (2) The counter threw away the **prompt/completion split and the model** — input and output differ ~5× in price, so cost was not derivable from what was stored. **Four traps NOVA hit, avoided here**: an unpriced model is **`null`, never 0** (NOVA's `estimateCost` returns 0 with no log, so unknown models silently record as free and vanish from every rollup — already biting there), *"no tokens reported"* is **unknown, not free** (that is the streaming bug's exact signature), the **vendor's own charged cost is preferred** over the local table via `usage: {include: true}` so the figure is what was actually billed, and **USD is stored AND displayed as USD** (NOVA renders unconverted USD behind a `£` sign; a pound figure needs an FX source with a date on it). Every total carries an `unpriced` count and the panel shows it, because a total is only the whole story when that number is 0. ⚠ **Two callers bypass the routing tiers and report their own usage** — `claude.js`'s chat-tools turn and `standup-session.js` — so they must pass `{provider, model, taskType}` explicitly or the most expensive thing NEURO does (a bounded 5-round tool conversation) lands unattributable; verified live, a real chat turn recorded against a blank task before this was wired. The ledger's day boundary deliberately matches `ai-routing._todayStr()` (UTC), not the local one used elsewhere, or the panel would show two disagreeing "todays" for an hour every evening. Pinned by `ai-cost.test.js` (10).
- **The half-day planner — the diary is written TO, not asked (27 Aug 2026)** — `services/day-planner.js` + `routes/day-planner.js` (`/api/day-plan`), cron **07:15 morning / 12:30 afternoon, Mon–Fri**. `task-blocks` could block time since 18 Aug and had been used **once**, for a ten-task test block closed five minutes in; grep it for `webpush` and there are **no matches**, and its only scheduled hook is `sweep()` — *"has the outcome note been written?"*, a **detector, not a driver**. So it could see whether Nick had done the work and could never tell him to start, while creating a block cost six deliberate steps on a desktop screen he had to remember existed ("I forgot we had that"). The governing constraint is Vantage's: **his difficulty is INITIATION, not knowledge, and "anything that raises awareness without lowering the barrier is the wrong shape"** — proposing raises awareness, having already put it in the diary lowers the barrier, so this **auto-creates** (Nick's call, 27 Aug). **The half-day horizon is the design, not a limitation**: his diary moves under him, so an afternoon planned at dawn is planned against a calendar that no longer exists by lunch. `planWindow`/`estimateMultiplier`/`freeGaps`/`sizeOf` are **PURE** (the `pi-health.assess()` split). **Estimates are the point**: his only two real focus sessions ran **44 and 111 minutes against a planned 30** and nothing consumed that — `time-fit` still assumes a flat 30, so every block was sized against a number already known wrong. The multiplier is applied to the **ASSUMED duration only, never to a figure Nick typed** (silently inflating his own number is the planner disagreeing with him in his own calendar), is a **median not a mean** (one 3.7× runaway would drag a mean), clamped 1–4×, and below 5 samples reports `learned:false` and says so on the card. It is **never written back as the task's estimate** — that would launder a guess into a measurement, the one thing #87 rules out; the real number comes from the end-of-block prompt where the answer is known. Refusals: an **unreadable calendar refuses to plan** rather than booking blind (`calendarKnown:false` ≠ free); what did not fit is **reported as `overflowed`**, never silently dropped; *"nothing open"* and *"no gap long enough"* stay distinct reasons; **3 tasks per block, 2 blocks per half-day** against the ten-in-thirty-minutes block that is the only one ever made by hand; a single oversized task is still scheduled and marked `overpacked` rather than never fitting. ⚠ **The lock is not optional** — `run()` takes a **synchronous** in-process lock BEFORE planning and stamps the ledger **per successful create, never batched at the end**, because the cron and the route can overlap and that exact shape turned 27 Plaud admin blocks into **52 real calendar events**. Idempotency is a **ledger, not a calendar scan** (deleting a block is a DECISION); `POST /api/day-plan/forget` is the way back. **Deliberately NOT a `TRACKED_JOBS` catch-up job** — replaying a morning plan at 14:00 would block time that has already gone. ⚠ `gather()` reads `task-store.activeTodos()`, which returns the **LEGACY todo shape** (`task_id`, `estimateMinutes`), NOT task rows (`id`, `estimate_minutes`) — reading the wrong keys filtered all 148 open tasks out and reported *"nothing open to schedule"* against a full backlog, silently and plausibly; the mapping is `toPlannerTask`, pure, with the row shape pinned as a **negative** test. **`DAY_PLANNER_ENABLED` defaults FALSE** — it writes real events to a real calendar on a timer. ⚠ **IT BOOKED THE SAME TASK TWICE (8 Sep 2026).** `gather()` counted existing blocks as walls in TIME and never as claimed WORK — and because a block HOLDS its task open until the outcome note is written, a blocked task stays in `activeTodos()` and came back top-ranked on the very next run. Measured on the live Pi: four tasks each in two open blocks, one of them (#151) booked by this planner twice in one day, 11:05 by the morning run and 14:35 by the afternoon one. ⚠ **The deeper hole was that `addTask` refused a task already in another open block and the CREATION path said nothing**, so every route in (this timer, BlockTimeControl, the batch composer) could duplicate freely — the refusal now lives in `task-blocks.plan`, which every create goes through, so the PREVIEW refuses rather than the confirm. `ignoreBlockId` is the one exemption, for `reschedule`, which deliberately creates the new block BEFORE detaching from the old one. ⚠ A **terminal** block (dropped/released/complete) never holds a task hostage, or one abandoned window would make it unblockable for ever. ⚠ `blockedTaskIds()` returns **null on a read failure, never an empty set** — empty reads as *nothing is blocked* and is exactly the bug again; the planner then plans NOTHING and names the gap rather than booking blind, and `held.blocked` is REPORTED beside `held.personal` (which had been counted "so the number is reportable" and never reported), or a planner with nothing left to book looks identical to one with nothing to book. Rows predating the refusal still exist, so `setTickEverywhere`/`settleTaskElsewhere` stay as their settler. Pinned by `day-planner.test.js` (27) + 7 in `task-blocks-hold.test.js` (88), both rules mutation-checked.
- **A commitment extracted fourteen times is still one commitment (27 Aug 2026)** — the cross-note fold in `action-candidates.syncNoteActionCandidates`. Measured: **258 pending `capture_todo` actions collapse to 54 distinct commitments**, with single ones repeated 14×. Cause: Plaud writes several summary variants per recording, each landing as its own note, and the only dedupe was `getSaraActionsBySource` — **scoped to ONE note by design**, which is right for superseding and blind by construction to the other thirteen. Folding is **LOSSLESS**: the other meeting's wording and source line are appended to the survivor's payload as a `sightings` entry, so nothing a meeting said is discarded, it just stops being a separate row; and if the survivor is later **rejected** it leaves the pending pool so a fresh copy is created next sync (a rejection must not suppress the next sighting for ever). Reuses `task-dedupe`'s `scorePair`/`tokenize`/`buildIdf` via a new **pure `findEquivalent`** rather than a second matcher. ⚠ **It does NOT inherit `task-dedupe`'s 0.42, and that is the finding**: that number was measured on NEURO-vs-Microsoft tasks — two *independently worded* lists — while these are all extracted from Nick's own meetings and read "Nick will <verb> the <support noun>", sharing his stock vocabulary almost completely. At 0.42 it merged two live pairs and **both were wrong**, the worst folding *"meet Naomi on 26/27 Aug to review and sign the risk assessment"* into *"complete the remaining sections of the risk assessment form"* — hiding a **dated meeting** behind a form. Measured separation against the full pool (which is what sets the IDF): exact repeat / case variant / minor reword / clause dropped all **1.000**; worst live false positive **0.499**; genuinely new commitment **0.150**. **`FOLD_SCORE = 0.85`** sits in that gap, at the **conservative end deliberately** — the directions are asymmetric: a missed fold leaves a visible, cheap duplicate, a wrong fold hides real work in the one queue Nick uses to find what he owes. Verified live: **258 → 54 survivors, 204 folded, 0 non-exact merges**. `CAPTURE_DEDUPE_ENABLED=false` switches it off; both false positives are pinned as **negative** tests in `capture-dedupe.test.js` (11).
- **A cache with no writer is not a stale cache (27 Aug 2026)** — the Jira queue. `state-of-play` had been reporting *"Jira queue cache is stale — last fetched 54 days ago"*, which sent the reader after a refresh that does not exist. What actually happened: commit **48e6481 (3 Jul) DELETED the queue feature** — *"too much noise"* — including the sync that wrote `jira_tickets_cache`, leaving **`upsertTicket` with zero callers**. Then **three later commits reintroduced READERS**: `routes/queue.js` on 15 Jul, `working-memory` and `chat-tools` on 14 Aug. Nobody noticed because the table still held the **twelve rows that were in it the morning it was switched off**, so every reader got plausible data for ever. For seven weeks NEURO stated a frozen 3 July snapshot as current fact in **chat's `get_queue`, `/api/queue/summary`, three standup prompts, the EOD prompt and its daily-note line, `standup-accountability`, and `working-memory`** (which feeds the briefing and every chat turn's RAG) — every one guarding on `total > 0`, which is true of stale data. The first fix gated every consumer on a new `fresh` flag and restored `syncQueue()` behind `JIRA_QUEUE_SYNC_ENABLED` (default OFF), so the choice was one env var rather than a rebuild. **Nick took the other option the same day: RIPPED OUT (27 Aug 2026).** The queue was a deliberate product removal in July and had not been missed in seven weeks, and dead-but-readable code is precisely how the bug happened — so the deletion was finished rather than half-held. Gone: `getQueueSummary`/`upsertTicket`/`clearStaleTickets`/`getAllTickets`/`getAtRiskTickets`, `syncQueue` + the flag + its polling branch, `routes/queue.js` and its mount, chat's `get_queue` tool, the MCP `get_queue` tool, `activity.trackQueueSnapshot`, the queue blocks in `working-memory`/`claude.js`/`chat-context-v2`/`ai-provider`/`standup-session`/`standup-accountability`/`routes/focus`, six sites in `routes/standup.js`, and the `state-of-play` card. ⚠ **Escalations were NEVER part of this and are untouched** — `routes/escalation.js` queries Jira live (`fetchActiveEscalations`, `/rest/api/3/search/jql`) plus NOVA's `escalation_log`, and never read the cache; that path is what still surfaces the tickets that matter. **Thirteen call sites, not the eight the first pass counted** — the two nobody had found were the MCP `get_queue` (which called `/api/queue`, a path that never existed, so it had been 404ing, and had no freshness gate) and `standup-session`, which pulled the figures into both the session prompt and the daily note. `## Queue Watch` is gone from all three note writers; **`## Focus Today` / `## Carry-Overs` are the parsed contract and were not touched**. `working-memory.queueSummary` is kept as a permanent `null` so downstream readers stay valid and silent rather than throwing. ⚠ Two things found on the way out and worth keeping: `attention.js` read `ctx.queueSummary?.breaching`, **a field working-memory never set**, so `context-state`'s firefighting trigger has only ever been driven by escalations (the read is removed; `context-state` defaults it to 0, so behaviour is unchanged); and the `queue_snapshot` rollup case + the InsightsPanel "Queue EOD" stat went too — **checked against the live DB rather than assumed**: activity_log held **zero** such rows across 9,035 entries since 18 June, because both writers sat on EOD *completion* paths and `eod_done` has fired twice in that log's whole life. That stat had never once rendered. The table is left **defined and empty** in `schema.sql` following the `inbox_items` precedent (dropping it is a destructive migration that buys nothing). If SLA-at-risk awareness is ever wanted back, build it on the live escalation path — **not** a cache, and note the legacy `/rest/api/3/search` now answers **410 Gone**. Pinned by a negative test in `standup-session.test.js` (the daily note must carry no queue figures, not even a "no queue data" placeholder) and one in `state-of-play.test.js`.
- **Leave is READ, not declared (27 Aug 2026)** — `services/team-availability.js` + `GET /api/nudges/availability` (+ `/refresh`), fed by NOVA's new `routes/neuro-bridge-availability.ts`. NEURO went quiet on a day off only when Nick pressed the 🌴 button; NOVA has known the real answer all along, syncing People HR into `agent_availability` daily, and **Nick is in it — AgentId 24, PeopleHrId D2V00244**. Same rule as *who reports to Nick is READ not typed* and *1-2-1s are detected not declared*, one step later. **Why it needed a bridge route rather than a connection string**: `agent_availability` stores **no names** — `roster_id` is a bare integer pointing at `dbo.Agent.AgentId` in the KPI database on **another server**, with no FK and no join table, and the display name is stitched on at READ time from credentials in NOVA's admin settings (which live in a **file-based** `settings.json`, not the SQL `settings` table — that one lacks `kpi_sql_password` entirely and is a red herring, as is NOVA's empty `agent_roster` table). The full chain is **People HR EmployeeId → `dbo.Agent.PeopleHrId` → `AgentId` → `agent_availability.roster_id`**. NOVA's own `/api/agent/availability/*` do the stitching but sit behind `requireAreaAccess` JWT that the bridge secret does not satisfy — #65's exact species, a route that exists and answers 401. ⚠ **COMPLEMENT, NOT REPLACEMENT**: `GetHolidayDetail` returns **APPROVED leave only**, so an absence still awaiting a manager is legitimately absent (Nick had exactly that for 28 Aug on build day) — the manual flag is checked **FIRST and wins**, and covers same-day decisions and a dead network. **`nudgeSuppression()` is SYNCHRONOUS and called by seven triggers, so it must never do I/O**: `refresh()` is async and scheduled (30 min + 20s after boot), `snapshot()` is a sync cache read — the `working-days` live→cache pattern, for the same reason. A failed refresh **keeps the previous copy** rather than emptying it. **Every failure mode here is NEURO going silent when it should not have**, and a quiet system looks identical to a working one, so the pure predicate returns a REASON and refuses four things that merely resemble an all-clear: never fetched; an **empty roster** (broken, not a free team); Nick absent from the roster; and an agent with **no `PeopleHrId`**, who never syncs and so always looks available — absence of evidence reading exactly like presence. Identity is `NOVA_AGENT_ID` (24) when set, else an exact full-name match, and **which one answered is always reported**; name matching is case-insensitive but not fuzzy ("Nick Wardle" is someone else). `othersOff()` gives who on the TEAM is away — wanted by meeting prep, 1-2-1 booking and the day planner. Pinned by `team-availability.test.js` (12, pure). ⚠ **NOVA's half shipped on the `nova-codex` branch and is DEPLOYED (verified live 31 Aug 2026: `known:true`, `matchedBy:"id"`, roster 13, 20 absence days, a colleague correctly shown on annual leave).** This line previously said it still needed an IIS deploy and was repeated twice from the doc before anyone called the endpoint — check `GET /api/nudges/availability` rather than trusting this sentence.
- **Nobody is booked onto a day they are off (27 Aug 2026)** — three consumers of the People HR feed, added the moment it became readable. **1-2-1 booking**: `findSlot` takes the set of dates the OTHER person is off and skips them. Until this, the search could only see NICK's diary — his own leave was avoidable (`showAs:'oof'` among the events he holds), but the person being invited could be on a beach and the slot still looked free, because NEURO has never held `Calendars.Read.Shared`. `book()` emails a real invite to a real direct report, so that is not cosmetic. `findSlot` is the single funnel for propose / planAll / reschedule, so one change covers all three — including reschedule, where Graph mails an "updated" notice and landing on a holiday is the same mistake. **Day planner**: skips a day Nick is on booked leave; it auto-creates real events on a timer, so filling a holiday with focus blocks is the most visible way it could embarrass itself. **Meeting prep**: flags an attendee who is off — Graph's `responseStatus` answers a different question and is usually stale, since an invite accepted three weeks ago says nothing about a holiday booked since. ⚠ **Unknown NEVER blocks**: refusing to book because the bridge hiccuped would take the feature down, and booking blind is the survivable status quo. What must not happen is a blind booking that CLAIMS to have checked — so `propose`/`reschedule` return `awayCheck: checked|unknown` plus an `awayNote` naming the reason, `planAll` returns `awayChecked`/`awayUnchecked` BY NAME (a batch that silently checked nobody looks identical to one that checked everybody), and meeting prep carries `awayUnknown` per attendee so an absent flag means "nothing to report" rather than "I could not look". `daysOffFor` is pure and returns `known` **separately from the date set** — "free all fortnight" and "no idea" license opposite behaviour, and unknown covers a name absent from the roster and, the subtle one, an agent with no `PeopleHrId` whose absences never sync and who therefore always looks available. Full name only (the `entities.getRoster()` rule). Verified live: Zoe Rees is off 28–31 Aug and the proposal came back **1 Sep 15:00** — Friday the 28th is a working day and would otherwise have been the first slot offered. ⚠ `describe()` is ASYNC and `propose` spreads it; **spreading a promise yields `{}` silently** and shipped a proposal consisting of nothing but the two new fields. `one-to-one-reschedule.test.js` now asserts every `describe(` call site is preceded by `await`.
- **Notification governor (14 Aug 2026)** — every `webpush.sendToAll()` passes `_governor()`: quiet hours (`PUSH_QUIET_HOURS`, default `22:00-07:00`, `off` disables), 30-min dedupe on type+title+body, hourly cap (`PUSH_HOURLY_CAP`, default 6). `ALWAYS_DELIVER` types (escalation_alert, meeting_alert, system_alert, capture_failed, test) bypass quiet hours and the cap but NOT dedupe — the same escalation arriving via both the nudge path and the alert path is still one escalation. State persists to `agent_state.push_governor`: an in-memory budget resets on every restart, and the backend restarts several times a day. 25 call sites send push and none of them knew about each other.
- **A notification NEURO could not deliver is a fact worth keeping (28 Aug 2026)** — the `push_log` table + `GET /api/push/log` + two entries added to `ALWAYS_DELIVER`. Nick said push "feels inconsistent" and could not back it up; nothing in NEURO could either, because the only record was `console.log`. **Measured from the pm2 log: 661 sent, 141 suppressed, 13 failed** — so the transport is fine (>99% of sends reached at least one endpoint) and a second delivery channel (ntfy, Pushcut, Glance) would have fixed nothing. **The inconsistency was the governor doing its job invisibly**, and one real bug underneath it. ⚠ **`meeting-prep.js` sends `type: 'meeting_prep'` while `ALWAYS_DELIVER` listed only `meeting_alert`** (which `briefing.js` sends) — two separate meeting paths, one of them suppressible. The hourly cap swallowed **seven real "Meeting in 25 min — 1-2-1" alerts** and one weekly risk report. Invisible for the whole life of the governor because `webpush.test.js` asserted the bypass using `meeting_alert`, **a string that code path never sends** — the same species as #122's wrong metric name: a test over an invented identifier is green and proves nothing. `push-types.test.js` now **scans the source** for every type `sendToAll` is actually called with and fails unless each is in `ALWAYS_DELIVER` or an explicit `SUPPRESSIBLE` list (the `action-presenter.test.js` pattern), **with a positive control** so a broken scan cannot pass by absence — it immediately caught `weekly_risk`, which nobody had classified. Both of `sendToAll`'s silent returns (**no subscriptions**, **VAPID unset**) now record `undeliverable` with the reason: a 410 from Apple deletes the subscription, so NEURO going permanently mute looked exactly like a quiet day. Bounded to **90 days on write** (the email-triage lesson — an append-only store with no retention becomes the next pile). ⚠ **4 push subscriptions are registered, three dating from July** — every notification fans out to all of them; `DELETE /api/push/subscriptions` then re-subscribing from the PWA is the reset, at the cost of silence until he re-opens it.
- **A weekly task target, and a ring that reads without colour (29 Aug 2026)** — `services/weekly-target.js` + `routes/weekly-target.js` + `weeklyTarget` on the attention payload + the ring in `sara/widget`. The wins ledger already counted what Nick finished; what it could not answer is *"am I on track"*, because a running total has no denominator — 28 is only good or bad against something. One integer per ISO week in `agent_state`, counted from the **ledger** (`kind='task_done'`, `SUM(count)` not `COUNT(*)`) rather than a second counter, because two counters for one number is how the inbox came to report 37 and 3 on the same morning. **Four states, and keeping them apart is the product**: `unset` (nobody said what the week is for — NOT a target of zero, which would render as a full red ring saying "you did none of the nothing you set"), `unknown` (the ledger could not be read), `behind`, `on-track`/`met`/`exceeded`. **`behind` is judged on PACE, not the raw fraction** — 10 of 20 is on track on Wednesday and behind on Friday — and on a WHOLE task, so 5 of an expected 5.4 is not a failure dressed up as a judgement. Pace spreads over WORKING days and caps at Friday, or a quiet weekend would make him look progressively worse for doing exactly what he should. ⚠ **THE RING MUST READ IN MONOCHROME.** iOS renders lock-screen accessories with a vibrancy tint, so hue is stripped and the requested green-for-done / red-for-not would be one flat colour saying nothing — the signal is **FILL** (solid done, faint outstanding), with colour applied on top only where it survives. Exceeding the target draws an **inner second lap**, which needs no colour to be understood. Drawn as segments via `DrawContext.fillEllipse`, the best-supported thing it does, rather than an arc path. ⚠ **`suggest()` proposed a target of ZERO on its first live run** — weeks `[12,3,0,0,0,0,0,0]`, where six of those zeros were weeks before `task_done` rows existed (they begin 14 Aug; the wins table goes back to 1 June). A median over them is 0, which `setTarget` then correctly refuses. `recentWeeks` now marks a week ending before the ledger began as `known:false` with `done:null` — the "absence is never a zero" rule, biting hardest where the output is a number Nick is asked to commit to. **Nothing is ever set automatically**: a target NEURO picked is one he has no reason to feel anything about, which is the entire mechanism. The **morning standup asks for it** when unset (`set_weekly_target` tool), states it when already set and never re-asks, and **EOD never gathers it at all** — the guard is the CONTEXT, not the tool list, since `toolDefinitions` deliberately does not filter by kind. ⚠ Built in collision with a concurrent session; see the 29 Aug mistakes entry — their service survived as the better design, my duplicate `/api/wins/target` was removed.
- **On duty is not the same question as activity (28 Aug 2026)** — `context-state.resolveDuty()` (pure) on the attention payload. Nick can be AWAY at 11am on a Tuesday (on duty, just not at his desk) or STEADY at 22:00 (a working day, but finished). `activity` decides ranking; `duty` decides what KIND of thing a surface shows — off duty, the widget shows what he DID (wins, the week's shape) rather than what he owes. ⚠ **The hours MIRROR `PUSH_QUIET_HOURS` (07:00–22:00), not a fresh guess.** The first cut invented 08:00–18:00 and flipped the widget to a day-off view at 18:14 on a Friday while he was plainly still working, hiding everything. NEURO already had exactly one considered statement about when to leave him alone; a second, narrower one is how two parts of a system disagree about the same evening. Still wider than `task-blocks`' 09:00–17:30 (a different question — where a meeting can be booked), pinned against it by a test. ⚠ **Unknown resolves to ON duty**: off-duty rendering HIDES work, so it errs towards showing it, and `known` is reported separately from the answer. A **critical** item shows even off duty — hiding a breaching escalation because it is Saturday is the wrong failure.
- **The rest of the day, on the attention payload (28 Aug 2026)** — `agendaFor()`. Deliberately NOT fed through `decision-engine`: that pool is things needing a DECISION, and a meeting already accepted is not one — routing it there would make every meeting something to action. It is the shape of the day, not a queue. ⚠ **Rolls forward to the first day ahead that HAS something**, not merely tomorrow: Nick looks at this on a Friday evening when tomorrow is an empty Saturday, and a one-day roll left the widget just as blank. `scope` names the day (`tomorrow`, else `monday`) and is rendered verbatim so no client is a second opinion about which day these belong to. No countdown across a day boundary — "in 15 hours" reads as imminent. ⚠ **An unreadable diary NEVER rolls forward**; filling `known:false` with tomorrow turns "I cannot see your diary" into a confident statement about it. Tomorrow is a SEPARATE read, not a wider window on `_calendarInput`, because `context-state` decides "am I in a meeting" off that same list.
- **The home-screen widget is the third renderer of one feed (28 Aug 2026)** — `sara/widget/neuro-attention.js`, a [Scriptable](https://scriptable.app) script rendering `GET /api/attention` on the iPhone home screen. Chosen over Glance/Widgy because it **pulls** — nothing leaves the tailnet, versus pushing escalation summaries and named reports into a third party's cloud on a subscription. It renders and decides nothing: `say`, `speech` and `tab` are all composed server-side, so the widget, the Surface and the notification for one thing cannot phrase it three ways. **`attention` cards now carry `tab`**, resolved through the shared `resolveSaraLiteTab` rather than re-derived client-side — the widget would otherwise be a fourth copy of the routing, and a card and its notification landing on different tabs is the invariant that rule exists to protect. **The four silences must stay distinct** and each has its own words: unreachable ("I couldn't ask"), pool unavailable ("this is NOT an all-clear"), quiet (in a meeting / off), and genuinely nothing pending — a blank card is four different facts. `dropped` and `gaps` are surfaced as "N held back · N couldn't be read", never swallowed. ⚠ **The API token lives in the iOS Keychain, never in the file** — the repo is public, which is how the PIN leaked in July. ⚠ **The widget file must contain NO BACKSLASHES — none, anywhere, comments included.** It is the one file in the repo that reaches its runtime by being **copied as text** (vault or QR → paste into Scriptable), and a backslash does not survive that trip: `replace(/\/+$/, '')` arrived on the phone as *"invalid escape in identifier"* and the whole widget refused to parse. Use `String.fromCharCode()` for control characters, `split/join` instead of regex replace, `endsWith`/`slice` instead of anchors. `widget-source.test.js` pins it (and immediately caught two in the comments that *explained* the rule — the exemption written for those was removed, because a rule with a carve-out for the bits describing it is not a rule). That test is also the only thing that ever parses the file: nothing `require`s it, so before it existed **only the phone would discover a syntax error**. Same species as the mistakes-log entry about regex-bearing JS in a heredoc — there the fix was avoiding the pipeline, here the pipeline is unavoidable so the file is written to survive it. ⚠ **Delivery is the VAULT, not the QR** — `backend/scripts/export-widget-to-vault.js` writes it to `Scripts/SARA Widget (Scriptable).md` (a `vault-exclusions` TRANSIENT dir, so 944 lines of JS never reach embeddings or entity extraction), fenced with FOUR backticks because the script is full of template literals. Obsidian puts a copy button on the block: one tap instead of five, and it round-trips byte-identically. **Re-run the exporter on every widget change.** After the first paste it barely matters — `selfUpdate()` pulls from GitHub on each in-app run and reports `updated`/`current`/`failed`, so a stale export corrects itself; it runs **in-app only, never from the widget**, because it executes fetched code on a device holding the API token. ⚠ Do NOT name a helper `config` in a Scriptable script: that shadows Scriptable's own `config` global (`config.runsInWidget` / `config.widgetFamily`) and breaks the entry point. ⚠ **Scriptable has no `Weather` class** (checked against the API docs) — the forecast comes from Open-Meteo, keyless, with location resolved on a manual run and cached, since a widget refresh that waits on GPS renders blank. ⚠ **iOS cannot deep-link an installed PWA**: an `https://` tap opens Safari, not the SARA Mobile icon. `shortcuts://run-shortcut` is the only route that hands control to it, at the cost of the destination tab — `OPEN_SHORTCUT` is the switch, empty by default because a link to a Shortcut that does not exist is worse than Safari. Widget refresh is budgeted by iOS (~15–30 min, not guaranteed) — that is true of a native WidgetKit widget too, so it is not a reason to build one.
- **Notion is a published read surface, not a peer store (31 Aug 2026)** — `services/notion-sync/`
(`blocks`, `reconcile`, `config`, `notion-api`, `vault`, `index`) + `routes/notion-sync.js` +
`components/NotionSyncPanel.jsx`, sidebar **Notion Sync**, plus a card and a connect box in
**Settings → Integrations**. The point Nick settled on: Notion holds *"who I am and what I do"* so
ChatGPT can read it from the iOS app — **role, projects, identity**, and explicitly NOT meeting notes,
1-2-1s or anything derived from them. `Meetings` (263), `Plaud` (417), `People` (42) and `Daily` (148)
are permanently out, on third-party-data grounds as much as volume.

  **TWO mapping kinds, and the second one is the whole story.** `tree` pairs a Notion page (as a
CONTAINER) with a vault folder, its child pages becoming notes. `page` pairs one vault note with one
page BODY. ⚠ The first cut had only `tree`, and it did not fit: Nick's tree is ~30 curated topic pages
that hold content in the body and have **no children at all**, so mapping those as `tree` syncs nothing
on pull and buries content a level down on push. The model was wrong, not the content. A `page`
mapping **never creates** a Notion page — the page is the one picked, and creating one would put a
second copy beside it.

  **⚠ Three bugs that only live data found, none of which a test would have caught.** (1) `enabled()`
filtered on `vaultFolder`, which a `page` mapping deliberately blanks — so all five were silently
dropped from the run and the sync **reported a clean pass having done nothing**. (2) One-way mappings
**deadlocked on their own first sync**: "both exist, never paired" was treated as undecidable, which is
right for two-way and wrong where the mapping already named the winner. (3) Notion **400s on a code
language outside its enum** and does not fall back — `dataview` appears **811** times in this vault
(`tasks` 272, plus `ts`/`tsx` aliases), and it killed four MOCs on the first real publish.

  **⚠ The language clamp lives at the API boundary, NOT in `blocks.js`.** That file's contract is
round-trip stability; rewriting `dataview` to `plain text` during parsing would make every note
containing one churn between the two systems for ever. Notion's constraint belongs where Notion is.
Both halves are pinned. The enum is **copied from Notion's own error response**, not from the docs —
this repo has been bitten twice by an invented identifier (`sleep_core_hours`, `meeting_alert`).

  **⚠ The two one-way modes are ASYMMETRIC when both sides moved, and it is about what is LOST.**
`push-only` proceeds and SAYS it overwrote a change: Notion is a published window and the thing
overwritten is a copy. `pull-only` stays a CONFLICT: what would be overwritten is a note in Nick's own
vault, which is his writing and not a copy of anything. Two-way conflicts either way — a merge needs a
common ancestor and a block identity that survives markdown editing, and Notion gives neither.

  **The round trip is where bidirectional Notion syncs die.** ~30 block types map cleanly to nine, so
`blocks.js` is built for **STABILITY** — `toMarkdown(toBlocks(toMarkdown(b))) === toMarkdown(b)` — not
fidelity. An unstable converter makes a note nobody edited hash differently every pass, and presents as
"the sync is broken" rather than "bold got dropped". Caught on the first run: `***bold-italic***`
re-parsed as bold-containing-a-stray-asterisk because `**` was tried first. **The supported set is
CLOSED**; anything else becomes a `<!-- notion:keep:N -->` marker with its raw JSON stashed in state and
put back on push — the obvious alternative deletes a database view or an embed the first time Nick edits
the note. An unresolvable marker **refuses the push**. ⚠ A **`child_page` is skipped outright, never
stashed** — found by probing a real page: Notion REJECTS `child_page` on the append endpoint, so every
parent page with children would have failed to push. Rendering it as a `[[wikilink]]` is worse — it
re-parses as a bullet, so every push appends a duplicate.

  **Change detection is three-way, never "newest wins"**: Syncthing rewrites mtimes, NEURO's hooks touch
files, and Notion stamps `last_edited_time` when anyone nudges a page, so "newer" regularly names a side
that did not change. Each side is compared to the END OF THE LAST SYNC — vault by content hash, Notion by
`last_edited_time`. ⚠ The hash covers the **BODY with our own frontmatter keys stripped**, or the stamps a
pull writes would read as a vault edit and push straight back — a two-system loop nobody typed into.

  **⚠ NOTHING IS EVER DELETED ON EITHER SIDE, and that is structural** — there is no `unlink`, `rm`,
`rename` or `truncate` anywhere in the service; the only vault mutation is create-or-overwrite. It is
what made Nick deleting the whole D&D folder a two-minute inconvenience: the standalone exporter rebuilt
it. A missing page or file is reported as `orphan-*`; an archived Notion page keeps the vault copy.
Conflict copies use the **`.sync-conflict-` infix already in `vault-exclusions`**, so both halves stay out
of embeddings for free.

  **⚠ `Personal/` is REFUSED outright and never offered in the picker** — disciplinary prep, the fraud
investigation, GP notes and three OH documents. A mapping is the most complete possible exfiltration of a
folder to an external service, and this one is read by an external AI. Fails CLOSED on an unreadable
vault, or it could be bypassed by configuring while the disk is unmounted; an unreadable vault also
refuses the whole RUN rather than reading as empty (every note would decide orphan).

  **Config lives in NEURO, never a file** — the repo is PUBLIC. Mappings in
`agent_state.notion_sync_mappings`; the token in `agent_state.notion_sync_token`, read at CALL time so a
paste works with no restart (`.env` still wins where set). The token is **never returned by any route** —
`credentialSource` reports `env`/`stored`/`null` only. The 15-min cron is registered unconditionally and
checks a DB flag INSIDE the tick, so the toggle needs no restart either. Both were an SSH session, a
`read -rsp`, an `.env` append and a `pm2 restart` until Nick pointed out that is six steps of friction on
a system whose premise is that his bottleneck is initiation.

  ⚠ **Duplicate page titles make a title-only picker unusable** — SARA Memory has TWO `Decisions`, TWO
`Current State`, TWO `Preferences`. Pages carry a full breadcrumb, resolved locally from the same search
result set (Notion returns a parent id, no parent title). ⚠ **The D&D tree is owned by the standalone
`notion-dnd-sync/`** and is marked IGNORED here — mapping it would put two writers on one page tree. That
is what the ignore state is for: a coverage list is only worth reading if "not mapped" means "a gap".

  ⚠ The run **lock is not optional** (a cron plus a "Sync now" button is `plaud-admin-blocks`'
52-blocks-where-27-were-wanted shape, and the duplicate here is a Notion PAGE); state is written **per
note, never batched**. A push **REFUSES above 900 blocks** rather than truncating — a half-published page
looks finished. Deliberately **not a `TRACKED_JOBS` job**: it compares live state each pass, so a missed
run self-corrects. ⚠ `normalisePageId` scans the ORIGINAL string with hex boundaries — stripping dashes
first glues the URL into one run and the "e" of "Page" is a hex digit, so every id came back shifted.
**Live as of 31 Aug 2026**: 11 mappings (5 `page` push-only, 1 `tree` push-only for `MOCs/`, 5 `tree`
pull-only into `Notion/`), 5 pushed + 16 created, vault verified against the `/mnt/backup` snapshot as
content-identical. Pinned by `blocks.test.js` (22), `reconcile.test.js` (21), `config.test.js` (25),
`vault.test.js` (9), `token.test.js` (12).
- **Calendar writes (14 Aug 2026)** — `POST /api/calendar/events` creates on `/me/events` via `graphWrite`; needs `Calendars.ReadWrite`. `POST /api/calendar/parse` turns free text ("2pm tomorrow with abdi for 30 mins") into a draft and creates NOTHING — `event-parser.js` is regex-first (instant, works with the Pi offline), and only spends a model call via `aiRouting.runTask('event_parse')` when the rules find no time. The composer always confirms before creating, because attendees mean Graph emails real invites. `contact-directory.js` resolves names in trust order: People note `email:` frontmatter → senders already in the triage blob → Graph `/me/people` (`People.Read`). It never guesses — >1 match comes back `ambiguous` for Nick to pick, 0 comes back `unresolved`. **All 15 team People notes now carry `email:` (14 Aug 2026)**, so every direct report resolves from `vault` without touching Graph — instant, and it still works with Graph down. This was not cosmetic: Naomi, Adele and Maria each have a *personal* address (gmail/yahoo) sitting beside their work one in Graph, so all three came back `ambiguous`, which the booking path treats as unresolved and would have booked with **nobody invited**. Local contacts are matched before Graph is consulted, so the frontmatter short-circuits it. Keep `email:` populated when adding a person.
- **Calendar timezone** — Graph answers in UTC unless asked otherwise, and the UI slices times straight out of the string, so every BST event read an hour early. `fetchCalendarEvents` now sends `Prefer: outlook.timezone="Europe/London"` (`EVENT_TIMEZONE`, env `NEURO_TIMEZONE`), which fixes the times AND makes Graph read the day window as local. Writes pass naive wall-clock + the same zone name, so no code converts offsets by hand. The ICS fallback had the same bug independently — `parseIcsDate` now converts trailing-`Z` values via `Intl` against `NEURO_TIMEZONE`, deliberately NOT the host clock, since the Pi may run in UTC. Anywhere building a date string must use local getters, never `toISOString()`.
- **The person graph (14 Aug 2026)** — `People/*.md` filenames ARE the roster; every consumer reads the folder, and nothing hardcodes a name list. `entities.js` used to carry a 20-name `TEAM_MEMBERS` array and store the **first** name (`Hope`), while `person-detail` looked mentions up by **full** name against an exact-match query — so every person page's Mentions block was empty by construction. Now: `getRoster()` (5-min cache) reads People/, matches full names always and a first name **only when it maps to exactly one person** (Nathan is both Button and Rutland), whole-word (`includes()` fired "Liam" inside "William"), and stores the full name. `getMentionsOf()` folds in legacy first-name rows so pages aren't blank until the nightly sweep re-walks. Attendee extraction in `imports.js` matched `### Attendees` only — present in 6 of 229 meeting notes, because PLAUD writes `## Mentioned` — so `people:` frontmatter was empty almost everywhere; `PEOPLE_HEADING_RE` now takes either at `##`/`###`. Unmatched names are recorded **bare**, never as `[[People/{slug}]]`: inventing that link is what produced the dangling `{Name}`/`Speaker 1` links. Backfill for existing notes is `POST /api/imports/restamp-people` (`restampMeetingPeople`, dryRun by default, backs up to `Scripts/.lint-backups/`) — it rewrites ONLY the `people:` block by hand, deliberately NOT via `updateFrontmatter`, whose line-based reserialise silently drops list values.
- **An alias earns the same uniqueness test a first name gets (16 Aug 2026, #38)** — the ticket claimed `People/_aliases.md` would rescue the ambiguous first names. **It was backwards, and building it as written would have re-created the four-Lucys bug**: `Chris` is listed on BOTH Chris Middleton and Chris Smith, `Nathan` on both Nathans, so the file asserts those names twice rather than disambiguating them. (It could never have worked anyway — `parseFrontmatter` returns `""` for a YAML block list, so `fm.aliases` was empty for all 30 notes that carry one.) So an alias is rejected on **three** rules, and the middle one is the one the ticket missed: two people claim it; it is a first name the ROSTER finds ambiguous (only Andrea Melisa lists `Andrea`, but a second Andrea exists); or it is someone else's full name. What survives is the real value — `Seb`, `Nath`, `Steve R`, and the Plaud mis-transcriptions, so **`Naomi Winkworth` → Naomi Wentworth is already solved IN DATA** and #39 is only a file rename now. ⚠ Two silent bugs found building it: an alias **TIER** in `matchLocal` is the obvious implementation and is WRONG — `Nath` matched nothing (Nathan Button has no `email:`, so he is not in the contact list) and execution **fell through to "starts with", returning Nathan RUTLAND**; the alias now canonicalises the QUERY up front so every tier below matches the real name and Graph is asked for "Nathan Button". And a name the roster knows is ambiguous resolved anyway when only one candidate had an address, because `localContacts()` is built from notes with `email:` and cannot see the ambiguity. A hand-typed address is written back to the People note by a **hand-written single-line edit** — `updateFrontmatter` would have deleted the `aliases:` block this made load-bearing.
- **Who reports to Nick is READ, not typed (17 Aug 2026, #13)** — `services/team-roster.js` derives the roster from `People/` frontmatter. The ticket said "`seed.js` hardcodes a departed employee"; the same list was typed out in **six** places — `team-health.TEAMS`, `email-priority.DIRECT_REPORTS`, `meeting-prep-view.TEAM_MEMBERS`, `ChatPanel.detectPersonDraft`, the chat `SYSTEM_PROMPT` and the kiosk seed — all predating the rule two lines above and none of them following it. They had drifted **four** ways and **the vault was right about every one**: Arman left the business, Willem moved teams, Nathan Rutland was listed 2nd Line (vault: 1st) and Sebastian Broome 1st (vault: 2nd). That asymmetry is the argument — a note gets updated when something happens to a person, an array only when somebody remembers it exists; `team-health.js` even carried a comment reading *"if this goes stale, hoist it into settings"*. **`entities.getRoster()` is deliberately not enough**: it answers "is this a name NEURO knows" over all 41 notes, including Nick's manager and externals, whereas "is this one of Nick's people, and on which team" is only in the frontmatter. Three rules. Exclusion is **`archived`**, not `direct-report: false` alone — the pair is what proves a note was maintained *after* someone left. Team labels map by **CATEGORY, not by person** (a team does not leave the business), and an unmapped team renders as itself rather than vanishing. A first name identifies someone **only when it points at exactly one person, judged against the whole vault** — Nathan Rutland is a report but Nathan Button exists, so `Nathan` resolves to nobody. `GET /api/team-health/roster` serves it to the frontends, which cannot require a service, with the ambiguous first names already dropped; it **never falls back to a baked-in list**, because no roster means no draft button, which beats a document filed under someone who left. `TEAMS` stays exported as a **getter** — a frozen snapshot is what went wrong. ⚠ `PeopleBoard.jsx` keeps a hardcoded copy **on purpose** (PeopleHR ids and display notes no People note holds) and was the ONE copy that had not drifted; if it ever disagrees with the vault, the vault wins. Live: 13 reports, 12 unambiguous first names. Pinned by `team-roster.test.js`.
- **The 1-2-1 tracker is generated (17 Aug 2026, #31)** — `services/one-to-one-tracker.js` renders `Areas/1-2-1 Tracker.md` from People frontmatter + `one-to-one-detect`. It had called itself *"single source of truth"* and *"read by Morning Standup"* and **both were false — nothing has ever parsed it**; it sat frozen from April with every date four months past, still listing two people who had gone. Rendering is **surgical between `<!-- neuro:1-2-1-tracker -->` markers** like vault-hygiene, so Cadence Rules, the Status Key, Open Actions and Related survive; the first run anchors on the **`## Tracker` heading**, not "the first table", because Cadence Rules is a table and comes first. It renders **only what NEURO can know** — the "Invite Sent?" / "PeopleHR Updated?" columns were dropped rather than emitted as ❌, which would state a fact nothing checked. State words come from `cadenceState`, so tracker, board and nudge cannot drift. Two refusals: an empty roster renders a **read-failure warning, never an empty table** (nobody-reports-to-Nick and the-read-failed are different facts), and an unchanged run returns `null` so the mtime is not churned into every recent-notes scan (#78). Hooked onto **`syncPeopleNotes({apply:true})`, not the scheduler** — that is the one place the stamps change, so it cannot render dates that are about to move; it is never allowed to fail the sync. `POST /api/1to1/tracker` forces it, dry-run by default. Added to `vault-exclusions` GENERATED_FILE_PATTERNS, **measured**: it was already holding 37 `extracted_entities` rows and 3 embedding chunks.
- **People gap pass** — `services/people-gap.js` is the only thing that ever proposed a People note (person-profile.js is reachable only from its route and the MCP tool, both manual — which is why no People note had been touched since 3 July). Sources are all pre-existing: meeting-note attendee sections, triaged inbox senders on an internal domain (`PEOPLE_GAP_DOMAINS`, default `nurtur.tech` — customers and vendors would flood the folder), and `calendar_cache` organizers (that table has no attendee column, so organizer is all there is). A name needs 2+ sightings and 2+ words. Follows the vault-hygiene convention: the nightly 10pm pass is **READ-ONLY** and reports to `Documents/System/Vault Audit/`; creating stubs is an explicit `POST /api/people-gap/apply`. Stubs carry `status: auto-stub` and no 1-2-1 cadence — NEURO doesn't know whether the person is a report.
- **1-2-1s are detected, not declared (14 Aug 2026)** — `services/one-to-one-detect.js` derives `last-1-2-1` from the meeting notes in `Meetings/`, because the hand-maintained frontmatter froze in March 2026 while 1-2-1s carried on happening: the Team board showed people 100+ days overdue who had been seen in July. Two rules carry it. (1) **A prep note is not a 1-2-1** — `type: meeting-prep` notes are NEURO-generated *before* a meeting and carry `meeting-type: "1-1"` like the real thing; Heidi had five, each generated off the same stale date with no meeting behind any of them, so counting them would close the loop on itself. `type` must be `meeting` or `transcript` (an orphaned transcript is often the only surviving evidence — the Plaud reset archived some summaries). (2) **Attribution needs the body to agree** — frontmatter `people:` links are Plaud-generated and often incomplete (the 4 Jun three-hander links only Stephen), so a candidate must also dominate the mentions (`MIN_MENTIONS` 12, `DOMINANCE` 0.6), and a title naming 2+ of the team is a group meeting outright. `syncPeopleNotes({apply})` stamps frontmatter and **only ever moves a date forward**, so a manual entry for a meeting not yet written up survives. Runs in the 10pm scheduler block; cached in `agent_state.one_to_one_index` with a 5-minute TTL — a full scan is only ~40ms on the Pi, and vault-hooks deliberately do not fire for Syncthing-delivered notes, so without the TTL a 1-2-1 recorded this morning would show yesterday's date until 2200. `GET /api/1to1/recent` feeds the board's "Last 1-2-1" card. **The stamp is the LAGGING copy and must never be the arbiter (20 Aug 2026)** — because `syncPeopleNotes` only runs at 22:00, `last-1-2-1` reads months old for the whole of the day a 1-2-1 is written up, and every consumer read it alone. Live: Hope Goodall's card rendered the summary of that morning's note and, one line above it, *"Met 2026-08-19 — no note"* — both halves of one card, one from the detector and one from the stamp, disagreeing about a note already on disk. `effectiveCadenceFields(name, fm)` folds the newest detected note in at READ time (nothing is written; the sync still owns the vault), used by the nudge (`obsidian.getUpcoming121s`) and `team-health`; `PeopleBoard.get121Status(fm, detectedLast)` does the same client-side off the `oneToOnes` payload it already holds. ⚠ **The fold must recompute `next-1-2-1-due` too** — it is derived from `last-1-2-1` and so is stale by exactly the same amount, and folding only the last-held date trades *"no note"* for a spurious *"overdue by 98d"*. The pure half is `foldDetected(fm, detected)` (the `pi-health.assess()` split), pinned in `one-to-one-cadence-state.test.js`. The **stale `1-2-1-booked: 2026-08-19` against a meeting actually held on the 20th needs no code**: nothing re-reads the calendar for a meeting moved in Outlook, but a note dated on or after the booking makes it spent and the 22:00 sync clears it. **Vault notes are mixed CRLF/LF — normalise before any line-anchored regex** (`\r` is a JS line terminator, so `/^key:\s*(.*)$/` silently fails on every CRLF line).
- **What counts as a working day (16 Aug 2026, #25)** — `shared/working-days.cjs` (pure predicate) + `backend/services/working-days.js` (the data). Before this, a repo-wide grep for "bank holiday" returned **one** hit: a hardcoded three-date array in `obsidian.parseNinetyDayPlan()`, already wrong for its own year (missing 25 May 2026) and dead besides — the 90 Day Plan folder was archived on 12 Aug, so that function returns null. **Everywhere else "working day" meant nothing more than Mon–Fri, in five separate copies.** Not cosmetic: `bookAll()` creates real Graph events and emails real invites to Nick's direct reports, and `SEARCH_DAYS` is 21 — so the **31 Aug Summer bank holiday sat inside the LIVE booking window** when this was found (31 Aug is a Monday; a six-person batch filled it). Split like `pi-health.assess()` / `cadenceState()`: the `.cjs` half is browser-safe with **no DB or network**, takes an optional non-working set, and omitting it is exactly the old Mon–Fri behaviour; the service half owns the feed. **The failure direction is the whole design** — this is a network call and *failing open books meetings on Christmas Day*, so it never falls back to "every weekday works". Three sources and `status()` always names which answered: `live` → `cache` (`agent_state.bank_holidays`) → a **compiled-in floor**. The floor is cheap because `https://www.gov.uk/bank-holidays.json` (division `england-and-wales`) is a *static publication covering 2019–2028*, not a live API, so a Pi that has never reached the internet still knows about Christmas; it is a floor, **not a substitute** — the weekly refresh keeps it honest and `ageDays` makes a frozen list visible. A structurally valid response carrying **no events counts as failure** and does not overwrite a good copy (#56's rule). **Leave is not a property of the day**, so it is not in the holiday set: `leaveDates()` reads `showAs: 'oof'` from events the **caller already holds** — `findSlot` has a whole window in hand, and a slot search that pauses for I/O per day is one that times out. Graph's all-day end date is **exclusive**, so a week off is one event and the end date is not leave. An all-day OOF already blocked a day *by accident* (`findGapInWindow` treats non-free as busy); a **timed** one did not, and nothing anywhere could say the word "leave". `findSlot` is the single funnel for propose / planAll / reschedule, so one change covers all three. `standup-session` now names **why** a day is not working instead of asserting "Nick works Monday to Friday" at a bank holiday. `due-dates.cjs` dropped its copy, but **both frontends import that file directly and so still get plain Mon–Fri** — they have no route to the DB, and a preset button one day wrong at Christmas is a far smaller problem than an invite on one. Weekly refresh via `scheduleWeekly`, so catch-up (#80) fires it on first boot; **deliberately NOT in `state-of-play`'s `TRACKED_JOBS`** — a missed week is harmless given the floor, and a board that warns about a benign lapse is one nobody reads. `GET /api/time/working-days` reports the **observed** source, not a configured boolean (#65). Pinned by `working-days.test.js` + two booking tests.
- **1-2-1 booking (14 Aug 2026)** — `services/one-to-one-booking.js`, "Book now" on the Team card. Two-step like `event-parser`: `propose()` reads the calendar and returns a draft, `book()` creates it — attendees mean Graph emails a real invite, so nothing is created by looking. **Nick's booking rules are expressed as two windows rather than a pile of exclusions** — `AM_WINDOW` 10:00–12:00 and `PM_WINDOW` 14:00–16:30 (bounds in minutes so 16:30 is expressible). That makes "never at 9am", "never 12–2" (the gap between them IS lunch) and "never after 4.30pm" (a slot must *finish* inside its window, so a 30-min 1-2-1 starts no later than 16:00) true by construction. Plus "never when a meeting already exists" (clash detection; tentative blocks, only free/cancelled don't) and `MAX_PER_DAY = 2` via `countOneToOnes()`, which matches existing 1-2-1s by subject however they were named. PM is tried first — every entry in the vault's 1-2-1 Tracker reads "PM". Working days only, from `next-1-2-1-due` or tomorrow. **Only Nick's calendar is visible** — reading a colleague's free/busy needs `Calendars.Read.Shared`, which NEURO doesn't hold, so the slot is guaranteed free for Nick and merely offered to them. Booking stamps `1-2-1-booked` but NOT `last-1-2-1` — that only moves when a note proves the meeting happened. **`1-2-1-booked` vs `next-1-2-1-due` (16 Aug 2026)** — one field used to carry both, and it made NEURO nag Nick to book 1-2-1s that were already in his diary. `one-to-one-detect` writes `next-1-2-1-due` as *when the next one is OWED* (`last-1-2-1` + cadence); `book()`/`reschedule()` were overwriting it with the date of the meeting they had just created, and every reader — the 1-2-1 nudge, the Team board, chat context — read it the first way. So a booking wrote a reminder to make the booking, then flipped to *"these need booking now"* the day after the meeting, because `last-1-2-1` deliberately doesn't move without a note. All 12 direct reports were in that state. What the state MEANS is now `one-to-one-detect.cadenceState()` — a **pure function taking a plain object**, so the ranking is testable without a vault (same split as `pi-health.assess()`), and the nudge / board / chat cannot drift apart on what "overdue" means. Five states: `booked` (in the diary → **silent**), `unwritten` (booked date passed, no note → *"write it up, or rebook if it was cancelled"* — NOT "book one", and this is what keeps a **cancelled** 1-2-1 visible instead of silently counting as held), `overdue`, `due-soon`, `ok`. A booking is spent when `syncPeopleNotes` sees a note dated on or after it — clear it, or the person reads `booked` forever; an *earlier* unrelated note must not cancel a future booking. `routes/obsidian.js`'s cadence-recompute guard used to infer "booked" from *any* future due date (the only signal available while the fields were merged) and now reads the real field. Migration: `backend/scripts/migrate-121-booked.js` (dry-run default, backs up to `Scripts/.lint-backups/`) identifies a booking deterministically with **no calendar lookup** — the detector can only ever write `last-1-2-1 + cadence`, so a due date LATER than that could not have come from it (Stephen: last 2026-03-26 fortnightly → detector would write 2026-04-09, stored 2026-08-18). Pinned by `one-to-one-cadence-state.test.js`. **"Book all outstanding"** on the SARA card runs `planAll()`, which fetches the calendar ONCE and reserves each allocation back into that working set — proposing people individually returns the same free slot to everyone, so booking a dozen would stack them all on one another. Reserving as it goes also lets `MAX_PER_DAY` count the 1-2-1s the plan is itself creating, so a batch spreads across days; most overdue first, so a diary that runs out short-changes the least neglected. `bookAll()` is sequential and fault-isolated (real invites to real people: one Graph failure must not abandon the rest, and nothing is retried) and re-checks each slot for a clash immediately before creating it. Verified live 14 Aug: `book()` created a real event and stamped the note.
- **Rescheduling a 1-2-1 (16 Aug 2026)** — `findOneToOne` / `proposeReschedule` / `reschedule` in the same service, "Move" on the Team card (`RescheduleDialog`), routes `/api/1to1/{find/:person,propose-reschedule,reschedule,moves/:person}`. **It finds the meeting that EXISTS rather than one NEURO booked** — `book()` never stored a Graph event id, and starting to store one would still leave every hand-booked 1-2-1 (most of them) unreachable; the match is attendee email first, subject as fallback, and a meeting merely NAMING the person ("Team Meeting - Hope handover") is not a 1-2-1. **PATCH, never cancel-and-recreate** (`microsoft.updateCalendarEvent`): Graph mails an "updated" notice, where a cancellation followed by a fresh invite reads to a direct report as the 1-2-1 being dropped, and loses the thread and body. **The event is excluded from its own clash check** (`findClashExcluding`) or it collides with the meeting it IS and every slot on its day is refused. A synthesised `graph-…` id from the ICS/bridge fallback is **refused up front** rather than 404ing after Nick confirms. `next-1-2-1-due` follows the meeting; **`last-1-2-1` does NOT** — same rule as `book()`, only a note proves it happened. Slot rules are reused, not re-implemented, so AM/PM windows, MAX_PER_DAY and working days all still hold. **The move COUNT is the actual feature**: `one-to-one-detect` catches a 1-2-1 that has not happened, but one slid three times looks healthy right up until it is 40 days overdue, because nothing recorded that it moved. History lives in `agent_state.one_to_one_moves` (KV, capped 20/person, following standup-session and focus-session), and the count is shown **before** the confirm, never only after. Pinned by `one-to-one-reschedule.test.js`.
- **1-2-1 prep is NOVA's, not NEURO's (14 Aug 2026)** — `POST /api/1to1/prep`, `GET /api/obsidian/people/:name/121-prep/latest`, the MCP `generate_1to1_prep` tool and the board's prep buttons were all removed; NOVA's `one21-service.ts` owns prep, sessions and action tracking. `services/one-to-one-prep.js` survives unreferenced — delete it once NOVA is confirmed to cover everything.
- **The People card stopped running its own 1-2-1 (4 Sep 2026)** — three controls left `PeopleBoard.jsx`, and the test for each was the same: *does it decide a date or an invite (NEURO's, because NOVA explicitly declined the calendar — see `cancelOpenSessions`), or does it run the conversation (NOVA's)?* **(1) The n8n performance-review pipeline is DELETED** — `1-2-1 Snapshot`, `Weekly Review`, the lookback/next-steps selects, `ApprovalPanel`, `routes/n8n.js`, `services/n8n.js`, `obsidian.writeReviewToVault` and the `n8n` row on `/api/status` + the Admin page. It POSTed to an n8n webhook that read the KPI database NOVA owns and came back needing a SECOND approval queue in NEURO, duplicating `runDayBeforePrep` / `gatherPrepSignals` / `runWeeklyKpiEmail`, which run on a schedule with no n8n hop. ⚠ The n8n workflows themselves (`NICK-AGENT - Trigger 1-2-1 Snapshot`, `SUB - Performance Review Snapshot`, `SUB - Performance Review Approval Form`) are STILL ACTIVE and were deliberately not touched — deactivating prod workflows is Nick's call; nothing triggers them now, and `/121/draft` and `/121/final` are gone, so a manual run 404s rather than half-working. The `n8n` status row went with it because it only ever meant "N8N_API_KEY is set" and that key had exactly one consumer; n8n still talks TO NEURO (Jira ingest, training sync) over the API token and needs no key here. **(2) The `Last 1-2-1` / `Next 1-2-1 due` date INPUTS are gone**, shown read-only with their provenance instead. Both facts have been derived since 14 Aug — `one-to-one-detect` reads the written-up note, `nova-121-writeback` stamps from NOVA's `completed_at`, and `get121Status` recomputes the due date from last + cadence at READ time (which is why the stored one is stale within a day and the badge already ignored it). Typing one made this a fourth writer racing three detectors, against *a 1-2-1 is DETECTED, not declared*. What stays is the three things nothing can detect: cadence, employment status, and Nick's notes. **(3) "N actions owed" now counts NOVA's `agent_121_actions`**, via `openActions` added to the bridge's `/121/state` and read by `GET /api/1to1/open-actions`. ⚠ It used to count NEURO tasks whose TEXT HAPPENED TO MENTION THE PERSON'S NAME (`personSummaries[name].tasks`) — a fuzzy match over Nick's own todo list, rendered on a 1-2-1 card under words that read as a fact about a colleague. The three answers stay distinct: a count, ABSENT for someone NOVA does not track (never 0), and `ok:false` when NOVA could not be asked. ⚠ **A NOVA older than this change answers `/121/state` perfectly and carries no `openActions` at all**, which would map to `{}` and render as a team owing nothing — the reader-before-writer shape again — so a roster with names but not one countable field is refused as an old bridge, while a genuinely empty roster stays `ok:true`. `OPEN_ACTION_STATUSES` is ONE list on the NOVA side, shared by the count and `/121/completed`'s split, because the number on the card and the checkboxes written into the vault note must describe the same set; `delivered` and `missed` are both CLOSED. **Cadence needed nothing** — NOVA has no editor for it (display only; `setAgentCadenceDays` is bridge-only), so there was no second writer to close. Pinned by `people-board-source.test.js` (4, source scans with a positive control) + `routes/one-to-one-actions-routing.test.js` (8, real HTTP).
- **Nudges** — types live in `nudges.NUDGE_TYPES`. Two kinds: *tone* nudges (standup/todo/eod/journal…) draw escalating lines from the message pools, and *fact* nudges (`escalation`, `email`) state what's actually outstanding and re-state it on every nag via `syncFactNudge()` — which raises, updates in place, or clears one banner. Escalation is driven by `jira.syncEscalations()`/`markEscalationsSeen()`, email by the inbox scan and item dismissal, so both self-clear the moment the work is done. Snooze is per-type with a caller-chosen duration (`POST /api/nudges/:type/snooze` `{minutes}`, default 30, capped 24h); the banner stores snoozed-until timestamps, not booleans. Todo targeting ranks MoSCoW `must` first, matching `buildTodayLane`.
- **The NOVA bridge is not working redundancy (16 Aug 2026, #65)** — `novaBridgeFetch` is NEURO's "Priority 2" fallback for when MSAL Graph is down, and **most of the paths it calls do not exist on NOVA**. Probed live: NOVA serves eight routes (`/escalation-reasons`, `/ticket/:key`, `/escalations`, `/escalate`, `/flagged`, `/status`, `/calendar`, `/mail`); `/mail/{id}`, `/todo/lists`, `/todo/tasks` and `/planner/tasks` are **absent**, so the request falls past NOVA's bridge router into its app auth and answers **401**. The comments at those four call sites say so now — do not read them as redundancy. #65 was filed as "map recipients in the bridge branch, ten lines"; that mapping is correct and is now there, but **the branch cannot run**. The subtler failure is the one that mattered: a route that DOES exist answers **HTTP 200 with the failure nested in `data`** (`{ok:true, data:{error:"Failed to acquire token…"}}` — NOVA's msgraph token is expired), which `novaBridgeFetch` handed back as a payload; every caller read `.id` off it, got undefined and returned `null`, so **a dead bridge was indistinguishable from an empty mailbox and nothing logged**. Now detected, logged and recorded per path in `_bridgeHealth`, with 401/404 classified `unsupported` rather than `error`. **Nothing blocks a call** — if NOVA gains a route the next success clears the entry by itself. Keys are normalised (`/mail/:id`, `/ticket/:id`) or the map would grow one entry per message. `getMailAccessStatus()` reports what was *observed*, not just `bridgeConfigured` — **"configured" was never the same claim as "works"**. User-visible half: cached triage entries carry `fromEmail` but **no participants**, so on a Graph outage Reply works and reply-all silently comes back empty — an unreachable thread looked exactly like a one-to-one email. `replyDefaults.threadKnown` + the composer's "thread unavailable" line fix that. Pinned by `nova-bridge-honesty.test.js`.
- **The escalation card (16 Aug 2026, #53/#54)** — `decision-engine.collectEscalations` builds it; `FocusPanel` and `BriefingPanel` render it. The list used to render only at **2+**, so exactly ONE escalation left the card with no anchor anywhere on it: the title becomes the ticket, and both "Open →" and the Briefing card click route to the in-app dashboard filter via `action-surfaces.cjs`, never to Jira. The quietest day was the one you could not click through. One row is still a list (`>= 1`); it drops its summary in that case because the title already carries it, and the hover underline moves onto the key, which is then the whole link. **What `getUnseenEscalations()` sends is decided by measurement, not by what Jira happens to return** — status/priority/assignee travelled into both panels and nothing rendered any of them. Over all **41** escalations Jira has ever held (the 6 open now would have said "always Unset, always Nick" and got this wrong): priority `Unset` 33 / Normal 5 / Major 2 / Critical 1; assignee Nick 23 / unassigned 7 / five others 11; status of the open six Open 3 / Reopened 2 / Waiting on Development 1. So neither "show all three" nor "drop all three" is right: each field has a **default that is a fact about the queue, not about the ticket**, and a tail worth interrupting for. `"Unset"`, `"Open"` and Nick's own name are nulled **in the service, not in each panel** — three consumers agreeing by construction beats two panels drifting — so the common row renders no badges at all (3 of the 6 live rows are silent). No assignee becomes **`"Unassigned"`**: on an escalation that is the finding, not an absence. Pinned by `escalation-detail.test.js`.
- **Sleep has TWO sources and they are never summed (16 Aug 2026, #122)** — `apple-health.rollupSleepNights` prefers the Watch's **staged** breakdown (core/deep/rem) and falls back to the single whole-night `sleep_asleep_unspecified_hours` sample from a second app ONLY when there is no staged sleep at all (3 nights of 728). Adding them double-counts a night to ~2x. `asleepSource` (`staged|unspecified|none`) names which answered, so no consumer re-derives the choice, and `HealthCard` draws one flat desaturated bar for an unstaged night rather than inventing a shape from one number. Preferring staged is measured, not a coin toss: across the 324 nights carrying both, the whole-night sample disagrees by >0.5h on **241**, in BOTH directions — over-stating by up to 3.4h and under-stating by up to **9.7h**. ⚠ **The real bug underneath was a metric NAME mismatch**, and it is the one to remember: ingest writes Apple's own labels (`sleep_asleep_core_hours`), while the route, the rollup's stage sets AND the card were all written against a guessed `sleep_core_hours`. **A wrong metric name returns zero rows, not an error** — so the staged rows were never fetched, the stage bar was empty by construction, and the feature was dead for **725 of 728 nights** with a green suite over it, because the fixtures used the invented names too. Hence `sleepStage()` canonicalises the aliases **once** rather than three places learning the same mapping, and the route fetches by PREFIX via `db.getSleepSamples()` — never by a list of guessed names. `efficiency: null` and `inBedHours: 0` are BY DESIGN (there is no In Bed metric among the 66). Ingest untouched: this is a read-time rollup choice. Pinned by `apple-health.test.js`.
- **Two years of health data that reached nothing (29 Aug 2026)** — `services/health-daily.js` (one row per day + readiness) + `services/health-signals.js` (trends) + the `health_daily` table + `/api/health/{today,history,readiness,signals,rollup}`. Measured first: `health_samples` holds **1,060,499 rows across 66 metrics** going back to Aug 2024, arriving that morning, and the ONLY consumers were the desktop `HealthCard`, the MCP `get_health` tool and `/api/health/*`. Grep `briefing`, `day-planner`, `decision-engine`, `attention`, `context-state`, `working-memory`: **zero** references. Nothing about Nick's body reached the Surface, the widget, the briefing, a nudge or the planner — a screen he had to go and find, which by his own framing is the shape that does not get used. ⚠ **And one of the three read paths was DEAD.** `services/health.js` read the KV blob `agent_state.health_data_<date>`, written only by `POST /api/health/ingest` — but the phone moved to the FreeReps app, which posts to `/api/v1/ingest/` and writes `health_samples` ONLY. Live check: `agent_state` held **no `health_*` keys at all**. So chat context (`claude.js`), journal prompts (`routes/journal.js`), the `server.js` readiness probe and `/today` + `/history` + `/status` had ALL been returning null for months — silently, because a missing blob reads as *"no data yet"*, not *"the writer is gone"*. Same species as the Jira queue cache: readers outliving their writer. **The blob is deleted; everything derives from `health_samples` now**, so a second store cannot fall behind the first. `health_daily` is **MATERIALISED** (following `wins`) because several consumers want to JOIN a day and a join over a million rows is not something to put on the planner's 07:15 path. **Medians, not means**, for HRV and RHR — `stress-score`'s rule, reused rather than re-picked. **`complete` is load-bearing**: today is a PARTIAL day and averaging it in drags every average down all morning. Every threshold is set against his MEASURED 90-day range (HRV 13.2–27.0, RHR 70.0–83.3, sleep 5.1–9.7h with only 6 nights under six) — a rule firing on a tenth of a normal quarter is one he learns to ignore, which also costs him the real one. `readiness()` refuses three ways: no baseline → `known:false` with a reason and never a cheerful middling number; a missing input is reported, never counted as a good one; and it **never diagnoses** — Apple Health cannot separate exercise, illness, alcohol and a hard week, and `stress-score`'s caveat is inherited rather than quietly dropped. `health-signals` reports TRENDS (RHR held up 3 days, wrist temp up 2 nights, HRV suppressed, weekly sleep debt) plus **sensors that went quiet** — blood pressure stopped 1 Apr 2026 and blood glucose in Sep 2025 and nothing had noticed, because a metric with no recent rows returns an empty result rather than an error. Quietness is judged as a RATIO against each metric's own cadence (heart rate arrives every two minutes, body weight every few days — no shared threshold works) with a 14-day floor. **Pull-only: nothing here notifies**, because nudge volume is the budget that argues against everything else. ⚠ `time_in_daylight` is in **SECONDS** — it averages 3,189/day, which as minutes would be 53 hours; the units column is not stored on a sample, so this is inferred from the values and converted once, in `SCALAR_METRICS`. ⚠ **The window must be bounded at BOTH ends**: the first `sync()` bounded only the start, so a backfill walking backwards read through to the present every chunk, the 20,000-row cap kept the newest rows, and the oldest chunk overwrote two years with nulls — 744 days written, **328 with any HRV**. Fifth instance of that species. Backfill is `scripts/health-backfill.js` (chunked, idempotent, passes the real `today` separately from the window end). Hourly cron at :25, deliberately NOT a `TRACKED_JOBS` job — idempotent over a trailing 10-day window, nothing to replay (the `wins` call). Live: **743 complete days, 2024-08-16 → 2026-08-28**. Pinned by `health-daily.test.js` (18) + `health-signals.test.js` (16).
- **The score and the sentence must name the same thing (29 Aug 2026)** — `health-daily.readinessSentence()`. Caught on the FIRST live reading rather than by a test, and twice over. Score 31 (`low`), driven by resting heart rate 82 against a usual 78 (crossed its threshold, effect **-0.67**) AND HRV 14.82 against 16.3 (z = **-0.93**, INSIDE the ±1.0 band, effect **-0.46**) — so HRV supplied two thirds as much of the number as the heart rate did and the sentence did not mention it, because its note read *"HRV in your normal range"*. A reader would reasonably conclude one figure had produced a 31. **A threshold decides how to PHRASE a reading, not whether it counted**: crossed contributors get their full note, and near-threshold ones leaning the same way with `|effect| >= 0.2` are named alongside — *"resting heart rate 4bpm above normal, with HRV also on the low side"*. Where NOTHING crosses but the score still moved, it says exactly that (*"nothing on its own, but…"*), which is a third distinct claim from either "your heart rate is up" or "everything is normal". ⚠ Selection is on a structured `flag` plus the measured `effect`, **never a regex over the prose** — the first cut filtered contributors with `/below|less|above normal/.test(note)`, so rewording a note would silently change which facts got reported. Pinned by 8 tests in `health-daily.test.js`, including the live case as a fixture.
- **Where the health read actually surfaces (29 Aug 2026)** — three consumers, and each is a deliberate choice about interrupting. **The morning brief** carries `readiness` always (free on a screen already being read) but mentions it in the PUSH **only when the day is off his own baseline** — *"About normal today"* appended every morning is padding, which is how the line above it stops being read; and it is **deliberately NOT fed to the model**, because `_aiSynthesis` handed *"HRV is down"* writes advice, and *"take it easy today"* is a recommendation drawn from three numbers by something that cannot tell exercise from illness. **MCP `get_health`** leads with readiness and reports what has changed, carrying each finding's caveat rather than dropping it. **`HealthCard`** renders both scores with their WINDOW named — ⚠ readiness and `stress-score` legitimately disagree and did so visibly on day one: *"Balanced 52"* beside *"Running low 31"*, where stress's CURRENT HRV (16.3ms, its last few readings in a 6-hour window) is readiness's **baseline**, because readiness compares the whole day's median against the fortnight. They are `Right now` and `Today`. Deliberately **not merged, and stress-score not retuned** — the acute read is a different signal from the daily one, and quietly changing the numbers of a working pinned service to tidy a card is the wrong trade.
- **My Health, and two hypotheses that came back empty (29 Aug 2026)** — `components/HealthPanel.jsx` (sidebar, beside Pi Health: one is the machine's vitals, the other is Nick's). `HealthCard` answers *"how am I today"* in a glance and that is all it should do; everything else here is a TREND across 744 rolled-up days, and a trend needs an axis. Trends are **small multiples — one measure per chart, one axis each**; never two y-scales, because HRV in ms and resting heart rate in bpm share no scale and plotting them together invents a relationship out of the units. ⚠ **A gap in the data is drawn as a GAP** (`segments()` breaks the path at nulls) and each chart states **what fraction of its window carries a reading** — the phone syncs at iOS's discretion so missing days are normal, a line interpolated across a fortnight the watch was off charge is the chart telling a story nobody measured, and a chart drawn from 9 of 90 days otherwise looks identical to one drawn from 90. **Sleep stages moved to a validated SEQUENTIAL ramp** in both the page and the card: the stages are ordered and this is one measure split by depth, so one hue getting lighter is the honest encoding — and it is the only one that survives colour blindness here. The palette it replaced (blue / purple / blue-grey) measured **ΔE 0.1 between deep and REM under deuteranopia — literally indistinguishable** — and 8.0 for normal vision, which is why that bar had always been hard to read; validated against the dark surface, 2px segment gaps load-bearing. The SVG is sized from the **measured container**, not a stretched viewBox: `preserveAspectRatio="none"` is the obvious way to make a chart responsive and scales x and y differently, turning the hover marker into an ellipse that changes shape with the window. ⚠ **A wide re-roll runs nightly at 03:10 (120 days, chunked)** because the phone BACKFILLS: no sample in the last week arrived stamped more than 10 days late, but the **worst arrival lag in the last month is 730 days**, so a sample landing today stamped last March would never reach `health_daily` — the row computed when that day was empty would stand for ever. `syncRange`/`chunkPlan` are shared with `scripts/health-backfill.js` rather than living only in it, and `chunkPlan` is pure and pinned because the SHAPE of the reads is the thing that matters. **Two hypotheses were measured and BOTH came back empty, which is a result and is why neither shipped a feature**: readiness does not predict output (p = 0.97, above), and **sleep TIMING is already consistent** — across the last fortnight onset ran 22:05–23:25 and wake 06:23–09:50, the wider wake spread being non-work days; the 11:12 / 14:06 extremes over 90 nights are naps landing on the following night's key, which is the documented wart in the wake-date rule and not a sleep problem. His duration is fine too (7.7h median, 6 nights under six in 90). Sleep is not the lever here; **daylight is the only measurably low number** (38 min/day median, 11–20 min recently) and is deliberately left as something he can see rather than something NEURO nags about.
- **"I've read it", and it comes back if it happens again (8 Sep 2026)** — `health-signals.partitionAcked` (PURE) + `acknowledge`/`unacknowledge` + `POST`/`DELETE /api/health/signals/:id/ack` + the button on each row of **What's changed**. Every finding here is a TREND or a SILENCE, so it is true for as long as the condition lasts — `blood_glucose` has been quiet for 354 days and said so every morning — and that is correct behaviour AND how the panel stops being read: the four rows Nick has already understood bury the fifth he has not. ⚠ **ONE RULE DOES ALL OF IT: an acknowledgement is cleared the first time its finding is ABSENT.** That is what "until it reoccurs" means. A quiet metric that starts arriving again drops out of the pass, its ack goes with it, and the next stoppage is a new thing to be told about; an acknowledged trend stays acknowledged for as long as it lasts, clears when it ends, and speaks again on the next episode. **No occurrence signature to invent and no date in the key** — which matters because `quiet:<metric>` carries no date (only the multi-metric `quiet:<day>` does), so keying a dismissal on the id alone would have hidden a resumed-then-stopped sensor for ever. ⚠ **It is an ACKNOWLEDGEMENT, not a resolution**: acked findings are still RETURNED (on their own list, behind a "N read — still true" toggle with a way back), and **`allClear` is computed on ALL of them**, because a system reporting itself healthy because the warning was dismissed is telling Nick what he just told it. ⚠ **Acking something not in the CURRENT pass is REFUSED** — a stale screen acking a finding that has already cleared would pre-emptively silence the NEXT occurrence, the one thing this promises not to do. ⚠ **Pruned only on a sound pass** (no gaps): a finding missing because the read failed has not cleared, and dropping the ack there nags him about something he has already read the moment the disk hiccups; written only when something actually changed, so a polled read writes nothing. An unreadable store is **never overwritten** (the `triage-shadow._load` rule). `ambient.js` reads the same snapshot, so "I've read it" also stops SARA raising it. Pinned by 5 pure tests in `health-signals.test.js` + `routes/health-signals-ack-routing.test.js` (6, real HTTP, **mutation-checked**, seeded with a REAL stopped metric rather than a stubbed snapshot — ⚠ and note `sensorsQuiet` parses SQLite's `YYYY-MM-DD HH:MM:SS`, so an ISO fixture with a trailing `Z` parses to NaN and is skipped in silence, which is how a fixture in the wrong shape passes for the wrong reason).
- **The planner's health rule is a PREFERENCE, and the measurement is why (29 Aug 2026)** — `day-planner.capacityFor()` plans a lighter half-day when recovery is down (1 block, 60 min, 2 tasks). ⚠ **`DAY_PLANNER_HEALTH_CAPACITY` defaults FALSE, on evidence rather than caution.** The obvious justification — *"he gets less done on low-recovery days, so plan less"* — was TESTED before shipping (`scripts/measure-health-work.js`, permutation test, each day bucketed by a readiness score built only from the days BEFORE it, so nothing is judged against its own future): low readiness **25 days, mean output 8.08**; normal or high **64 days, 7.92**; **p = 0.97**. There is no effect there to build on, and only **5** short nights fell in the 89-day overlap so the sleep arm is reported as **untested**, not as a null result. The measurement is weak (n=89, and the wins count is a noisy proxy skewed by commit-heavy days) — so it is not evidence the rule is WRONG, it is the absence of the evidence that would make it right. That leaves *"plan me a lighter morning when I'm run down"* as a perfectly reasonable **want**, and a completely different claim from a prediction: Nick switches it on. Three rules survive in the code and are pinned: it **only ever reduces** (a good night must not buy a busier morning), **unknown plans as normal** (a watch left on charge must not earn a lighter day, and a failed read must not stop the planner), and it **never reorders the work** — that would be `rankTasks` overruled by a heart-rate reading, turning the plan into the quick-wins list this file already refuses to become. A reduced plan **says so in the notification**, or a quietly lighter morning is indistinguishable from a planner that has stopped finding gaps.
- **Stress score (14 Aug 2026)** — `services/stress-score.js` scores HRV against a **rolling 14-day personal baseline**, never in absolute terms: 45ms is good for one person and poor for another, so an absolute threshold would be noise. Stats are robust (median + MAD, not mean + stdev) and HRV is handled in **log space** because it is log-normally distributed. Below 7 days / 20 samples it returns `status:'calibrating'` and `score:null` rather than inventing a number; with no HRV reading in 6h it returns `'stale'`. Heart-rate-above-resting is a 30% term that only counts if <60min old — it exists because the watch samples HRV a handful of times a day but HR every few minutes. **Apple Health cannot distinguish exercise from stress**, so a >35% HR elevation adds a `caveats` string rather than being silently folded in. Score is clamped 2–98 — a bare 0 or 100 implies certainty the data doesn't support. Backed by `health_samples` (metric, value, recorded_at) with `UNIQUE(metric, recorded_at)` so a repeated poll folds instead of skewing the baseline. `/api/health/ingest` now ALSO **merges** into the daily KV blob instead of overwriting it — at a 30-min cadence a post carrying no HRV would otherwise null out the morning's reading. Pinned by `stress-score.test.js`; retuning the curves should fail those tests.
- **Scheduled jobs catch up after a restart (15 Aug 2026, #80)** — `node-cron` is in-process with NO catch-up, so anything due while `neuro-backend` was restarting simply never happened: the 02:30 sweep ran **9 nights in 45** and the Friday hygiene pass **4 Fridays in 7**, against 34 days of Pi uptime and 49 backend restarts. Never downtime — deploys. `scheduleDaily`/`scheduleWeekly` in `scheduler.js` stamp `agent_state.scheduler_last_run:<name>` on every run and `runCatchUp()` fires anything whose slot has passed unrun, staggered 45s + 60s apart because several walk the whole vault. **A job missed YESTERDAY is not run today** — catch-up means "this slot passed and was missed", not "replay history". Dates are stamped in LOCAL time (the Pi may run UTC). `watchdog.checkScheduledJobs()` alerts on daily >3d / weekly >10d, so a job that stops is now loud. Tracked: `nightly-sweep`, `nightly-rollup`, `embeddings-rebuild`, `weekly-review`, `weekly-hygiene`, `knowledge-reflection`. Pinned by `scheduler-catchup.test.js`.
- **MoSCoW/lane calibration: words that say WHEN, not WHAT (15 Aug 2026, #73/#74)** — `todo-intelligence.js` splits `URGENT_NOW` (urgent, asap, today, before lunch, breach, disciplinary) from `DOMAIN_TERMS` (review, approval, customer, sla, escalat, payroll, ticket, queue). Only the first promotes to `must`. The old single list mixed them, so a fair description of Head of Technical Support made nearly every task a MUST and then `priorityFromMoscow` made it high — a tag every row shares sorts nothing, while still driving Focus, the lane AND the nudge count. `needsToday` also **no longer includes `context === 'queue'`**: that alone put "Make amends to Customer Portal" in Must Move Today because of one noun. Two latent bugs fixed alongside: `decorateTask` never passed `overdue`/`dueToday` through (so `buildFollowThroughCandidate`'s arms for them read undefined and could never fire), and `triageTodo` **ignored the `todayStr` it was given** and read the wall clock instead, which made every date test untestable. `laneReason()` names the actual qualifying test per row. Pinned by `todo-intelligence.test.js`.
- **Reaching past the actions cap (15 Aug 2026, #108)** — `/api/actions` takes `type|kind|owner|source|month` + `offset`/`limit` and returns `filteredTotal`/`hasMore`/`facets`. Before it, "show more" only revealed what had already been SENT, so **347 of 467 pending actions were unreachable**. `month` comes from the NOTE date in `sourcePath`, not `created_at` — the backfill ran in one night across March–August, so keying on creation would put all 463 in one bucket. `POST /api/actions/bulk-reject` takes the same filter: **reject only, never approve** (approving runs executors, one of which sends email), requires a non-empty filter, **refuses outright if the matched set contains anything outbound**, and supports `dryRun` so the confirm quotes a true number. Pinned by `routes/actions-filter.test.js`. **Scoped reads on `sara_actions` are indexed (#107b)** — `type` plus an expression index on `json_extract(payload,'$.sourcePath')`, confirmed with EXPLAIN QUERY PLAN to SEARCH rather than SCAN. ⚠ An expression index whose text does not match the query **exactly** is ignored silently, so change both or neither.
- **The 90-day plan is gone, and was cut at the SOURCE (#52)** — the plan ended in July and its folder was archived 12 Aug, so `obsidian.parseNinetyDayPlan()` returns null. It was injected in **more than three places** and re-walked the vault looking for the archived folder on EVERY request, because a null is not cached. Removed from `working-memory` rather than from each reader, so the remaining guarded readers no-op by construction — `decision-engine.collectPlanClosure` opens `if (!p || !p.over …)`, checked rather than assumed. Verified dead before removal (`getPlan()` → null, `getPlanTasks()` → []).
- **Time awareness (15 Aug 2026, #87)** — `services/time-fit.js` + `GET /api/time/gap` / `/api/time/what-fits`, rendered by `TimeFitCard` above the Must Move lane. `tasks.estimate_minutes` is NULL when not estimated, snapped **up** to coarse buckets (5/15/30/60/120/240/360/480) — rounding down is how a "this fits" stops being trusted. ⚠ **The top bucket used to be 240 and everything above it was CLAMPED to it (27 Aug 2026)**, so a task Nick knew was a two-day job was recorded as half a day, silently, and then offered up by `time-fit` and the day planner as something that fits in an afternoon — a ceiling that rewrites the answer is worse than no estimate. Above the top bucket a value now rounds **up to the whole hour**, capped at a working week (2400) beyond which it returns `null` as a typo rather than a guess. `normEstimate(value, {exact})` is the escape hatch for a number Nick **typed**: the buckets exist because nobody has "37 minutes" to give, but someone who has gone and entered it does, and snapping it is the planner disagreeing with him about his own work (`task-blocks`' "an explicit request is never snapped to a bucket", one level down). Carried as `estimateExact: true` on the create/PATCH body; `TimeFitCard`'s **Custom…** option is the only caller, and its hours input is what makes anything longer than a day expressible at all. An un-estimated task is assumed 30 min and **flagged `assumed: true`** on every row, never silently; the estimate control sits on that row, because asking for 128 estimates up front is how the priority field ended up 18% populated. Reads `calendar_cache`, not Graph — an instant answer is the feature; and `calendarKnown: false` keeps "I can't see the diary" distinct from "you're free". A 5-min buffer is left before the next meeting; all-day/cancelled/free events are not walls, tentative ones are. Ranking is the existing `rankTasks` order **cut** to what fits, never re-sorted by duration — that would quietly become the quick-wins list that already exists. Nothing learns durations from completion timestamps yet, deliberately: there is no body of finished estimated tasks to calibrate against. Pinned by `time-fit.test.js`.
- **Focus sessions and the way back (15 Aug 2026, #88/#89)** — `services/focus-session.js` + `/api/session`, rendered at the top of `AdhdPanel`. That panel named activation energy as *the* blocker and then only ever answered it with a SMALLER task; a session container is the other answer, and the return prompt is why it was worth building. **Elapsed is FOCUS time, not wall clock** — paused stretches are excluded, or "twenty minutes in" is wrong the instant you are pulled away, which is the only case #89 exists for. **An un-estimated task is assumed 30 min and SAYS SO** all the way to the screen (`plannedAssumed`), importing `ASSUMED_MINUTES` from `time-fit` rather than re-declaring it so #87 and this cannot drift. **A runaway session goes `stale` and ASKS** — never auto-completes (that invents a win) and never vanishes (that loses the thread); crossing midnight is stale whatever the numbers say. **Starting something else INTERRUPTS**, it does not refuse or silently replace: without `force` the route 409s and returns the running session so the client can name it. `recovery()` returns `resume` (paused, still live), `settle` (ran away or crossed midnight) or null — an ACTIVE non-stale session is deliberately not a prompt. **`jira.syncEscalations()` calls `noteInterruption()` but nothing ever auto-pauses**: an escalation landing is not proof Nick switched, and guessing would corrupt the one number the prompt rests on. **PULL-ONLY** — nothing notifies or pushes (nudge volume is the signal allowed to argue against building more). `start()` resolves a task by `task-store.dedupeKey` when given text without an id, because decision-engine items carry a slug (`todo-overdue-top`), not a task row. State lives in `agent_state` (`focus_session` + a 50-deep `focus_session_history`), following standup-session. **Real durations are recorded and deliberately not consumed** — #87 ruled out learning from them for want of data; this is what creates it. Pinned by `focus-session.test.js`.
- **A win is DETECTED, not declared (17 Aug 2026)** — `services/wins.js` + the `wins` table + `/api/wins`, feeding `AdhdPanel`'s Momentum card and "Done today" on the **Today** tab. **Measured before building, and the measurement IS the argument**: over thirty days NEURO recorded **four** completions, against **271 commits**, 57 executed SARA actions and a full diary — and that card was opened **nine times** in the window, showing `0`, no streak and "Nothing logged yet" on every one. The reward surface was not missing, it was STARVED, because the only thing feeding it was self-report — a tickbox, and a tickbox is the first thing avoidance eats. So the ledger now follows the rule the rest of the system already follows everywhere else (*who reports to Nick is READ not typed*, *1-2-1s are detected not declared*, *the tracker is generated*); this was the last hand-typed ledger and the one where hand-typing cost most, because the failure was not a wrong number on a report but NEURO telling Nick he finished four things in a month he shipped 271 commits in. Six sources, all LOCAL reads: git, `sara_actions` executed, `sent_replies`, activity_log completions/rituals, `decisions`, and 1-2-1s via `one-to-one-detect` (the `one_two_one_done` tracker has no callers anywhere). **Three refusals.** (1) **A failed source is a GAP, never a zero** — `collect()` returns `gaps`, carried through `build()` to the card and logged by the scheduler; a wins count that silently reads 0 while the work is happening IS the bug being fixed, so a feed that does it when git is unreachable reproduces it exactly. (2) **No points, no score** — a weighted total claims a commit and a 1-2-1 are commensurable, which is false and gameable by the one person it serves; the number has one job, to be TRUE. (3) **Every row names its evidence** (commit sha, action id, activity id); `manual` is the single source allowed none, because "I had a hard conversation" has no artefact and excluding it is its own dishonesty. **Commits fold to ONE ROW PER REPO PER DAY** carrying the count — at ~9/day, one row each makes git 90% of the ledger and buries the admin work the feature exists to reward (same species as one transcript filling every search result). `dedupe_key` is UNIQUE so `sync()` is idempotent across the hourly cron, startup and a backfill range: a count that climbs on its own destroys the only property that matters. **Rows are MATERIALISED, not derived on read** (following `sent_replies`) — several sources are rolling caches that lose their own history, and a counter that shrinks when a cache rolls is worse than none; it also makes the feed scrollable, which is half of why `git log` feels good. `streakFrom` is **pure** (a Set + an anchor, no DB, no clock) and keeps adhd-dashboard's original rule: weekends do not break a streak, and today still empty is a day in progress, not a break — getting that wrong makes the streak read 0 every morning, the single most demoralising possible bug here. ⚠ `collect()` **sorts activity rows by id**, because `created_at` has second precision and ties come back in NO guaranteed order — the first cut read them in query order and the "Done today" list shuffled; the original `_winsToday` had already learned this and said so. **Deliberately NOT sourced**: meetings (`calendar_cache` has no attendees column, so a meeting and a solo focus block are indistinguishable — `plaud-admin-blocks`'s `attendeesOther()` cannot reach it, and an inflated feed is worse than none) and vault writes (2,229 in 30 days, overwhelmingly Syncthing and the import pipeline). Both are in `KNOWN_GAPS` and shown on the card. Hourly cron at :20, deliberately **not** a `TRACKED_JOBS`/catch-up job — sync reads a trailing window and is idempotent, so a missed hour self-corrects and there is nothing to replay (same call as `bank-holidays`). **The backfill is not optional**: an honest wins feed that starts empty tomorrow reproduces the bug it was built to fix. **The win ARRIVES, it is not only stored** — `headline(summary)` is pure and phrased ONCE on the server, returned on `GET /api/wins` and consumed by both surfaces: `sara/app`'s `completeTask()` (the single completion funnel, so all three owner branches acknowledge identically) shows it the moment a task closes, and the 5pm EOD nudge LEADS with it. That nudge is ridden deliberately rather than adding a wins push — nudge volume is the one signal allowed to argue against building more, and a second daily interruption to say "well done" is how the first stops being read. It already asked for *"one win"*; NEURO now knows them. **`headline()` returns null on zero** — there is no encouraging version of an empty day, and a fabricated or cheerful line is the register `sara-voice` rejects; a ledger failure returns null too, and must never cost Nick either the EOD prompt or a completion he already made. Pinned by `wins.test.js` (12). ⚠ **"This week" is the CALENDAR week, Monday to today, and resets on a Monday (31 Aug 2026)** — `summary().doneThisWeek` and `bySource` were a ROLLING seven days, so the lock-screen widget read *"64 finished this week"* on a Monday morning that had barely started: a number labelled "this week" that never resets is one nobody can act on. The boundary is **`weekly-target.weekStart`, borrowed not re-derived**, so the ring and the count beside it cannot disagree about which week it is (the ring counts `task_done` only — different number, same week), and the Sunday case (`getDay()` calls it 0, so a naive `dow - 1` moves a day FORWARD into a week that has not begun) is only got right once. `doneLast7` keeps the rolling read; **`last7`/`best7` stay rolling on purpose** — the sparkline is explicitly a seven-day SHAPE, not a week.

- **What Nick owes Chris, tracked but never GRADED (7 Sep 2026)** — `services/pip-deliverables.js` (pure `assess()`) + `GET /api/weekly-risk/deliverables` + `DeliverableTracker` in WeeklyRiskPanel. Nothing here is a new measurement: `weekly-risk` has recorded every published and every sent summary since it shipped and `management-log.assess()` already computes both competency-3 and competency-4 figures — this joins them and adds the calendar, and where a number disagrees with those screens, **those screens are right**. ⚠ **THE BURN-DOWN WAS REFUSED, and the reason is written down in VANTAGE**: `self.js` says of exactly this data *"None of this is scored or graded... a tool that turned it into a performance number would be doing the opposite of the job."* Chris assesses the PIP; a percentage rendered here is a number Nick argues with instead of a list he acts on, and on a bad week it is a screen telling a man whose failure mode is avoidance that he is failing. So: counts, names, dates — **no percentage, no score, no RAG, no trend arrow**, pinned by tests scanning the payload for that vocabulary on BOTH the service and the route. Three refusals, all mutation-checked. ⚠ **PRODUCED AND SENT ARE DIFFERENT FACTS** — `weekly-risk` keeps `publishedAt` and `sentRecord` apart deliberately, a published week is a draft on disk, and reporting it as delivered would tell Nick he had sent something Chris has never seen; `producedNotSent` is its own list. ⚠ **`sentRecord`, NEVER `isLocked`** — locked answers *"may this screen rebuild?"*, which reopening a week changes, and reopening must not erase the fact that Chris received it. ⚠ **A WEEK BEFORE THE CADENCE EXISTED IS NOT OWED**: the Monday-midday standard was agreed at the 1-2-1 on **12 Aug 2026**, three weeks after the PIP began, so `WEEKLY_OWED_FROM` is `2026-08-10` and counting from `PIP_START` would manufacture failures against a standard that did not exist. ⚠ **The current week is `due` before midday Monday and `late` after** — a tracker that opens on Monday morning already calling the week missed is one he stops opening. ⚠ **An unreadable store is a NAMED GAP, never a miss** (`known:false`, `log:null`) — *"I could not look"* and *"you did not do it"* are opposite facts and only one is an accusation. `hrUnknown` is carried but is explicitly **not a finding**, following `management-log`'s own rule. Plan dates (`PIP_START` 27 Jul, `PIP_REVIEW` 11 Sep, `PIP_END` 11 Oct) are hard-coded like `management-log.BASELINE_DATE`: fixed by the document, not observable, and a failed read would otherwise silently measure nothing. ⚠ Built in NEURO, not VANTAGE, and the reason is the boundary rather than convenience — **the PIP is personal, not departmental**, and the live data (`weekly-risk`, `management-log`) is already hosted here, so the alternative was a sixth write across a bridge whose closed set of five is load-bearing. Pinned by `pip-deliverables.test.js` (19) + `routes/pip-deliverables-routing.test.js` (6, real HTTP — the setup writes weekly-risk's own state key and **checks it through `publishedAt()` before asserting**, so a moved key fails as "stale test" rather than as "produced nothing").
- **Everything counted FINISHING, and finishing is not the hard half (7 Sep 2026)** — `services/initiation-signals.js` (pure `assess()`) + `GET /api/session/signals` + `signals` on `/api/adhd` + `InitiationRow` in AdhdPanel + a `task_triaged` activity event written by `task-store.updateTask`. **The measurement that motivated it is the shape of the code itself**: `wins` counts finished work, `weekly-target` counts closed tasks, `_momentum` counts both — and Nick’s stated failure mode is INITIATION. `focus-session` had recorded every start, every shrink and every planned-vs-actual pair since it shipped and **nothing had ever read them back**; triage was recorded nowhere at all. Four acts are counted, none of them finishing: **starts**, **shrinks**, **triage**, **estimate accuracy**. ⚠ **A START COUNTS EVEN IF IT WENT NOWHERE** — `endedReason` is never filtered on, because rewarding only the starts that became finishes is the surface that already exists. ⚠ **The LIVE session counts**: it is not in history until it ends, so a history-only count is short by one for exactly as long as he is working; deduped on session id, because a session is briefly readable from both sources as it ends. ⚠ **YOU CANNOT BEAT AN ESTIMATE YOU DID NOT MAKE** — a `plannedAssumed` session is measured against NEURO’s own 30-minute default, so it is EXCLUDED and the exclusion is reported (`assumedExcluded`); scoring him against a number the system invented is #87’s laundering rule wearing a game’s clothes. ⚠ **TRIAGE IS COUNTED PER TASK, NEVER PER FIELD-WRITE** — set a due date, clear it, set it again is ONE task triaged; a count of writes is trivially gameable by the one person it serves. `task_triaged` is logged **on a real change only** (compared against the row), the `task-store` transition rule, or TaskEditPanel’s one-PATCH save would log a triage every time Nick pressed Save out of habit; the fields are the ones deciding the SHAPE of work (`moscow`, `priority`, `due_date`, `estimate_minutes`, `origin`, `domain`) and `text`/`notes`/`status` are deliberately absent so one act cannot land in two ledgers. **First estimates are counted separately** because `time-fit` and `day-planner` are both starved of them. ⚠ **NO STREAKS, NO SCORES** — pinned by a test scanning the payload for the words; the comparison is `wins.typicalDay`'s, Nick against his own median. ⚠ **A weekday inside the covered window with no session is a REAL ZERO** (dropping them makes `typical` the typical of days he already started something, flattering every ordinary day into reading as below par) **but a day before the history begins is UNKNOWN** — `HISTORY_LIMIT` is 50, so a window reaching the cap yields a FLOOR and `complete:false` says so. ⚠ The **close-out** (`estimateCloseout`, phrased once server-side per `wins.headline`) is returned by `POST /api/session/finish` and is the first time the planner’s learning has ever been shown to Nick — and **zero is a MEASUREMENT, not an absence**: guarding it on `> 0` silently dropped the line from the shortest sessions, so it renders “Under a minute”. Also wired into the EOD (`ctx.initiation`): on a day that finished nothing but started four things, that was the only true good news available and the conversation could not see it. Pinned by `initiation-signals.test.js` (31, three rules mutation-checked) + `routes/initiation-routing.test.js` (14, real HTTP, incl. the task-store→activity_log→read chain a service suite cannot see).
- **Two of the three task owners were not counted (30 Aug 2026)** — the wins ledger is built on the `task_done` activity event and `task-store.updateTask` was the only writer of one. But task-store owns ONE of the three things `sara/app`'s `completeTask()` can close: `/api/todos/complete-ms` (Microsoft) and `/api/todos/toggle` (a plain vault checkbox) logged **nothing**, so finishing either moved no number anywhere — not Momentum, not "Done today", not the weekly-target ring. Measured live the morning it was found: **2 recorded completions against 5+ ticks in SARA**, and the card said 2. Same failure the ledger exists to remove, one owner along — it was not wrong about what it could see, it was blind to two thirds of what can be ticked. Both routes now record, via `_recordCompletion` in `routes/todos.js`. ⚠ **complete-ms records BEFORE the Graph push**, deliberately: it already returns `ok:true, pushed:'none'` when Graph refuses, because the vault line is what NEURO reads — a win conditional on Microsoft answering would go missing on exactly the days auth has expired. ⚠ **It skips when a NEURO task carries that `ms_id`**: SARA calls BOTH routes for a linked row (task-dedupe links them, NEURO leading) and task-store has already logged it — inflating is strictly worse than missing, since an invented win makes every other number suspect. `obsidian.toggleTask` now returns `{status, text}` so the route knows what was ticked without a second read, and **does not log itself** — `suggestion-engine`'s `complete_task` toggles the mirror line for a task it already closed through task-store, so logging there would double it. ⚠ The new rows key on the **thing closed** (`task:ms:<id>`, `task:file:<path>#<line>`) so an untick-and-retick folds; task-store's rows **keep `activity:<id>`** — changing that formula would re-insert thousands of historical wins under new keys inside the sync window, the count silently doubling itself. Pinned by 2 tests in `wins.test.js`, one negative.
- **A completion Microsoft would not take is HELD, not lost (30 Aug 2026)** — `services/ms-push-queue.js` + `/api/todos/ms-queue` (GET / POST `/drain` / DELETE `/:msId`), drained every 10 min. `complete-ms` flipped the mirror, tried Graph, fell back to the Power Automate webhook and then **stopped** — a warning string and nothing else. ⚠ **And the completion did not merely fail to reach Microsoft, it UNDID itself**: `Tasks/Microsoft Tasks.md` is regenerated wholesale from Graph every 30 min on weekdays, so a ticked task reappeared as open inside half an hour with the warning long gone — work done, silently reverted, offered back as if it had never happened. Graph auth expiring is not rare here (the device-code flow needs re-running and `getAccessToken()` returns null for the whole window), which is precisely when this bites. Held in `agent_state.ms_push_queue` — **KV, and it MUST be persisted**: the backend restarts several times a day on deploys and an in-memory queue would lose exactly what it exists to hold. ⚠ **The mirror suppression is the load-bearing half** — `syncMicrosoftTasks` asks `pendingIds()` and skips those lines; without it the retry works and Nick still sees the task reappear, ticks it again and queues a second push. Suppression ends the moment an item leaves `pending`, so a permanently failed push gives the task **BACK** rather than hiding it for ever, and `heldSkipped` is **logged** — a task missing from the mirror must always have a stated reason, or this is indistinguishable from the sync losing work. ⚠ **Almost nothing is terminal, and that is the finding**: the obvious split — retry `auth`, give up on `not_found` — is wrong, because `setPlannerPercent` returns `not_found` when it cannot read the etag and `graphFetch` returns **null on a 401**, so a real Planner task reports `not_found` throughout an auth outage. Only `no_task_id` gives up immediately (nothing to retry against); everything else is bounded by attempts **AND** age (12 / 7 days — a ten-minute drain burns the attempts in two hours, shorter than a weekend of expired auth, while an item erroring before it can attempt would otherwise sit pending for ever). **Nothing is ever silently dropped**: an exhausted item becomes `failed`, keeps its reason, and is reported **critical** in `state-of-play` (a pending one is `warn`) — deleting it would recreate the original bug with extra steps. Idempotent by `msId`, sequential and fault-isolated (`bookAll()`'s rule), `forget()` is the manual way back (`plaud-admin-blocks`' rule), and an unreadable queue **suppresses nothing** rather than taking the vault sync down. Deliberately not a `TRACKED_JOBS` job — durable and live-read, so a missed pass self-corrects. Queued only when the webhook did NOT fire: a webhook that returned OK completed the task through Power Automate. Pinned by `ms-push-queue.test.js` (9).
- **NEURO can triage a task it does not own (2 Sep 2026)** — `services/ms-task-local.js` + `PATCH /api/todos/ms/:msId/local` + `MsLocalControls` on the expanded Microsoft row. A Planner card holds a title, a due date, a description and a percentage, and that is all Microsoft will hold — so MoSCoW, priority 1-3 and "am I actually mid-way through this" had nowhere to live, and **18 open Microsoft tasks were unrankable by construction**: visible, completable, editable against Graph, and impossible to triage. ⚠ **NOTHING here reaches Microsoft, and that is Nick's ask rather than an implementation shortcut.** `wip-ms` one route up is the PUBLIC version — it writes `percentComplete` to Planner, which his team reads. This is the private one: *working on* without telling the board, *blocked* which **Planner cannot express at all**, and a MoSCoW letter that is NEURO's ranking vocabulary and has no business on somebody else's card. Pinned by a source scan asserting the service names no Microsoft path, because the plausible future regression is somebody wiring the state to `wip-ms` "for consistency" and publishing *blocked* onto a shared board. KV in `agent_state.ms_task_local` (the `ms_todo_list_by_task` / `ms_push_queue` precedent — an annotation on somebody else's row earns no table), **persisted** because a triage decision that evaporates on the next deploy looks exactly like one that worked. Four rules: an **omitted field is left alone while an explicit null clears it** (a control that sets MoSCoW must not wipe the state beside it just by not mentioning it); an **unrecognised value is REFUSED with a 400, never normalised into a clear** — "I did not understand you" and "clear it" are different requests; an entry with nothing left in it is **deleted, not stored as a row of nulls**; and `annotate()` **never overwrites a NEURO-owned row** — a linked task already carries a real `moscow` off the `tasks` table, and two disagreeing triages with no way to tell which was read is worse than one. Folded in **BEFORE** `decorateTask` and `rankTasks` on both `/api/todos` and `/api/todos/focus`, or the letter renders on the badge and changes no ordering; the write invalidates `vaultCache`'s scored cache for the same reason. ⚠ **The controls render OUTSIDE `MicrosoftTaskControls`**, which correctly refuses to show anything when Graph could not be read — NEURO's own triage depends on Microsoft not at all, and losing the ability to say MUST because sign-in expired would kill the feature on exactly the days the list is hardest to work with. ⚠ Deliberately **no prune** of entries for closed tasks: the only callers holding a live ms_id list are the two read paths and one of them (`/focus`) is FILTERED, so pruning against an overdue-only view would delete every annotation on a task that is merely not late — an uncalled prune waiting to be wired up is worse than none. Pinned by `ms-task-local.test.js` (11) + `routes/ms-task-local-routing.test.js` (6, real HTTP). Alongside: `.todo-edit` was **one wrapping flex line holding every group**, so a Microsoft task's title box was a few characters wide, its notes box smaller than the sentence in it, and "Due" ran on from "Priority" with nothing but a gap between them — it is one control per row now, label in a fixed left column.

- **Half of a shared card is not half a task (2 Sep 2026)** — a third `ms-task-local` state, **`mine-done`**, plus `myPartDone` through `triageTodo`/`decorateTask` and the `/focus` filters. Some Planner cards have sub-tasks split between Nick and other people: **his bits are done, theirs are not, and NEURO had no way to be told** — so four cards sat at the top of the list reading `MUST · 12d overdue · HIGH` for work he had already finished. The only exits were to tick it (closing a card his team is still working on, in a board NEURO cannot put back) or to move the due date, which is a lie about when it was committed to. ⚠ **It is a STATE, never a completion, and the row is untouched** — still open, still visible in All, still carrying its due date, never ticked here or on the board; the only thing it loses is the right to demand action from Nick today. Pinned by a test asserting `done`/`status`/`due_date`/`text` are all unchanged by the annotation. ⚠ **The date is dropped for the TRIAGE and kept for DISPLAY**: `owedDate` (null when `myPartDone`) feeds `classifyMoscow`, `priorityFromMoscow`, `overdue` and `dueToday`, so the card stops being MUST/HIGH and leaves the Must Move lane — measured on the four real rows, `must/high/overdue` → `should/normal/not-overdue`, lane 2 → 0 — while the row still shows how late the board is, in a neutral chip rather than red. Hiding the date would say the card is fine when somebody is still waiting on it; painting it red says he is late for work he has done. ⚠ **An explicit `mustdo` is NOT suppressed** — that is a call Nick made himself, and a later annotation does not get to overrule it silently; the lane row carries `myPartDone` so that case is still marked. ⚠ **`annotate()` runs BEFORE the `/focus` date filters**, not after — annotating afterwards leaves the row ranked correctly and still sitting at the top of the Overdue tab, which is the screen the whole thing exists to clear; `all` never filters it out, because a false *"there is nothing there"* is worse than a false *"you are late"*. The chip is deliberately **not green** (green reads as done) and its wording says the card is still open. ⚠ Only reachable on rows NEURO does not own — `annotate` skips anything with a `task_id`, so a **task-dedupe-linked pair cannot be marked this way**; that is untouched, not solved. Found on the way and fixed: **the MoSCoW annotation had never survived decoration** — `decorateTask` reads `metadata.moscow` and falls through to classifying the text, so a stored `could` came back `must` (verified on a real row) and the letter appeared to stick only when the classifier happened to agree; `annotate` now writes it into `meta` as well. Pinned by 3 tests in `ms-task-local.test.js`, 2 in `todo-intelligence.test.js` and 1 real-HTTP route test.
- **The Must Move lane can be ARGUED WITH (2 Sep 2026)** — `opts.deferred`/`keyFor`/`held` on `todo-intelligence.buildTodayLane` + `POST /api/todos/lane/{defer,undefer}` + an `undefer` verb on `attention-lifecycle.act` + `NotToday` in TodoPanel. Nick, looking at the lane: *"what if I decide the task isn't a Must Do Today?"* — and the answer was **you cannot**. Membership is recomputed on every read (`moscow === 'must' || overdue || dueToday || priority high`), so there was nothing stored to override, and the lane had **no dismiss, no snooze, no defer** at all. The indirect levers do not work either, which is the part worth keeping: **dropping the MoSCoW leaves `overdue` carrying `needsToday` on its own**, so for the three live rows (all MUST, 5–12 days overdue) the ONLY exit was to move the due date — a lie about when the thing was committed to — or to abandon it. A lane that cannot be argued with is one he stops reading, and his failure mode is avoidance. ⚠ **It reuses `attention_records` rather than adding a second suppression map**, and the key is the point: `todo:<slug of text>` is the SAME key `decision-engine`'s todo cards use, so *"not today"* in the lane is the same statement as deferring that task on the Now page — two stores would let one surface contradict the other about a decision made once. ⚠ **The record is opened at the moment Nick decides, never on the read path**: `GET /api/todos` is polled, so upserting a record per lane row per poll would write continuously to say nothing; the read only asks `deferredKeys()`, which is a cheap read of open records. ⚠ **Only DEFER is offered, never dismiss** — "not today" is about TIMING and "never show me this" is about the TASK, which is still open and still owed. ⚠ **The filter runs BEFORE the slice**, so a snoozed row does not leave a gap — the next-ranked task moves up and the lane stays five deep, or putting something off would feel like losing capacity for the day (caught by a test: the `.slice(0, limit)` was still in the chain and the lane came back four long). ⚠ **What is held back is REPORTED** (`laneHeld`, with reason and return time, and a "Bring it back") — a shorter lane is indistinguishable from one that found less work, and an all-snoozed lane would otherwise render as a clear day. ⚠ **It fails OPEN**: an unreadable lifecycle shows the WHOLE lane and says so in `laneGaps`, rather than hiding work on the strength of not having looked. ⚠ **The REASON is the product, not decoration** — `friction.js` parses `deferred` events back as evidence about the work (a task put off three times for `too-big` is a finding), so the button opens four reasons rather than snoozing straight off, and an unrecognised reason is **REFUSED rather than stored as `unspecified`**, which would lose exactly the signal this collects. `undefer` is the way back (every other decision here has one — `restore`, `unmerge`, `unlink`, `forget`) and **deliberately does NOT erase the deferral event**: cancelling is changing your mind about when, not a claim you never put it off. `keyFor` is INJECTED so `todo-intelligence` stays free of the DB and there is one slug implementation. Pinned by 3 tests in `todo-intelligence.test.js` (23) + `routes/lane-defer-routing.test.js` (8, real HTTP).

- **A merged pair is one task with two records, and you pick whose words it wears (2 Sep 2026)** — `lead` on `task-dedupe.linkPair` + `linkedMsMap()` + `msCounterpart` off `parseVaultTodos` + two Merge buttons in `TaskDedupe`. ⚠ **The counting and the completion push ALREADY worked** — `linkPair` has taken the `ms_id`, suppressed the mirror line and PATCHed Graph on completion since 17 Aug. What was wrong was the WORDING and the blindness: the button read *"Same task — keep NEURO's"*, which describes a choice about which task to THROW AWAY — the one thing this never does — and the surviving card then never showed what the board calls the thing, so the Planner wording vanished at the exact moment Nick confirmed the two were the same. It is *"Merge — lead with NEURO's"* / *"… with Planner's"* now. **`lead` decides whose WORDS the single row carries and nothing else**: both records survive either way, NEURO counts it once either way, ticking it completes both either way. ⚠ **Adoption is a ONE-OFF COPY, not a subscription** — nothing re-applies it when the Planner card is later renamed, and no `lead` flag is stored, because a flag only something in the future would honour is a reader waiting for a writer (the Jira cache's exact shape). ⚠ **Adoption NEVER blanks what NEURO already had**: an absent Planner due date is *"the board does not track one"*, not *"clear the date Nick set"* — identical in a null, and only one of them is a decision. ⚠ **The link stands even when the adoption fails** — `updateTask` refuses a text that would collide with another task's dedupe key, and the pair being the same task is a separate fact from whose wording won; the card SAYS the wording was not adopted rather than reporting a clean merge over a title anyone can see is unchanged. **The counterpart is read LIVE off the mirror, never copied onto the row**: `linkedMsMap()` gives ms_id → taskId, and `parseVaultTodos` attaches `msCounterpart` at the one point that holds both halves, so a Planner rename shows up on the next sync instead of the card quoting words nobody uses. ⚠ A test-fixture trap worth knowing: `createTask` FOLDS a repeated wording into one row (dedupe_key is UNIQUE), so two fixtures sharing a sentence are ONE task and the second link then correctly refuses as already-linked — the matcher being right and the test being wrong. Pinned by 5 tests in `task-dedupe.test.js` (36).

- **A block that did not happen can be MOVED (2 Sep 2026)** — `task-blocks.reschedule()` + `POST /api/task-blocks/:id/reschedule` + `RescheduleForm` in the whole-block action row. The only ways out of a missed window were *"Nothing to write up — close all N"* and *"Didn't happen — drop all N"*, and dropping loses the MEMBERSHIP — for the live nine-task block that is nine re-selections in the picker. Nick's difficulty is initiation, so a recovery costing more than the missed block did is a block that never gets rebooked; the move button is FIRST of the three, because a recovery buried behind two ways to give up is one nobody uses. ⚠ **A TICK IS FINISHED WORK AND NEVER TRAVELS.** Ticked items stay on the old block, still `awaiting-writeup`, still owed their note — carrying them into a future slot would put a completion back in the diary and make the new block's note responsible for evidence about a sitting that already happened. So the old block SURVIVES whenever anything in it was ticked, and its calendar event is deleted **only** when nothing was: an empty block in a past hour is clutter, but one that produced real work is a record of where the time went. Asking to move a ticked task is **REFUSED by name, never quietly skipped** — silently leaving it behind looks like the reschedule half-worked. ⚠ **Order is load-bearing: the new block is created FIRST.** A taken slot, an unreadable diary or a Graph refusal leaves the old block untouched, whereas detaching first and then failing would leave the tasks in NO block at all — worse than the missed window. ⚠ **On a full move the MEMBERSHIP goes too, not just the status** (caught by a test, not by reading): `drop()` deliberately keeps its items so `restore()` can put them back, and doing that here would put every task in TWO live blocks the moment anybody restored the old one — two holds, two outcome notes, and a sweep with no way to say which sitting the evidence belongs to. A partial move goes through the existing `removeTask` (so the old event's subject and body are rewritten) and then re-renders the note's checklist, which would otherwise still list work that has gone. A `released`/`complete`/`dropped` block is refused — those ended deliberately, and a new slot is not an undo; `restore` is. The time field is **optional**, because *"tomorrow, wherever it fits"* is the answer most of the time and making him pick an hour is the friction being removed. Preview is the EXISTING `GET /plan/:taskIds` — no dry-run flag, because a second way to ask where it would go is a second answer free to disagree. Pinned by 7 tests in `task-blocks-hold.test.js` (64).

- **A task joins a window that already exists (4 Sep 2026)** — `task-blocks.addTask()` + `latestEndFor()` (pure) + `POST /api/task-blocks/:id/tasks` + a chooser on `BlockTimeControl`. A block has held MANY tasks since 18 Aug and there was no way to put one INTO an existing block: `removeTask` had shipped without its twin, so adding a fifth task to a four-task block meant dropping the block and re-picking all five. Nick's difficulty is INITIATION, and a five-step way to do something he had already decided to do is a task that never gets blocked. The control now offers the upcoming blocks FIRST and "New block…" second — joining is the cheaper act and the one that had no route at all. ⚠ **The window GROWS by what the task is thought to take** — adding work to a fixed window is only overpacking it silently — **but only as far as the diary allows**: `latestEndFor` caps the new end at the next real event minus `BUFFER_MINUTES`, and it borrows `findSlot`'s own "cancelled and free-marked events are not walls, tentative ones are" rule rather than deciding again, because two answers about what counts as busy is how the search that PLACED a block and the extension that lengthens it come to disagree about one diary. A shortfall is **REPORTED and names what is in the way**, never refused — a deliberately tight window is Nick's call, a double-booked one is not. ⚠ **An unreadable diary lengthens NOTHING and still adds the task**: membership takes nobody else's time, only the extension needs to know what is around it, and `known` is returned separately from the answer so "free until 15:00" and "I could not look" cannot be conflated. **Three refusals**: a window that has already PASSED (putting work into a sitting that has been is a claim it was done then — `reschedule` is the way to move a missed block); a task already in ANOTHER open block (two live blocks on one task is two holds, two outcome notes and a sweep with no way to say which sitting the evidence belongs to); and a full block. Adding the same task twice **FOLDS to `already:true` and says so** rather than rendering a tap that did nothing. ⚠ **Deliberately NO estimate write-back** — `schedule` writes one only for a SINGLE-task block, where the window IS the judgement about that task; this block holds several, so splitting its window across them would invent a number per task. A window holding anything un-estimated becomes `minutes_assumed` whatever it was before (the partly-estimated rule). ⚠ **`MAX_TASKS_PER_BLOCK` moved into the SERVICE** — two paths now add to a block and a cap enforced in the route is a cap `addTask` walks past. ⚠ A Microsoft-mirror card still cannot be blocked at all: a block hangs off a NEURO task row, and the mirror has none. Pinned by 6 pure tests in `task-blocks.test.js`, 11 in `task-blocks-hold.test.js` and `routes/task-block-add-routing.test.js` (7, real HTTP, **mutation-checked** — renaming the path fails 6 of them; it exists because a POST on `/:id/tasks` beside a DELETE on `/:id/tasks/:taskId` is exactly the shape that has gone wrong here before).

- **The Must Move lane can be EDITED (4 Sep 2026)** — `TaskEditPanel` in TodoPanel, rendered by both the lane and the list. The lane is the first thing Nick reads and it was read-only: a must could be ticked, marked WIP or deferred, and **not one field on it could be changed** without scrolling down and finding the same task again in the list below — so the fields most worth correcting were the ones furthest from where the mistake was visible. The block of controls was lifted out of `TodoItem` rather than copied, because two copies would be two cards free to disagree about what a task can be told. ⚠ **The lane's expansion key is PREFIXED (`lane:<rowKey>`), deliberately not the bare one the list uses** — a must-move task is usually in both places, and a shared key would open BOTH cards at once: two `TaskControls` over one task, each with its own unsaved draft and its own Save. One expansion state across the whole panel then guarantees only ever one draft in flight. ⚠ Clicks inside the open panel are **stopped** at its wrapper, or the card would close underneath the button being pressed — the same bubbling trap `TaskControls` already guards against on the row below.

- **A card's edits are a DRAFT until Save (2 Sep 2026)** — `TaskControls` and `MsLocalControls` in TodoPanel. Every control on an expanded task card used to PATCH on click, and each patch refetched and RE-RANKED the list — so setting a MoSCoW moved the card out from under the cursor before the priority beside it had been touched, and triaging one task meant chasing it up the screen through four separate re-sorts. ⚠ **Worse, and found on the way: `TaskControls` had no `stopPropagation`**, so every button inside it also bubbled to `.todo-text-col`'s onClick and COLLAPSED the row being edited — one tap, two unwanted movements, neither of them the thing that was clicked. Triage is one thought, so it writes once, on a deliberate Save, in a single PATCH (`patchTask` already took an arbitrary field object). ⚠ **The draft resets on the row's IDENTITY (`rowKey`), never on its values** — the panel refetches on a timer and after every write elsewhere on the screen, so an effect keyed on `todo.moscow` and friends would wipe half-finished edits whenever a background poll landed, silently and indistinguishably from a button that never registered. The `domain` baseline reads **with its default applied** (`t.domain || 'work'`), because that is what the buttons render as selected — comparing a drafted `'work'` against a stored null would make an untouched task read as dirty the moment it was opened. A failed save **keeps the draft exactly as it was**: resetting it would throw away the decision as well as the write. The Save row **names what is unsaved** rather than saying "unsaved changes", which on a card left open a while is a prompt to go hunting. ⚠ **`BlockTimeControl` and `HouseholdToggle` are deliberately NOT drafted** — they are not fields, they are actions with consequences of their own (a real calendar event; a task published to VESTA, which Nick's partner reads on a public URL), each already gated by its own confirmation, and parking them behind a shared Save would make one button whose blast radius ranges from "changed a letter" to "put this on the internet". The half-typed-date guard `dueDraft` existed for is gone by construction: nothing is written until Save, so an incomplete value in the picker is just an incomplete draft.
- **A recurring task is not finished by being completed (1 Sep 2026)** — recurrence read from Graph, carried onto the mirror line as `<!--rec:monthly-->`, phrased once in `shared/ms-task.cjs` (`recurrenceToken` / `recurrenceLabel` / `occurrencesBehind` / `rolledNotice`) and rendered by TodoPanel. Nick ticked *"Guild/F&C MI reports"* **three times in three days** and it came back every time — and **nothing was broken**: the PATCH reached Graph, `[ToDo] Completed` was logged, and To Do did what recurrence means — closed THAT occurrence, rolled the **SAME task id** forward, put `status` back to `notStarted` and advanced `dueDateTime` by one month. The 08:45 mirror sync then read it from Graph as open and wrote it straight back. So a completion that worked perfectly was **indistinguishable from one NEURO had lost**, and the push queue was empty, auth was healthy and the webhook never fired: there was nothing wrong in the machinery, the SCREEN was silent. Measured live: 5 recurring tasks, three monthly ones **5–6 occurrences behind** (each tick moves them one) and two DAILY ones **413 and 406 behind** — those can never be cleared by ticking and want rescheduling in To Do, which is a decision, not a bug. ⚠ **The marker is an HTML COMMENT, never text on the line** — anything on the line lands in the task's own wording and from there in its dedupe key (the `### `-heading rule, one field along); preserved explicitly through `setTaskFields` and `setTaskPercent`, or a rename silently drops it. `completeTodoTask` **RE-READS the task after the PATCH** — deliberately a second GET rather than trusting the PATCH body, because *"go and look at what is there now"* cannot be wrong — and is **never allowed to fail the completion**: the occurrence IS closed, so an unreadable re-read means "we cannot tell you what happened next", not "that did not work". `complete-ms` then **repaints the mirror to what Microsoft now holds** (unticked, new due date) instead of leaving it claiming a state Graph does not agree with for half an hour and then appearing to lose it — the tick is NOT undone, it did something real — and the client is told, so the row is not painted done. ⚠ **A pattern NEURO cannot name is `repeats`, NEVER a guessed frequency**, and not-recurring stays **null**: "it comes back and I could not say when" and "it is done when you tick it" are different facts. Likewise `occurrencesBehind` returns **null** where it cannot count honestly — null and 0 are different answers, and a 0 standing in for "no idea" reads as an all-clear. Planner carries **no `recurrence` field at all** in Graph's response (checked live over 286 tasks), so nothing is invented for it. The wins ledger keys on `task:ms:<id>`, so repeated occurrences of one task **fold into a single win** rather than inflating — and equally, next month's genuine occurrence will not count; that is a pre-existing trade-off, not a decision taken here. Pinned by `ms-task-recurrence.test.js` (19), incl. a **forbidden-wording** test — the whole bug was a working completion reading as a lost one, so a notice saying "failed" would restate it in words — and a fixture copied off the **live account**, not from the docs (`sleep_core_hours` / `meeting_alert` twice over).
- **A switch in Settings, and a light that was lying (3 Sep 2026)** — `JIRA_ASSIGNED_SYNC_ENABLED` joins the `feature-flags` registry as `jira_assigned_sync`, so turning Jira ingestion on is a toggle in **Settings → Switches** read at CALL time rather than an SSH session, an `.env` edit and a `pm2 restart` — six steps of friction on a system whose premise is that the bottleneck is initiation (the `vesta_photo` idiom: **the environment still WINS where explicitly set**, and the panel then disables the control and names the variable that owns it). ⚠ **The DRY RUN is deliberately ungated and only APPLYING is gated** — the preview is what you consult in order to decide whether to flip the switch, so gating it made it unavailable at exactly the moment it was wanted; every write branch sits behind `if (apply)`, so it costs two read-only JQL calls, and the result carries `enabled` because a preview of work that will never happen on its own reads exactly like one that will. **Now enabled.**

- **A correct refusal that nobody could see (4 Sep 2026)** — `jira-tasks.keysByTaskId()` + `jiraKey` on `toTodoShape` + `RefusalNotice` in TodoPanel. *"There is no manual tick"* is Nick's own rule and it worked exactly as written: `updateTask` threw, the route answered **400 with the ticket named**, and the panel `console.error`'d it. So ticking a Jira-linked task did **nothing at all, in silence** — a working rule experienced as a broken checkbox, which is the failure this codebase refuses everywhere else. Two halves, and the first is the one that matters: **the refusal is now SHOWN, using the server's own words verbatim** (it is the one place that knows which ticket; a second phrasing on the client is a second answer free to drift), and it is a **separate notice from `HoldNotice`** because a hold means the completion landed and is waiting for a write-up while a refusal means nothing moved — rendering one as the other sends Nick off to write a note for a task that is still open. The second half is that the row **says which ticket closes it BEFORE the tick is refused**: nothing on any read path had ever known about the link, because `keyForTask` walks the whole ledger per task and is quadratic over a list of 150. ⚠ `jiraKey` is read from the **LEDGER, never from `source`** — a ticket taken off Nick is unlinked and the task becomes his to close again, which is precisely why `isJiraOwned` asks the link rather than the column. ⚠ `toTodoShape` **builds the map itself when a caller does not pass one**, rather than defaulting to empty: an empty map makes every row read *"Jira does not close this"*, which is the answer that lets the silent refusal straight back in. ⚠ The checkbox is deliberately **NOT disabled** — a disabled control on a touch screen explains nothing, and the tap is what surfaces the reason. Pinned by 3 tests in `jira-tasks.test.js`, incl. a **negative** (a task Nick owns must carry `jiraKey: null`, or a screen would take the tick away from him — the failure in the worse direction).
- **A cache key with no writer is not a stale cache, second instance (3 Sep 2026)** — ⚠ **`jira_last_sync` / `jira_status` / `jira_last_error` have had NO WRITER since 3 July 2026.** They went with the queue feature, which took the writer and left the readers — the same species as the queue cache itself, and the obvious key to build a Jira health row on. They sat frozen while **`/api/status` reported `status: "ok"` over a two-month-old timestamp** and the Admin page rendered it as *"connected · Last sync 19:11"*, so a row built on them would have shown a permanently stale light for a feature that works perfectly every five minutes. **The live signal is `escalation_last_sync`** — the only Jira read on a timer — and BOTH surfaces now judge that same key so they cannot disagree; `/api/status` says **`unknown` rather than `ok`** when nothing has reported, because a green light nobody earned is what it was doing wrong in the first place. The dead keys are left in `agent_state` (harmless; deleting them is a destructive migration that buys nothing) and are read by nothing. Pinned by `signals-jira.test.js` (8, **two negative** — neither the row nor `/api/status` may read the dead key).
- **Model output stopped signing Nick's name to it (3 Sep 2026)** — `addTodoFromChat` passed `{ trigger: 'todo-from-chat' }` and `addTodoToMasterList` reads `options.origin`. **The key never matched**, so the store's `|| 'manual'` fallback took over and every task the model created through the `[ADD TODO: ...]` marker landed stamped `source: 'manual'` — indistinguishable in the data from something Nick typed into his own list. Invisible because it fails in the direction that looks fine: a plausible task with a plausible provenance, nothing errored, nothing missing. It stamps `origin: 'chat-marker'` now. ⚠ **The trap that hid it is the naming**: the parameter is called `origin` and feeds the **`source` COLUMN**. The `origin` COLUMN is a different question entirely (commitment vs continual improvement) and is left **null** here deliberately — that is `inferOrigin`'s documented answer for every route INTO the store, because knowing a human did not type something is not the same as knowing who wanted the work. Both halves are pinned, so a future tidy-up that infers an origin from the route fails. ⚠ **Still reachable**: the suppression is a PROMPT instruction ("do not use the marker while you have tools"), not a code gate — the regex handler in `claude.js` runs on every response and the base prompt still advertises the marker for the no-tools path.
- **An unnamed writer stops being called Nick (3 Sep 2026)** — `createTask` defaulted `source` to `'manual'`, which made *manual* a **CLAIM rather than a fact**: any writer that forgot to name itself had its output silently attributed to the one person in the system who can be held to it. Not hypothetical — it is the mechanism by which the chat marker above passed model output off as something Nick typed. It is **`'unattributed'`** now, and deliberately a STRING rather than null: `origin` has documented null semantics and few readers, while `source` is read all over the estate (`/^MS /.test(t.source)`, `isJiraOwned`, task-dedupe, the wins ledger) and a null would turn a provenance gap into a crash somewhere unrelated. **It also LOGS** — a default that fires in silence is the whole bug. Two further copies of the same claim were removed so one place decides: `obsidian.addTodoToMasterList` passes the source through unset, and **`routes/tasks.js`** stopped defaulting (it is human-facing but reachable by any machine client holding the API token, so it was asserting *"a person typed this"* for callers that never said so; its only caller sends `source: 'manual'` explicitly, because it is the one place that knows). ⚠ **The SCAN is the point** — `task-attribution.test.js` walks `services/` and `routes/` and fails if any `createTask` call omits a source or re-implements the `'manual'` default, with a positive control. This class of bug was findable only by hand-auditing 15 call sites, which is how it went unnoticed; the scan found `routes/tasks.js` immediately, which is the argument for it.
- **State of play (15 Aug 2026)** — `services/state-of-play.js` + `GET /api/state-of-play`, rendered by `components/StateOfPlay.jsx` at the **top of the sidebar, above Briefing** (Briefing answers "what about today"; this answers "what shape is everything in"). Split like `pi-health`: `snapshot()` runs the SELECTs, `assess()` ranks what is wrong and takes a **plain object**, so the ranking is testable without a DB. **The ordering IS the product** — the panel renders the top of that list as its focus band, so stale caches and stopped jobs outrank big-but-true numbers. It exists because the Jira cache had been stale since **3 July** and nothing said so: every panel reading it renders happily whatever the age of the data, so a staleness only surfaces when something is looking for it. Watchdog's own 3d/10d thresholds are **reused, not re-picked** — two numbers for "this job has stopped" is how a dashboard disagrees with the alert that woke you. `TRACKED_JOBS` is a **literal list, not derived from `agent_state`**, or a job that stopped stamping entirely would be invisible by construction. Three states, not two: `ok` / `stale` (critical) / `never` (**info, not warn** — run-tracking shipped after the jobs existed, so `embeddings-rebuild` read "never run" on day one while demonstrably mid-rebuild; unknown is not broken, and a board opening with false warnings is one nobody reads by week two). **What counts as outbound is `action-presenter`'s call, never a list of type names** — the first cut hardcoded one and immediately disagreed with the Actions queue: `draft_reply` READS outbound and is classified `write`, because approving it sends nothing (gate 1 of 2 drafts the words and queues a separate `reply_email`). Three places must mean the same thing by "leaves the building" — the Actions queue, `bulk-reject`'s refusal guard, and this — so all three ask the presenter. Note `getPendingSaraActions` **defaults to limit 10**; pass a real bound. Dates are **local**, never `toISOString()`. Read-only throughout — this panel must never be the reason something changed. Pinned by `state-of-play.test.js`.
- **Escalations are TWO populations (16 Aug 2026, #94)** — `syncEscalations()` queried `fetchEscalationTickets()`, the **request-type arm alone**: escalations a customer raised through the portal. Every ticket the team moved into the Escalations **tier** was invisible to the count, the Focus card, the briefing AND the nudge — the surface Nick checks every morning, understating by two thirds. Measured live: narrow **6**, both arms **17**. `fetchActiveEscalations()` already OR'd them and nothing used it. **Three traps make the naive swap wrong, all silent.** (1) It returns objects already flattened by `mapEscalationIssue` — no `.fields`, so the old `issue.fields.summary` reads write blank summaries and null dates. (2) `ESCALATION_FIELDS` never requested `comment`, so `nickCommented` would be false for all 17 and the whole queue lands *unseen*; comments are requested only by the caller that reads them (`searchEscalations(jql, {withComments})`), and **`nickCommented` is `null` when not requested, never `false`** — "we did not look" must not be what puts a ticket on the card at 23:00, so the sync reads `!== false`. (3) **`briefing.checkEscalationAlerts` is the louder path and had the same narrow query**: it pushes once PER ticket and `escalation_alert` is in webpush's `ALWAYS_DELIVER`, bypassing quiet hours and the cap — 11 of the 17 were absent from its seen list. So **both paths backfill on their first widened run** (`escalation_wide_seeded` / `escalation_alert_wide_seeded`): the alert path records the 11 as already-alerted, the sync path stamps no `focus-session` interruptions. They did not *arrive*; NEURO started looking. **What is deliberately NOT backfilled is their unseen state** — Nick had already replied to 12 of the 17 and those never reach the card (`!hasComment && !seen`); the 5 left, 6–65 days old, ARE the finding, and seeding them `seen` would make the fix a no-op on the surface it exists to correct. They arrive as ONE count-based banner on the `escalation` nudge type, which is **not** in `ALWAYS_DELIVER` and so still respects quiet hours. Two things checked rather than assumed: Jira caps the inline `comment` field at 20 and returns the **NEWEST** 20 (NT-14855 came back `startAt: 32, total: 52`) — the oldest 20 would read "no reply" on exactly the long churning threads an escalation becomes; and `/search/jql` **dropped `total`**, so `isLast` is the only cap signal and it is logged loudly (same species as the 1,958-key JQL and calendar `$top=50`). #54's badge rules still hold on the wider population. Pinned by `escalation-wide.test.js`. **The urgency arm now states its own window (17 Aug 2026, #104)** — `routes/escalation.js` asked NOVA's log for 90 days, a cap that would silently stop badging *"still open 91 days after being escalated"*, the ticket most worth seeing. **Measured first and the incidence is ZERO**: the log holds exactly TWO manual escalations over its whole life, both from 15 Aug, and widening 90 → 3650 days moves 1,935 rows to 2,352 and finds the same 2. Widened regardless (`URGENCY_WINDOW_DAYS`, fifth instance of the species) but **the load-bearing half is the honesty**: `/active` returns `urgency: {state, windowDays, logRows, manual}` so the count describes its own edge and `off` / `error` / `ok` stay distinguishable — only `ok` means the number is the whole population. ⚠ Verified live: **NOVA ignores the `type` query param** and returns all ~2,352 rows whatever you ask for, so the client-side `escalation_type === 'manual'` filter is load-bearing, not belt-and-braces. Pinned by `escalation-window.test.js`.
- **The weekly risk report is generated, not retyped (17 Aug 2026)** — `services/weekly-risk.js` + `services/management-log.js` + `routes/weekly-risk.js`, building the two PIP deliverables Nick owes Chris: the **Weekly Risk & Anomaly Summary** (competency 2, due **by midday every Monday** — agreed at the 1-2-1 on 12 Aug) and the **Management Actions & Conversations Log** (competencies 3 and 4). The first edition was assembled by hand off a NOVA screen, which is fine once; the failure mode of a hand-built compliance report is that it silently stops being built, in front of the person assessing the PIP. Split like `pi-health` / `state-of-play`: `snapshot()` reads, `assess()` judges, `render()` writes the note — `assess()` is **pure**, so the anomaly rules (which are the product, not the numbers) pin without NOVA, a vault or a clock. **Two refusals carry the design.** (1) **It never invents what it cannot reach**: every source records whether it answered, `greenPct` is **null and never 0** with no data, and a failed source is ranked ABOVE every finding — not because it is the worst news but because it decides how much of the rest to believe. A report reading "0 breaches" because NOVA was down is a false all-clear. (2) **It never asserts the manual sections on Nick's behalf** — overtime, escalations-to-Chris, actions and data-quality judgements are his, stored per week and carried forward, and `publish()` **refuses with named blockers** while any is unanswered (the `action-presenter` shape). `null` is meaningfully distinct from `0`/`[]` throughout: nil overtime is a *claim*, no overtime figure is an *absence*, and an empty escalation list must be a decision. Rules: a compliance KPI below 95% for **3+ consecutive weeks counted backwards from the latest** (so a recovered slide stops reading as ongoing) escalates; ageing ranks by **ratio to target, not raw days** (Tier 2's 162-vs-2 is 81× and beats Development's 249-vs-31); reason-code capture escalates on **share** not count; a **zero against a live higher-is-better target** is flagged as a probably-stalled pipeline rather than performance; and the KPI snapshot defaults to the **latest date that HAS rows**, returning `ageDays`, because it builds at 07:30 Monday when n8n may not have run and "today, empty" renders as a team with no KPIs. **Data crosses from NOVA over the bridge, and had to**: `jira_kpi_daily` lives in **techservicesjsm** (NOVA's own `dbo.kpi_daily` is a different, dead table — last row 5 Jun 2026), `nova_app` gets `Login failed` there, and `/api/kpi-data/*` sits behind `requireAreaAccess` JWT that the bridge secret does not satisfy — a route that exists and answers 401, #65's exact species. So NOVA gained `routes/neuro-bridge-kpi.ts` (`kpi-snapshot`, `kpi-trend`, `escalation-stats`; read-only, `bridgeAuth` **exported and shared** rather than copied) and `services/kpi-targets.ts` (the target fallbacks `kpi-data.ts` already held twice — the report's headline is a red-vs-green count, so a fourth drifting copy is a report that disagrees with the board Nick reads it beside). **Competency 4's baseline is derived, never stored**: `wasOverdueAt(row, '2026-07-27')` is a statement about 27 July, and the obvious "count what is overdue now" answers a different question every day it runs; an item closed *after* the baseline was still open *on* it. **Working days, not calendar days** throughout — `shared/working-days.cjs` (which takes and returns **Date objects, not date strings**), so the five-working-day standard and the two-working-day logging rule survive a bank holiday inside the window. `logged_at` is separate from `entry_date` because the gap between them IS the competency-3 measurement, and it is caller-supplied **only for an import naming its source** — a meeting note written on the day is contemporaneous evidence; a freely backdatable stamp makes the measurement unfalsifiable. Scheduler builds it Monday **07:30** and pushes; it never publishes or sends. Tracked in `state-of-play`'s `TRACKED_JOBS` — unlike `bank-holidays`, a missed run here is a missed deliverable with a named recipient and a deadline. Seeded from the 12 Aug 1-2-1 by `backend/scripts/seed-management-log.js` (dry-run default): 19 rows, of which **14 have no due date** — reported as a gap rather than dated by guesswork, since that gap is precisely what competency 3 asks Nick to close. Pinned by `weekly-risk.test.js` + `management-log.test.js`.
- **Voice on the phone (16 Aug 2026, #109/#111)** — `frontend/src/voiceUtils.js` and `sara/app/src/voiceUtils.js` are now **byte-identical below their headers**, each header naming the other; the three iOS fixes are in both. `unlockAudio` and `isAudioUnlocked` are exported, and the second is load-bearing: iOS needs a gesture before `speechSynthesis` works, and a cold start landing on Focus can render **before any touch**, so the briefing is left UNSPOKEN and retried on the next interaction rather than dropped silently — a dropped utterance is indistinguishable from a broken toggle. **A nudge's words are the notification BODY, not its title** (`"SARA — Escalation"` is a label): the body was never forwarded from `sw.js`, so the card could only ever see the label, and speaking "SARA nudge" is worse than silence. It now travels both paths — `postMessage` for a warm app, a query param for a cold start — and is **rendered** under the title as well as spoken, so tapping a notification no longer means remembering what it said. Both gated on the persisted `sara_voice_out` toggle, still OFF by default. `sara/app` ships via Netlify (`sara-nickward`, base `sara/app`, → sara.nickward.co.uk) on push to main; **`VITE_BUILD_LABEL` is derived in `vite.config.js`** from `COMMIT_REF` → local git SHA → `dev` (#110), because the committed `.env.production` set it to the static `prod-mobile` and a label identical on every build cannot say which bundle the phone is running.
- **One inbox scan, one store, one meaning of "urgent" (26 Aug 2026)** — there were **TWO** inbox triages over the same mailbox and nothing reconciled them: `email-triage.js` → `agent_state.email_triage` (what InboxPanel renders, what Nick's Done/dismiss buttons write), and `inbox-scanner.js` → the `inbox_items` table (what the urgent-email push notification counted, via `nudges.getUrgentEmails`). **Found by the count disagreeing out loud: SARA pushed "37 urgent emails" at a panel showing 3 ACTION** — and 37 was exactly `urgency='high'` × `(action-required, decision-needed, escalation)` in that table. Three compounding causes, and the third is the general lesson: `emailTriage.dismissEmail()` never called `db.dismissInboxItem()`, so actioning the panel moved nothing; **no frontend had ever called `POST /api/microsoft/inbox/dismiss`**, so in practice the table was write-only — all 114 rows read `dismissed = 0`, oldest 14 Aug; and `clearStaleInboxItems()` (the 24-hour purge) was reachable ONLY from the manual `/api/email/triage/clear` route, while `cleanupOldDismissed(7)` deletes only already-dismissed rows, i.e. none. So the number was a **twelve-day running total presented as a current state** — the failure the whole codebase guards against everywhere else (*a win is DETECTED not declared*, *who reports to Nick is READ not typed*), reached here by having two writers instead of one. Fixed at the source rather than by reconciling: **`inbox-scanner.js` is DELETED**, `server.js` no longer starts it, its three unused `/api/microsoft/inbox*` routes are gone, and its six `db` helpers are removed (the table is left defined and empty — a comment in `schema.sql` says why; dropping it is a destructive migration that buys nothing). `email-triage.getUrgentEmails()` is now the ONE predicate — `lane === 'urgent' && !dismissed` — asked for by `nudges.js` rather than re-derived there, the same rule that keeps three surfaces agreeing on what "leaves the building" means. `getFlaggedItems()` moved to the same service, keeping the retired scanner's shape so `routes/context.js` and `claude.js`'s chat context (both of which were also reading the pile) needed no new vocabulary; it returns **`lastScan: null` when triage has never run**, because "we haven't looked" must stay distinguishable from "your inbox is clear" — that conflation is what the pile was. The nudge now fires from `runTriage()` (the pass that produces the list it describes) and again from `dismissEmail()`, so clearing the last urgent email silences the banner immediately instead of at the next run. Knock-on worth having: two scanners were paying the AI to classify the same mailbox on two schedules; now one does. ⚠ The nudge's own vocabulary changed with the store — a triage record has `from`/`fromEmail`, **not** the retired table's `from_name`/`from_email`, and reading the old ones yields `undefined` in a push notification. Pinned by `email-triage-urgent.test.js` (5), whose central test asserts a dismissal moves the panel count and the nudge count together.
- **Every fetched email reaches the model, and the store is bounded (26 Aug 2026)** — two things left over from the consolidation above, both found by looking rather than by a failure. **(1) The model only ever saw the first 20.** `classifyEmails` built its prompt from `emails.slice(0, 20)` and then mapped the answer over ALL of them — and `fetchRecentEmails(24, 40)` fetches 40, so on any busy day the back half was never classified; worse, an email with no answer fell through to `aiCategory = 'FYI'`, **indistinguishable from the model having read it and said FYI**. The newest 20 were triaged and the rest were quietly filed as informational, which is why FYI was always the biggest bucket. Now batched at `CLASSIFY_BATCH = 20` with batch-local indices mapped back to global ones (getting that wrong lands batch 2's verdicts on batch 1's emails — pinned). **20 stays the BATCH size rather than becoming one call of 40**: `triageEmails` allows 1024 reply tokens and 40 items of JSON overruns it, which truncates the array, fails the parse and drops the whole run to deterministic rules — the same silent degradation wearing a different hat. Batches run **sequentially** (shared daily budget, rate limits, nobody waiting), a failed batch costs only itself, **no answer is stored as `aiCategory: null` and `aiClassified: false`, never `'FYI'`**, and a run that could not classify everything **logs loudly** — that warning is what immediately exposed a broken verification harness the same day. **(2) The store had become the next pile.** Measured: **748 entries, 743 dismissed, 668 KB**, parsed in full on every read (the nudge, both context feeds, the panel polling), growing ~57/day with nothing pruning — not wrong the way `inbox_items` was wrong, but the same mistake one step later. A dismissed entry is kept for exactly two reasons and neither needs the whole email: the `runTriage` merge must not resurrect it while its id can still return in the 24-hour fetch window, and the #70 feedback score reads `urgency`/`category`/`dismissReason`. So dismissed entries are **compacted to those six fields on write** and **pruned after `DISMISSED_RETAIN_DAYS` (7)** — comfortably wider than the 24-hour window. ⚠ **Pruning must cost history, not throw it away**: anything judged folds into `agent_state.email_triage_feedback_rollup` on its way out and `getDismissFeedback()` reads rollup + live, or the classifier's only free feedback signal silently resets every seven days and forever looks like a young classifier with a small sample. `clearDismissed()` folds in too — Nick wanting a fresh triage is not Nick wanting it to forget he corrected it. A dismissal with **no timestamp is kept rather than aged by guesswork**. All writes go through one `storeTriage()`. Live result: **668 KB → 122 KB**, feedback intact at 107 judged, and it now settles at ~7 days of compacted rows instead of growing forever. Pinned by `email-triage-urgent.test.js` (14).
- **"Needs action" — triage can be wrong in two directions (26 Aug 2026)** — `promoteEmail()` + `POST /api/email/triage/promote/:emailId` + the amber button on any non-ACTION card in `InboxPanel`. #70 gave over-ranking a button ("Not relevant") and left **under-ranking with none** — the expensive direction, because a buried ACTION is work Nick never sees, and the only fix was to go and find the email in Outlook, which teaches him the classifier cannot be corrected. **It is NOT a dismissal**: "Not relevant" says *take this away*, this says *you filed it wrong, keep it in front of me* — the email moves into the ACTION group and **stays on screen**. It lands in the **`reply` lane, never `urgent`**: the urgent lane is what drives the push notification, and a correction Nick makes while reading the panel is not a reason to interrupt the person already reading it. It never demotes an urgency the deterministic rules already rated higher. ⚠ **The load-bearing part is that it survives the merge** — `runTriage` re-classifies every 30 minutes and only ever preserved `dismissed`, so an override it overwrote would drop back to FYI within the half hour: the button would appear to work and then quietly undo itself. `promoted`/`promotedAt` are preserved and re-applied by `applyPromotion()` exactly as `dismissed` is; verified live against a re-classification. A promotion that moved nothing (`not in triage`, `already dismissed`) **404s with the reason** rather than returning a success the panel would render as a change. Feedback counts both directions now: `misrankRate` = (`notRelevant` + `underRanked`) / `judged`, with `overRankRate`/`underRankRate` kept separate so a rate that moves can be attributed to a fault — ranking things too high and burying them are different faults with different fixes. **`judged` counts VERDICTS, not emails**: an email promoted and later marked done carries two, because Nick said two separate things about it. ⚠ Found while testing: **`readRollup()` normalises the stored blob AND is what the fold reads before incrementing**, so a counter it forgets to carry resets to 0 on every write — `underRanked` could never have reached 2. Any new counter must be added there or it silently caps at 1; pinned by a test that promotes three. Pinned by `email-triage-urgent.test.js` (19).
- **The FYI section ages out, and nothing owed does (7 Sep 2026)** — `ageOutInformational` (PURE) + `purgeAgedInformational` + `POST /api/email/triage/purge-fyi` (with `dryRun`) + `fyiAgeOutDays` on the triage payload. Nick: *"I currently have 715 FYI emails — ain't no way I'm ever going to get to them."* That is the correct reading of the number: a pile that only grows is one nobody opens, and its count makes every other figure on the panel harder to read. ⚠ **It sweeps the SECTION, not the CATEGORY, and that is the finding** — measured on the live store, 715 is **224 `FYI` + 491 `IGNORE`**, which `InboxPanel` renders under ONE `FYI (N)` heading; clearing the category alone would leave the heading reading 491 and look like a fix that did not work. ⚠ **`ACTION` and `DELEGATE` never age out at any age** — *"nothing leaves the panel by age"* still holds for everything actually owed (1 Sep, above); only the informational lanes go, and only because being informational IS the claim that not reading them costs nothing. ⚠ **A PROMOTED entry is never aged out**: that button is Nick overruling the classifier, and a NEURO rule does not get to overrule it back (the category test already covers it, since `applyPromotion` re-files to ACTION — the guard is explicit anyway). ⚠ **The age is the RECEIVED date, never the classification** — a re-classified email is not a new one, and keying on classification time would reset every email's clock on any pass that re-read it. ⚠ **No readable received date means KEPT** — not knowing how old something is is not evidence that it is old. ⚠ **`aged-out` is NEURO's own act**, so exactly like `left-inbox` and `sender-muted` it is absent from `DISMISS_REASONS` (no caller can pass it) and excluded from `isJudged` — 492 timed-out FYIs reading as verdicts Nick gave would swamp the #70 feedback score. `days <= 0` (or NaN) switches the rule OFF rather than ageing everything out, which is the failure direction with no way back. ⚠ **It runs on the SKIPPED triage branch too** — the rule is a function of the CLOCK, not of new mail, so gating it behind "the fingerprint changed" would leave an FYI that crossed the seven-day line sitting there until something else happened to land. `EMAIL_TRIAGE_FYI_AGE_DAYS` moves the window; the panel STATES it, served from the backend so screen and sweep cannot disagree. Verified live: **715 → 234 in the section, 492 closed, urgent/reply/action/delegate untouched, feedback score unmoved at 258 judged.** Pinned by 10 tests in `email-triage-urgent.test.js` (46) + `triage-purge-routing.test.js` (3, real HTTP — whose header records that route ORDER was **measured** here and is not currently load-bearing, rather than claiming a bug it does not catch).
- **"Not relevant" is a statement about the SENDER (4 Sep 2026)** — `muteSender`/`unmuteSender`/`listMutedSenders` + the pure `applySenderMute` + `agent_state.email_triage_muted_senders` + `GET`/`DELETE /api/email/triage/muted` + a collapsed rules list in `InboxPanel`. Nick pressed the button ten times on one newsletter and it kept coming back. **Measured on the live store: THIRTEEN National Club Golfer emails between 19 Aug and 3 Sep, thirteen distinct ids, every one undismissed** — because the verdict was recorded against ONE MESSAGE and the mail it gets pressed on is a newsletter with a fresh id every day. Nothing anywhere generalised a dismissal to a sender. It now mutes on the **FIRST press** (Nick's call, 4 Sep — the alternative was earning it after two), sweeps what is already in the panel in the same breath, and files future mail automatically. ⚠ **THE RULE IS THE DURABLE THING, NOT THE DISMISSED ENTRY** — entries are compacted and pruned, the rule is not, so a muted sender's mail is re-filed on every pass however many times its entry has gone. ⚠ It mutes an **ADDRESS, never a display name**, and that was vindicated live: "National Club Golfer" sends from `editorial@` (the daily) AND `dedicated@` (sponsored blasts), so a name match would have been one rule over two senders — both are muted, and 8 + 4 in-panel emails were swept. ⚠ **`sender-muted` is NEURO's own act, not one of Nick's verdicts**: absent from `DISMISS_REASONS` (no caller can pass it) and excluded from `isJudged`, exactly like `left-inbox` — he judged the sender ONCE, and counting the twelve auto-filed messages would let one press swamp the whole #70 feedback score. Refusals: an unusable address is **REFUSED, never stored as a rule keyed on `""`** (which would match every email whose sender could not be read); an already-actioned entry **keeps his verdict** (`done`/`replied` is his record of what he did); and Nick's **own address** is refused — read from `microsoft.getSignedInAddress()` (the MSAL cache) and **PASSED IN by the route**, because that call is async while this path is sync and the env var reached for first (`MS_USER_EMAIL`) was **invented and has no writer anywhere in the repo**, so the guard would have read undefined for ever. ⚠ An **unresolvable** self-address still MUTES rather than refusing — breaking the button on the days Microsoft auth has expired is the worse failure, and the real safety net is that every rule is **listed and one-click revocable in the panel**, which un-muting deliberately does NOT use to resurrect the mail it filed. **Also fixed, the same complaint wearing its own hat: `DISMISSED_RETAIN_DAYS` was a literal 7 while `LOOKBACK_DAYS` had widened to 14 on 1 Sep** — so a dismissal on mail still sitting in the Inbox was pruned on day 7 and re-fetched on day 8 as genuinely new mail (the retention comment still described a 24-hour window). It is **DERIVED from the lookback** now, so the two cannot drift again, pinned by a test asserting the inequality. Verified live end to end: 616 emails re-triaged, **zero golf emails came back**. Pinned by 10 tests in `email-triage-urgent.test.js` (37) + `triage-muted-routing.test.js` (3, real HTTP — ⚠ `GET /triage/muted` sits on a router with `GET /triage/:emailId` and was first written BELOW it, #70's bug verbatim; the test was mutation-checked by moving it back and watching two of three fail).
- **Nothing drops out of triage by age (1 Sep 2026)** — the merge in `runTriage` was `existing.filter(e => e.dismissed)` plus whatever the fetch returned, and the fetch was `fetchRecentEmails(24, 40)`. So the store's memory was **exactly backwards: mail Nick had FINISHED with survived seven days, and mail he had not dealt with vanished 24 hours after it arrived** — carried by nothing, with no log line. The ACTION lane emptied itself overnight, and **a promotion expired in a day**: `applyPromotion` runs inside the `classified.map`, so the button whose entire meaning is *keep this in front of me* stopped holding anything once the email left the window. `$top=40` with no paging was a second, quieter cut of the same mail. Four changes, and the middle two are what make the first affordable. **(1) The window is `LOOKBACK_DAYS` (14), PAGED** — `microsoft.fetchRecentEmailsDetailed` returns `{emails, complete, source}` and walks `@odata.nextLink` up to `MAX_FETCH` (500); `fetchRecentEmails` stays as the array-or-null wrapper. **(2) The merge is keyed by id and every entry is positively accounted for** — an undismissed entry the fetch did not return is CARRIED FORWARD, indefinitely, because age is never a reason to drop something Nick has not answered. Keying on id also removed a latent duplicate: a dismissed email still inside the window used to appear in BOTH halves of the old list. **(3) An email that has demonstrably LEFT the Inbox is closed as `left-inbox`** — that, not the clock, is what keeps the store bounded. ⚠ **Absence is only evidence when the read was `complete` AND the message arrived comfortably inside the window** (`DEPARTURE_GRACE_MS`, 15 min): a capped or part-failed page walk is a short list indistinguishable from a small mailbox, and reading absence out of it would sweep the panel; anything older than the window is kept for ever, because "I did not look" is not "it is gone". The NOVA bridge takes a COUNT and no date window, so it reports `complete: false` and nothing may ever be concluded from it. `left-inbox` is **deliberately absent from `DISMISS_REASONS`** (only the merge can write it) and **excluded from `isJudged`**, or NEURO's own observation would pad the #70 feedback score with verdicts nobody gave. **(4) A model answer is REUSED, never re-bought** — an email's text never changes, so neither does its classification; `classifyEmails(emails, prior)` batches only mail with no answer yet, which includes mail whose earlier batch FAILED (the case `aiClassified: false` exists to distinguish). Without it, 14 days × a 30-minute cadence would re-classify hundreds of emails every time one arrived. The **deterministic** half is always re-run on the freshly fetched fields, so read/flagged state stays current. `force` bypasses the reuse as well as the fingerprint — a manual "Run triage" means re-run properly. ⚠ **The first run after this lands classifies the whole 14-day backlog** (up to 25 batched calls, once). Pinned by `email-triage-urgent.test.js` (24), including negatives: an incomplete read concludes nothing, and a 30-day-old unanswered email survives an empty fetch.

- **The reuse was real and the STORE threw the answer away (8 Sep 2026)** — `DISMISSED_FIELDS` did not list `aiClassified`, so `compact()` stripped the model's verdict off every dismissed entry the moment it was written. `classifyEmails` decides what to buy with `!prior.get(e.id)?.aiClassified`, so a compacted entry is **indistinguishable from mail the model has never seen** and is re-classified on EVERY run, for as long as it stays inside the 14-day fetch window (retained 21 days). ⚠ **The rule one bullet up was correct and unreachable** — the reuse code was never broken, the thing it reads was being deleted, which is why nothing looked wrong: a re-classification returns a perfectly good answer and costs money in silence. Measured on the live store: **961 dismissed entries, ZERO carrying `aiClassified`; only the 33 undismissed ones had it** — so a run logged *"25/623 already classified — reused, 598 sent to the model"*, three times a day at 30 cloud calls a pass. ⚠ **The visible symptom was not the inbox at all**: it blew `OPENROUTER_DAILY_CALL_LIMIT` (100) and `MAX_ESCALATIONS_PER_HOUR` (20) before 09:00 — 102 escalations in hour 8 — after which everything else was refused cloud and fell to Ollama, which cannot hold a conversation, so the STANDUP died with *"Could not reach any AI provider"* and the Pi sat at 99% CPU and 74.9°C running `llama-server`. Two features starving a third through a shared budget. `aiCategory` and `triagedAt` travel with the flag because reuse reads all three — a flag whose verdict has gone says "judged" and means nothing, and the stamp records when the mail was JUDGED, not when a pass last walked past it. ⚠ **The historical 961 cannot be back-filled** (the verdicts are gone), so the first run after this pays once more and settles. Cost of the fix: ~50 bytes an entry. Pinned by 2 tests in `email-triage-urgent.test.js`, the compaction one **mutation-checked** — reverting `DISMISSED_FIELDS` fails it.
- **Dismissing an email says WHY (16 Aug 2026, #70)** — `dismissEmail(emailId, reason)` takes `done` / `not-relevant` / `replied`, defaulting to `unspecified` for anything unrecognised rather than storing a client's string as data. **`replied` is deliberately not folded into `done`**: a sent reply is the strongest evidence triage was RIGHT, and merging the two throws away the only positive signal the classifier produces. `getDismissFeedback()` / `GET /api/email/triage/feedback` score it by urgency/category so a bad rule is locatable, counting **only what Nick has judged** — an email still in triage is not evidence, and folding those in would improve the score purely because he hasn't got to them — and returning **null, not 0**, when nothing has been judged. ⚠ The route **must stay above `/triage/:emailId`**: Express matches in registration order, so declared after it, "feedback" was read as an email id. Pinned by `triage-feedback.test.js`.
- **Decisions were never logged, not merely never rendered (16 Aug 2026, #28)** — `parseDecisions()` in `claude.js` + `DecisionsPanel`. The ticket said "logged decisions render nowhere"; measured first, the `decisions` table held **0 rows** and `Decision Log/decisions.md` held **ONE entry in five months** — a pleasantry with a doubled bullet, not a decision. Cause: **both system prompts document `[DECISION: text]` and the parser matched `[DECISION] text`**, so the colon form could never fire. Its three siblings in the same function (`ADD TODO`, `MEETING NOTE`, `UPDATE PERSON`) are all `\[X:\s*(.+?)\]` — DECISION was the only one out of step and the only one with an empty table. **A view built on the ticket as written would have rendered an empty screen forever and looked finished.** The documented form is now bounded by the closing bracket (the old unbounded capture is exactly how that one entry swallowed a sentence of chat); the legacy bare form is kept but bounded to a line, both deduped, and a leading bullet stripped. `parseDecisions` is **pure and exported** so the contract pins without a DB or vault, and one test asserts **the PROMPTS still document the form the parser accepts** — a rename must fail there rather than silently emptying the table for another five months. `DecisionsPanel` gets a **Sidebar entry**, not just an `App.jsx` case: routable-but-unreachable is the same hole one step later (as `TodoPanel` was). Its empty state reads as working-and-waiting and distinguishes "couldn't ask" from "there are none". ⚠ `obsidian.appendDecision` writes a `## date` header **per decision**, so two on one day produce two headers — pre-existing, cosmetic, untouched.
- **A sent reply leaves a record (16 Aug 2026, #69)** — `services/sent-replies.js` + the `sent_replies` table + `GET /api/email/replies`. Replying dismissed the email from triage and **that was the entire trace** — the only evidence lived in Outlook's Sent Items, so "I answered that on Tuesday" was unanswerable from NEURO, and outbound mail was the newest write path in the system and the least observable. Deliberately the **one-row version** (Nick's call): no AI, no commitment extraction, nothing to tune — the richer version that feeds the body to the meeting-note extraction now has something to be built ON, the same order as #88/#89. Hooked onto the existing `dismissEmail(id,'replied')` send path rather than adding a parallel record. **Recorded AFTER the send and never allowed to fail the request** — the mail has already left, so a bookkeeping error must not reach Nick as a failed send; `record()` swallows and logs. **The one judgement is recipient provenance**: on a plain reply/replyAll **Graph** chooses the addressees, not NEURO, so the route only knows them for certain when the composer passed an explicit `to`. `recipients_source` is `explicit | inferred | unknown` and the UI says *"from the thread, not confirmed"* rather than presenting a guess as the record (#65's rule — the difference between a record and a guess when the question is "who did I copy?"). Subject/sender are **denormalised on purpose**: the triage blob is a rolling ~290-entry cache and a reply must stay answerable long after the email rolled out. Surface is a collapsed **REPLIED** section in `InboxPanel` — a replied email is dismissed, so it leaves the triage list entirely and there is **no dismissed view** to hang a "Replied" badge on. Route is top-level `/replies`, never under `/triage`, so it can't be parsed as an email id (#70). `total` is returned beside the page, and **nonsense input falls back to the default rather than the nearest legal value** — `limit=-5` clamping to 1 returns one row and looks like the truth. Pinned by `sent-replies.test.js` against a scratch DB.
- **SARA's opening line must be about the day she was actually SHOWN (7 Sep 2026)** — the pure `checkSaraGrounding` in `ai-provider.js`, plus the prompt it guards. Live at the top of the briefing and **spoken aloud**: *"Avoid the meeting. / Stay focused on tasks."*, then on the next refresh *"Start the meeting now. / Attend / Don't worry about the tasks for today."* ⚠ **THERE WAS NO MEETING** — the three items were two todos and an email. The model invented one, gave contradictory instructions about it across refreshes, and told Nick to ignore the only real work on the list. **Three causes, compounding.** (1) ⚠ **`toneGuide.split('.')[0]` kept only the FIRST SENTENCE of the tone instruction**, so `assertive` reached the model as *"He is avoiding something."* with *"Name the drift as a fact, not a failing, and give him the smallest first move. No escape routes, no lecture"* thrown away — a diagnosis with no direction, and it echoed **"avoid"** straight back. Same decay as the weekend prompt rotting to prohibitions (`prompt-parity.test.js`), one surface along. (2) ⚠ **`itemSummary` — type + title + REASON — was BUILT AND NEVER USED**; the user message passed bare titles, so a 1.5b local model was asked for a directive with nothing to base one on, and invention is the predictable result rather than bad luck. (3) **Validation was `length > 80`.** ⚠ **The sting: `assertive` fires BECAUSE avoidance was detected** — the one register written to counter Nick's stated failure mode was the one handing him permission. **The prompt fix is necessary and NOT sufficient**, because a small model asked for a directive can always invent: the output is now CHECKED against the items it was given and a drifting line is **DISCARDED**. ⚠ Returning `null` is already the "AI unavailable" path, so the caller falls back to `buildDeterministicSara` with **no new branch** — and the rejection is **LOGGED with its reason**, because a silent fallback is indistinguishable from the model being offline. Three rules: the **directive** may not name a kind of thing that is not in the list; the directive must DIRECT (no *avoid / skip / ignore / postpone / don't* — that is permission, not a smallest first move); and the **ignore** line may not name the TOP item's type. ⚠ **Rule 1 is scoped to message+action, deliberately NOT the `ignore` line**, whose whole job is to name a category that can wait (*"Lower-priority imports and email can wait"* says exactly that about things which are absent). ⚠ **The check errs towards REJECTING**, and is tuned to that asymmetry: a false reject costs a generic-but-true line, a false accept puts fiction at the top of the briefing and speaks it. ⚠ **A word earns a place in the subject vocabulary ONLY if it identifies ONE kind of thing** — `queue` and `call` are out, because the deterministic builder's own escalation line is *"Open escalation queue"* and a rule that rejects the grounded fallback's wording is not strict, it is wrong; caught by the test asserting the two always agree, not by reading. Matching is whole-word on a normalised string (`entities.js`'s rule — `includes()` fired "Liam" inside "William"). Pinned by `sara-line-grounding.test.js` (18) — including both real shipped lines as fixtures, and **four wiring tests against a stubbed provider** proving `enhanceFocus` APPLIES the check, that the WHOLE tone instruction reaches the model and that the item types and reasons do; **all three fixes mutation-checked**.
- **One SARA voice, two modes (16 Aug 2026, #112)** — `SYSTEM_PROMPT` and `WEEKEND_SYSTEM_PROMPT` are composed from `IDENTITY` / `WHO_IS_NICK` / `CORE_TRAITS` / `CORE_RULES` in `claude.js`; each adds only what is genuinely mode-specific. They had drifted once in the way that matters: the weekend personality decayed to **prohibitions only**, keeping every rule that suppresses output and none that generates character, which is why a Saturday hello came back accurate and lifeless. `prompt-parity.test.js` guards exactly that — it asserts `CORE_TRAITS` stays **majority-generative** rather than becoming a ban list again, as well as that both prompts interpolate the shared blocks.
- **One SARA voice, every surface (16 Aug 2026)** — the #112 blocks moved to `services/sara-voice.js`, because chat was never the only place SARA speaks and the drift was already loose one level out: `standup-session.js` carried its own summary of her, `briefing.js` had a one-line *"You are SARA, Nick's executive AI assistant"*, and `routes/journal.js` had **no voice at all** — a task description that could have been written for any assistant. So the three surfaces where she talks to Nick about his day were the three furthest from the personality carefully written for chat. **Two sizes, deliberately**: `VOICE_FULL` for conversational surfaces (chat, standup, EOD) and `VOICE_COMPACT` for the ones that emit a sentence or two — journal prompts, briefing synthesis, question generation — which may land on a 1.5b local model with a 2048-token context, where a full personality spec crowds out the task and makes the output *worse*, not more characterful. **Two surfaces were also bypassing the router entirely**: `routes/standup.js` and `routes/journal.js` called Ollama with a hardcoded `fetch` BEFORE consulting `ai-routing`, which quietly defeated the policy — `standup_questions`/`eod_questions` are in `LATENCY_SENSITIVE_TASKS` precisely so they go to OpenRouter first, and no prompt rewrite survives a 1.5b model. Journal had **two differently-worded prompts** for one feature (local and cloud) and only one ever got edited; it is now one routed call, still Ollama-first because `journal_prompts` is correctly not latency-sensitive. Nudge pools: the todo tier-5 pool was **absurdist** at exactly the point the standup pool had already worked out was wrong — five nags in, the problem is friction, and a joke about sentient todos reduces none of it; both gradients now run light → wry → **warm and specific about the next action**. Push notifications now all introduce themselves as **SARA, never NEURO** (six scheduler jobs and the import pipeline said NEURO), since NEURO is the brain and SARA is the half that *comes to him*; titles are display-only in both service workers, so that is copy, not routing. ⚠ **The one place this voice must NOT go is anything that leaves the building** — the email drafts, the 1-2-1 invite body and the chase messages say "as Nick Ward" on purpose, because that mail sends under his name. She writes TO Nick in her voice and FOR Nick in his. `prompt-parity.test.js` pins the consumer list, the compact block's size, and the push sender. Second pass over the written copy found **most of it already right** — `action-presenter` and `watchdog` state the fact, the consequence and the move without hedging, and were left alone; churning good copy to prove a pass happened is how a voice gets worse. What was actually wrong: `decision-engine.collectNudges` interpolated the nudge **slug** into the card (`"121 reminder"`, `"Complete journal"` — SARA naming her own internals at Nick), now a `NUDGE_LABELS` map covering all eight `NUDGE_TYPES`; `"Consider delegating these"` hedged on a card whose whole job is to say these aren't yours; `TONE_INSTRUCTIONS` in `ai-provider` were written in the **third person** ("the user is likely overwhelmed"), which is how a model starts narrating Nick on a card he is reading himself; and the two 🎉 empty states in `sara/app` were the "celebrate this small win" register the spec rejects. **The first cut of the shared blocks was a compression of Nick's ~2,000-word spec into ~25 lines and silently lost five behaviours** — they are back, and split by who needs them: the **missing middle** (name the real distinction, never observe that nuance exists), **useful initiative over constant activity**, and **use what you already know without re-asking or announcing it** are general, so they live in `sara-voice`; the **senior-technical-partner** rules (architecture first, small reversible changes, never claim untested work ran) and the **debugging order** (what we know → what we don't → likeliest → test → fix → verify, one hypothesis at a time) are in `claude.js`'s `SYSTEM_PROMPT` ONLY, because the standup debugs nothing and the journal has no architecture to respect — carrying them in the shared block would tax every ritual message. `prompt-parity.test.js` pins all five, including that the two chat-only ones stay OUT of the shared block.
- **What SARA surfaces, and when to say nothing (25 Aug 2026)** — `services/context-state.js` (pure) + `services/attention.js` + `routes/attention.js`, `GET /api/attention` (+ `/context` for the cheap situational read alone). **The rule that stops this becoming a second decision engine: context RE-RANKS and GATES, it never ADDS candidates.** `decision-engine.evaluate()` stays the one place something becomes worth surfacing, with the dismiss/snooze/category-suppression it has already learned; all this layer decides is which of that pool fits the moment. The single exception is explicit and carries no work — when the context IS the answer ("you're in a meeting"), the primary is a `kind:'context'` card, distinguishable from `kind:'item'` so nothing can mistake the frame for a job. **`primary: null` is a correct answer**: an ambient surface that always has something to say is a nudge machine, and nudge volume is the one signal allowed to argue against building more. `context-state` is PURE (no DB, no network, no clock — `now` is passed), the `pi-health.assess()` split, resolving nine bounded states in order: `unknown → in-meeting → pre-meeting → firefighting → in-focus-session → ritual → off → away → steady`. **Being in a meeting outranks a breaching queue** — not because it matters more but because it is the one state where interrupting is actively wrong; it is the only one that sets `quiet` during work hours. **A missing input is never a zero** — `{known:false}` on the queue means "we could not look", lands in `unknowns` and costs confidence, and `steady` (we looked, nothing stood out) stays a different fact from `unknown` (we could not look). ⚠ **The two powers have different bars: QUIET follows the context and fails towards silence; DROPPING requires a confident read.** A low-confidence context may re-order but must never hide work — hiding real work on a bad guess is the failure that ends the feature — and nothing is dropped silently (`dropped` names every item and why). ⚠ **`confidence.level` is capped by COVERAGE, not left to the score**: measured on the first live run, 4 of 7 inputs answered (no calendar, no location, no presence) and the arithmetic landed on exactly 0.75, so the read called itself `high` while its own rationale said three inputs could not be read — and `level` is precisely what licenses the gate to hide things. "High" now means every input answered. Speech is **deterministic, no model call** (polled continuously by two surfaces; must be instant and work with the Pi offline — `event-parser`'s regex-first rule), and a context card is **never spoken** — "you're in a focus session" said to someone in a focus session is pure interruption. Pinned by `context-state.test.js` (23) + `attention.test.js` (18).
- **A cached calendar event knows whether other people are in it (25 Aug 2026)** — `calendar_cache.attendees_other`. Graph is asked for `attendees` and `upsertCalendarEvent` **dropped them on write**, so nothing reasoning off the cache could tell a 1-2-1 from a solo focus block — and half Nick's diary is solo blocks, so the ambient surface's single highest-value moment (prep before a real meeting) was impossible by construction. Judged once in `calendar-sync`, at the only point a live attendee list exists, using **`plaud-admin-blocks.attendeesOther()`** (promoted out of `_internals`) rather than a second copy — measured against the signed-in address, never a count, because Graph lists the organiser among the attendees on some events and not others. ⚠ **Three-valued and it must stay that way**: 1 / 0 / **NULL for "we could not tell"**, since the NOVA bridge supplies no attendee list and with no signed-in address Nick's own entry cannot be told from anyone else's. `context-state` requires **exactly `true`** to call something a meeting, so an unknown can never be announced as one — fail-closed, unchanged. No backfill needed: sync is replace-by-window, so the next sync rewrites every row.
- **The phone is an outbox, not a second brain (30 Aug 2026)** — `services/mobile-snapshot.js` + `services/mobile-sync.js` + `routes/mobile.js`, and `sara/app/src/mobile/{localStore,outbox,useNickNow}.js`. Full contract in **`docs/mobile-contract.md`**. **Versioned in the PATH** (`/api/mobile/v1/…`), not a header, because the phone caches responses on disk and replays operations across app upgrades — an old bundle must keep talking to the endpoint it was written against; the payload carries its own `schema` too, so a cached snapshot can be identified without knowing which URL fetched it. **`GET /v1/nick-now`** is a NEURO-OWNED contract, deliberately defined rather than allowed to become "whatever SARA's presentation model is this week", and it holds the three rules the rest of the system already holds: every item carries a canonical id + `source` + `updatedAt`; a section that could not be READ is `{known:false, why}` and **never an empty list**; retrieval is **POINTERS** (title/path/updated), never private source content — the pointer is what is useful offline, the content is what needs the connection. It **re-ranks nothing**: `decision-engine` stays the one place something becomes worth surfacing, `attention`'s gate stays the one place it is filtered, and the snapshot carries `say`/`tab`/`scope` **verbatim** so the phone cannot phrase a fact a fourth way. ⚠ `event:derived:<start>:<subject>` is the agenda id because `agendaFor` carries no Graph id — deterministic and stable across refreshes, and prefixed `derived:` so nobody tries to PATCH with it. **`POST /v1/sync/operations`** is append-only and idempotent: the DEVICE owns the identity of an intent (`operationId`, generated before the network), NEURO owns the record it produces, and the ledger is `mobile_sync_operations` `UNIQUE(device_id, operation_id)`. ⚠ **`applyOperation` is SYNCHRONOUS from the ledger read to the ledger write and must stay so** — better-sqlite3 is synchronous and this is one Node process, so that read-modify-write genuinely cannot interleave; it is a real mutex here and would **not** be safe across processes (`plaud-admin-blocks`' rule). ⚠ A **`pending`** row can only survive a crash MID-APPLY, and is **never replayed** — the note may or may not have landed, so it is reported `needs-attention` and the device keeps its copy; guessing loses a capture in one direction and duplicates it in the other. ⚠ **`todo.complete` accepts a NEURO task id ONLY.** The other two owners a mobile tick can have (a Microsoft mirror, a `filePath`+`lineNumber`) work online through `completeTask.js` but are **not unambiguous offline** — a line number recorded hours ago can name a different row by the time it replays, which is 27 Aug's bug with a delay bolted on. ⚠ **No capture text in the ledger, the logs or the diagnostics**; the payload's canonical homes are the vault and the tasks table, and a copy in a ledger is a second store. **The conflict rule is deliberately small and documented**: new captures are append-only, existing records stay server-owned, and a mutation that cannot be applied **preserves local intent** and is reported as needing attention — no merge, no resolution UI, because a merge needs a common ancestor the phone does not have. A batch containing rejections still answers **200** (a non-2xx makes the client discard the receipts it needs in order to STOP retrying). A held completion (`task-blocks`' outcome-note rule) comes back `applied` with `detail.held` and a reason, so the phone says the tick was held rather than showing a completion that did not happen. **On the device**: every capture now goes through the OUTBOX, online or off — it used to POST straight to `/api/capture/*` with no offline path at all, so *"I had signal"* and *"it saved"* were the same code and the same words; there are five states now and they differ (`queued on this device` / `sending` / `confirmed` / `failed` / `needs-attention`). ⚠ **The draft is cleared ONLY once the operation is safely on disk** — if local persistence itself fails the words in the box are the last copy in existence, and clearing them is how a capture is destroyed by the thing built to save it. ⚠ **An IndexedDB upgrade may create stores and rewrite the cache but must NEVER delete or clear `operations`**, which holds captures that have not reached NEURO; pinned twice, by a `fake-indexeddb` test that reopens a v1 database holding an unsent capture and by a source assertion on the upgrade body. ⚠ **No secrets in IndexedDB** — the PIN stays in `localStorage` (written by `api.js`) and a test pins that the store module never touches it; the store is described to Nick as what it is, **device-local and NOT encrypted**, evictable by iOS. ⚠ **Replay is FOREGROUND-ONLY** — Safari implements no Background Sync and does not keep a PWA worker alive to drain a queue, so the triggers are launch / foreground / online / after a capture / an explicit "Send now"; claiming otherwise would mean a capture Nick believes is on its way while the phone is in his pocket. **"Clear local data" REFUSES while anything is unsent** and says how many; the force option reports what it destroyed. `capture-store.js` extracts the note writer out of `routes/capture.js` so the route and the outbox are ONE writer. Pinned by `mobile-sync.test.js` (18), `mobile-snapshot.test.js` (16), `routes/mobile.test.js` (8, real HTTP — a green service suite says nothing about routing) and `mobile-store.test.js` (14) + `mobile-outbox-e2e.test.js` (7), the last of which drives the real outbox against a real HTTP NEURO and proves the whole promise: **a todo captured offline survives an app reload and reaches NEURO exactly once, however many flushes happen.**
- **Three primary modes, and the Surface is not retired (30 Aug 2026)** — `sara/app`'s nav is now `PRIMARY` (Capture / Now / Review, always on screen) plus `SECONDARY` (everything else, including the SARA Surface, behind "More"). Neuro Mobile is the app Nick OPENS; SARA is the layer that comes to him, so the ambient Surface stays exactly where it was as a destination — it is still what notification routing lands on, and it is one tap away. The 25 Aug rule is unchanged: a thing he cannot find is worse than a menu he does not need. ⚠ **`now` and `review` had to be added to `SARA_LITE_TABS`** in `shared/action-surfaces.cjs`, or a notification routed to either falls through to Focus in silence; `action-surfaces.test.js` reads BOTH arrays out of `App.jsx` now, and it is the thing that caught it.
- **An attention item REMEMBERS (30 Aug 2026, Phase 3 Gate 1)** — `services/attention-lifecycle.js` + `attention-settings.js` + `attention_records`/`attention_events`. Full contract in **`docs/attention-contract.md`**; read it before touching this. Before it, an attention item lived for exactly ONE HTTP REQUEST: `decision-engine` recomputed the pool on every call and the answer was rendered and thrown away. Three things followed. **Nothing could be acknowledged** — the only durable state was a suppression timer, which cannot tell *"I have seen this"* from *"hide it for 30 minutes"* from *"this is finished"*. **A notification had no idea what it was about**: all 30 `sendToAll` sites pass free text and the governor deduped on a fingerprint of THAT TEXT, so a meeting alert counting down ("in 25 min" → "in 10 min") produced a fresh fingerprint each pass and every one went out — the rule *"the same card must not repeatedly notify across widget, push, kiosk and mobile"* was not merely unenforced, it was **unexpressible**. And **item ids were unstable**: `collectOverdueTodos` emits `todo-overdue-top` for one overdue task and `todo-overdue-summary` for two, so a dismissal silently stopped applying the moment a second task went overdue. ⚠ **This adds no candidates and changes no ranking** — `decision-engine` stays the one generator, `attention.gate()` the one re-ranker; this layer only gives a surfaced thing a durable identity. `dedupeKeyFor` names the THING (`todo:<slug>`, `meeting:<event_id>`, `nudge:<type>` — never the row id, which is recreated daily). Six states: `active` / `acknowledged` (**stays visible, never notifies again** — the distinction the suppression map could not express) / `deferred` (with a REASON, because a thing deferred three times for `too-big` is a different problem from one deferred for `not-now`) / `suppressed` / `resolved` / `expired`. ⚠ **Terminal states NEVER re-match** — the same `dedupe_key` opens a NEW record, which is what makes a daily recurrence work without the key carrying a date and stops yesterday's dismissal silencing today's standup. `notify_signature` is `urgency|tier`, **deliberately not the text**. Four refusals: **surfacing without evidence is allowed, INTERRUPTING is not** (hiding real work on a bookkeeping gap is the worse error; operational alerts are exempt or the rule eats what it protects); the **sweep never ages records out while the pool is unreadable**, and ages to `expired` not `resolved` because *Nick decided nothing*; an **operational push never overwrites a pool record** it shares a key with, or the nag escalation the tone is built on is flattened to the push default; and the webpush funnel **fails OPEN** and says so in `push_log` — the contract wants no notification without a record, but it wants an unanswered escalation more. Enforced INSIDE `sendToAll`, so all 30 call sites are covered with no edits. Controls live in `agent_state.attention_settings` (quiet hours **default to `PUSH_QUIET_HOURS`** rather than a second guess — a narrower one is how two parts of a system disagree about the same evening). Pinned by `attention-lifecycle.test.js` (23) + `routes/attention-lifecycle-routing.test.js` (6, real HTTP). Payload is **additive**: every field `GET /api/attention` returned is unchanged.
- **The notification prompt is never raised on launch (30 Aug 2026)** — `usePushSubscription` called `Notification.requestPermission()` the moment the PIN was accepted, putting the browser's ONE-SHOT dialog in front of someone who opened the app to write a thought down. On iOS a denial is close to permanent, so this is the most expensive thing the app can do. Split in two: the automatic path only **re-registers an already-granted** subscription (guarded on `Notification.permission !== 'granted'` as the FIRST check, before any await), and the prompt is reachable only from `enableNotifications()`, called by an explicit tap in the SARA **Controls** screen. Pinned by `push-permission.test.js` **with a positive control**, because node has no `Notification` and the only other thing that would catch a regression is Nick's phone.
- **One panel must not take the app down (30 Aug 2026)** — `frontend/src/components/ErrorBoundary.jsx`, wrapping the view **inside** the shell so the sidebar and chat survive. It exists because `StateOfPlay` kept rendering a "Support queue" card after the Jira queue was removed on 27 Aug, so `queue.staleDays` threw on `undefined` — and with **no error boundary anywhere in the app** React unmounted the ENTIRE root. The reported symptom was therefore not "State of play is broken" but ***"a number of menus fail to open"***, because once the root is gone every later click does nothing until a reload. It **names the error on screen** (a blank panel and a crashed panel look identical, and neither says which) and `viewKey` clears it on navigation so a bad screen cannot latch the good ones shut. ⚠ When a service stops emitting a block, **grep the frontends for it** — the backend was correct here for three days while the UI was fatal.
- **SARA opens on SARA, and there is ONE field (30 Aug 2026)** — Nick's call: the phone opens on the **Surface** again (Phase 2 had moved it to `Now`; opening on a list makes it an app you read, when the point is that she is PRESENT), and a launch **intent still wins** so a notification lands on the thing that pinged him. `sara/shared-ui/Field.{jsx,css}` is now ONE source imported by **both** `sara/app` and the Pi kiosk — two copies of her presence would drift exactly the way `voiceUtils.js` did. It needed no re-tuning for the bigger panel because its density is per **AREA**, not a node count. The kiosk gets `screens/presence/PresenceView` and `DEFAULT_VIEW = PRESENCE`, with the field driven by `provenance` so an unreachable brain **looks** unresolved. ⚠ The `presence` **alias** had to be removed from `views.js` — it pointed at mission-control from when no presence screen existed, and would have silently rewritten every request for the new screen into the briefing. ⚠ **The kiosk cannot yet render the attention feed**: `sara/backend` has no route to `/api/attention`, so Presence shows her presence and the honest connection state and **invents no ranking** — a second one on that side is precisely what `state/inference.js` was retired for. Full convergence (Nick: *"the pi app and sara mobile should essentially be the same app"*) means making the kiosk a direct NEURO client like the phone; note the kiosk is **React 18** and the phone **React 19**.
- **A focus session can be made SMALLER (30 Aug 2026, Phase 3 Gate 3)** — `focus-session.shrink()` + `needs-smaller` + `stepAway()` + `checkIn()`, with `services/transitions.js` (PURE) for the seams of the day. Every control the session already had answers **WHEN** — pause, resume, abandon — and Nick's difficulty is **INITIATION**: his own rule is that *anything that raises awareness without lowering the barrier is the wrong shape*. Shrinking is the only one that lowers it, so it is the first button everywhere it appears and it is **on every return prompt** (looking at a thing you walked away from is exactly when "this is too big" is the true answer; a menu without it pushes you to abandon, which loses the thread and reads as failure). `shrink({step})` names the smaller thing and carries straight on — naming it IS the unblocking; `shrink({})` parks the session in **`needs-smaller`**, a real state and NOT a pause ("not now" and "I'm stuck on how big this is" are different problems). ⚠ **Shrinking is never recorded, phrased or scored as failure** — the count, the original wording and the final step ride into history as EVIDENCE, because a task shrunk three times is a finding about the WORK. `stepAway()` is Nick saying he was pulled off it, deliberately NOT `noteInterruption` (something ARRIVED, clock keeps running, because NEURO cannot know whether he switched) and NOT `pause` (a decision to stop) — and named that way because `/api/session/interrupt` already means the first and silently redefining it would break every caller. ⚠ **`interrupted` and `needs-smaller` are BANKED states**, so `_isStale` treats them like `paused`; without that a 15-minute task stepped away from at 09:00 went stale by 10:30. **Check-ins are a PULL, never a push** — nothing here notifies, the prompt is there because Nick already opened a surface, and body-doubling is precisely the feature that would justify a timer to itself; a check-in **touches the clock in no way** (presence, not time) and **nothing is inferred from a MISSED one** (being heads-down is why one gets skipped). The end-of-session reflection is optional and **finishing never depends on it** — a field you must fill to close a session is a reason not to close sessions. **Transitions** compose on `/api/attention` so all four surfaces render one decision: they **PROPOSE and never act** (no timer, no calendar write, no completion), an **unreadable diary yields NO transition** rather than "nothing coming up", a meeting is `context-state.isRealMeeting` (now EXPORTED — `attendeesOther` must be exactly `true`, undecidable fails closed, because half the diary is solo blocks), being IN a meeting is silence, and the session prompt is carried **verbatim** from `recovery()`. Pinned by `transitions.test.js` (10, pure) + `focus-session.test.js` (34).
- **Meeting prep shows its evidence (30 Aug 2026, Phase 3 Gate 4)** — prep is read seconds before Nick walks into a room with a real colleague, and its material is an **automated parse of 232 meeting notes** whose own service notes say some rows are misparses. ⚠ **`waiting_on.source_path` had existed since the feature shipped and `_buildPrep` was DROPPING it** — the evidence was discarded at the one surface where it matters most. Commitments now carry `sourcePath` / `sourceDate` / `sightings`, and an unattributed row **says so** ("no source recorded — worth checking before raising") rather than looking identical to a sourced one. The wording changed with it: **"Owes you (2)" → "Noted as outstanding"**, because the first states as FACT that a named colleague failed, on the strength of a parse Nick cannot see — the brief's rule is *never imply a person failed, ignored or promised something without evidence*. Live proof of why: the top commitment for one report dates from **30 April**, four months old, and the old line said only "11 outstanding from Naomi". ⚠ **Five swallowed catches** in `_buildPrep` (two to `console.warn`, three to nothing) now push named `prep.gaps` — an unreachable vault used to render as a prep sheet with no commitments, indistinguishable from a colleague who owes nothing. **Sending stays gated**: prep drafts nothing outbound and a chase still queues a `sara_action` for approval, pinned by a test that fails if the file ever acquires `sendMail`/`sendDm`/`graphWrite`.
- **How visible SARA is, and what changes it (31 Aug 2026)** — `sara/shared-ui/Field.jsx`. Nick: *"crank up the visibility of SARA's presence — I always want to see her"*, then *"what triggers it?"* Four inputs (`activity`, `confidenceLevel`, `quiet`, `degraded`, plus `pressing`) and three outputs (`depth` = how much order arrives, `period` = seconds between settles, `dim` = brightness). ⚠ **THE SPLIT THAT MAKES IT HONEST: `dim` is PRESENCE, `depth`/`period` are THE READ.** `quiet` dropped dim to 0.45 and so conflated them — being in a meeting made her invisible rather than calm. Raising the FLOOR while leaving the SETTLE to carry the state is the only way to crank visibility without the field claiming things it has not read, so *the coherence on screen is the coherence of the read* is exactly as true as before. ⚠ **The real bug was that the edge cull ran AFTER `dim`**: an edge computed 0.012, `quiet` multiplied it to 0.0054, under the 0.013 floor — so **NO EDGE DREW AT ALL** and the nebulous connected nodes lost their connections in precisely the state a desk kiosk sits in most of the day. Dimming did not dim them, it DELETED them. Now tested on the undimmed value (`EDGE_CULL_ALPHA`), with the resting substrate doubled (nodes 0.07 → 0.14, edges 0.012 → 0.03), quiet at 0.78, blind at 0.9 (being unable to see is not a reason to disappear — an unresolved field must be visible enough to read as unresolved rather than as an empty screen), the low-confidence depth floor at 0.45 (below ~0.4 a settle stops being legible as one, so "unsure" and "not working" were the same picture), and the scrim's far stops eased 0.74/0.90 → 0.60/0.80 because that is where her words sit. ⚠ **PRESSING IS A SLOW PULSE** (~6.5s, `1 → 1+amp`, brightens and never darkens — dipping below the floor would make her LESS visible when something needs him). It survives the no-decorative-oscillation rule only because it is STATE-DRIVEN: calm pool, no breathing at all. `firefighting` no longer shortens the settle to 5.5s, because once the pulse arrived that told the same fact twice and read as agitation. **Quiet still pulses** — quiet has always meant she will not SPEAK, never that she may hide a breaching escalation. **Blind never pulses**, because she cannot know whether anything is pressing. Under `prefers-reduced-motion` the pulse becomes a steady LIFT rather than vanishing: that is a request for less movement, not less information. ⚠ Note the settle is **LOCAL** — a 150px bloom around a point that jumps each cycle — not the whole field breathing. ⚠ **SUPERSEDED IN PART on 11 Sep 2026 — colour carries the state, visibility does not** (`sara/shared-ui/fieldDrive.mjs`, PURE). Nick, 6 Sep: *"it should be the colour of her presence that changes, not the visibility."* The iOS app (`Wardy-uk/nuero-ios`, `FieldDrive.swift`) made that change with 151 tests; the web half was written the same day (38491b3) but ⚠ **did not reach `main` until 11 Sep**, so for five days every web surface (phone PWA, Pi kiosk, and the laptop's Electron window, which loads sara.nickward.co.uk) went on fading her out in one fixed blue while the iOS commit said the web had it. The drive and palette now live in the pure module with tests, where an unpushed change to them fails a suite rather than only a screen. The rules: **`dim` is 1 in every state** (quiet 0.78 and blind 0.9 are gone) and `intensity` drives a two-segment **blue → orange → red** ramp (quiet 0.08, low→high confidence 0.37→0.60, firefighting 0.75, pressing 1 even when quiet). ⚠ **Blind is NOT on the ramp** — it is **grey** (`unresolved`) and never settles, because blue would give an outage the colour of a quiet afternoon. ⚠ **The resting alphas went to the iOS values, 0.42 / 0.24** — the iOS commit called its higher numbers a SwiftUI-only correction and said 0.14 / 0.03 were right for a browser canvas; **rendered side by side in the laptop's actual Electron window that was false** (blind and steady were near black), and with colour now the state channel a faint substrate hides the state too. The scrim rule stands: a hard-to-read sentence moves the SCRIM, not these. Pinned by `field-drive.test.js` (10, incl. source scans that Field.jsx draws with the module and has no local fading `drive()`; the fade and blind-grey rules **mutation-checked**).
- **A deleted feature is not an outage (31 Aug 2026)** — `SOURCE.RETIRED` in `sara/backend/src/state/provenance.js`. Nick, at the kiosk: *"why does SARA on Pi say partly live?"* Because the Jira queue was DELETED in July 2026 and the state engine still reported that domain `unavailable` — three domains live, one removed on purpose, rolled up to `mixed` — so the banner read *"Partly live — some of NEURO could not be read"* for **seven weeks** over a healthy read. `stateEngine.js` even said so in its own detail string; nobody had connected that to the banner. **A warning that is always on is a warning nobody reads, and it costs the real one** — the stale-Jira-cache lesson (a reader outliving its writer) one level up, in the UI. RETIRED is excluded from `rollUp` but stays `available:false` with null counts so no screen can read a fact out of it; a genuine outage elsewhere still reads `mixed`, and all-retired reads `unavailable` rather than manufacturing good news. ⚠ Caught by the suite: the first cut returned `at_risk` instead of `sections`, failing contract validation and capping confidence at `low` — a fix for a cosmetic banner quietly making the whole read look worse. Knock-on worth knowing: with the phantom outage gone, confidence rose `moderate` → `high`, and confidence drives the field's settle depth, so she resolves more fully too. Pinned in `stateEngine.test.js`.
- **SARA is spoken to, not navigated (31 Aug 2026)** — `backend/services/sara-surface.js` (PURE) composing `surface` / `dashboard` / `utterances` onto `/api/attention`, rendered by `sara/shared-ui/Dashboard.jsx` and the sentence row in `AttentionSurface`. Nick's statement of what SARA actually is: *"SARA is a manifestation so shouldn't be bogged down in menus — she should be a series of dashboards, and everything she can do should be achievable conversationally. She should adapt what she's showing me, but she isn't a click here to do this interface... we should allow that when I can't speak to her, but that's the principle."* Three things follow and the module is all three. **The dashboard is an ANSWER, not a destination** — it changes because the situation changed or because he asked, never because he went looking, so WHICH one is a decision and decisions are composed server-side beside `say`/`speech`/`tab`. **Every action is a SENTENCE**: `utterances` are the literal words (*"make it smaller"*, *"not now — an hour"*, *"that's done"*) each carrying a STRUCTURED intent, so no client parses language and the mute path and the spoken path are one vocabulary rather than two competing ones. **The buttons are the FALLBACK** — they exist for when he cannot speak, which is why each reads as the thing he would have said; a test bans UI verbs (`Defer`, `Dismiss`, `OK`) outright. ⚠ **It adds no candidates and re-ranks nothing** — `decision-engine` stays the one generator, `attention.gate()` the one filter, and `surface` FOLLOWS `context.activity` rather than re-deriving it. That boundary is the whole difference from `sara/backend/src/state/inference.js`, retired for computing its own activity enum and recommended-view map from SEEDED inputs and rendering the suggestion BESIDE the canonical one: here there is one brain, and the choice is **rendered rather than advised**. Eight surfaces (`blind` / `in-meeting` / `firefighting` / `pre-meeting` / `session` / `ritual` / `off-duty` / `steady`) mapping 1:1 off the nine activities. ⚠ **`blind` is the ONLY override and it outranks every activity** — a dashboard drawn over work SARA could not see is a confident picture of a day nobody read. ⚠ **An unrecognised activity resolves to `steady`, never to nothing**: a missing surface renders blank, and silence is a valid answer for a notification and never for a screen. ⚠ **Composed LAST in `build()` and never allowed to fail the feed** — a null `surface` reads to every client as "render it the way you did before this existed", and the payload is otherwise **ADDITIVE**, so the Scriptable widget (which reads `say`/`speech`/`tab` and nothing else) is untouched. Dashboards render only what the payload ALREADY carries — none of them fetches, which is what keeps this pure and free on every poll. Honesty rules inherited rather than re-decided: an unread diary **says so and is never an empty day**; gaps ride on **every** dashboard, not just the blind one, and are **named, never counted**; `attendeesOther` stays three-valued so *"on your own"* is said only when the brain knows; times are **SLICED out of the string**, never parsed (the BST bug, third repo); `plannedAssumed` survives to the figure; a shrink is stated as **evidence about the work, never failure** (forbidden-wording test); off duty shows what he **DID**, never what he owes, with a `critical` item the documented exception; an unset weekly target is **not a target of zero**. ⚠ **Utterances are BOUNDED by the record's allowed actions** and a card with no `recordId` gets **no act utterances at all** — offering a sentence NEURO will refuse is worse than offering none, because he will have said it out loud first. ⚠ **`shrink` / `step-away` / `finish` are `session` intents, NOT `act` ones** — they live on `/api/session/*` and the attention lifecycle 400s on all three; caught before shipping by reading the routes rather than assuming the verb sets matched, and pinned by a test asserting every `act` intent carries a lifecycle verb. ⚠ **Nothing an utterance offers LEAVES THE BUILDING** — no send, reply, book or chase; those queue behind the desktop approval gate, and an ambient surface that can send is one that can send by accident. *"Make it smaller"* **leads** on the session surface (initiation is the difficulty; shrinking is the only control that lowers the barrier rather than rescheduling it) and *"Show me everything"* is **always last and never dropped**, capped at `MAX_UTTERANCES` so the escape hatch cannot become the menu it replaced. Verified live: bank-holiday Monday → `off` → `off-duty`, weekly target `unset` rendered as *"No target set for this week"* rather than a zero bar, three gaps named. ⚠ **ASKING MOVES THE DASHBOARD (31 Aug 2026, evening).** `surfaceForQuestion(text)` is a small DETERMINISTIC route table (`event-parser`'s regex-first rule — this runs on a path three surfaces poll, so it must be instant, free, identical every time and work with the Pi's models offline); `GET /api/attention?ask=` carries it, bounded to 200 chars. Before it, asking streamed an answer and left the screen showing whatever it had been showing, which made the conversation a chat window sitting ON a dashboard rather than the thing that drives it. ⚠ **No match returns null and null means LEAVE THE DASHBOARD ALONE** — guessing a panel from a question she does not understand is how she comes to answer a different question from the one asked, and the streamed answer shows either way so a miss costs nothing. ⚠ **A question can NEVER move the surface off `blind`**: answering "what's in my inbox" with a confident panel while she cannot see his work is exactly what that state exists to prevent. `askedSurface` reports that the screen moved because he ASKED rather than because the day did — a surface that changes under him with no explanation is the dishonest half of being adaptive — and **utterances follow the surface SHOWN**, or the mute path disagrees with the screen it is attached to. The client fires the re-read **in parallel with the answer, unawaited**, so the dashboard turns as he finishes speaking and a failure never costs him the reply. ⚠ **Two dashboards got real bodies**: `gather()` now reads `jira.getUnseenEscalations()` and `email-triage.getUrgentEmails()` — both LOCAL reads, no network on a polled path — so firefighting shows the actual unanswered tickets instead of the generic pool under the word "live now", and `inbox` exists as a **surface reachable only by ASKING** ("what's in my inbox" is a question, not a situation, which is why the dashboards and the activities are deliberately not the same list). Each failure is a NAMED gap: *unreadable escalations* under the word firefighting renders as an explicit refusal, never an empty box, and an unread inbox is not a clear one. The inbox rows read `from`/`fromEmail` — the triage record's fields — because `from_name`/`from_email` belonged to the deleted `inbox_items` table and yield `undefined`, which is the bug the urgent-email nudge shipped with. Pinned by `sara-surface.test.js` (45, pure).

- **What is on NOW, what is next, and saying each of them ONCE (8 Sep 2026)** — `nowSlot` / `countdownFor` / `coveredBy` (all PURE) in `sara-surface.js`, `now` + `covered` on `/api/attention`, `end` carried on agenda events, and the `now` band + countdown in `sara/shared-ui/Dashboard.jsx`. Nick, looking at the Surface: the next thing should count down from thirty minutes, there should be a **current** section, and *"find a better way to present this so I dont see the same thing three times"*. The last one was literal and measured off his screenshot: one meeting was the **transition prompt**, the **primary headline** AND an agenda row; one task was the headline, a `dashSteady` row appended beneath the day, and a card in the list below it. ⚠ **Every one of those was a CORRECT decision taken three times by three layers that could not see each other** — which is why the fix is one server-side judgement (`covered`) rather than three renderers each matching titles their own way. ⚠ **`covered` is ADVISORY and filters NOTHING**: `secondary` leaves the composer exactly as it arrived, every other consumer (widget, mobile snapshot) still sees the whole pool, and a renderer that ignores it shows the old repetitive screen but never a wrong one — a framing layer quietly removing items from the feed is the second brain `inference.js` was retired for. ⚠ Matching is **exact-on-title, never fuzzy**, because the asymmetry decides it: a false match HIDES a real item from the list Nick uses to find what he owes, a miss only shows it twice, which is the state being fixed. ⚠ **`transitionIsPrimary` is reported as a FACT, never as an instruction to hide the headline** — whether the transition is on screen is CLIENT state (he can dismiss it), so `AttentionSurface` folds the wording only while the prompt is actually showing, and **folding the wording never folds the actions**. ⚠ **The `now` slot speaks about the DIARY, not about him**: *"Nothing in the diary"* is checkable, *"you're free"* is a claim about his workload that a calendar cannot support with 150 open tasks one panel below — and an **unreadable diary yields `null`, never "free"**, the same refusal the panel note already makes. ⚠ **A running event is the `now` slot and NOT also a row**: "what am I in" and "what is coming" are different questions, and answering both with one list is what put the same meeting on screen twice. `until HH:MM` is **sliced** off `end` (now carried through `agendaFor`), never parsed — the BST bug's rule, unchanged. ⚠ **`COUNTDOWN_MINUTES` is 30 (Nick's number) and only the NEXT row gets one**: beyond that the clock time IS the useful fact, and a countdown on every row makes none of them mean anything. ⚠ **`minutesAway: null` is "across a day boundary", NOT zero** — printing *"now"* over tomorrow's first meeting is a placeholder rendered as a fact — and an all-day event has no minute to count down to. ⚠ The countdown is **suppressed on the row the transition is already counting down**, and the number is **composed server-side and never recomputed client-side**, or it keeps ticking against a payload that stopped refreshing. ⚠ An empty-but-READ diary now says *"Nothing else in the diary."* rather than going silent, since a blank panel and a broken one look identical. Pinned by 11 tests in `sara-surface.test.js` (48), five of them **mutation-checked** — restating the primary fails 2, letting the countdown duplicate the transition fails 1, letting an unreadable diary read as free fails 3, hiding the diary in a meeting fails 1 and repeating the headline as a `now` band fails 1.

  ⚠ **AND THE IN-MEETING SURFACE SHOWED NOTHING AT ALL** (found the same day, on the live screen: *"rest of day is missing"*). `dashInMeeting` returned no rows under the label *"nothing, on purpose"*, which conflated two different restraints: SARA does not put WORK in front of him while he is in a room with people — and `attention.gate()` has already held it back, which the foot's *"3 held"* line states — but that was never a reason to withhold the DIARY. So the one screen he looks at mid-meeting withheld the two facts he actually wants there: when this one is due to end, and what is after it. Neither is something to decide about. It is now labelled *"what’s after this"* with the remaining agenda, the end time in the note, and the restraint restated in EVERY branch (pinned) so the panel can never read as a complete picture of what is waiting. ⚠ It is the ONE surface with **no `now` band**, deliberately: the headline above it is the context card (*"In a meeting / You’re in X"*), so a band naming X again is the duplication being removed one screen along — and the running meeting is not a row either. ⚠ **"Due to end at 14:30", never "ends at"**: it is the scheduled end, not a claim about when he gets out. Alongside, `context-state._quotedSubject` **trims a subject before quoting it** — Graph returns trailing whitespace and the quote mark is exactly what makes it visible, so the Surface read `You're in "Nurtur - Micom (Commercials) ".`; an empty subject falls back to the unnamed wording rather than quoting nothing.

- **"That’s finished" — a meeting that ended early (8 Sep 2026)** — `services/meeting-finish.js` (pure `keyFor`/`prune`/`applyTo` + a KV store), `POST /api/attention/meeting/{finished,resume}`, `meeting` on the attention payload, and a `meeting` intent leading the in-meeting utterances. Nick: *"we should probably add a 'done' button for when a meeting ends early ;)"*. Being in a meeting is the ONE state where SARA interrupting is actively wrong, so `context-state` sets `quiet` and `attention.gate()` holds everything back — correctly, and entirely off the CALENDAR’s word. A meeting scheduled to 14:30 that broke up at 14:10 therefore cost **twenty minutes of a working day in which the surface refused to help**, with no way to tell her otherwise. The diary is a plan; this is the one fact about it only Nick has. ⚠ **IT RELEASES A STATE AND CAN NEVER CREATE ONE** — there is deliberately no route to declare yourself INTO a meeting (the calendar decides that, and a manual way to make SARA go quiet is a mute button wearing a meeting’s clothes), and `applyTo` can only move an end EARLIER, clamped so it can never precede the start. Pinned both ways. ⚠ **KEYED TO THE OCCURRENCE, NEVER A BOOLEAN**: a flag reading *"not in a meeting"* would silence the state for the NEXT one — a different room, different people — so the key is `id + start` (with a `start + subject` fallback, because the ICS and NOVA-bridge paths synthesise ids that are not stable across a sync), and a recurring meeting’s Tuesday cannot match its Wednesday. An **unidentifiable** event is REFUSED rather than stored: an entry matching nothing is indistinguishable from an expired one, and an entry matching everything would silence the whole diary. ⚠ **IT EXPIRES AT THE MEETING’S OWN SCHEDULED END** and is pruned there — past that the calendar agrees with it — and an entry that cannot be aged out is dropped rather than kept for ever. ⚠ **NOTHING IS WRITTEN TO THE CALENDAR**: the meeting is not Nick’s to shorten, other people are in it and Graph would mail every one of them, so this records where HE is and `scheduledEnd` keeps the diary’s own fact beside it. Pinned by asserting the cache row is byte-identical after a press. ⚠ **THE OVERRIDE IS APPLIED ONCE, AT `_calendarInput`**, rather than threaded as a flag through `context-state`, `agendaFor` and `transitions` — three consumers each remembering to honour a field is three chances to forget one, and a surface still calling him "in a meeting" after he has said he is out is worse than the twenty minutes being fixed; rewriting the effective `end` makes every consumer (including any written later) right by default, and hands `transitions` its post-meeting *"anything to capture?"* prompt for free, which is the moment the follow-ups are still in his head. ⚠ **THE EVENT COMES FROM THE LIVE FEED, NEVER THE REQUEST BODY** — a client posting its own start/end times is one that can silence any meeting it likes, including an invented one; all it may send is the key SARA gave it, and **a STALE key is refused with the right one named** (the phone polls, so its key can be the meeting BEFORE this one). ⚠ **A SOLO BLOCK CANNOT BE FINISHED**: `attendeesOther` must be exactly `true`, the same test `isRealMeeting` makes and BORROWED from it rather than re-implemented, because releasing a state that was never set is a button that appears to do something and does not — half his diary is solo blocks. ⚠ **THERE IS A WAY BACK** (`resume`), non-optional: the cost of a wrong press is SARA speaking up in a real meeting, the exact failure the quiet state exists to prevent; resuming something already resumed is a SUCCESS, not an error. ⚠ An **unreadable store overrides NOTHING** — the diary’s word stands and she stays quiet, which is the safe direction. ⚠ It is a **`meeting` intent, NOT `act`**: the in-meeting primary is a CONTEXT card with no `recordId` and the lifecycle would refuse the verb — the same reason `shrink`/`step-away`/`finish` are `session` intents. The kiosk needed **no change**: it mounts the same `Surface.jsx` and `attention` is already a named door in `neuroProxy`. Pinned by `meeting-finish.test.js` (14, pure) + `routes/meeting-finish-routing.test.js` (10, real HTTP, driving cache row → route → `agent_state` → back through `_calendarInput`) + 3 in `sara-surface.test.js` (51); the stale-key, clamp, solo and expiry rules are all **mutation-checked**.
- **One surface, two shells (30 Aug 2026)** — `sara/shared-ui/AttentionSurface.jsx` + `.css` is rendered by BOTH the phone (`sara/app` Surface) and the Pi kiosk (`sara/frontend` Presence). Nick's steer was that the two "should essentially be the same app"; the half that matters is the RULES and the WORDS, and those now live in one file: the **three distinct silences** ("I can't see your work" / "staying out of the way" / "nothing pressing", and only the last is good news), the transition that leads and only PROPOSES, the defer row that asks how long and records a REASON, what the gate held back, and dismiss offered only when the record allows it. ⚠ **The CHROME is deliberately not shared** — the phone passes a mic, a speech toggle and a notification-arrival card as SLOTS; the kiosk passes none, because a kiosk carrying a mic it cannot use is worse than one without. ⚠ **The root box cannot be shared and is per-app**: the phone cancels `.app__view`'s padding so the field bleeds to the screen edge, while the kiosk sits under a banner and must BOUND its height — copying the phone's `min-height: 100vh` across is exactly what pushed SARA's words below the fold. ⚠ **The kiosk passes no `onAct`**, so the shared component omits the action row entirely rather than showing buttons that would silently fail without a credential. What blocked sharing was never the transport — `Surface` only depended on it by reaching for `apiFetch` directly, i.e. coupling INSIDE the component. The kiosk still reads through `sara/backend`'s passthrough, and that stays a decision about where the CREDENTIAL lives (a direct client would put a NEURO token in a browser on an always-on desk screen), not about how alike the two apps are. Guarded by `kiosk-views.test.js` from the backend suite, because `sara/frontend` has no runner of its own. ⚠ **SUPERSEDED IN PART on 31 Aug 2026** — Nick: *"make the Pi version of SARA the same as phone app - same for the desktop."* The kiosk no longer has a screen set of its own: `sara/shared-ui/tabs.jsx` is ONE registry (three primary modes + ten secondary) mounted by BOTH shells, and `sara/frontend/src/App.jsx` is now the phone's shell carrying the kiosk's own chrome (provenance banner, Exit, and NEURO's clock/lock display states) rather than fourteen `/api/state` screens. **The DESKTOP comes free**: `sara/desktop-electron` loads this same frontend from `SARA_URL`. `action-surfaces.test.js` reads the shared registry now, so a tab missing from `SARA_LITE_TABS` is caught for both shells. ⚠ **What made it possible was `sara/backend/src/routes/neuroProxy.js`** — mounted LAST on `/api`, after every named door, forwarding to NEURO with SARA's credential attached, so the kiosk runs the phone's screens with **no NEURO token in a browser on an always-on desk screen**. It is an **ALLOWLIST, not an open proxy, and that is the entire safety model**: `server.js` binds 0.0.0.0, so a blanket forward would put `/api/capture-links` (VESTA accounts and PINs), `/api/microsoft/auth` and `/api/actions/:id/approve` (which SENDS EMAIL AS NICK) behind an unauthenticated touchscreen. 18 doors, matched on the **whole first segment** so `capture` opens and `capture-links` does not; traversal refused outright rather than normalised; anything else refused **locally, before the network** (`neuroCapture`'s rule). The upstream status and body survive unrewritten — a screen has to tell a 401 from a 500 from a 200 carrying `{ok:false}`. The vault key is attached for `/vault` paths ONLY, because NEURO's vault router has its own `requireApiKey` and a key sent on every route is a key handed to routes that never needed it. Pinned by `neuroProxy.test.js` (8, real HTTP, six of them refusals). ⚠ **THE LEGACY KIOSK SCREENS ARE DELETED (31 Aug 2026, evening)** — all fifteen, plus `ViewRouter`, `ViewSwitcher`, `PlannedView` and their CSS: 38 files. Safe once `firefighting` and `inbox` became real dashboards, because what the deletion would otherwise have cost was `ExecutiveDashboard`'s live escalation read and email triage. **The reframing is the point** — under Nick's principle those were never screens to keep or bin, they were two dashboards welded to a menu. `state/views.js` survives STRIPPED to an id vocabulary with the registry removed: `saraState` threads those ids through ~888 lines of navigation nothing renders, and unpicking that is a real refactor, so the honest interim is keeping the words those paths speak and deleting the part that CLAIMED each id names a screen you can open. `App.css` is 74 lines of pure theme now. ⚠ **A `@media (max-width:900px)` block still held the 212px sidebar** and was caught by a test rather than by reading — a media query is where a dead rule hides longest, because it only fires at a width nobody tests at. ⚠ **THE SHELL FIELD IS DRIVEN, not hardcoded** (`sara/shared-ui/useFieldDrive.js`): it was pinned at `quiet`+`low` on every screen but the Surface, so she looked identical whether the queue was on fire or the day was empty, and when the pulse arrived it could not fire there either — a critical item never reached him unless he happened to be looking at her own screen, which is the opposite of an ambient surface. It polls SLOWLY (150s) and is **skipped while the Surface is mounted**, which already has a live read; it starts DEGRADED rather than calm, because before the first read she genuinely cannot see anything. `isPressing` lives there and is IMPORTED by `AttentionSurface` rather than restated — two definitions of pressing on one screen are two answers free to drift. ⚠ **The mic is gated on CAPABILITY, not on device**: the kiosk mounts the same component and the Pi 4 has no microphone, so "🎤 Talk to me" would have failed on the tap; nothing asks whether it is "the kiosk", so a mic plugged into the Pi brings the button back on its own. ⚠ **Navigation is `NetworkFirst`** in `sara/frontend/vite.config.js` (4s timeout, cached shell still covers a real outage): the precached `index.html` was answering navigations, so every deploy painted the PREVIOUS build first — three screenshots of the panel after successful deploys showed a UI from before 30 August over a server serving the new bundle correctly. A screen showing last week's app while looking completely fine is the failure this codebase refuses everywhere else, introduced by the CACHE rather than the data. ⚠ **`ExitButton` moved into the nav** as a real `.navbtn` (the phone's class, not a copy), LAST, with its two-step confirm kept as a popover ABOVE the bar — three controls cannot share one tab-width slot and shrinking the guard is how a stray palm comes to close the only exit a keyboardless kiosk has; Cancel is the wider button, because the safe answer should be easier to hit. ⚠ **The FIELD now renders in every state SARA is seen in** (Nick: *"crucially the nebulous connected nodes must be present whenever I see SARA"*) — it was reachable only from inside `AttentionSurface`, so it drew when the feed was good and vanished in the three states that still say her name: both of Presence's blind branches, the lock screen (which drew a pulsing **ORB**, deprecated permanently by `MANIFESTATION.md`, and whose frosted-white ground had to become her own #06080c for the field to read at all), and the clock screen. It also sits behind the whole shell of BOTH apps, driven `quiet` and **suppressed on the Surface**, which mounts its own from the real payload — a confident settle out in the shell would be the field asserting a read that never happened, and two stacked fields would put a placeholder under an honest one. Pinned in `kiosk-views.test.js`.
- **A field nobody can see is still being painted (2 Sep 2026)** — `sara/shared-ui/FieldCover.jsx` + `still` on `Field` + `MAX_EDGES`/`ALPHA_BUCKETS` + `sustainedClock` in `sara/backend/src/routes/presence.js`. Found on the Pi 4 wall display: chromium **pegged at 100% of a core for 1d17h**, plus 67% in the GPU process, while Nick was out. The display verdict was CORRECT (`locked`) and `/sys/class/backlight/panel_backlight@1/brightness` was genuinely **0** — SARA was asleep. Nothing had told the browser. ⚠ **A KIOSK PAGE IS NEVER `document.hidden`**, so Field's own *"stops dead when the page is hidden"* battery guard protects against a condition that **cannot occur on a wall display**; backlight-off is not page-hidden. ⚠ **The overlays render OVER the live shell, not instead of it** — `locked`/`clock` are siblings of `<main>`, so everything they cover stays mounted, polling and animating. ⚠ **And the first fix missed the field that was actually running**: suppressing the shell field did nothing, because the kiosk opens on `DEFAULT_TAB = 'surface'` which `active !== 'surface'` had already excluded, while **`AttentionSurface` mounts a Field of its own that no guard in `App.jsx` reaches** — re-measured after deploying and it was still 23% + 23%. So the cover is a property of the **SUBTREE**: `FieldCover` is a **context, deliberately not a global flag**, because what matters is which SIDE of the overlay a field is on — the overlays render OUTSIDE the provider, since the clock screen's own field is lit and animating and a global would stop the only one anybody can see. Covering **stops the loop and keeps the pixels**, toggled through a ref in its own effect and **never as a dependency of the substrate build** — uncovering happens exactly when Nick walks back into the room, and rebuilding there would flicker all of it. The lock screen's field is **`still`** (one frame, no loop, the reduced-motion path): not nothing, because the display agent can die and the light can return before the verdict does. ⚠ **`NODE_MAX` bounds the wrong thing** — edges are quadratic in LOCAL density and nothing capped them: a 340x700 phone builds ~1,950, the 720x1280 panel ~8,700, because the seed clustering concentrates 50 nodes per blob. `MAX_EDGES` (3000) thins **UNIFORMLY** (shuffle, then truncate) so the mesh gets sparser without changing shape, and sits above anything a phone builds, so the reference look is untouched. Draws are batched **one path per alpha bucket** (9,564 calls a frame → 34); ⚠ **the bucket ENDPOINTS are exact** — midpoints are the obvious implementation and lift the RESTING alpha by 25%, which is where nearly every edge sits, and a perf change may not re-tune numbers Nick set by eye. ⚠ **A LOCK REASON CANNOT OUTLIVE ITS EVIDENCE** — the sustained-room clock was only ever set, never cleared, so it ran unbroken from the last confident bedroom sighting and the screen locked as `in-bed` saying *"Goodnight."* at one o'clock on a Wednesday afternoon; right state (`away` locks too), wrong reason, and the reason is what she says out loud. Cleared only when the watch is **absent from every room AND HA positively reports away** — both halves load-bearing (`absent` alone clears it for a flat battery at 3am; `away` alone cannot be trusted about which bed he is in), and **`absent`, never `unreadable`**, the same distinction the `lastRoom` hysteresis makes. Split out as the **pure `sustainedClock`** so the rule pins without a route, a radio or a clock; ⚠ its timestamps are tested for **null, never truthiness** — 0 is a real timestamp. Live result: renderer+GPU **~167% → ~6%**, load average **3.11 → 1.18**. Pinned by 5 tests in `kiosk-views.test.js` (mutation-checked against the reintroduced bug) + 4 in `presence.test.js`.
- **The desktop joins the attention contract, and "Do it" stops lying (Phase 4)** — `frontend/src/useAttention.js` + `components/AttentionCard.{jsx,css}`, rendered by **Now, Focus and Briefing alike**. Desktop was the last surface still deciding for itself: it read `/api/focus` and wrote to `/api/focus/{dismiss,snooze,hide-today,action-done}`, which is a suppression TIMER, not a lifecycle — so "seen it", "not now" and "not mine" collapsed into one gesture on desktop while staying distinct on the phone, and a card acknowledged there came straight back here. ⚠ **The bug: `BriefingPanel`'s "Do it" POSTed `/api/focus/action-done`**, which calls `nextActionEngine.logOutcome()` AND `engine.dismiss()` — so the button that merely OPENED a thing recorded it as a completed outcome and hid the card, before any work was done. Invisible for its whole life because **a card vanishing looks exactly like the button having worked**. `FocusPanel`'s "Done" did the same and **never closed the task**, so a tick left the work open and hid the reminder about it; its "Defer" POSTed `/dismiss`, destroying the not-now/not-mine distinction at the moment Nick expressed it. Its escalating `DEFER_MESSAGES` ("You're avoiding this") went too — the count lived in `localStorage`, so it was a fact about a BROWSER rather than about the work, and the wording was a claim about Nick no evidence supported. **The action semantics are the product**: `open` navigates and **calls nothing** (giving it a route is how it acquires a side effect later); `start` starts a focus session and moves **no state** (picking work up is not finishing it — the record is told only so the friction read has the evidence); `complete` is Nick's explicit confirmation and is the **ONLY** path that resolves; `defer` carries a REASON (`not-now` / `waiting-on-someone`); `dismiss` teaches suppression and touches no work. Nothing auto-completes, auto-defers, auto-dismisses, auto-starts or sends. ⚠ **`completionTargetFor` is PURE and returns a LOOKUP, not an id** — `collectOverdueTodos` emits a slug (`todo-overdue-top`) and carries no task id, so the only handle is the task's normalised text, the same key `focus-session.start` matches on; a meeting or an email pile returns **null**, and the response says *"card cleared, no task was closed"* rather than implying both. A tick held by `task-blocks`' outcome-note rule comes back **held**, never completed. `/api/focus` stays mounted and its GET is still read for `sara.briefing` and `tone` — but **no desktop surface writes to it**, pinned by `desktop-attention-source.test.js` (a source scan, with a positive control that the route still exists, so a pass means the CALLERS went and not the string). The legacy `/dismiss`/`/snooze` fallback fires **only** when a card has no `recordId`, says so on the card, and has no entry for `action-done` at all. ⚠ **The kiosk and the phone are migrated too**: `sara/backend/routes/actions.js` no longer has a `/focus/done` route at all, and `sara/backend/src/routes/attention.js` gained `GET /records` + `POST /records/:id/act` — a PASSTHROUGH that forwards the action verbatim and lets NEURO decide what it means, with the body **bounded to the contract fields** so a proxy cannot one day carry something NEURO trusts and the kiosk should not set. A write there reports a refusal as a refusal (never the feed's `200 + available:false`, which is right for a POLL and wrong for a button press). `present()` now exposes **`engineId`** for one job only — a legacy screen holding a decision-engine item id and nothing else (the kiosk's `FocusView`, the phone's `Focus` tab) can find the canonical record and act on THAT; it is unstable by construction and must never be stored. The kiosk's Presence surface finally passes `onAct`: the old objection was that acting needs a credential the kiosk lacks, which was true of the BROWSER and never of `sara/backend`, and the alternative was leaving `action-done` in place. ⚠ **`done-focus` has NO fallback** — a dismissal is not a completion, and substituting one for the other is the bug being removed, so an unresolvable card says so and records nothing. Pinned by `sara/backend/test/attentionAct.test.js` (9), including a source scan asserting no route proxies `action-done`.
- **`Now` is the default screen, and it is the ADHD view promoted (Phase 4)** — `AdhdPanel` (view id still **`today`**, deliberately: every deep link, `?view=` param and notification route keeps working; only the label changed). It is primary nav on both the sidebar and the mobile bottom bar, and the default launch view; a notification's launch intent still wins. **Promoted rather than rebuilt** — it already owned every control that lowers the barrier to STARTING (session container, return prompt, "make it smaller", quick wins), and a thin new screen composing them would have been a THIRD "what should I do?" surface, which is the thing this phase removes. One change only: "Right now" renders the canonical `AttentionCard` instead of `/api/adhd`'s own `rightNow`. Order is the argument — return prompt and session sit ABOVE any general suggestion, because the cost of an interruption is the failure to return. Briefing and Focus moved to MORE as supporting views; nothing was deleted. Non-primary panels are `React.lazy`, with `Suspense` **inside** `ErrorBoundary` so a chunk that fails to download is caught and named like a panel that throws; `Now`, Capture, Ask, auth and the offline queue stay **eager**, because a spinner at the moment the barrier needs to be lowest is the wrong trade and Capture behind a round trip is a capture that fails when the network does. Main chunk 359 kB, under Vite's warning for the first time.
- **SARA can see his body and his day (31 Aug 2026)** — `services/ambient.js` (pure `assess()` + pure `standStillness()`) + `GET /api/ambient` + an `ambient` block on `/api/attention` + a section on the shared `AttentionSurface`, so phone and kiosk render it from ONE file. Nick's ask: she should behave like J.A.R.V.I.S. — sat down at home on a weekend, suggest getting up; a health metric worth noting, tell him; not eaten, suggest lunch; not exercised, poke him. **The finding was that voice was never the gap, blindness was**: `context-state` already resolved nine states and `attention.gate()` already decided what surfaces — what was missing was INPUTS, and most were already in the house (the HA Companion feed, `health-signals`' trends which **nothing had ever consumed**, and dietary data already in `health_samples` because MyFitnessPal writes to Apple Health and NEURO already ingests it — **no MFP API integration was ever needed**). ⚠ **THE RULE THE FILE TURNS ON: NOT LOGGED IS NOT NOT DONE.** `dietary_energy_consumed` last has a sample on 30 Mar 2026 and `dietary_water` on 3 Feb 2025, so "no food logged today" is true EVERY day — a naive read tells Nick he has not eaten, every lunchtime, for five months. Each diet observation first asks whether the habit is LIVE (`LOGGING_DAYS_REQUIRED` 2 of the trailing 7) and reports `unknown` **with the reason** when it is not; same species as "an unread domain is null, never 0", applied to a habit. ⚠ **It adds no candidates to `decision-engine` and re-ranks nothing** — an observation is a fact about right now, not a thing to decide about, so it rides BESIDE the pool and renders BELOW it; "you've been sitting for two hours" must never compete with a breaching escalation for the primary slot. This is a sensor, not a second brain (`sara/backend`'s `inference.js` was retired for being the latter). ⚠ **NOTHING HERE NOTIFIES — pull only.** Six new interruption sources is exactly how SARA becomes a pest and gets muted, and nudge volume is the one budget allowed to argue against building more; earn the push later. Other refusals, all pinned: **Focus mode vetoes the sedentary nudge outright** (he has told the phone to leave him alone — more current and more deliberate than anything inferred); **someone who never records exercise is never told he has stopped** (no habit to have broken, and saying so invents a standard he never set); an **incomplete day** cannot count towards a gap; health findings are **passed through WITH their caveat**, never re-derived, because Apple Health cannot separate exercise, illness, alcohol and a hard week; an empty list is **never padded** and `allClear` is only true when nothing went unread either. ⚠ **Caught on the FIRST live run, not by the suite: a stopped sensor is an UNKNOWN, not an observation.** All four slots came back filled with quiet-sensor findings (blood pressure stopped April, glucose September, fifteen dietary metrics March) with three more dropped to the cap — all true, all would have rendered identically every day for ever, and they were burying the RHR/HRV/sleep trends the feature exists for. `quiet:*` findings now fold into `unknowns`, which is what they actually are.
- **The Apple Watch, not the phone, answers "has he been sitting" (31 Aug 2026)** — `ambient.standStillness()`, pure. ⚠ **Raw accelerometer is NOT readable and never will be from here**: HealthKit exposes no raw motion type and the Watch's CoreMotion stream is reachable only by an app running ON the watch. But `apple_stand_time` IS Apple's accelerometer-derived answer to the same question. Measured shape: **HOURLY buckets of minutes stood, and an hour with no standing has NO ROW AT ALL** — that absence means "did not stand" and "watch was on charge" identically, which is the trap. **`heartRate` is the corroboration**: measured over 32 live hours it arrives 13–19 samples an hour continuously, including all night (he sleeps in the watch — hence sleep stages and wrist temperature), so a missing stand hour WITH heart rate is real sitting and one without is a watch that was off. **The watch OUTRANKS the phone** because it is on his body and the phone may be on a desk in another room — proven the day it was built, the watch reported at 09:00 while the phone was 14 hours stale; the phone stays a fallback and **says so when it answers** ("your watch didn't answer"). Four pinned refusals: the **CURRENT hour never counts** (it may be four minutes old, and reading a partial hour as "no standing" reports a run at one minute past every hour); **a night in bed is not eight hours of sitting** and sleep BREAKS the run, so the morning is a fresh start; a run **ENDS where the evidence ends** rather than being discarded (two solid hours stay true even if the watch came off after them); and the watch saying "he got up" is **never overridden** by a phone reading `Still`, or the fallback quietly becomes the primary whenever it is more pessimistic. **`STAND_QUIET_HOURS` is 2, not 1, deliberately** — Apple's own stand reminder fires at 50 minutes past, and firing at an hour would be SARA repeating a notification he has already had on the device he is already wearing. Pinned by `ambient.test.js` (34).
- **What the laptop is doing (31 Aug 2026)** — `services/desktop-activity.js` (pure `sanitiseApp`/`currentRun`/`atLaptop`/`assessDesk`) + `routes/desktop.js` (`/api/desktop/activity`, machine-token auth) + `desktop-agent/` (a PowerShell reporter + a logon scheduled task). Nick's Tier 2: *"not working on my laptop (can SARA detect that?)"* and *"too long coding — 4 hours in VS Code?"*. **Both yes, and neither is reachable from anything NEURO already had**: the phone knows where he is, the watch knows whether he is sitting, the calendar knows whether a meeting is running, and none of them can tell working from not-working, or four hours of one job from four hours of twelve. ⚠ **THE PRIVACY LINE IS NOT NEGOTIABLE: the FOREGROUND PROCESS NAME and nothing else** — never a window title, never a path, never a URL, never keystrokes. Not caution for its own sake: a VS Code title carries the file and the workspace, a browser title the page, and an **Outlook title the SUBJECT LINE** of whatever is open, which on this machine means customer names, ticket subjects and the contents of a disciplinary folder. `"Code"` answers the question; `"risk-assessment-naomi.docx - Word"` answers it no better and cannot be un-sent. **`sanitiseApp()` enforces it again server-side** so a careless FUTURE reporter is truncated rather than trusted (pinned with five real title shapes from the machine), and a **LOCKED session stores no app at all**. Refusals: a laptop gone quiet is **UNKNOWN, never "not working"** (asleep / off / off the tailnet look identical — the stale-phone and watch-on-charge rule again); a **GAP in the samples ENDS the run** rather than being bridged, or a lunch break becomes part of a four-hour stretch; idle ends it (`GetLastInputInfo` is keyboard+mouse, so reading counts as idle and the threshold tolerates a long think); and it **states the fact without judging** — four hours on one thing is a good day as often as a stuck one, pinned by a forbidden-wording test. The mention is **REMEMBERED** (`LONG_RUN_REMIND_MINUTES`), because several surfaces poll this and a fresh observation per poll is the same line over and over. Fires at **three** hours, not four — the useful moment is before the fourth. Storage is a **bounded ring buffer in `agent_state`** (`focus_session_history`'s precedent, ~12h at a 2-min cadence), not a table: disposable state, nothing queries it by anything but recency. ⚠ The installer **proves the path before scheduling anything** — one sample, PRINTED so the privacy claim is checkable rather than taken on trust, posted, and only then registered — and the **token goes in a per-user config file, NOT the task arguments**, which are visible to anything that can list tasks. Verified end to end on the real Windows machine, and **INSTALLED and running** since 31 Aug. ⚠ *Always running* takes **two triggers**: at-logon starts it, and a **15-minute repeating trigger with `MultipleInstances=IgnoreNew`** is a self-healing watchdog needing no extra code — already running, the extra start is discarded (`LastTaskResult 0x800710E0`, which is the design working, not a fault); not running, it is back within a quarter of an hour. ⚠ **No `-RepetitionDuration`**: `[TimeSpan]::MaxValue` is the widely-copied idiom for "indefinitely" and Task Scheduler **rejects** it (`P99999999DT23H59M59S`, out of range) — omitting it is what means indefinite. ⚠ **The installer VERIFIES before claiming**: the CIM cmdlets report failure as a NON-terminating error, so `$ErrorActionPreference='Stop'` does not stop the script, and the first run printed *"Installed and started"* immediately after `Register-ScheduledTask` had failed — an installer that lies about what it installed is worse than none, because nothing else will ever tell you. ACL hardening on the token file needs `SeSecurityPrivilege`, which an unelevated shell lacks: it is attempted and **warned about rather than aborting** (the file is under `%LOCALAPPDATA%` either way). Pinned by `desktop-activity.test.js` (26).
- **The laptop keeps a history, and each machine keeps its own (2 Sep 2026)** — `services/desktop-daily.js` (pure `rollup`/`shouldReplace`/`dayKey`) + the `desktop_daily` table + per-host buckets in `desktop-activity.js` (`runAcross`, `isActive`, `hosts()`) + `GET /api/desktop/daily` and `POST /api/desktop/daily/sync`, hourly at **:50** and 40s after boot. **Built instead of a RescueTime integration, on measurement.** Nick already pays for RT and asked whether its API would be better; pulled live over three months against this agent and the wins ledger it was **worse**: RT's history reaches back only 3 months, holds **9 blind weekdays** where NEURO had hard evidence of a full day, and **19% of all recorded work sits on days RT saw nothing** — including 1 Sep, where RT reported **0.16h against 8.21h** of measured active time **on the machine it was installed on**. Its own numbers looked like a quiet Tuesday; nothing in RT could say it had stopped watching. ⚠ **And RT is not cross-device either** — all 221 activities in 30 days are Windows (`lockapp`, `shellexperiencehost`, `credentialuibroker`, `supportassistui`), not one iOS or macOS signature, and its API exposes **no per-device breakdown at all**, so "is it running everywhere" is a question RT itself cannot answer. Its productivity pulse also measured as near-redundant: **r = −0.96 against meeting count** (VS Code scores very-productive, Teams neutral), i.e. a coding-vs-meetings ratio NEURO already derives from git and the calendar; the non-circular test against non-git output was **r = 0.42, p = 0.35**. The one thing RT genuinely has is **browser destinations**, bought by hoovering up full window titles — the line this agent refuses to cross.
  ⚠ **Samples are bucketed BY HOST, and that was fixed BEFORE the second install, not after.** One 400-slot ring for every machine is correct with one machine and silently wrong with two: both hosts compete for the same slots (halving the window `MAX_SAMPLES` exists to guarantee), and — the worse one — `currentRun` walks ONE list looking for CONTIGUITY, so the other machine's sample lands mid-run in a different app and **ends it**. Every run length would read short, with nothing logged: the RescueTime failure reproduced in our own code. `runAcross` is the pure cross-host read and **always reports which host answered**; an ACTIVE host outranks one merely switched on. ⚠ **A run on one machine is only unbroken if he was not on ANOTHER during it** — two hosts each report a tidy three-hour stretch for an afternoon spent switching, true of each list and false about the man — so the run starts after the last moment another machine saw real use and **names what interrupted it**; an idle other machine interrupts nothing. `UNKNOWN_HOST` is written and read in one module (the `(plan unknown)` rule) and `MAX_HOSTS` (4) stops a rolling hostname growing the blob. Migration from the single ring is **lossless** (every sample already carried its host) and `mentioned` is COPIED into each bucket rather than dropped — a line that stays quiet slightly too long beats one that repeats the moment we deploy. ⚠ `attention-learning`'s `desktop-switch` rule now scopes to **one machine, chosen from the window itself** — merged, a second machine's samples always name a different app, so "he switched" would be trivially true and **every nudge would learn as having worked**; and never from `run()`, which judges freshness against now rather than when the nudge landed.
  ⚠ **A GAP IS NOT TIME AT THE DESK.** Time is attributed to the interval BETWEEN consecutive samples, and an interval longer than `FRESH_MINUTES` is dropped entirely — a laptop asleep 12:30–13:30 did not spend that hour in VS Code. `MAX_GAP_MINUTES` is **borrowed from the sensor, not picked again**. The invariant is `present = active + idle + locked`, pinned; an interval belongs to the state of the EARLIER sample, which is what makes it hold and is also why a run ends at the first sample naming a different app (9:00–10:00 in Code with chrome first seen at 10:02 is **62 minutes, not 60** — documented, not accidental). A locked stretch records **no app**; a day with one sample still reports that sample, because `sample_count: 0` reads as "the agent never ran", a different fact. ⚠ **NEVER OVERWRITE A FULLER ROW WITH A THINNER ONE** — the ring holds ~13h, so a rollup after a restart sees only part of yesterday, and writing that over a complete row silently turns an eight-hour day into a two-hour one (the health-backfill bug: 744 days written, 328 with any data). `shouldReplace` refuses, **says which day it refused** and is reported in `skipped`. ⚠ **Rows are ONE PER (day, host) and are never summed across machines** — two hosts used in the same hour each counted it, and the samples needed to take a union are gone; `getDesktopDays` bounds **distinct DAYS, not rows**, or a plain LIMIT returns one day's pair and calls it two days. An empty buffer is a reported **gap**, never a quiet day. ⚠ The hourly cadence is not cosmetic: unlike the health rollup, which reads a table that keeps its own history, **anything not rolled up within the ring's window is GONE**. Deliberately **not** a `TRACKED_JOBS` job — idempotent over a trailing window and it refuses to make things worse, so a missed hour self-corrects. Day keys are LOCAL (the Pi may run UTC), pinned with the assertion made only where the timezone makes it mean something. The strongest downstream consumer is the **day planner's estimate multiplier**, which currently learns from about two real focus sessions and reports `learned:false`. Pinned by `desktop-daily.test.js` (17) + `routes/desktop-routing.test.js` (5, real HTTP).
- **RescueTime, as a second opinion that is CONTINUOUSLY AUDITED (2 Sep 2026)** — `services/rescuetime.js` (pure `parseRows`/`sanitiseActivity`/`foldDays`/`assessDay`/`coverage`/`shouldStore`) + the `rescuetime_daily` table + `routes/rescuetime.js` (`/api/rescuetime/{status,daily,sync,key}`) + `RescueTimeCard` in **Settings → Integrations**, cron every 3h at `:05`. Built only AFTER `desktop_daily` existed, and that ordering is the argument: the reason not to integrate was that RescueTime's coverage is unverifiable from inside RescueTime, and `desktop_daily` is an independent local measurement of the same machine on the same day that RescueTime cannot influence. ⚠ **So the senses row asks "does RescueTime AGREE with what was measured?", never "did the API answer?"** — the second is what every RescueTime dashboard already reports and it was green through all nine blind weekdays. **Two things are taken and one is refused**: categories and **browser domains** (the one real gap in NEURO's own sensor, which collapses every browser to `chrome` by design) — and **NOT the productivity pulse**, measured at **r = −0.96 against meeting count**, i.e. a coding-vs-meetings ratio NEURO already derives from git and the calendar; it is not fetched, not stored, not exposed, pinned by a test asserting no fold can produce one.
  ⚠ **The privacy boundary is `sanitiseActivity` and it earned its existence immediately**: a REAL activity row pulled that day was `web.plaud.ai&response_type=code id_token&scope=name email&response_mode=web_message&frame_id=…` — an OAuth flow with its parameters attached. Everything after the first separator is discarded; `restrict_kind=document` is never requested. Two leaks were caught by the tests rather than by reading: the app branch kept a **full window title** (`NT-14855 Sandford escalation - Outlook`), fixed by **reusing `desktop-activity.sanitiseApp`** rather than writing a second, gentler copy — two sanitisers over one class of data means the weaker one is the leak; and a bare **filename satisfies the hostname regex** (`risk-assessment-naomi.docx` is a valid host as far as the pattern is concerned, and carries a colleague's name), hence a small file-extension denylist. ⚠ Rows are parsed **BY HEADER NAME**, never by position (a ranked pull leads with `Rank`, an interval pull with `Date`), and an unexpected shape **REFUSES** — a silently empty result reads as "he did nothing that day", which is the exact failure being detected. The day key is RescueTime's OWN, in the account timezone, used verbatim.
  ⚠ **Three refusals in `assessDay`**: a day the AGENT did not properly cover (incomplete, or under `MIN_DESK_SAMPLES`) yields **`unknown` with a reason** and never a verdict — blaming RescueTime for our own blind spot is how the check loses its credibility; **only UNDER-reporting is ever flagged**, because RescueTime legitimately records more (another machine, or time before the agent existed) and flagging "over" is a permanent false alarm on a working feed; and since RescueTime exposes **no per-device breakdown at all**, the comparison is made against the agent's busiest host that day and **says which** — an approximation, named as one. `AGREEMENT_FLOOR` is 0.4 and deliberately generous (the failure being caught is 0.016, not 0.8), and is only ACTED on past `MIN_CALIBRATION_DAYS` (7) — below that the state is **`calibrating`**, which renders as its own thing and never as a green light it has not earned (the `stress-score` idiom). ⚠ **An empty answer never overwrites a day that had hours** (`shouldStore`) — `desktop-daily.shouldReplace` in its other form. The key lives in `agent_state.rescuetime_key`, read at CALL time, `.env` wins where set, and is **never returned by any route**, pinned by a real-HTTP test that greps every response for the value and for its first eight characters. Deliberately **no startup sync** unlike the desktop rollup beside it: that drains a local ring that loses data, this is a third-party network call and the backend restarts several times a day. Pinned by `rescuetime.test.js` (21) + `routes/rescuetime-routing.test.js` (7, real HTTP) + a render test asserting the card is MOUNTED rather than merely written.

  ⚠ **THE DENOMINATOR IS ACTIVE TIME, NEVER PRESENT (8 Sep 2026).** `assessDay` divided RescueTime's reported minutes by `present_minutes` — and `present = active + idle + locked` by construction — so every minute the laptop sat switched on and untouched was counted against a service that by design logs nothing while nobody is working. An accusation generator, and the file's own header had said "measured ACTIVE time" since it was written; the code did not agree. Measured on the live store: against present the last week read **0.80 / 0.04 / 0.45 / 0.82 / 0.82 / 0.59**, against active **0.92 / — / 0.83 / 1.07 / 0.93 / 0.81** — the same feed, judged fairly, and the difference between "just above the floor" and "genuinely agrees". ⚠ **The case that matters is 6 Sep: present 2.2h, active ZERO**, because Nick was working on the Mac, which runs neither RescueTime nor the desktop agent. The old denominator turned *"the agent has nothing to compare"* into *"RescueTime under-reported"* — a red row generated by a machine being left switched on. It is now `unknown` via the existing `measured <= 0` refusal, which is the honest answer for a day spent working somewhere NEURO cannot see. **`busiest` sorts by active time too**: "which machine was he working at" is the question, and an idle laptop has the most present minutes and did none of the work. ⚠ **The Mac is a real and unclosed coverage gap** — `desktop_daily` has exactly one host (`DESKTOP-8LGF9RR`), so a Mac day is invisible to both halves of this comparison and can only ever be `unknown`. Pinned by 2 tests, **mutation-checked** — restoring `present_minutes` fails 3.
- **NEURO Health — every sense, and whether it works (31 Aug 2026)** — `services/signals.js` (pure `rate()`) + `GET /api/signals` + a **Her senses** section at the top of `PiHealthPanel`, whose sidebar label became **NEURO Health**. ⚠ **The view id stays `pi-health`** so every deep link and `?view=` param keeps working — the same call as `today` for Now. The argument for it is this session's own history: **three separate blindnesses found in one morning and every one INVISIBLE** — the HA phone feed returning null for five weeks (with the code blaming the phone), `health-signals` computing trends nothing read, and dietary logging stopping in March with nothing noticing, because a metric with no recent rows returns an empty result rather than an error. **A dead sensor and a quiet one look identical from every other screen.** ⚠ **FIVE states, not two, and `off` must NEVER render like `stale`** — not having the desktop agent installed is a DECISION, an agent that stopped talking is a problem, and two states that look alike is exactly how the phone stayed broken for five weeks; `off` is grey, unalarming, and cannot win the headline. ⚠ **Each signal carries its OWN cadence** (the phone reports on significant change so a motionless hour is quiet by design; heart rate arrives every few minutes; the vault changes daily) — a shared threshold would scream about one and stay silent about another, the `sensorsQuiet` lesson applied to SOURCES rather than metrics. Every row says **what she LOSES when it goes dark**, in plain words; a row naming only the source teaches nothing about why it matters. **Diet is `off`, never `stale`**, when it goes quiet — that is Nick not logging, a fact about him rather than a fault, and it is the row that explains why the food and water observations never appear. ⚠ **Location deliberately gets NO row**: `location.lastSource()` is in-memory and resets on every backend restart (several a day on deploys), so it would sit amber for a working feature, and a status page with a light nobody believes is one nobody reads. Two things **checked rather than assumed**: there is no `calendar_last_sync` key (freshness is `calendar_cache.fetched_at`) and `ha` had no synchronous cached-state accessor — guessing either would have produced a permanently red light for a working feature. Live on first run: 6 live, 1 off. READ-ONLY throughout.
- **Friction is READ from evidence, never inferred from silence (Phase 4)** — `services/friction.js` (pure `assess()`) + `GET /api/friction` + `FrictionSection` on `Now`. **Four** signals, each with a stated evidence bar: repeated attention deferrals **with a reason** (≥2, 21-day window, reason named only when it DOMINATES — a thing put off once for each of three reasons is not a pattern about any of them), ≥2 recorded shrinks on one task, the live `needs-smaller` state, and recorded step-aways. ⚠ **The fifth was `waiting_on` and it was REMOVED (31 Aug 2026).** Live on the Now page it rendered **four "Naomi to …" lines** under a heading reading *"Friction noticed"* — 316 open rows feeding a five-slot list, two of them **123 days old** (there was a minimum age and no maximum, so an April commitment showed for ever). The bar it failed is not attribution but **whose act the evidence records**: a `waiting_on` row evidences that somebody said they would do something, and NOTHING anywhere records that Nick is blocked on it — so presenting it as what got in his way asserts a fact nothing measured, against a named colleague, under a heading that says it got in his way. That is meeting-prep's *"never imply a person failed"* rule, which this very file quotes, breached four times over. They already have a better home in `WaitingOn` (People board + PersonDetail, pull-only); this was a second, worse copy of that list wearing the wrong label. ⚠ **What stays is a defer Nick made with the reason `waiting-on-someone`** — that is HIM saying he is blocked, an act he performed and NEURO recorded, and the distinction is the whole point. Pinned by negatives asserting a sourced row, and a 123-day-old one, still produce nothing, and that no colleague's name can reach the payload by any route. ⚠ **What it refuses to infer is the product.** Nothing from a **missed check-in** — being heads-down is exactly why one gets skipped, so four hours of silence must read identically to four check-ins (pinned). Nothing from **`interruptions`** — that array holds pauses and things that merely ARRIVED, and `noteInterruption` leaves the clock running precisely because NEURO cannot know whether Nick switched; reading it here would build a claim about his attention out of other people's timing, so `focus-session` gained a **separate `stepAways` array** for the times he SAID so. No streaks, no scores, no diagnoses, no "avoidance" language — pinned by a forbidden-wording test over every generated line. **No evidence, no insight, and no consolation line in its place**: with nothing recorded and nothing unreadable the section renders NOTHING. A failed source is a NAMED gap; `complete` is what keeps "nothing in your way" apart from "I could not look". Deliberately separate from `adhd-dashboard._avoidance()`, which stays: that card reasons about things NOT done (an absence, always open to a second reading), this one only about things Nick DID.
- **A captured task reaches Obsidian BEFORE anything says it was captured (Phase 4)** — `capture-store.appendTaskCapture()` writes one append-only line to `Tasks/Captured/Task Captures YYYY-MM.md`, then the task row is created. The vault is the durable record; the `tasks` table is the **operational projection** of it (status, MoSCoW, due dates, Microsoft links); `Tasks/NEURO Tasks (export).md` is a read-only VIEW of that table, and the export header now says so. Before this the capture was DB-first and the vault heard nothing until the export next ran — up to an hour in which the only copy of the thought lived in a SQLite file the vault knew nothing about, while the UI had already said "Added to todos". ⚠ **Append-only, and never into the export**, which is rewritten wholesale on every task change — editing it destroys the record on the next run. ⚠ **A vault miss does NOT fail the capture**: refusing a capture is the one failure this area exists to prevent, so an unconfigured or unreachable vault still creates the task and reports `vault.written:false` with a reason, which the panel says in words. Only a total miss is a 500; **vault-saved with a failed task row is a 207 PARTIAL** — "the words are safe, it just is not on your task list yet" — because "failed" over a thought already on disk sends Nick to retype it. Provenance both ways: the line carries `<!--neuro-task:ID-->`, the row carries `origin_path`. Exactly-once offline is the mobile LEDGER's job (`applyOperation` is synchronous from ledger read to ledger write), so a replayed capture appends no second line. `Tasks/Captured/` sits under `Tasks/`, which `action-candidates.shouldSkipPath` already skips — otherwise every captured task would come back as a candidate to capture again — but it is **not** in `vault-exclusions`, so the durable record stays searchable. Capture's *"I'll route it"* is gone: classification and entity extraction run AFTER the write and can fail, so the line now claims only what is guaranteed. Pinned by `capture-durability.test.js` (6, real vault + real HTTP).
- **Catalogues — one engine, many lists (31 Aug 2026)** — `services/catalogue.js` + `routes/catalogue.js` (`/api/catalogues`, behind the PIN) + `components/CataloguesPanel.jsx` (sidebar **Catalogues**). The fridge is not a fridge feature — Nick: *"I might want to catalogue my vinyl collection, or my hiking equipment"* — so one catalogue is one markdown file under `Catalogues/` in the vault, **self-describing** (its own frontmatter declares `sections:` and `shared:`, so there is no registry to drift the first time he makes one by hand in Obsidian, which is how he will make most of them). ⚠ **`shared` is what VESTA can read and it DEFAULTS FALSE** — VESTA sits on the PUBLIC internet behind Tailscale Funnel, and the failure directions are not symmetric: a private list nobody can see is an inconvenience, a shared one he did not mean to share is on the internet. So only a literal `true` shares (the string `"true"` does not, pinned), sharing is **its own route** rather than a key in a general update, the panel **creates every catalogue private** with no `shared` control on the create form, and the share toggle **asks first and names where the list goes**. Un-sharing does not ask. Anything the parser does not understand is **PRESERVED**, never dropped — two people type into these by hand. ⚠ **`render`'s own `*(empty)*` placeholder read back as user content**: unrecognised, it was preserved into `trailing`, re-rendered under the sections and read again on the next save, so **every write appended one more copy, for ever**, to any catalogue with an empty section — which every newly created one is. The existing stability test compares the PARSED fields and is blind to it; the fix is a skip in `parse` and the pin is on the **TEXT** (`render(parse(x)) === x` across five passes) plus, in the routing test, on the **file on disk** across add/remove cycles. Same species as the outcome-note fence: a placeholder the system wrote must never read back as something a person typed. The panel keeps *"I couldn't read this"* and *"there is nothing in it"* apart everywhere (an unreadable vault reports itself rather than rendering as a man who owns nothing), surfaces `already:true` so a fold does not look like a broken tap, and reads `items[section.toLowerCase()]` — a contract pinned in the routing test, because a component reading a key the route does not send renders empty and raises nothing. VESTA's fridge screen reads the catalogue slugged **`kitchen`**, and the panel says so when there isn't one. Pinned by `catalogue.test.js` (16) + `routes/catalogue-routing.test.js` (9, real HTTP).
- **Retrieval scope is a promise, not a filter on one source (Phase 4)** — `services/retrieval.js`, rewritten. `scope` was applied inside `keywordSearch` **only**; semantic results were never checked against it and carry the HIGHEST fusion weight (1.2), so asking for `folder:Meetings` reliably returned notes from outside Meetings **ranked above** the ones inside it. Now enforced three times, deliberately redundantly — inside each source, on each source's output, and again after fusion; the last is the guarantee, because a source added later cannot leak past it by forgetting. `folder:` is **segment-aware** (`Meetings` is not `Meetings archive/`) and the reverse containment the old code allowed is gone; the walk PRUNES to the folder rather than filtering after. `person:` matches the person's own note or a **whole-word FULL-name** body match (`entities.js`'s rule — "Liam" inside "William"), and unreadable is **not** a match. An **unrecognised** scope admits nothing — fail closed, or a typo hands back the whole vault labelled as scoped. ⚠ **The filesystem-order early stop is gone**: the walk used to halt at `maxResults*2` files, so the answer was "the first N the directory listing happened to yield" and a strong match late in the walk could not outrank a weak one early in it. Depth 4 → **12** (meeting notes live at `Meetings/YYYY/MM/`, so the old cap silently removed whole subtrees while they went on looking searchable), and hitting that or the 5,000-file cap is **REPORTED**, never swallowed. Degradation is honest: `embeddings.semanticSearch` returning `null` means "could not answer" → `semanticAvailable:false` with a reason while keyword and temporal still answer, so an outage never renders as an empty vault; an EMPTY semantic result set is a different fact and reports available. ⚠ **Over-limit notes are now visible**: a note past `MAX_CHUNKS_PER_FILE` is recorded in `agent_state.embeddings_truncated` (persisted — the backend restarts several times a day) and comes back marked `indexIncomplete:true`, with `getEmbeddingHealth().truncated` listing them; a note that shrinks below the cap clears its entry. `search()` keeps its bare-array shape for every existing caller; `searchWithHealth()` is wired into **chat RAG** (`chat-context-v2` tells the model in words when semantic was unavailable, and marks a partly-indexed note, because three keyword hits look exactly like three hits from a healthy hybrid search and a model reading a thin retrieval as "the vault has little on this" answers confidently from nothing), the **`search_vault` chat tool** (which also gained `scope`), and **`GET /api/vault/search`**. ⚠ That route was a **THIRD substring walker** carrying its own copy of every bug above — depth 4, an early stop at 20 in filesystem order, no ranking, no semantic arm — and it is what the **MCP `search_vault` tool** calls, so every external Claude Code session searching this vault was getting the crudest of the three answers and could not tell, because a substring walk always returns something. It runs on the unified retrieval now, with `dir` as a real `folder:` scope; the response keeps `matches[].text` (which `VaultBrowser` renders) alongside `excerpts`, and `matches[].line` is **null rather than invented**. The MCP tool gained a `dir` argument and prints a banner when semantic was unavailable. Pinned by `routes/vault-search-routing.test.js` (7, real HTTP — note the whole router sits behind `requireApiKey`, which 503s everything when `VAULT_API_KEY` is unset). Pinned by `retrieval-scope.test.js` (11, real temp vault, semantic arm stubbed to return out-of-scope paths on purpose).
- **An incomplete search is never proof that nothing exists (31 Aug 2026)** — the completeness contract on
`services/retrieval.js`, plus `frontend/src/vaultHealth.js` and a **Her senses**-style banner in
`VaultBrowser`. The scope work above made retrieval honest about *semantic* degradation and left the
FILESYSTEM half silent: a depth cap, a file-scan cap and an unreadable vault all returned the same short
list as a complete search of an empty folder, and only one of those is evidence. `searchWithHealth` now
carries **`truncated` / `truncationReasons` / `keywordComplete` / `temporalComplete`**, built from what the
WALK did — ⚠ **never from the result count**, which is the proxy that makes the whole thing meaningless
(pinned both ways: a complete zero-result search is NOT flagged, a full result set from a degraded one IS).
`temporalComplete` is **null when no range was asked for**, distinct from `true`. `keywordSearchDetailed` /
`temporalSearchDetailed` return `{results, traversal}` and the array-returning originals stay as wrappers,
so every existing caller is untouched. `describeIncompleteness(health)` is the ONE sentence, shared by chat
RAG, the `search_vault` chat tool and the MCP tools — **traversal incompleteness is treated exactly like a
dead index**, because they are indistinguishable from the outside.
  ⚠ **`/api/vault/search/temporal` was the LAST bespoke walker** and carried every bug `/search` was
rebuilt to remove (depth 4, an early stop at `limit * 3` in filesystem order, no ranking, no semantic arm,
no scope, no health). It runs on the unified retrieval now, gained a `dir` scope, and — caught by its own
first test run — ⚠ **a date range had become a ranking HINT rather than a bound**: fusion mixed in keyword
and semantic hits that never saw the window, so a search for last week returned a note from March ranked
above it. `matchesDateRange` (pure) gates the fused set, and a note whose mtime cannot be read is **not in
range**, the same way an unreadable note is not in scope.
  ⚠ **Scoped semantic recall no longer means "the global top 200, then filter"** — a `folder:` scope whose
notes all ranked 201st and below came back EMPTY and looked exactly like an empty folder.
`embeddings.semanticSearchDetailed` takes a **`pathFilter` applied BEFORE ranking** (the index already
stores `relative_path`, so no migration was needed) — exact recall, and cheaper. A `person:` scope cannot be
decided from a path, so it stays a post-filter, but a **bounded** one (`SCOPED_POST_FILTER_LIMIT` 500) that
**reports `boundedRecall`** rather than a fixed window that never said so. **The scope is never widened to
compensate**, and an unrecognised one still admits nothing.
  ⚠ **The drop-box had been writing into the repository.** `path.join('', 'Tasks', 'Capture.md')` is
RELATIVE, so an unset `OBSIDIAN_VAULT_PATH` did not fail — it created `backend/Tasks/Capture.md`, reported
success, and drained into it forever, with Obsidian writing to one file and NEURO reading another.
`resolveVault()` refuses **not-configured / not-absolute / unreadable / not-a-directory**; `capturePath()`
and `logPath()` return **null, never a fallback**; `ensureCaptureFile()` returns a structured refusal and
**never throws**, because the startup path must not die there. The scheduler logs **one configuration
warning instead of a successful setup message**. The generated artifact was confirmed byte-identical to
`TEMPLATE`, untracked, and removed.
  UI: `assessSearchHealth` is PURE (the `pi-health.assess()` split) and keeps **error / incomplete /
complete** apart — a failed request is never rendered as an empty result, an empty-but-incomplete one says
*"No matches found in the available search results. This is not confirmation that the vault contains
nothing."* verbatim, and ⚠ **a complete search is NOT hedged**, or the banner is one nobody reads by week
two. Also fixed on the way: every result row rendered **`Lnull:`**, because `line` is null by design.
Pinned by `retrieval-completeness.test.js` (15), `vault-temporal-routing.test.js` (9, real HTTP),
`capture-vault-safety.test.js` (8, child processes with the env scrubbed) and
`vault-browser-health.test.js` (10, dynamic-import of the ESM module + a source scan with a positive
control).
- **The index walked to depth 4 while search walked to 12 (31 Aug 2026)** — `services/vault-walk.js` is now
the ONE traversal, shared by `retrieval.js` and `embeddings.listVaultFiles()`. They had disagreed about how
much vault exists: retrieval was fixed to depth 12 with a reported cap and the embeddings inventory was
still hard-coded at **depth 4**, so a note in `Meetings/2026/08/deep/` was findable by keyword and **could
never be indexed for semantic search** — permanently, silently, and invisibly, because a hybrid search
always returns something. The DEPTH/CAP policy is shared; the EXCLUSION policy stays per-caller on purpose
(`Daily/` is out of embeddings and deliberately kept for entities; retrieval additionally drops `Templates`
and `Vault Audit`). ⚠ **An unreadable folder is now RECORDED, not skipped** — both old walkers did
`try{readdir}catch{continue}`, so a broken permission or an unmounted disk produced a smaller answer that
called itself complete.
  ⚠ **`semanticAvailable` only ever meant "the query embedded".** It said nothing about whether the index
holds the vault, so a half-built index answered every search looking whole. `embeddings.refreshCoverage()`
measures **eligible / indexed / unindexed / stale / failed / truncated / excluded / inaccessible** and
stores it in `agent_state.embeddings_coverage`; `getCoverage()` is a **cheap synchronous KV read** on the
search path and the walk happens on a schedule (25s after boot, hourly at :35, and at the end of every
rebuild) — the `team-availability` live→cache split, for the same reason. `db.getEmbeddingIndexSummary()`
exists because `getAllEmbeddings()` loads every 1024-float vector, and reading the whole index to count it
is how a health check becomes the reason the Pi is slow. **A failed run records WHICH FILES** in
`agent_state.embeddings_failed` — their old rows deliberately survive, so from the outside a partly failed
rebuild is otherwise identical to a clean one. ⚠ **`complete` is THREE-VALUED**: `true` / `false` /
**`null` = never measured**, which is reported as its own state and never as `true`; it is durable, so
`null` is only ever seen on a brand-new install. ⚠ **A scope narrows what COUNTS as a gap** —
`coverageForScope` — but a partial WALK is never scoped away, and the scope is never widened to compensate.
  `health` gains `semanticCoverageComplete` / `semanticCoverageReasons` / `semanticIndexedCount` /
`semanticEligibleCount` / `semanticUnindexedPaths` (bounded sample; the counts are exact), and
`describeIncompleteness()` treats a coverage gap **exactly like** a dead provider or a capped walk, because
all three produce the identical short list. `health.truncated` deliberately keeps its narrower meaning
(traversal only) so existing consumers do not shift under them.
- **A sibling directory shares the prefix (31 Aug 2026)** — `routes/vault.js` containment. The guard was
`resolved.startsWith(path.resolve(VAULT_PATH))`, a **STRING** test: with the vault at `C:\Vault`, the path
`../Vault-old/secret.md` resolves to `C:\Vault-old\secret.md`, which starts with `C:\Vault` — so it
**passed**, and read / write / append / list / **delete** / export all operated outside the vault on a path
the guard had just approved. Not theoretical: `nuero-vault` has siblings on the Pi, and the vault holds a
`Personal/` folder of disciplinary prep, a fraud investigation and OH documents. Now `path.relative` decides
(`..`, `..<sep>`, or absolute = outside), plus: a **missing or unreadable vault root REFUSES** rather than
resolving against the process working directory (the capture drop-box bug, one service along), a NUL byte
is refused, and **symlink escape** is closed by `realpath` on the deepest EXISTING ancestor — which is what
lets a genuinely new file still be created while stopping one being created *through* an escaping link. The
two `path.join(safeDir, filename)` sites (export-docx, person-doc) go back through the guard rather than
being trusted because their halves looked fine apart. Pinned by `vault-path-safety.test.js` (21, real HTTP;
the symlink case skips honestly where Windows will not create one).
- **An invalid date became an UNBOUNDED search (31 Aug 2026)** — `retrieval.parseDateRange` /
`parseDateBound`, both PURE. `new Date('lastweek')` is `Invalid Date` and **every comparison against NaN is
false**, so the old filter excluded nothing: a typo'd bound silently searched all of time and returned the
answer labelled as a date range. Now only a real `YYYY-MM-DD` or a full ISO timestamp is accepted;
`2026-02-31` is refused rather than rolled into March; an inverted range is refused; and the route answers
**400 with the offending field**. ⚠ **Omission is not a typo** — an absent bound keeps the 30-day default,
an invalid one is an error, and conflating them is what made this invisible. ⚠ **A date-only `to` now means
the END of that day** — `to=2026-08-31` used to mean midnight and dropped the whole of the 31st from the one
search whose entire purpose is a date range. The response returns **normalised ISO bounds** plus
`range.fromDefaulted`/`toDefaulted`. The range check runs **BEFORE** the vault check, because a caller error
is the same error whatever the environment. Pinned by `temporal-range.test.js` (14) + 7 route tests.
- **Brain Health moved out of SARA into NEURO (31 Aug 2026)** — `components/BrainHealthPanel.jsx`, sidebar
**Brain Health** (view id `brain-health`), replacing SARA's `brain` tab (`BrainManagement`, deleted). The
division of labour decides it: NEURO is the brain and the NEURO app is Nick's DIRECT access to it, while
SARA comes to him — and vault maintenance is neither ambient nor something worth interrupting him about.
It is a desk job with dated reports to read and writes to weigh, and on a phone the reports were unreadable.
⚠ **Removing a SARA tab is never only a deletion**: an id missing from `SARA_LITE_TABS` routes its
notifications to Focus IN SILENCE. The notification KIND `brain` is deliberately KEPT (`vault_hygiene`,
`plaud`, `imports`, `knowledge_reflection`, `sweep_complete` still resolve to it, and the desktop still
navigates to Imports); only its SARA destination is gone, so it now falls through to the **Surface** like
everything else with no dedicated tab. Two things the SARA screen got wrong and this fixes: **read and
WRITE were the same grey Run button** — `Plan links` previews while `Apply links` and `Connect orphans`
edit real notes and `PLAUD repull` re-downloads recordings — so effect is now a per-job badge AND the card's
left edge (unmarked / amber / blue), and **nothing said what an action would do**, so every job states what
it READS, what it CHANGES, what it will NOT do, how to get back (`Scripts/.lint-backups/` + changelog) and
where the report lands. Anything that writes is **two-step**: it refuses until its own preview has run, and
the confirm quotes that preview's real numbers — the `event-parser` / `one-to-one-booking` / `task-blocks`
idiom. A failed scan renders *"missing, not zero"* rather than a clean bill of health. Pinned by
`brain-health-migration.test.js` (9, source scans with positive controls), incl. that the vault-hygiene
notification still resolves to a tab that exists.
- **A thinking pause is not an idle connection (9 Sep 2026)** — `server.keepAliveTimeout = 65_000` / `headersTimeout = 66_000` on the `app.listen` handle, plus an honest Retry in BOTH standup clients. Nick's standup died mid-conversation with the browser's bare **`Failed to fetch`**. ⚠ **Nothing was wrong with the standup, the AI stack or the Pi** — checked before touching anything: the backend had been up **20 hours** with no restart (so not the concurrent-session deploy shape), load was 0.13, only **8 AI calls all day** against the cap and `lastFallbackReason: null` (so not 8 Sep's budget starvation), and there was **no log line of any kind**, because the request never reached a handler. `app.listen()` was never given timeouts, so Node's default applies: **measured on the live Pi, the server sent FIN 6,006ms after answering**. He reads SARA's question, thinks, types for thirty seconds, presses send — and the browser writes the POST into a socket closed twenty-five seconds earlier. ⚠ **A GET survives this and a POST does not**: a browser re-sends an idempotent request on a dead socket and will not re-send a POST, so of everything in NEURO the exposed case is precisely the one interaction built around Nick pausing to think. `headersTimeout` MUST stay above `keepAliveTimeout` or Node cuts off the request that just won the race — the same bug, later.

  ⚠ **The client half made it worse, and it is the reason this reads as a crash.** Both `StandupSession.jsx` and `sara/app` `Standup.jsx` assumed *"the server has already been told"* — true of a **503 from a failed turn** (the route persists the message before calling the model and hands its transcript back) and **false of a transport failure**, where the words exist nowhere but the browser. So Retry sent the literal string **`(continue)`**, the server appended THAT as his reply, and **SARA answered a placeholder while what he actually wrote was silently discarded** — under a banner reading *"Nothing you've typed is lost — it's saved on the Pi."*, which was a claim about the wrong failure. Confirmed against the live session: the stored transcript ends at the assistant's 08:32:43 turn and his next message is **not in it**. The two are now told apart by whether the error carried a `session` whose last message is his (`landed`); unlanded, Retry **re-sends the real text** with `optimistic:false` so it is not double-pushed, and the note says the message did not reach the Pi rather than promising it did. Pinned by `keepalive.test.js` (3, source scan with a positive control, **mutation-checked**) — ⚠ deliberately **regex-free**, because the first version's backslashes were eaten by the shell heredoc that wrote it and it passed for the wrong reason (the `sara/widget` no-backslashes lesson, one pipeline along).
- **Offline-first** — IndexedDB caching via `idb` library, service worker for PWA
- **Per-component CSS** — each component has its own `.css` file, no Tailwind
- **SPA fallback** — non-API routes serve `index.html` with no-cache headers
- **Scheduler** — `node-cron` based background jobs (Jira sync, nudge checks, etc.)

## Deployment
- Runs on Raspberry Pi 5 (16GB) at home
- Accessible via Tailscale VPN
- Frontend built with Vite, served by Express in production
- PWA installed on iPhone
- Worker runs on separate Pi 4
- **Node 22.22.2 must be on PATH for any pm2 command** — Node 20 segfaults better-sqlite3.
- ⚠ **Never pipe a deploy step to `head`/`tail`.** A pipeline's exit status is the LAST command, so a failed `git pull` sails straight through an `&&` chain and the Pi builds, tests and restarts at the PREVIOUS commit — it cost a silent wrong-commit deploy on 16 Aug. Confirm with `git log --oneline -1` **on the target**, never with the pull's own output. Equally: **never scp a scratch script into the Pi's repo working tree** (use `/tmp`), because an untracked file there blocks the next `--ff-only` pull.
- ⚠ **Before exempting any route from auth, run `tailscale serve status` first.** pi5 serves `https://pi5.tailecb90f.ts.net` → `127.0.0.1:3001` with **Funnel ON**, and Tailscale proxies both tailnet AND public traffic from loopback — so a guard trusting `127.0.0.1` publishes the route to the internet. Identity must come from `Tailscale-User-Login` / `Tailscale-Funnel-Request`, failing CLOSED on bare loopback.

### Backups (#59, 16 Aug 2026)
Local: `backup-data.sh` keeps **28** rotated DB copies under `/mnt/data/backups/nuero-db`. ⚠ The DB nearly doubled with the Apple Health backfill (235 → 447 MB), so that directory is heading for **~12.5 GB** as the rotation cycles — survivable (422 GB free), but it is a decision, not a surprise.

Off-site: nightly root cron 02:20, rclone `crypt` to Backblaze B2 bucket `pi5-neuro-offsite`, filenames AND directory names encrypted, watchdog-monitored. **Ships only the half that cannot be regenerated** — vault + Home Assistant + ONE current `agent.db` + syncthing config; `backups/` is excluded deliberately (rotated copies of the same DB, derived and growing daily). Restore was actually performed, not assumed. Bucket lifecycle is `daysFromHidingToDeleting: 1` — **without it a nightly 250MB DB version eats the 10GB free tier in six weeks, invisibly**, because the size check only counts current files. Setup and the two outstanding items (store the crypt password + salt OFF the Pi; regenerate the B2 master key) are in `backend/scripts/backup-offsite-SETUP.md`.

### Tests
- ⚠ **`npm test` must never touch the real vault (#119).** `node --test` globs `test-*.js`, so four Tier smoke scripts used to run on every invocation while asserting nothing — and `test-tier1.js` defaulted `OBSIDIAN_VAULT_PATH` to the REAL vault and created notes in it. They are now `backend/scripts/smoke-*.js` (the rename is what removes them from discovery), refuse to run without an explicit `OBSIDIAN_VAULT_PATH`, and `no-live-vault-in-tests.test.js` pins it. Keep it that way.
- **A test that pins a date must pin EVERY call it makes.** `outcomes.recent`/`trend` resolved "now" themselves while their siblings `computeWeek`/`snapshot` took an anchor, so a pinned fixture only half-pinned the module and the suite broke on a date rollover rather than a code change. Both now take an anchor, and a test asserts the anchor is HONOURED so a silent revert to `new Date()` fails in the week it is written.
- **A green suite says nothing about routing.** Call any new route against the running server before calling it done — a literal path registered after a sibling parameterised one is parsed as its parameter.

## GitHub
Repo: `Wardy-uk/nuero` (note: typo in repo name is intentional/historical)

## Post-Build Rule
**After every successful build or significant feature completion, update this CLAUDE.md to reflect any new routes, services, views, patterns, or architectural changes. Keep it accurate to the codebase.**

## Session Start Ritual
At the start of every session, before writing any code:
1. Read `.claude/memory/handoff.md` — pick up where the last session left off
2. Read `.claude/memory/mistakes.md` — avoid repeating past errors
3. Read `.claude/memory/patterns.md` — follow established conventions

## Session End Rule
When context is getting long (~60%+), or Nick says "write a handoff", or a task is complete — write a handoff summary to `.claude/memory/handoff.md` following the skill in `.claude/skills/handoff.md`.
