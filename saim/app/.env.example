# SAiM light-touch app — build-time config (Vite reads VITE_* at build).
#
# Production (Netlify build): point at the NEURO brain's Tailscale Serve HTTPS URL.
# Tailnet-only — every device that uses SAiM is on Tailscale, so the brain is never
# exposed to the public internet. Example (replace with the Pi 5's real ts.net name):
#   VITE_API_URL=https://pi5.tailXXXX.ts.net
VITE_API_URL=

# Optional but strongly recommended for production:
#   VITE_ALLOWED_HOSTS=mobile.example.com,saim-mobile.netlify.app
#   VITE_CANONICAL_URL=https://mobile.example.com
# If SAiM Mobile is deployed on the wrong domain, the app will show a hard warning
# instead of pretending to be offline.
VITE_ALLOWED_HOSTS=
VITE_CANONICAL_URL=

# Optional label shown in the app header to make builds obvious.
# Example:
#   VITE_BUILD_LABEL=prod-mobile
VITE_BUILD_LABEL=

# Vault API key — sent as X-Api-Key on /api/vault* routes (brain re-indexes on write).
VITE_VAULT_API_KEY=

# Dev only: where `vite` proxies /api to (defaults to http://localhost:3001, the NEURO backend).
# NEURO_BACKEND_URL=http://localhost:3001
